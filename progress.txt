# Progress - OSv2 Full Parity

## 2026-04-16 — Initial Setup

### Context
- Archived previous ralph run (foundry-osv2-api-alignment, 93 stories) to archive/2026-04-16-foundry-osv2-api-alignment/
- Generated new PRD with 60 user stories (US-100 through US-159) across 8 phases
- PRD based on 6-agent parallel research: OMS, OSS, Actions, Frontend, Tasks, Palantir OSv2 docs

### Phases
- Phase 1 (US-100–US-111): Core Action & Query Gaps — Link rules, Interface rules, Undo, Polygon filters, Fuzzy search, Value Type constraints
- Phase 2 (US-112–US-123): Ontology Branching & Governance — Branch/Proposal/Review/Merge workflow, Export/Import
- Phase 3 (US-124–US-131): Automate Engine — Schedule/DataChange triggers, Action/Function/Notification effects, Retry
- Phase 4 (US-132–US-135): WebSocket Subscriptions — Server, subscriptions, NATS routing, frontend integration
- Phase 5 (US-136–US-140): OSDK Code Generation — Framework, TypeScript/Python/Go SDK gen, versioning
- Phase 6 (US-141–US-145): Developer Console — App registration, OAuth 2.0, API Playground, Usage Metrics
- Phase 7 (US-146–US-152, US-159): Ontology Manager UI — ObjectType/Property/LinkType/ActionType/Interface CRUD, Schema Graph, Audit History
- Phase 8 (US-153–US-158): Frontend Enhancements — Export, Bulk ops, TimeSeries charts, Maps, Notifications, Import wizard

### Codebase Patterns
- Action rules in pkg/actions/rules.go — ExecuteRules() dispatches by rule type
- CollapseEdits in pkg/actions/collapse.go — dedup per (objectType, primaryKey); link edits keyed by (linkTypeRID, sourcePK, targetPK)
- Internal edit type pattern: editTypeUpsert (UPSERT) is internal to actions pkg, resolved in Executor.Prepare before publishing
- ObjectExistenceChecker interface on Executor: injected like FunctionDispatcher, nil→graceful default to CREATE
- Interface-backed rules: objectType resolved from params via resolveObjectTypeParam ("objectType" or "objectTypeApiName"); validated in Executor.Prepare after ExecuteRules
- Validation requiring OMS repo (interface membership, link type resolution) happens in Executor.Prepare, not in ExecuteRules (keeps ExecuteRules pure)
- bleveObjectChecker (in integration tests) wraps index.Manager + ontologyAPIName for existence checks via idx.Document(pk)
- Where operators in pkg/oss/where/converter.go — ConvertToBleveQuery switch on type
- Aggregation metrics in pkg/oss/aggregation/engine.go — switch on AggregationSpec.Type
- OMS models in pkg/oms/models.go — all entity structs
- OMS repository interface in pkg/oms/repository.go — ~60 methods
- Migrations in migrations/ — sequential 000001-000022, next is 000023
- Frontend routes in web/src/App.tsx
- API hooks in web/src/hooks/
- Sidebar nav in web/src/components/layout/Sidebar.tsx
- Link edits (LINK_CREATE/LINK_DELETE) flow: Rule → Edit → NATS → Consumer.applyLinkCreate/applyLinkDelete → UpsertLinkEdge/DeleteLinkEdge
- Consumer has separate LinkEdgeWriter (SetLinkEdgeWriter) and LinkEdgeDeleter (SetLinkEdgeDeleter) interfaces for M2M edge writes/deletes
- Executor.Prepare resolves link type API names to RIDs via GetLinkTypeByAPIName before publishing
- Pre-existing build errors in pkg/oms/admin_handlers.go and execute_query_goja_test.go — not blocking tests in other packages

---

## 2026-04-16 - US-100
- Implemented createLink rule type for M2M link creation through Actions
- Files changed:
  - pkg/funnel/types.go — Added EditTypeLinkCreate constant + LinkTypeRID/TargetPrimaryKey fields on Edit
  - pkg/actions/rules.go — Added createLink case to Rule struct and executeRule dispatch
  - pkg/actions/collapse.go — CollapseEdits deduplicates LINK_CREATE by (linkTypeRID, sourcePK, targetPK)
  - pkg/actions/executor.go — resolveLinkEdits resolves API name → RID; countEdits counts addedLinksCount
  - pkg/funnel/consumer.go — LinkEdgeWriter interface + applyLinkCreate + LINK_CREATE handling in batch path
  - pkg/actions/actions_test.go — 10 new unit tests for rules, collapse, executor
  - pkg/funnel/funnel_test.go — 5 new tests for consumer LINK_CREATE handling
  - pkg/actions/create_link_integration_test.go — Integration test: createLink → searchAround finds linked object
  - pkg/apierror/error.go — Added NewBadRequest (pre-existing missing function)
- **Learnings for future iterations:**
  - Link edits have separate tracking in CollapseEdits (separate key space from object edits)
  - The consumer skips link edits for Bleve indexing, history recording, and embedding generation
  - Rule.LinkTypeAPIName is resolved to LinkTypeRID in Executor.Prepare, not in ExecuteRules (keeps ExecuteRules pure)
  - Integration tests can wire in-memory EdgeStore that implements both LinkEdgeWriter and links.EdgeRepository
---

## 2026-04-16 - US-101
- Implemented deleteLink rule type for M2M link deletion through Actions
- Files changed:
  - pkg/funnel/types.go — Added EditTypeLinkDelete constant
  - pkg/actions/rules.go — Added deleteLink case to executeRule dispatch
  - pkg/actions/collapse.go — CollapseEdits handles LINK_DELETE dedup + LINK_CREATE+LINK_DELETE cancellation
  - pkg/actions/executor.go — resolveLinkEdits resolves LINK_DELETE edits; countEdits counts deletedLinksCount
  - pkg/funnel/consumer.go — LinkEdgeDeleter interface + SetLinkEdgeDeleter + applyLinkDelete + LINK_DELETE routing in batch/single paths + history skip
  - pkg/actions/actions_test.go — 9 new unit tests for deleteLink rules, collapse cancellation, executor, countEdits
  - pkg/funnel/funnel_test.go — 4 new tests for consumer LINK_DELETE handling
  - pkg/actions/create_link_integration_test.go — Added DeleteLinkEdge to inMemoryEdgeStore + integration test: createLink → deleteLink → searchAround returns empty
- **Learnings for future iterations:**
  - LINK_DELETE mirrors LINK_CREATE symmetrically: same fields (LinkTypeRID, PrimaryKey as source, TargetPrimaryKey)
  - CollapseEdits cancellation is directional: CREATE+DELETE→cancel, DELETE+CREATE→last wins (recreate)
  - resolveLinkEdits now handles both LINK_CREATE and LINK_DELETE (unified filter)
  - Consumer skips LINK_DELETE for Bleve indexing, history recording, and embedding generation (same as LINK_CREATE)
---

## 2026-04-16 - US-102
- Implemented createOrModifyObject rule type (upsert behavior) for Actions
- Files changed:
  - pkg/actions/rules.go — Added editTypeUpsert internal constant + createOrModifyObject case in executeRule dispatch
  - pkg/actions/executor.go — Added ObjectExistenceChecker interface, SetObjectExistenceChecker method, resolveUpsertEdits (converts UPSERT→CREATE/MODIFY based on existence)
  - pkg/actions/actions_test.go — 9 new tests: parse, execute (with/without PK), executor (exists/not-exists/no-checker), collapse (resolved upsert then modify/delete)
  - pkg/actions/create_link_integration_test.go — Added bleveObjectChecker adapter + integration test: upsert non-existent→CREATE, process through consumer, upsert existing→MODIFY
- **Learnings for future iterations:**
  - Internal edit types (editTypeUpsert) should be package-level constants resolved in Executor.Prepare, not exported through funnel
  - ObjectExistenceChecker follows the same injection pattern as FunctionDispatcher: nil→graceful degradation (defaults to CREATE)
  - Bleve's idx.Document(pk) returns a doc if it exists — the simplest way to check object existence without a search query
  - When no PK is found in params, createOrModifyObject auto-generates one (same as createObject), making it always a CREATE
  - resolveUpsertEdits runs after resolveLinkEdits but before tagEditsAsUserSource, keeping the Prepare pipeline sequential
---

## 2026-04-16 - US-103
- Implemented interface-backed action rule types (createInterfaceObject, modifyInterfaceObject, deleteInterfaceObject)
- Files changed:
  - pkg/actions/rules.go — Added InterfaceAPIName field to Rule struct + 3 new rule cases + resolveObjectTypeParam and isInterfaceRule helpers
  - pkg/actions/executor.go — Added validateInterfaceRules step in Prepare pipeline: resolves Interface and ObjectType by API name, checks ListObjectTypeInterfaces for membership
  - pkg/actions/actions_test.go — 7 new tests: parse interface rules, execute create/modify/delete interface rules, missing objectType error, executor validates implementing type, executor rejects non-implementing type + interfaceAwareMockRepo
  - pkg/actions/create_link_integration_test.go — Integration test: createInterfaceObject with Building implementing GeoEntity → object created in Bleve + non-implementing type rejected
- **Learnings for future iterations:**
  - Interface-backed rules resolve objectType from params ("objectType" or "objectTypeApiName"), not from the Rule config
  - Validation requiring OMS repo access (interface membership) is done in Executor.Prepare post-ExecuteRules, keeping ExecuteRules pure
  - Rules and edits maintain 1:1 correspondence — this allows Executor.Prepare to cross-reference rule metadata with generated edits for validation
  - The extended mock pattern (interfaceAwareMockRepo embeds mockOmsRepo) works cleanly for adding interface resolution without touching other method stubs
---
