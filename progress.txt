# Progress - OSv2 Full Parity

## 2026-04-16 — Initial Setup

### Context
- Archived previous ralph run (foundry-osv2-api-alignment, 93 stories) to archive/2026-04-16-foundry-osv2-api-alignment/
- Generated new PRD with 60 user stories (US-100 through US-159) across 8 phases
- PRD based on 6-agent parallel research: OMS, OSS, Actions, Frontend, Tasks, Palantir OSv2 docs

### Phases
- Phase 1 (US-100–US-111): Core Action & Query Gaps — Link rules, Interface rules, Undo, Polygon filters, Fuzzy search, Value Type constraints
- Phase 2 (US-112–US-123): Ontology Branching & Governance — Branch/Proposal/Review/Merge workflow, Export/Import
- Phase 3 (US-124–US-131): Automate Engine — Schedule/DataChange triggers, Action/Function/Notification effects, Retry
- Phase 4 (US-132–US-135): WebSocket Subscriptions — Server, subscriptions, NATS routing, frontend integration
- Phase 5 (US-136–US-140): OSDK Code Generation — Framework, TypeScript/Python/Go SDK gen, versioning
- Phase 6 (US-141–US-145): Developer Console — App registration, OAuth 2.0, API Playground, Usage Metrics
- Phase 7 (US-146–US-152, US-159): Ontology Manager UI — ObjectType/Property/LinkType/ActionType/Interface CRUD, Schema Graph, Audit History
- Phase 8 (US-153–US-158): Frontend Enhancements — Export, Bulk ops, TimeSeries charts, Maps, Notifications, Import wizard

### Codebase Patterns
- Action rules in pkg/actions/rules.go — ExecuteRules() dispatches by rule type
- CollapseEdits in pkg/actions/collapse.go — dedup per (objectType, primaryKey); link edits keyed by (linkTypeRID, sourcePK, targetPK)
- Internal edit type pattern: editTypeUpsert (UPSERT) is internal to actions pkg, resolved in Executor.Prepare before publishing
- ObjectExistenceChecker interface on Executor: injected like FunctionDispatcher, nil→graceful default to CREATE
- Interface-backed rules: objectType resolved from params via resolveObjectTypeParam ("objectType" or "objectTypeApiName"); validated in Executor.Prepare after ExecuteRules
- Validation requiring OMS repo (interface membership, link type resolution) happens in Executor.Prepare, not in ExecuteRules (keeps ExecuteRules pure)
- bleveObjectChecker (in integration tests) wraps index.Manager + ontologyAPIName for existence checks via idx.Document(pk)
- Where operators in pkg/oss/where/converter.go — ConvertToBleveQuery switch on type; ConvertToBleveQueryWithOpts for fuzzy-enabled conversion
- Fuzzy search: MatchQuery.SetFuzziness(n) on text operators; options threaded via ConvertOptions through logical operators
- Aggregation metrics in pkg/oss/aggregation/engine.go — switch on AggregationSpec.Type
- OMS models in pkg/oms/models.go — all entity structs
- OMS repository interface in pkg/oms/repository.go — ~60 methods
- Migrations in migrations/ — sequential 000001-000024, next is 000025
- OntologyBranch/BranchChange models in pkg/oms/models.go; PG CRUD in pg_repository.go (CreateBranch, GetBranch, ListBranches, CloseBranch, CreateBranchChange, ListBranchChanges)
- Branch mock stubs pattern: create mock_branches_test.go in each package (pkg/oms, pkg/actions, pkg/oss, pkg/links, test/e2e, test/northwind, test/chinook, pkg/index/rehydrate_test.go inline)
- ObjectFetcher interface on Executor: injected like ObjectExistenceChecker, nil→graceful degradation (PrevEdits omitted)
- PrevEdits is a parallel slice to Edits in PreparedAction; serialized to ActionLog.PrevEdits JSONB (null entries for CREATE/LINK edits)
- Frontend routes in web/src/App.tsx
- API hooks in web/src/hooks/
- Sidebar nav in web/src/components/layout/Sidebar.tsx
- Link edits (LINK_CREATE/LINK_DELETE) flow: Rule → Edit → NATS → Consumer.applyLinkCreate/applyLinkDelete → UpsertLinkEdge/DeleteLinkEdge
- Consumer has separate LinkEdgeWriter (SetLinkEdgeWriter) and LinkEdgeDeleter (SetLinkEdgeDeleter) interfaces for M2M edge writes/deletes
- Executor.Prepare resolves link type API names to RIDs via GetLinkTypeByAPIName before publishing
- ValueType constraints: Constraints struct in pkg/types/constraints.go; Property → ValueType via TypeConfig {"valueTypeApiName": "..."} (no migration); validated in executor.go step 12 (validateValueTypeConstraints)
- Pre-existing build errors in pkg/oms/admin_handlers.go and execute_query_goja_test.go — not blocking tests in other packages
- Revert logic in pkg/actions/revert.go — generateReverseEdits maps CREATE→DELETE, MODIFY→MODIFY(prevState), DELETE→CREATE(prevState), LINK_CREATE→LINK_DELETE, LINK_DELETE→LINK_CREATE
- AlreadyRevertedError sentinel type for 409 Conflict on double-revert
- Repository additions: GetActionLog(id) and UpdateActionLogStatus(id, status) — added to oms.Repository interface + PG impl + all mocks
- OpenAPI spec lives in api/openapi.yaml AND must be copied to cmd/server/openapi_spec.yaml; contract tests enforce this (TestContract_AllRoutesDocumented, TestContract_EmbeddedSpecMatchesCanonical)
- Bleve GeoShapeQuery (v2.5.7) works on GeoShape-indexed fields, not geopoint fields; for point data, use PointInPolygon in-memory fallback
- Geo tests use setupGeoIndex() with NewGeoShapeFieldMapping() to index GeoJSON Point features for polygon query testing
- doesNotIntersect* operators: negate via BooleanQuery(MatchAll + MustNot(inner)); for BoundingBox use GeoShapeQuery("polygon","intersects") over GeoBoundingBoxQuery for GeoShape field compat
- PolygonQuery input format: {"polygon": [[[lon,lat],...]] } — GeoJSON Polygon coordinates; converted to [][][][]float64 for Bleve

---

## 2026-04-16 - US-100
- Implemented createLink rule type for M2M link creation through Actions
- Files changed:
  - pkg/funnel/types.go — Added EditTypeLinkCreate constant + LinkTypeRID/TargetPrimaryKey fields on Edit
  - pkg/actions/rules.go — Added createLink case to Rule struct and executeRule dispatch
  - pkg/actions/collapse.go — CollapseEdits deduplicates LINK_CREATE by (linkTypeRID, sourcePK, targetPK)
  - pkg/actions/executor.go — resolveLinkEdits resolves API name → RID; countEdits counts addedLinksCount
  - pkg/funnel/consumer.go — LinkEdgeWriter interface + applyLinkCreate + LINK_CREATE handling in batch path
  - pkg/actions/actions_test.go — 10 new unit tests for rules, collapse, executor
  - pkg/funnel/funnel_test.go — 5 new tests for consumer LINK_CREATE handling
  - pkg/actions/create_link_integration_test.go — Integration test: createLink → searchAround finds linked object
  - pkg/apierror/error.go — Added NewBadRequest (pre-existing missing function)
- **Learnings for future iterations:**
  - Link edits have separate tracking in CollapseEdits (separate key space from object edits)
  - The consumer skips link edits for Bleve indexing, history recording, and embedding generation
  - Rule.LinkTypeAPIName is resolved to LinkTypeRID in Executor.Prepare, not in ExecuteRules (keeps ExecuteRules pure)
  - Integration tests can wire in-memory EdgeStore that implements both LinkEdgeWriter and links.EdgeRepository
---

## 2026-04-16 - US-101
- Implemented deleteLink rule type for M2M link deletion through Actions
- Files changed:
  - pkg/funnel/types.go — Added EditTypeLinkDelete constant
  - pkg/actions/rules.go — Added deleteLink case to executeRule dispatch
  - pkg/actions/collapse.go — CollapseEdits handles LINK_DELETE dedup + LINK_CREATE+LINK_DELETE cancellation
  - pkg/actions/executor.go — resolveLinkEdits resolves LINK_DELETE edits; countEdits counts deletedLinksCount
  - pkg/funnel/consumer.go — LinkEdgeDeleter interface + SetLinkEdgeDeleter + applyLinkDelete + LINK_DELETE routing in batch/single paths + history skip
  - pkg/actions/actions_test.go — 9 new unit tests for deleteLink rules, collapse cancellation, executor, countEdits
  - pkg/funnel/funnel_test.go — 4 new tests for consumer LINK_DELETE handling
  - pkg/actions/create_link_integration_test.go — Added DeleteLinkEdge to inMemoryEdgeStore + integration test: createLink → deleteLink → searchAround returns empty
- **Learnings for future iterations:**
  - LINK_DELETE mirrors LINK_CREATE symmetrically: same fields (LinkTypeRID, PrimaryKey as source, TargetPrimaryKey)
  - CollapseEdits cancellation is directional: CREATE+DELETE→cancel, DELETE+CREATE→last wins (recreate)
  - resolveLinkEdits now handles both LINK_CREATE and LINK_DELETE (unified filter)
  - Consumer skips LINK_DELETE for Bleve indexing, history recording, and embedding generation (same as LINK_CREATE)
---

## 2026-04-16 - US-102
- Implemented createOrModifyObject rule type (upsert behavior) for Actions
- Files changed:
  - pkg/actions/rules.go — Added editTypeUpsert internal constant + createOrModifyObject case in executeRule dispatch
  - pkg/actions/executor.go — Added ObjectExistenceChecker interface, SetObjectExistenceChecker method, resolveUpsertEdits (converts UPSERT→CREATE/MODIFY based on existence)
  - pkg/actions/actions_test.go — 9 new tests: parse, execute (with/without PK), executor (exists/not-exists/no-checker), collapse (resolved upsert then modify/delete)
  - pkg/actions/create_link_integration_test.go — Added bleveObjectChecker adapter + integration test: upsert non-existent→CREATE, process through consumer, upsert existing→MODIFY
- **Learnings for future iterations:**
  - Internal edit types (editTypeUpsert) should be package-level constants resolved in Executor.Prepare, not exported through funnel
  - ObjectExistenceChecker follows the same injection pattern as FunctionDispatcher: nil→graceful degradation (defaults to CREATE)
  - Bleve's idx.Document(pk) returns a doc if it exists — the simplest way to check object existence without a search query
  - When no PK is found in params, createOrModifyObject auto-generates one (same as createObject), making it always a CREATE
  - resolveUpsertEdits runs after resolveLinkEdits but before tagEditsAsUserSource, keeping the Prepare pipeline sequential
---

## 2026-04-16 - US-103
- Implemented interface-backed action rule types (createInterfaceObject, modifyInterfaceObject, deleteInterfaceObject)
- Files changed:
  - pkg/actions/rules.go — Added InterfaceAPIName field to Rule struct + 3 new rule cases + resolveObjectTypeParam and isInterfaceRule helpers
  - pkg/actions/executor.go — Added validateInterfaceRules step in Prepare pipeline: resolves Interface and ObjectType by API name, checks ListObjectTypeInterfaces for membership
  - pkg/actions/actions_test.go — 7 new tests: parse interface rules, execute create/modify/delete interface rules, missing objectType error, executor validates implementing type, executor rejects non-implementing type + interfaceAwareMockRepo
  - pkg/actions/create_link_integration_test.go — Integration test: createInterfaceObject with Building implementing GeoEntity → object created in Bleve + non-implementing type rejected
- **Learnings for future iterations:**
  - Interface-backed rules resolve objectType from params ("objectType" or "objectTypeApiName"), not from the Rule config
  - Validation requiring OMS repo access (interface membership) is done in Executor.Prepare post-ExecuteRules, keeping ExecuteRules pure
  - Rules and edits maintain 1:1 correspondence — this allows Executor.Prepare to cross-reference rule metadata with generated edits for validation
  - The extended mock pattern (interfaceAwareMockRepo embeds mockOmsRepo) works cleanly for adding interface resolution without touching other method stubs
---

## 2026-04-16 - US-104
- Implemented PrevState recording in ActionLog for undo support
- Files changed:
  - pkg/oms/models.go — Added PrevEdits field (json.RawMessage, JSONB) to ActionLog struct
  - migrations/000023_action_log_prev_edits.up.sql — ALTER TABLE adds prev_edits JSONB column
  - migrations/000023_action_log_prev_edits.down.sql — DROP COLUMN rollback
  - pkg/actions/executor.go — Added ObjectFetcher interface, SetObjectFetcher method, fetchPrevEdits step in Prepare pipeline (step 12), PrevEdits field on PreparedAction, PrevEdits serialization in CommitBatch
  - pkg/oms/pg_repository.go — InsertActionLog includes prev_edits column ($7)
  - pkg/actions/actions_test.go — 6 new tests: CREATE→null entry, MODIFY→previous properties, DELETE→full previous object, no fetcher→nil (backward compat), mixed edits parallel slice, link edits→null entry + mockObjectFetcher helper
- **Learnings for future iterations:**
  - PrevEdits is a parallel slice to Edits (1:1 index correspondence); CREATE and LINK edits get nil, MODIFY/DELETE get previous properties map
  - ObjectFetcher follows the same injection pattern as ObjectExistenceChecker: nil→graceful degradation (PrevEdits omitted from ActionLog)
  - The Bleve-based ObjectFetcher implementation (for integration tests) should use indexMgr.Search with DocIDQuery + Fields=["*"], not idx.Document() which returns internal types
  - findPrimaryKey uses convention: "primaryKey", "{objectType}Id", or "id" — test params must match these patterns
  - When testing multi-rule actions, all parameters must be declared in ParameterDef (validation rejects unknown params)
---

## 2026-04-16 - US-105
- Implemented Action Undo — Revert endpoint
- Files changed:
  - pkg/oms/repository.go — Added GetActionLog(id) and UpdateActionLogStatus(id, status) to Repository interface
  - pkg/oms/pg_repository.go — PG implementations for GetActionLog (SELECT by ID with PrevEdits) and UpdateActionLogStatus (UPDATE status)
  - pkg/actions/revert.go — New file: AlreadyRevertedError, RevertResult, Executor.Revert (load log → check status → generate reverse edits → publish → mark REVERTED), generateReverseEdits (CREATE↔DELETE, MODIFY w/ PrevState, LINK_CREATE↔LINK_DELETE)
  - pkg/actions/handlers.go — Added Revert handler (POST /actions/revert, 409 for double-revert, 404 for not found)
  - cmd/server/main.go — Route registration: POST /api/v2/ontologies/{ontologyApiName}/actions/revert
  - api/openapi.yaml + cmd/server/openapi_spec.yaml — OpenAPI spec for revert endpoint
  - pkg/actions/actions_test.go — 11 new tests: CREATE→DELETE, MODIFY→restore, DELETE→recreate, mixed edits, double-revert 409, not found, link edits reversal, publish batch, handler success/409/404 + mockPublisher
  - 8 mock files updated with GetActionLog/UpdateActionLogStatus stubs
- **Learnings for future iterations:**
  - Revert is separate from the Prepare→CommitBatch pipeline — it generates edits from ActionLog, not from rules
  - generateReverseEdits is a pure function (no side effects) — good for testing
  - AlreadyRevertedError follows the StaleObjectError pattern: sentinel type checked via errors.As in handler
  - Contract tests (TestContract_AllRoutesDocumented) enforce that every chi route has an OpenAPI spec entry — always update api/openapi.yaml AND copy to cmd/server/openapi_spec.yaml
  - When adding Repository methods, update ALL mock implementations (9+ files across tests) — use the Explore agent to find them all
---

## 2026-04-16 - US-106
- Implemented withinPolygon, intersectsPolygon, doesNotIntersectPolygon, doesNotIntersectBoundingBox operators
- Files changed:
  - pkg/oss/where/polygon.go — New file: PointInPolygon ray-casting algorithm for in-memory point-in-polygon test
  - pkg/oss/where/converter.go — Added PolygonQuery type, parsePolygonQuery, polygonToGeoShapeCoords helpers; 4 new convert* functions using bleve.NewGeoShapeQuery for polygon ops and BooleanQuery negation for doesNotIntersect* ops; added doesNotIntersectBoundingBox/doesNotIntersectPolygon to switch
  - pkg/oss/where/match.go — Added extractGeoPoint, matchPolygon (ray-casting via PointInPolygon), matchBoundingBox for in-memory evaluation; added withinPolygon/intersectsPolygon/doesNotIntersect* to MatchClause switch
  - pkg/oss/where/converter_test.go — 18 new tests: 5 PointInPolygon unit tests, setupGeoIndex with GeoShape field mapping + GeoJSON Point docs (NYC/LA/London), 3 withinPolygon tests (inside/outside/invalid), 2 intersectsPolygon tests (inside/noMatch), 1 doesNotIntersectPolygon test, 1 doesNotIntersectBoundingBox test, 3 MatchClause polygon tests (inside/outside/doesNotIntersect)
- **Learnings for future iterations:**
  - Bleve's GeoShapeQuery works on GeoShape-indexed fields only (NewGeoShapeFieldMapping), not on geopoint fields (NewGeoPointFieldMapping)
  - For GeoShape field testing, documents must be indexed as GeoJSON: {"type":"Point","coordinates":[lon,lat]}
  - GeoBoundingBoxQuery does NOT work on GeoShape fields — for doesNotIntersectBoundingBox, convert bbox to rectangular polygon and use GeoShapeQuery("polygon","intersects")
  - GeoJSON polygon coordinates use [lon, lat] order; PointInPolygon treats x=lon, y=lat
  - The PolygonQuery value wraps GeoJSON coordinates: {"polygon": [[[lon,lat],...]] } — outer ring is polygon[0]
  - Bleve NewGeoShapeQuery expects [][][][]float64; for a single polygon, wrap as [][][][]float64{polygonRings}
- Custom Bleve query types: implement query.Query interface (Searcher method) using internal searcher package (NewMultiPhraseSearcher, NewTermPrefixSearcher, etc.)
- PhrasePrefixQuery pattern: expand prefix via IndexReader.FieldDictPrefix, build [][]string for NewMultiPhraseSearcher with alternatives at last position
---

## 2026-04-16 - US-107
- Implemented containsAllTermsInOrderPrefixLastTerm (autocomplete) operator
- Files changed:
  - pkg/oss/where/phrase_prefix_query.go — New file: custom PhrasePrefixQuery Bleve query type implementing query.Query; uses FieldDictPrefix to expand last term prefix, delegates to NewMultiPhraseSearcher for positional adjacency enforcement
  - pkg/oss/where/converter.go — Added containsAllTermsInOrderPrefixLastTerm case to ConvertToBleveQuery switch + convertContainsAllTermsInOrderPrefixLastTerm function (single-term→PrefixQuery, multi-term→PhrasePrefixQuery)
  - pkg/oss/where/match.go — Added containsAllTermsInOrderPrefixLastTerm case to MatchClause switch + matchContainsAllTermsInOrderPrefixLastTerm function (sliding window with prefix match on last token)
  - pkg/oss/where/converter_test.go — 6 new tests: setupAutocompleteIndex helper, multi-term order test (John S → John Smith, not Smith John), single-term prefix test (J → John/Jane/James), empty input test, exact match test, 2 MatchClause in-memory tests
- **Learnings for future iterations:**
  - Bleve's public query API (MatchPhraseQuery+PrefixQuery AND) cannot enforce positional order between phrase and prefix — need a custom query type
  - Custom query type implements query.Query by providing Searcher(ctx, IndexReader, IndexMapping, SearcherOptions) → uses internal searcher package
  - PhrasePrefixQuery expands prefix via IndexReader.FieldDictPrefix(field, prefix) → iterates DictEntry to collect all matching index terms
  - NewMultiPhraseSearcher(ctx, reader, [][]string, fuzziness, autoFuzzy, field, boost, opts) enforces positional adjacency with per-position term alternatives
  - searcher.NewMatchNoneSearcher(indexReader) provides empty result set for no-match cases
  - In-memory matching (MatchClause) uses sliding window over tokenized field value to enforce sequential order + prefix on last token
---

## 2026-04-16 - US-108
- Implemented exactDistinct aggregation metric type using map-based deduplication
- Files changed:
  - pkg/oss/aggregation/engine.go — Updated AggregationSpec.Type comment to include "exactDistinct"
  - pkg/oss/aggregation/bleve_agg.go — Added computeExactDistinct function (scans docs, deduplicates via map[string]struct{}) + added "exactDistinct" case to computeMetrics switch
  - pkg/oss/aggregation/aggregation_test.go — 2 new tests: exactDistinct with 5 unique values returns exactly 5, exactDistinct vs approximateDistinct comparison
- **Learnings for future iterations:**
  - computeExactDistinct uses fmt.Sprint(val) to normalize field values (string or float64) into map keys for deduplication
  - Unlike approximateDistinct (facet-based, limited to N terms), exactDistinct scans all docs up to MaxDocScanSize and returns truncated=true when exceeding the scan limit
  - Adding a new metric type: add case to computeMetrics switch in bleve_agg.go, implement compute function, update AggregationSpec.Type comment in engine.go
---

## 2026-04-16 - US-109
- Implemented collectList aggregation metric type that collects field values into a list per group
- Files changed:
  - pkg/oss/aggregation/engine.go — Added MaxItems field (*int, default 100) to AggregationSpec; updated Type comment to include "collectList"
  - pkg/oss/aggregation/bleve_agg.go — Added computeCollectList function (scans docs, collects field values up to maxItems limit, returns []interface{}); added "collectList" case to computeMetrics switch
  - pkg/oss/aggregation/aggregation_test.go — 4 new tests: groupBy category + collectList returns value arrays per group, maxItems truncation, default maxItems (all values returned), string field collection
- **Learnings for future iterations:**
  - collectList returns []interface{} (not []float64) because it supports any field type (string, numeric, boolean)
  - MaxItems uses *int pointer pattern (nil=use default 100), consistent with MaxGroups on GroupBySpec
  - computeCollectList initializes empty slice (not nil) when no values found, ensuring JSON serializes as [] not null
  - The truncated flag from collectList reflects scanSize truncation (total > scanned), not maxItems truncation
---

## 2026-04-16 - US-110
- Implemented fuzzy search with configurable Levenshtein edit distance for SearchObjects
- Files changed:
  - pkg/oss/where/types.go — Added FuzzyConfig struct (MaxEdits int) and ConvertOptions struct (Fuzzy *FuzzyConfig)
  - pkg/oss/where/converter.go — Added ConvertToBleveQueryWithOpts function; resolveFuzziness helper (defaults to 1 when Fuzzy present but MaxEdits omitted); fuzzy-aware variants: convertEqFuzzy, convertContainsAllTermsFuzzy, convertContainsAnyTermFuzzy; opts-threading variants: convertAndWithOpts, convertOrWithOpts, convertNotWithOpts; existing ConvertToBleveQuery delegates to WithOpts(nil)
  - pkg/oss/service.go — Added Fuzzy *where.FuzzyConfig field to SearchObjectsRequest
  - pkg/oss/handlers.go — Added Fuzzy *where.FuzzyConfig to searchRequestBody; passes through to SearchObjectsRequest
  - pkg/oss/service_impl.go — Uses ConvertToBleveQueryWithOpts with fuzzy config from request
  - pkg/oss/where/converter_test.go — 6 new tests: containsAllTerms match/no-match, containsAnyTerm fuzzy, eq fuzzy, default maxEdits, fuzzy through and clause; added searchWithWhereOpts helper + setupFuzzyTestIndex
- **Learnings for future iterations:**
  - Bleve MatchQuery.SetFuzziness(n) applies analyzer first then does fuzzy matching with Levenshtein distance — simplest way to add fuzzy to text queries
  - Fuzzy options must thread through logical operators (and/or/not) via recursive ConvertToBleveQueryWithOpts calls
  - resolveFuzziness centralizes default logic: nil→0 (disabled), Fuzzy present but MaxEdits=0→1 (default), otherwise use MaxEdits
  - Non-text operators (numeric, boolean, geo, prefix, wildcard) are unaffected by fuzzy — only MatchQuery-based operators get SetFuzziness
---

## 2026-04-16 - US-111
- Implemented Value Type constraint enforcement at write time
- Files changed:
  - pkg/types/constraints.go — New file: Constraints struct (Regex, MinLength, MaxLength, Min, Max, Enum), ValidateConstraints(value, constraints) function, toFloat64 numeric coercion helper
  - pkg/types/constraints_test.go — New file: 19 tests covering regex pass/fail, minLength/maxLength pass/fail, min/max pass/fail, int value range, enum pass/fail, combined constraints, nil value passthrough, empty/null/invalid constraints
  - pkg/actions/executor.go — Added validateValueTypeConstraints method (step 12 in Prepare pipeline): resolves ObjectType → ListProperties → parses TypeConfig for valueTypeApiName → loads ValueType → validates constraints; imported pkg/types
  - pkg/actions/actions_test.go — Added valueTypeAwareMockRepo (extends mockOmsRepo with objectTypes/properties/valueTypes maps + overrides for GetObjectTypeByAPIName, ListProperties, GetValueTypeByAPIName); 6 new tests: create pass, create fail (regex), modify fail (min/max), no ValueType passes, enum violation, nil property passes
- **Learnings for future iterations:**
  - Property → ValueType link uses TypeConfig JSON ({"valueTypeApiName": "..."}) rather than a dedicated DB column — avoids migration
  - validateValueTypeConstraints runs after interface validation (step 11) but before fetchPrevEdits (step 13) in the Prepare pipeline
  - Constraints are flat optional fields on a single struct, not typed discriminated unions — simpler for JSON storage and extensible
  - ValidateConstraints passes nil values unconditionally (nullability is enforced by existing types.Validate, not constraints)
  - The valueTypeAwareMockRepo pattern follows the interfaceAwareMockRepo pattern: embed mockOmsRepo, add typed maps, override specific methods
  - Rules require PropertyBindings (map[string]PropertyBinding) to generate edit properties — tests must include bindings, not just parameter definitions
---

## 2026-04-16 - US-112
- Implemented ontology branches migration and model (Phase 2: Ontology Branching)
- Files changed:
  - migrations/000024_ontology_branches.up.sql — CREATE TABLE ontology_branches (id TEXT PK, ontology_rid FK, name, base_version, status CHECK open/merged/closed, created_by, timestamps) + UNIQUE(ontology_rid, name); CREATE TABLE ontology_branch_changes (id TEXT PK, branch_id FK, change_type CHECK ADDED/MODIFIED/DELETED, entity_type, entity_rid, before_state JSONB, after_state JSONB, created_at)
  - migrations/000024_ontology_branches.down.sql — DROP TABLE rollback
  - pkg/oms/models.go — Added OntologyBranch struct (ID, OntologyRID, Name, BaseVersion, Status, CreatedBy, timestamps) and BranchChange struct (ID, BranchID, ChangeType, EntityType, EntityRID, BeforeState, AfterState, CreatedAt)
  - pkg/oms/repository.go — Added 6 methods: CreateBranch, GetBranch, ListBranches, CloseBranch, CreateBranchChange, ListBranchChanges
  - pkg/oms/pg_repository.go — PG implementations for all 6 branch/change methods with proper error handling (ErrNotFound, wrapPGError, nilIfNoBytes for nullable JSONB)
  - pkg/oms/branch_test.go — 10 integration tests: Create, Get, Get_NotFound, ListBranches, CloseBranch, CloseBranch_NotFound, UniqueConstraint, BranchChange_Create, BranchChange_ListChanges, BranchChange_BeforeAfterState, BranchChange_NullBeforeState_ForAdded
  - pkg/oms/mock_branches_test.go — Branch stubs for mockRepo + noopRepo
  - pkg/actions/mock_branches_test.go — Branch stubs for mockOmsRepo
  - pkg/oss/mock_branches_test.go — Branch stubs for mockOmsRepo
  - pkg/links/mock_branches_test.go — Branch stubs for mockRepo
  - test/e2e/mock_branches_test.go — Branch stubs for inMemoryOmsRepo
  - test/northwind/mock_branches_test.go — Branch stubs for inMemoryOmsRepo
  - test/chinook/mock_branches_test.go — Branch stubs for inMemoryOmsRepo
  - pkg/index/rehydrate_test.go — Branch stubs added inline for stubRehydrateRepo
- **Learnings for future iterations:**
  - Adding Repository interface methods requires updating ~9 mock implementations across the codebase; use separate mock_branches_test.go files per package
  - Mocks that embed oms.Repository interface (fakeOmsRepo, us006StubOmsRepo, pr02StubOMSRepo, contractOmsRepo) don't need explicit stubs
  - Mocks that embed another concrete mock (interfaceAwareMockRepo, valueTypeAwareMockRepo, linkAwareMockRepo) inherit from parent — no stubs needed
  - Branch tables use TEXT primary keys (not UUID) — consistent with other tables using RID-style keys
  - UNIQUE(ontology_rid, name) prevents duplicate branch names per ontology; wrapPGError converts PG unique violation to ErrDuplicate
---
