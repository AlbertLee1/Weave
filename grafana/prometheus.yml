global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: weave
    metrics_path: /metrics
    static_configs:
      - targets: ["weave:9117"]
        labels:
          service: weave

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
