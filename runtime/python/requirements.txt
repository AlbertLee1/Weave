fastapi>=0.115,<1.0
pydantic>=2.5,<3.0
uvicorn[standard]>=0.30,<1.0
scikit-learn>=1.3,<2.0

# Test dependencies are pinned loosely here so the same file can be
# used for both ``pip install -r requirements.txt`` (smoke-test the
# runtime locally) and the CI loop (``pytest`` against this tree).
pytest>=8.0
httpx>=0.27
