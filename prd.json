{
  "project": "Weave",
  "branchName": "ralph/osv2-full-parity-v2",
  "description": "Palantir OSv2 Full Parity V2 — 企业级认证/RLS/审计/GDPR、AIP 深化、Function 系统、OMS 高级元数据、OSS 高级查询、Actions 进阶、数据管道+Lineage、前端全量 UX、SDK 生态，达到 ~95% OSv2 功能深度",
  "userStories": [
    {
      "id": "US-200",
      "title": "Derived Properties 框架",
      "description": "作为本体建模者，我想定义基于公式的派生属性，无需存储即可实时计算。",
      "acceptanceCriteria": [
        "在 properties 表添加 derived BOOLEAN 和 formula TEXT 字段（migration）",
        "封装 Goja JS 表达式引擎到 pkg/types/formula 包",
        "沙箱约束：100ms 超时、无 I/O、只读对象属性",
        "单元测试覆盖 10+ 表达式（算术、条件、字符串拼接）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": "pkg/types/formula Goja sandbox + migration 000032 + 20+ unit tests"
    },
    {
      "id": "US-201",
      "title": "Derived Properties 查询集成",
      "description": "作为 API 调用方，我想在 loadObjectSet 和 withProperties 中返回派生属性值。",
      "acceptanceCriteria": [
        "pkg/oss/executor 加载对象后对每个 object 运行 formula",
        "withProperties 支持声明 derived property",
        "E2E 测试：定义 fullName = firstName + ' ' + lastName 并查询",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "DerivedPropertyDef.Formula + metric=formula; executor batch-loads base fields once and evaluates; LoadObjects E2E verified"
    },
    {
      "id": "US-202",
      "title": "Computed Properties 聚合型",
      "description": "作为数据分析师，我想定义基于关联对象聚合的实时属性（如 order_count）。",
      "acceptanceCriteria": [
        "新增 computed_properties 表（source_link_rid、aggregation、cache_ttl）",
        "查询时 lazy 触发聚合，默认 60s TTL 缓存",
        "API 返回含 _computed_at 时戳",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "migration 000033 computed_properties table; pkg/oss/computed.Resolver with 60s-default TTL cache + injectable clock; Value.ComputedAt surfaces _computed_at; ComputedPropertyStore narrow interface avoids bloating oms.Repository"
    },
    {
      "id": "US-203",
      "title": "Media Properties 存储层",
      "description": "作为本体建模者，我想声明 Media 类型属性以挂载文件/图片。",
      "acceptanceCriteria": [
        "支持 baseType='media' 并新增 media_assets 表（rid、mime、size、sha256、path、created_by）",
        "本地 FS 存储适配器，路径 data/media/{realm}/{yyyy}/{mm}/{sha256}",
        "相同 sha256 去重只存一份",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "migration 000034 media_assets table; pkg/media.Store content-addresses files at {realm}/{yyyy}/{mm}/{sha256} with sha256 dedup (shared blob, distinct RIDs); BaseType 'media' added as #22; MediaAssetStore narrow interface on PGRepository keeps Repository mocks untouched"
    },
    {
      "id": "US-204",
      "title": "Media 上传 API",
      "description": "作为客户端，我想上传文件并获得 mediaRID 以关联到 object。",
      "acceptanceCriteria": [
        "POST /api/v2/media multipart 上传（10MB 单文件上限）",
        "GET /api/v2/media/{rid} 下载带 Content-Disposition 头",
        "DELETE /api/v2/media/{rid} 检查引用计数后删除",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "pkg/media.Handler exposes POST/GET/DELETE /api/v2/media{/rid}; multipart 'file' field, optional 'realm', http.MaxBytesReader caps at DefaultMaxUploadBytes=10MiB; download sets Content-Disposition with both filename= and RFC5987 filename*=; delete only reclaims the blob when CountBySHA256 hits zero (dedup-safe); wired through ServerDeps.MediaStore + MediaCatalog with the uncached *PGRepository as the catalog backing"
    },
    {
      "id": "US-205",
      "title": "Media 前端上传 UI",
      "description": "作为编辑用户，我想在 ObjectDetail 中拖拽上传图片/文件。",
      "acceptanceCriteria": [
        "集成 react-dropzone 支持多文件拖拽",
        "显示上传进度条和图片缩略图预览",
        "删除操作有确认 dialog",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 6,
      "passes": true,
      "notes": "web/src/api/media.ts (XHR upload w/ progress) + useMedia hooks + MediaUploadZone wires react-dropzone + Modal confirm; ObjectDetail branches baseType='media' (array|scalar); 5 RTL tests cover placeholder/upload/progress/thumbnail/delete-confirm; vitest 468/468 pass; dev-browser skill not available in this harness — manual browser verification still pending"
    },
    {
      "id": "US-206",
      "title": "Union Types 支持",
      "description": "作为本体建模者，我想声明某属性可以是多种类型之一（如 contact: Email | Phone）。",
      "acceptanceCriteria": [
        "typeConfig 支持 { type: 'union', variants: [...] }",
        "JSON 序列化含 __type discriminator 字段",
        "Validation 逐 variant 尝试匹配",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Union as BaseType #23; DataType.Variants []DataType carries variant set (round-trips via existing Property.TypeConfig JSONB); Validate honors __type discriminator when present, otherwise tries variants in declared order; Coerce wraps result as {__type, value} for unambiguous round-trip; 15 new unit tests in pkg/types/union_test.go; sdkgen ts/python/go maps Union → unknown/Any/interface{}"
    },
    {
      "id": "US-207",
      "title": "Struct 嵌套属性编辑器",
      "description": "作为 UI 用户，我想在 Admin 中递归编辑 Struct 嵌套字段定义。",
      "acceptanceCriteria": [
        "递归式 field editor 支持添加/删除/重排子字段",
        "类型选择器支持嵌套 struct",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 8,
      "passes": true,
      "notes": "PropertiesEditor.StructFieldsEditor now self-recurses: when a field's type is set to 'struct' a nested editor renders inline (data-testid=struct-fields-{path}, dot-separated path like '0.1'); BASE_TYPES no longer filtered so nested struct is selectable; add/remove/up/down controls per row, up/down disabled on boundaries; serializeStructFields produces {fields:[{name,type,fields?}]} matching the existing {fields:[...]} wire shape; isStructTreeValid recursively enforces 'each struct must have >=1 non-empty-named child'; 6 new RTL tests cover nested-struct happy path, empty-nested-struct submit block, reorder, move button disabled states, nested remove; web vitest 473/473 green, tsc -b clean, vite build clean"
    },
    {
      "id": "US-208",
      "title": "Enum/Categorical 约束执行",
      "description": "作为系统，我要在 write 路径强制 Enum 约束。",
      "acceptanceCriteria": [
        "EditBatch 处理路径拦截非法值，返回 422 WEAVE_VALIDATION_ENUM",
        "错误响应含 allowedValues 列表",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "types.EnumViolationError typed err carries Value+AllowedValues; apierror.NewValidationEnum -> WEAVE_VALIDATION_ENUM (HTTP 422); executor.validateValueTypeConstraints unwraps via errors.As and emits APIError with property/value/allowedValues/objectType params; BatchError gains Cause+Unwrap so the typed err survives through ApplyBatchAtomic; handlers.Apply / ApplyWithOverrides / asBatchError check typedAPIError() before falling back to ActionFailed; tests: pkg/types Enum_TypedError + Enum_TypedError_MixedTypes; pkg/actions Executor_EnumViolation_ReturnsTypedAPIError422 + Handler_Apply_EnumViolation_422 + Handler_ApplyBatch_EnumViolation_422; legacy ValueTypeConstraint_EnumViolation rewritten to assert on the typed APIError contract"
    },
    {
      "id": "US-209",
      "title": "Bidirectional Links",
      "description": "作为本体建模者，我想声明 LinkType 的 inverse，使双向 searchAround 对称。",
      "acceptanceCriteria": [
        "link_types 表新增 inverse_link_rid TEXT 字段",
        "添加约束：inverse 对端 objectType 必须匹配",
        "searchAround 支持正向/反向方向参数",
        "Test：创建 A↔B 双向，正反向查询结果对称",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "migration 000035 ALTER TABLE link_types ADD COLUMN inverse_link_rid TEXT (nullable+partial-idx); LinkType.InverseLinkRID + ToWireJSON surfaces inverseLinkRid; PGRepository CRUD (Create/Get/List/ListOutgoing/ListIncoming/GetByAPIName/Update) threads the column via NULLIF($N,'') + COALESCE(col,''); CreateLinkTypeRequest/UpdateLinkTypeRequest accept inverseLinkRid (pointer on update for 3-state {omit, clear, set}); new validateInverseLinkPair helper enforces partner exists + same ontology + partner.(source,target) == this.(target,source) + self-reference rejected, returning 404 InverseLinkTypeNotFound or 400 InvalidParameter:inverseLinkRid with expected/partner endpoint hints; searchAround symmetry leverages existing links.Direction plumbing — setupEmployeeDept fixture now cross-references inverse_link_rid and 2 new tests TestBidirectionalLinks_SymmetricTraversal + TestBidirectionalLinks_RoundTripReturnsSource assert forward(A)==reverse(B) and emp1 round-trip; 7 new admin-handler tests (Create/Update happy, NotFound, EndpointMismatch w/ param hints, CrossOntology, ClearByEmptyString, Update mismatch); models_test asserts wire omitempty; short test suite green across pkg/internal/test/cmd"
    },
    {
      "id": "US-210",
      "title": "Link Properties（M2M 边属性）",
      "description": "作为建模者，我想在多对多链接上存储边属性（如 membership.role）。",
      "acceptanceCriteria": [
        "新增 link_properties 表和 link_values 存储",
        "CRUD API：PUT /api/v2/ontologies/{o}/links/{rid}/properties",
        "searchAround 返回结果含 edge properties",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-211",
      "title": "Composite Primary Keys",
      "description": "作为建模者，我想以多字段组合作为主键（例：orderId + lineNumber）。",
      "acceptanceCriteria": [
        "object_types.primary_key 字段改为 JSONB 数组",
        "路由支持复合 PK：/objects/{objectType}/{key1}:{key2}",
        "迁移保持向后兼容（单 PK 仍能用）",
        "Typecheck passes",
        "Tests pass（创建+get+update+delete 全流程）"
      ],
      "priority": 12,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-212",
      "title": "Object Type Inheritance",
      "description": "作为建模者，我想让 ObjectType 继承父类型的属性和链接。",
      "acceptanceCriteria": [
        "object_types 表新增 extends_rid TEXT 字段",
        "解析时 merge parent properties/links，子类可重写",
        "Diamond inheritance 检测并报错",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "migration 000038 adds nullable extends_rid + partial index; pkg/oms.ResolveInheritedObjectType walks chain and merges properties/outgoing-links (child overrides on api_name); ValidateInheritanceCandidate cycle guard; admin Create/Update accept extendsRid (UpdateObjectTypeRequest.ExtendsRID *string for tri-state clear) with same-ontology + cycle validation; new GET /api/v2/ontologies/{o}/objectTypes/{ot}/resolved endpoint returns merged view + extendsChain; import handler pre-allocates new RIDs to remap parent pointers regardless of order"
    },
    {
      "id": "US-213",
      "title": "Breaking Change Detection",
      "description": "作为治理者，我想在合并 proposal 前看到 breaking changes 风险报告。",
      "acceptanceCriteria": [
        "新增 GET /api/v2/ontologies/{o}/branches/{b}/breaking-changes 端点",
        "检测类型：property 删除、type 变窄、required 新增、PK 变更",
        "返回受影响的 ActionTypes 和 SavedObjectSets 列表",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 14,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-214",
      "title": "Interface 方法签名",
      "description": "作为建模者，我想为 Interface 声明方法签名，让实现 ObjectType 绑定对应 Actions。",
      "acceptanceCriteria": [
        "新增 interface_methods 表（name、params、returns）",
        "ActionType 可声明 implementsMethod 字段",
        "多态查询：通过 Interface 方法调用任一实现",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 15,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-215",
      "title": "Function Registry",
      "description": "作为开发者，我想定义和注册可复用的纯计算函数。",
      "acceptanceCriteria": [
        "新增 functions 表（rid、name、version、signature、body、runtime=goja|http）",
        "CRUD API /api/v2/ontologies/{o}/functions",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 16,
      "passes": true,
      "notes": "migration 000040 ALTER TABLE functions adds signature JSONB DEFAULT '{}' + runtime TEXT DEFAULT 'goja' with CHECK (goja|http); Function model gains Signature json.RawMessage + Runtime string; Function.Validate + ValidateFunctionSignature enforce shape (params array, per-param name, returns.type) at write time; PG repo round-trips signature via normalise/decode helpers so empty rows surface as nil JSON; CreateFunctionRequest accepts runtime+signature; UpdateFunctionRequest uses *string / *json.RawMessage for tri-state preserve/replace; openapi.yaml + embedded copy carry the new fields; 14 new unit + handler tests cover defaults, accept/reject paths, signature shape; legacy US-089 CRUD + routes untouched"
    },
    {
      "id": "US-216",
      "title": "Function Signature 类型系统",
      "description": "作为调用方，我要在调用前根据 signature 校验参数。",
      "acceptanceCriteria": [
        "Signature JSON schema: { params: [{name,type,required,default}], returns: {type} }",
        "运行时 validator 拦截非法参数",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 17,
      "passes": true,
      "notes": "ParseFunctionSignature + ValidateAndCoerceFunctionParams in pkg/oms/function.go (typed FunctionParamError carries Code: missing_required/type_mismatch/unknown_parameter/default_invalid). New POST /functions/{rid}/execute endpoint validates+coerces params before dispatching to the optional FunctionExecutor; degraded-mode (no executor wired) returns 503 with the coerced parameter map echoed. 14 validator tests + 10 endpoint tests."
    },
    {
      "id": "US-217",
      "title": "Function 版本管理",
      "description": "作为调用方，我想按 version 引用特定函数实现以保证稳定。",
      "acceptanceCriteria": [
        "版本号用 semver，新版本创建时不覆盖旧版本",
        "调用时指定 functionRid@version，默认 latest",
        "列表 API 按 version 排序",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 18,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-218",
      "title": "Function 执行配额与超时",
      "description": "作为平台运维，我要防止失控函数耗尽资源。",
      "acceptanceCriteria": [
        "单次执行限制：5s CPU 超时、128MB 内存上限",
        "每分钟调用配额（按 realm）",
        "超限返回 429 或 408",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 19,
      "passes": true,
      "notes": "pkg/functions.DefaultConfig bumped to 5s/128MiB; wrapGojaError translates goja InterruptedError into fnerrors.ErrTimeout / ErrMemoryLimit. New pkg/functions/quota sliding-window Limiter (per-key, default 60/min). OMSHandler.SetFunctionQuotaLimiter + 5s context.WithTimeout guard in ExecuteFunction: quota→429 FunctionQuotaExceeded (realm keyed), timeout→408 FunctionExecutionTimeout, memory→429 FunctionMemoryLimitExceeded. apierror.NewRequestTimeout (DEADLINE_EXCEEDED/408). 9 limiter tests + 4 runtime-sentinel tests + 6 handler tests covering all three error paths."
    },
    {
      "id": "US-219",
      "title": "Function Result Streaming",
      "description": "作为客户端，我想流式接收大结果。",
      "acceptanceCriteria": [
        "POST /functions/{rid}/execute?stream=1 返回 NDJSON 流",
        "SDK 支持 iterator 消费",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 20,
      "passes": true,
      "notes": "writeFunctionStream emits {item}/{error} NDJSON lines with chunked flush; pre-execution failures still surface as regular HTTP errors. Python SDK ships FunctionsAPI.execute_stream() — a generator that raises WeaveError on terminal error lines. 9 new SDK tests + 9 new Go handler tests."
    },
    {
      "id": "US-220",
      "title": "Function-to-Function 调用",
      "description": "作为 function 作者，我想在 function 内调用其它 function。",
      "acceptanceCriteria": [
        "Goja 沙箱注入 weave.callFunction(rid, params) API",
        "递归深度上限 8，检测循环调用",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 21,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-221",
      "title": "Function 结果缓存",
      "description": "作为平台，我要对纯函数结果缓存以降低重复成本。",
      "acceptanceCriteria": [
        "Function 可声明 pure=true 开启缓存",
        "LRU 缓存 10k entry，TTL 5 分钟",
        "Cache key: rid@version + hash(params)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 22,
      "passes": true,
      "notes": "migration 000042 adds functions.pure (default false); pkg/functions/cache.NewDefault = 10k LRU + 5min TTL keyed by rid@version + sha256(canonical-params); narrow oms.FunctionResultCache interface + OMSHandler.SetFunctionResultCache; ExecuteFunction gates cache lookup on fn.Pure && !streaming, emits cached:true on hit; errors never cached; param map insertion order canonicalised so {a,b}=={b,a}; 22 unit tests + 8 handler tests"
    },
    {
      "id": "US-222",
      "title": "Function-backed Actions",
      "description": "作为建模者，我想让 Action 通过调用 Function 实现业务逻辑。",
      "acceptanceCriteria": [
        "ActionType 新增 executeFunction 规则类型",
        "Function 返回 EditBatch 由 Action executor 应用",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 23,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-223",
      "title": "Time-travel Queries (as-of)",
      "description": "作为分析师，我想查询某个历史时间点的 object 快照。",
      "acceptanceCriteria": [
        "object_history 表添加 valid_from、valid_to 字段",
        "loadObjectSet 支持 ?asOf=2026-01-01T00:00:00Z 参数",
        "扫描 history 按时间过滤返回当时状态",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 24,
      "passes": true,
      "notes": "migration 000043 adds valid_from/valid_to + backfill window; InsertObjectHistory stamps valid_from = recorded_at and closes prior version's valid_to in the same call so [valid_from, valid_to) intervals are non-overlapping. PGRepository.SnapshotObjectsAt scans the half-open interval and skips DELETE tombstones. objectset.HistorySnapshotProvider narrow interface + SetHistorySnapshotProvider hook on the Handler; LoadObjects honours `?asOf=<RFC3339>` for base ObjectSets only (composite types get TimeTravelUnsupportedObjectSet 400). cmd/server wires the historySnapshotAdapter via the uncached *PGRepository. 8 unit tests in handler_asof_test.go + 3 integration tests in object_history_asof_test.go."
    },
    {
      "id": "US-224",
      "title": "ObjectSet Versioned Snapshots",
      "description": "作为分析师，我想保存 ObjectSet 快照以供后续对比。",
      "acceptanceCriteria": [
        "新增 object_set_snapshots 表（定义+物化 RID 列表+时戳）",
        "POST /objectSets/{rid}/snapshot 创建快照",
        "GET /snapshots/{rid} 返回冻结结果",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 25,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-225",
      "title": "Random Sampling",
      "description": "作为探索用户，我想从 ObjectSet 随机取样 N 条。",
      "acceptanceCriteria": [
        "ObjectSetDefinition 新类型 sample: {size, seed}",
        "实现用 ORDER BY random() 或 reservoir sampling",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 26,
      "passes": true,
      "notes": "Definition 新增 type=sample + Size *int + Seed *int64 (pointer shape lets absent Seed stay distinguishable from zero); executor uses Algorithm R reservoir sampling with math/rand.NewSource(seed || wall-clock). Size<=0 rejected at Validate, Size>=len(inner) returns the inner slice verbatim, empty inner returns empty without error. Truncated flag propagated from inner. 12 unit tests cover validate / parse / determinism / seed divergence / empty-inner / large-size. OpenAPI + embedded spec extended with type=sample + size + seed."
    },
    {
      "id": "US-226",
      "title": "Cross-hop searchAround",
      "description": "作为用户，我想遍历 3+ 跳链接路径。",
      "acceptanceCriteria": [
        "searchAround 接受 path: [link1, link2, link3] 多段参数",
        "中间 ObjectType 约束校验",
        "Typecheck passes",
        "Tests pass（3 跳完整遍历）"
      ],
      "priority": 27,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-227",
      "title": "Nested Aggregations",
      "description": "作为分析师，我想做 group by A 再 group by B 的多层聚合。",
      "acceptanceCriteria": [
        "Aggregation definition 支持 subAggregations 数组",
        "Bleve 分组后再次聚合",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 28,
      "passes": true,
      "notes": "AggregationRequest.SubAggregations + named per-bucket recursion in pkg/oss/aggregation; OpenAPI schema + ObjectSet aggregate handler wired; 5 unit tests."
    },
    {
      "id": "US-228",
      "title": "HyperLogLog Distinct",
      "description": "作为分析师，我想对大基数字段做近似 distinct。",
      "acceptanceCriteria": [
        "集成 github.com/axiomhq/hyperloglog",
        "approximateDistinct(precision=14) 聚合器",
        "误差控制在 <= 1%",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 29,
      "passes": true,
      "notes": "axiomhq/hyperloglog replaces Bleve-facet computeDistinct; AggregationSpec.Precision *int (default 14, range 4..18); sparse mode keeps small-cardinality exact; high-cardinality error verified <1% on 5k-distinct dataset; OpenAPI exposes precision with 4..18 bounds."
    },
    {
      "id": "US-229",
      "title": "Aggregation Having 子句",
      "description": "作为分析师，我想对聚合结果再过滤。",
      "acceptanceCriteria": [
        "Aggregation 新增 having: {metric, op, value} 字段",
        "实现后过滤执行",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 30,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-230",
      "title": "Geohash / H3 空间聚合",
      "description": "作为地图分析师，我想按地理网格聚合对象。",
      "acceptanceCriteria": [
        "集成 uber/h3-go 库",
        "geoProperty 分组器接受 precision 1-15 参数",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 31,
      "passes": true,
      "notes": "geohash groupBy type (pure Go mmcloughlin/geohash) w/ precision 1-12 — uber/h3-go was ruled out to preserve CGO_ENABLED=0 Dockerfile invariant; decoder tolerates []float64 / []interface{} / map{lat,lon|lng|latitude,longitude} shapes; 13 new tests in pkg/oss/aggregation/geohash_test.go cover default+custom precision, library-hash equality, out-of-range rejection, empty index, missing field, per-cell metrics, decoder shape tolerance; OpenAPI schema gets 'geohash' enum value + precision (1..12) field"
    },
    {
      "id": "US-231",
      "title": "Cube / Rollup",
      "description": "作为分析师，我想一次查询得到多维 cube 结果。",
      "acceptanceCriteria": [
        "groupBy 支持 cube=true 返回所有子集组合",
        "rollup=true 返回层级汇总",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 32,
      "passes": true,
      "notes": "AggregationRequest + AggregateObjectSetRequest Cube/Rollup; ExpandGroupByCombinations shared by Bleve + derived paths"
    },
    {
      "id": "US-232",
      "title": "Fuzzy Search",
      "description": "作为用户，我想容错匹配（Kafka 匹配 Kafca）。",
      "acceptanceCriteria": [
        "接入 Bleve FuzzyQuery，fuzziness 支持 0/1/2",
        "API 参数：search?fuzziness=1",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 33,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-233",
      "title": "Phrase Search + Slop",
      "description": "作为用户，我想查找带词间距的短语。",
      "acceptanceCriteria": [
        "解析 'quick fox'~2 slop 语法",
        "使用 Bleve PhraseQuery 执行",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 34,
      "passes": true,
      "notes": "pkg/oss/where.PhraseSlopQuery (custom Bleve query) + ParsePhraseSlopString accepts {phrase,slop} or Lucene-style \"quick fox\"~2; slop clamped to [0,32]; slop=0 multi-term delegates to bleve.MatchPhraseQuery, slop>0 uses custom position-walk searcher; 19 where unit tests + 4 handler E2E tests"
    },
    {
      "id": "US-234",
      "title": "Regex Search",
      "description": "作为高级用户，我想用正则搜索字段。",
      "acceptanceCriteria": [
        "API 参数：search?regex=^A.*",
        "执行时间上限 500ms 防 catastrophic backtracking",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 35,
      "passes": true,
      "notes": "pkg/oss/where.convertRegex (Bleve RegexpQuery + regexp/syntax Perl pre-validation, MaxRegexPatternLength=1024, lowercased to match analyser); index.Manager.SearchInContext + service-layer ctx.WithTimeout(RegexQueryTimeout=500ms) gated by where.HasRegexClause walk; handler ?regex=field:pattern shorthand splits on first colon and replaces body.where (mirrors fuzziness override). 8 where unit tests + 7 handler E2E tests; OpenAPI + embedded spec synced (regex param + operator listed in where description)."
    },
    {
      "id": "US-235",
      "title": "Search Highlighting",
      "description": "作为用户，我想看到匹配词在结果中的高亮。",
      "acceptanceCriteria": [
        "搜索响应含 _highlights: {field: [snippet...]} 字段",
        "用 <mark> 包裹匹配词",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 36,
      "passes": true,
      "notes": "WireObject gains Highlights map[string][]string rendered as `_highlights` (omitted when empty). SearchObjectsRequest.Highlight opts in via body `highlight: {style, fields}` or query `?highlight=true|false|field1,field2`. Bleve's html-style highlighter wraps matches with <mark>…</mark>. 8 unit/handler tests + round-trip assertion; contract trio green."
    },
    {
      "id": "US-236",
      "title": "Faceted Search",
      "description": "作为搜索用户，我想看到按类型/所有者/日期分面的结果计数。",
      "acceptanceCriteria": [
        "API 参数：search?facets=objectType,owner,createdAt",
        "响应含 facets: {field: [{value, count}]} 结构",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 37,
      "passes": true,
      "notes": "facets []string on searchRequestBody + SearchObjectsRequest; ?facets=a,b query param overrides body with InvalidFacets 400 on empty/whitespace; service adds bleve.NewFacetRequest per de-duped field; ObjectPage.Facets map[string][]FacetBucket with omitempty so unfaceted responses stay byte-identical; unknown / non-indexed fields surface empty bucket list so SDK key set stays stable"
    },
    {
      "id": "US-237",
      "title": "CJK 分词支持",
      "description": "作为中文用户，我想中文全文搜索按词而非字切分。",
      "acceptanceCriteria": [
        "集成 gojieba 或 Bleve CJK analyzer",
        "ObjectType 可配置 analyzer 字段",
        "Typecheck passes",
        "Tests pass（'中国银行' 能搜到 '中国银行总部'）"
      ],
      "priority": 38,
      "passes": true,
      "notes": "AnalyzerCJK constant + side-effect import of bleve/v2/analysis/lang/cjk; fieldMappingFor routes text + analyzer=cjk to Bleve's bigram CJK analyzer (unicode tokenizer + width filter + lowercase + bigram). PRD example '中国银行' matches '中国银行总部' via the new TestBuildMappingCJKAnalyzer. ObjectType opts in via existing TypeConfig.analyzer hint — no schema/migration change needed. Pure-Go (gojieba ruled out by CGO_ENABLED=0 invariant)."
    },
    {
      "id": "US-238",
      "title": "Batch Actions 原子事务",
      "description": "作为客户端，我想批量 actions 要么全成功要么全失败。",
      "acceptanceCriteria": [
        "POST /actions/applyBatch?atomic=true 支持原子模式",
        "PG 事务包裹，失败时 rollback 所有编辑",
        "NATS 发布在 commit 后",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 39,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-239",
      "title": "Saga / Compensating Actions",
      "description": "作为建模者，我想为 Action 声明补偿逻辑，长流程失败时自动回滚。",
      "acceptanceCriteria": [
        "ActionType 新增 compensate: actionRid 字段",
        "Saga coordinator 按逆序执行补偿",
        "Typecheck passes",
        "Tests pass（两步 saga，第二步失败则第一步补偿）"
      ],
      "priority": 40,
      "passes": true,
      "notes": "migration 000045 action_types.compensate_action_rid + partial index; ActionType.CompensateActionRID with NULLIF/COALESCE; admin handlers validate self-reference + same-ontology; pkg/actions.ApplyBatchSaga prepares in order, on failure runs compensators in reverse via resolveActionType + filtered-params Prepare; POST applyBatch?saga=true routes to coordinator; 4 saga_test.go cases + handler E2E"
    },
    {
      "id": "US-240",
      "title": "Async Long-Running Actions",
      "description": "作为客户端，我想启动长任务并轮询进度。",
      "acceptanceCriteria": [
        "POST /actions/apply?async=true 返回 jobId",
        "新增 action_jobs 表（status、progress 0-100、result）",
        "GET /actions/jobs/{id} 查询进度",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 41,
      "passes": true,
      "notes": "migration 000046 action_jobs table with PENDING/RUNNING/SUCCEEDED/FAILED status enum + progress 0-100 CHECK; pkg/actions.ActionJob + ActionJobStore narrow interface (mirrors MediaAssetStore pattern); Executor.SetActionJobStore setter-injected hook; POST /actions/{action}/apply?async=true spawns detached goroutine using copyAuthContext so ctx survives response flush; 202 Accepted {jobId, status} envelope; GET /actions/jobs/{jobId} returns full ActionJob row (404 via oms.ErrNotFound); degraded-mode fallback to sync when no store wired; pgActionJobStore lives in cmd/server (not pkg/oms) to keep dep direction clean; OpenAPI schemas AsyncApplyActionResponse + ActionJob + async path; 5 handler tests cover 202 flow, poll success, 404, failure surfaced on job, degraded-mode sync fallback"
    },
    {
      "id": "US-241",
      "title": "Action Progress 追踪",
      "description": "作为长任务 Action，我要在执行中上报进度。",
      "acceptanceCriteria": [
        "Goja 沙箱注入 weave.reportProgress(percent, msg) 函数",
        "NATS 发布进度事件",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 42,
      "passes": true,
      "notes": "pkg/functions ProgressReporter narrow interface + WithProgressReporter/ProgressReporterFromContext ctx helpers; registerWeaveShim always registers the weave global (previously absent when FunctionCaller nil) and attaches reportProgress(percent, message?) that reads reporter from ctx — clamps percent to [0,100], truncates floats, treats message as optional; weave.callFunction remains conditional on FunctionCaller; pkg/actions ProgressPublisher narrow interface + Executor.SetProgressPublisher/ProgressPublisher hooks + ProgressEvent/ProgressSubject wire shape (actions.progress.<jobId>); jobProgressReporter writes UpdateActionJob(Progress) AND PublishProgress fanout, both best-effort (log + continue on failure); cmd/server natsActionProgressPublisher wraps nats.Conn.Publish, wired in main when NATSConn != nil (degraded mode skips silently); handlers.runAsyncApply installs reporter on ctx via functions.WithProgressReporter before exec.Apply so Goja dispatch propagates it; 6 shim tests (happy path, clamp, no-reporter no-op, optional message, coexists with callFunction, absent-from-context) + 4 reporter tests (subject format, store+publish, publisher error non-propagation, nil publisher still updates store) + 1 end-to-end async-goja progress flow test"
    },
    {
      "id": "US-242",
      "title": "Approval Workflow 后端",
      "description": "作为治理者，我想让敏感 Action 需要审批。",
      "acceptanceCriteria": [
        "ActionType 新增 requiresApproval: bool 和 approvers: [roleRid] 字段",
        "新增 action_approvals 表",
        "API：POST /actions/approvals/{id}/approve 和 /reject",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 43,
      "passes": true,
      "notes": "ActionType gains RequiresApproval bool + Approvers []string (JSONB in migration 000047); pkg/actions ActionApproval + narrow ActionApprovalStore interface + PG adapter in cmd/server/action_approval_store.go; Apply handler routes to serveApprovalEnqueue (202 PendingApproval) when RequiresApproval && store wired, degrades to sync when store absent; ApproveAction/RejectAction endpoints with user.ID-or-user.Roles ∩ approvers auth check, PENDING-only transitions (409 on re-approve); 6 tests cover enqueue + degraded mode + approve/reject happy-path + forbidden + conflict + 404; OpenAPI + embedded spec updated with 4 schemas (PendingApprovalResponse/ActionApproval/ApprovalReviewRequest/ApprovalReviewResponse) + 2 paths + requiresApproval/approvers fields on ActionType"
    },
    {
      "id": "US-243",
      "title": "Approval Queue UI",
      "description": "作为审批者，我想看到待我审批的 Actions 列表并批准/拒绝。",
      "acceptanceCriteria": [
        "新增 /approvals 页面，显示待审核列表和参数详情",
        "批准/拒绝按钮含备注输入",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 44,
      "passes": true,
      "notes": "Backend: new GET /api/v2/ontologies/{o}/actions/approvals (handler ListApprovals with status/mine/limit filters; post-store OR-of-identity filter via userCanApprove) + ActionApprovalListFilter + PG jsonb ?? operator for approver membership + 4 unit tests; Apply handler + approve/reject unchanged. Frontend: new web/src/api/approvals.ts + useApprovals hooks + ApprovalsPage (/approvals and /approvals/:ontology) with status tabs, mine toggle, parameters <details>, Approve/Reject buttons opening Modal with reason textarea; sidebar 'Approvals' link when ontology active. 6 RTL tests cover list/modal/submit/empty/filter-switch/error. dev-browser skill not available — manual browser verification still pending."
    },
    {
      "id": "US-244",
      "title": "Action DSL 控制流",
      "description": "作为建模者，我想在 ActionRule 中用条件、循环。",
      "acceptanceCriteria": [
        "新增 rule 类型：if、foreach、switch",
        "嵌套深度上限 5",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 45,
      "passes": true,
      "notes": "Extended pkg/actions.Rule with control-flow fields (Condition/Then/Else, ItemsParameter/ItemVariable/IndexVariable/Rules, On/Cases/Default) + new Condition + SwitchCase types. ExecuteRules is now a depth-tracking recursive helper (MaxRuleNestingDepth=5); control-flow bodies reuse the same depth counter so any mix of if/foreach/switch is capped. if supports eq/ne/gt/gte/lt/lte/in/notIn/exists/notExists/truthy/falsy leaf ops + and/or/not logical ops via Condition.All/Any/Not. foreach iterates []interface{} (clean error on non-slice / missing param) and exposes itemVariable (+ optional indexVariable) to child rules via a shallow-cloned params map. switch matches on params[On] against each SwitchCase.When with numeric-aware equality (same toFloat64 helper as criteria.go), falling through to Default when no case matches. 17 new subtests in pkg/actions/control_flow_test.go cover each branch, depth-at-max success, depth-exceeded error, JSON round-trip. go test ./pkg/actions/ green; go build ./... + go vet ./... clean; short test/e2e + northwind + foundry_parity pass. executeFunction rules are still skipped by ExecuteRules; nested executeFunction inside control-flow is left unsupported for this story (Prepare's dispatch pass still iterates top-level only)."
    },
    {
      "id": "US-245",
      "title": "Parameter Validation DSL",
      "description": "作为建模者，我想声明参数约束（range/pattern/dependency）。",
      "acceptanceCriteria": [
        "集成 santhosh-tekuri/jsonschema 库",
        "ActionType schema 支持 JSON Schema Draft-07",
        "错误返回字段级详情",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 46,
      "passes": true,
      "notes": "Added santhosh-tekuri/jsonschema/v5 (pure-Go, CGO-friendly) + migration 000048 ADD COLUMN parameter_schema JSONB on action_types. pkg/actions/parameter_schema.go ships ParameterSchemaValidator (compile-cache keyed by raw-JSON bytes, Draft7), ParameterSchemaError (typed violation list) and APIError() renderer that emits WEAVE_VALIDATION_SCHEMA 422 via apierror.NewValidationSchema. Violation payload carries field (dot-path), keyword (JSON Schema keyword that failed), reason (human message) AND a full JSON-encoded 'violations' array so SDKs can surface every failure. Executor.Prepare gains a Step 3b hook between ValidateParameters and criteria evaluation; schema-compile failures fall through to the legacy ActionFailed 400 (not the author's fault). Tri-state ParameterSchema pointer on UpdateActionTypeRequest (nil/null-clear/non-empty-replace) matches prior art. 16 unit + handler tests cover nil/null/empty no-op, required/pattern/range/dependency/multi violations, happy path, compile-cache reuse, malformed-schema compile error, typed-error APIError() structure, handler wire-422 round-trip. Full pkg/ + cmd/ + internal/ + e2e/northwind/foundry_parity green; CGO_ENABLED=0 build clean."
    },
    {
      "id": "US-246",
      "title": "OIDC 集成",
      "description": "作为企业管理员，我想集成 OIDC provider（Keycloak/Okta）。",
      "acceptanceCriteria": [
        "集成 coreos/go-oidc v3 库",
        "AUTH_MODE=oidc 配置项 issuer/clientId/clientSecret",
        "/auth/oidc/login → /auth/oidc/callback 流程",
        "ID token 验签 + user 映射",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 47,
      "passes": true,
      "notes": "pkg/auth/oidc_handler.go /login+/callback (go-oidc v3 + oauth2); OIDCConfig in internal/config with AUTH_MODE=oidc shortcut + WEAVE_OIDC_* env; wired in cmd/server/main.go gated on discovery success; 12 handler tests via oidctest.Server + 4 config tests"
    },
    {
      "id": "US-247",
      "title": "OAuth2 完整流程",
      "description": "作为外部应用，我想通过 OAuth2 授权码获取访问令牌。",
      "acceptanceCriteria": [
        "实现 Authorization Code + PKCE 流程",
        "Refresh token 刷新流程",
        "Scopes 声明（读/写/admin）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 48,
      "passes": true,
      "notes": "Authorization Code + PKCE already shipped under US-142; this story adds (1) the refresh_token grant — full RFC 6749 §6 rotation: presented refresh is consumed, fresh (access, refresh) pair minted, replay returns invalid_grant, optional scope narrowing via NarrowScopes (widening rejected as invalid_scope), confidential clients require Basic-auth/secret + public PKCE clients refresh by client_id alone; (2) canonical scope catalogue ScopeRead/ScopeWrite/ScopeAdmin in pkg/developer/oauth_scopes.go with KnownScopes()/IsKnownScope(); 14 new tests across oauth_refresh_test.go + oauth_scopes_test.go all green"
    },
    {
      "id": "US-248",
      "title": "SAML 2.0 支持",
      "description": "作为企业 IT，我想通过 SAML IdP 登录。",
      "acceptanceCriteria": [
        "集成 russellhaering/gosaml2 库",
        "SP metadata 生成端点",
        "Assertion 验签 + user mapping",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 49,
      "passes": true,
      "notes": "pkg/auth/saml_handler.go wraps russellhaering/gosaml2 v0.11.0 (pure Go, CGO-clean) behind a narrow SAMLAssertionVerifier interface (BuildAuthURL / RetrieveAssertionInfo / Metadata) — handler unit tests stub the verifier; the production builder NewSAMLDepsFromConfig hydrates a real *saml2.SAMLServiceProvider from operator-supplied IdP cert PEM. Three endpoints under /api/auth/saml/{metadata,login,acs}: GET metadata returns the SP EntityDescriptor XML built directly (skipping gosaml2.Metadata() which insists on an SP encryption cert we don't ship), GET login mints a RelayState cookie + redirects to IdP SSO URL, POST acs validates the relay pair AND the assertion signature/audience/expiry then upserts a UserRecord keyed on email + mints a Weave LoginResponse identical in shape to /api/auth/login + /api/auth/oidc/callback. Email/displayName extraction walks a configurable attribute candidate list with sensible defaults (email/mail/Microsoft xmlsoap claim URI). IdP-initiated SSO supported when both RelayState form value AND cookie are absent together. SAMLConfig env wiring (WEAVE_SAML_*) + cmd/server/main.go boot block degrade loudly on bad cert / missing fields rather than crashing. 15 SAML unit tests + 4 config tests all green; full build (CGO_ENABLED=0) clean."
    },
    {
      "id": "US-249",
      "title": "Service Accounts",
      "description": "作为 DevOps，我想创建服务账户供 CI/CD 调用 API。",
      "acceptanceCriteria": [
        "新增 service_accounts 表（name、owner、scopes、expires_at）",
        "CRUD API /admin/service-accounts",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 50,
      "passes": true,
      "notes": "migration 000049_service_accounts creates the table with FK to users(id), TEXT[] scopes, RFC3339 expires_at, soft-delete via disabled_at, and a unique partial index on name WHERE disabled_at IS NULL so operators can recreate a SA under the same name after disabling the prior one. pkg/auth/service_account.go carries ServiceAccount + Validate + IsActive helpers (name regex ^[A-Za-z0-9][A-Za-z0-9._-]*$ up to 128 chars; pointer-typed ExpiresAt/DisabledAt). pkg/auth/service_account_repository.go is the narrow ServiceAccountRepository interface + PGServiceAccountRepository (Create/GetByID/GetByName/ListActive/Update/Disable) with dynamic partial-PATCH SQL and SQLSTATE 23505→ErrServiceAccountNameConflict translation. ServiceAccountUpdate uses *Description / *[]Scopes / **ExpiresAt pointer-fields so omit/clear/replace are all distinguishable. pkg/auth/service_account_handlers.go mounts the 5-endpoint CRUD: POST/GET/GET-id/PATCH/DELETE — owner defaults to the caller's UserFromContext, 409 on name conflict, 404 on unknown id, 400 on bad RFC3339, 401 on missing user. cmd/server/main.go wires deps.ServiceAccountRepo (uncached *PGRepository) and gates the routes behind RequirePermission(PermUserManage) so only admin-level roles can mutate; degraded mode (no PG) leaves the repo nil and the routes are not mounted (proven by TestUS249_ServiceAccountRoutesUnmountedWithoutRepo). 18 handler unit tests + 6 repository integration tests + 2 wire tests all green; full CGO_ENABLED=0 build clean; short test suite across pkg/auth, pkg/... and cmd/server green."
    },
    {
      "id": "US-250",
      "title": "API Key 自动轮换",
      "description": "作为安全管理员，我想定期轮换 API key。",
      "acceptanceCriteria": [
        "API key 增加 rotates_at 字段",
        "到期前 7 天发送告警事件",
        "Grace period：双 key 并存 7 天",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 51,
      "passes": true,
      "notes": "migration 000050 adds rotates_at + successor_id; APIKeyRecord gains RotatesAt/*SuccessorID plus IsRotationExpired + InRotationWarningWindow helpers; APIKeyRepository.Rotate (tx: insert successor + stamp predecessor atomically, ErrAPIKeyAlreadyRotated on double-rotate) + ListPendingRotations(now, within) with DefaultAPIKeyRotationGrace / DefaultAPIKeyRotationWarning = 7 days; middleware issues 401 APIKeyRotated once grace elapses but accepts both keys during overlap; POST /api/admin/api-keys/{id}/rotate + GET /api/admin/api-keys/rotations handlers emit audit events (api_key_rotate + api_key_rotation_warning); routes unmounted per US-006 legacy; 12 new tests (model helpers, handler happy + custom graceDays + 409 double-rotate + 403 non-owner + 404 missing + 401 unauth + rotations list + withinDays + audit emission + integration Rotate/ListPendingRotations)"
    },
    {
      "id": "US-251",
      "title": "用户/组/角色模型",
      "description": "作为管理员，我想将用户归组并赋予角色。",
      "acceptanceCriteria": [
        "新增表：users、groups、roles、user_groups、role_permissions",
        "CRUD API 和成员管理端点",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 52,
      "passes": true,
      "notes": "migration 000051 adds groups / user_groups / roles / role_permissions (users table pre-exists from migration 000007); seeds 5 built-in roles (viewer/editor/ontology-owner/admin/ingest-writer) with Builtin=true. Narrow GroupRepository (Create/Get/List/Update/Delete + AddMember/RemoveMember/ListMembers/ListUserGroups) and RoleRepository (Create/Get/List/Delete/UpdateDescription + SetPermissions/ListPermissions) with PG-backed impls kept off oms.Repository to avoid the cascade-stub problem (matches US-249 pattern). Admin handlers: GroupHandler + RoleHandler + UserRoleHandler mount 14 endpoints under PermUserManage guard — /api/admin/groups CRUD + /members, /api/admin/roles CRUD + /permissions, /api/admin/users/{id}/roles grant+revoke. Built-in role protection at handler layer (409 BuiltinRoleProtected on DELETE + PUT permissions). List endpoint resolves empty builtins via static RolePermissions() fallback so wire responses match resolver. Grant handler validates BOTH user existence + role registry presence before UpsertUserRole. UserRoleRevoker narrow interface added to avoid widening UserRepository mock cascade. 38 unit tests + 11 PG integration tests all green."
    },
    {
      "id": "US-252",
      "title": "LDAP/AD 目录同步",
      "description": "作为 IT，我想从 LDAP/AD 周期同步用户和组。",
      "acceptanceCriteria": [
        "集成 go-ldap/ldap/v3 库",
        "Cron 同步（可配置间隔）",
        "删除的用户软标记 disabled",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 53,
      "passes": true,
      "notes": "migration 000052 adds users.disabled_at + users.ldap_dn + users.last_synced_at + groups.ldap_dn + groups.last_synced_at + ldap_sync_runs audit table; pkg/auth/ldap.go houses LDAPSyncConfig + narrow LDAPClient interface (prod = goLDAPClient wrapping go-ldap/ldap/v3 with paged search + StartTLS + bind, tests stub); LDAPSyncStore narrow CRUD on *PGLDAPSyncStore (separate file ldap_repository.go, kept off UserRepository to avoid mock cascade); LDAPSyncService.Sync runs users-pull → upsert (ldap_dn keyed) → mark orphans disabled (only for ldap_dn IS NOT NULL rows so locally-provisioned password users untouched) → groups-pull → upsert + ReplaceGroupMembers; LDAPSyncScheduler runs initial sync on Start then ticks every cfg.Interval (default 1h, configurable via WEAVE_LDAP_INTERVAL); LDAPConfig added to internal/config with WEAVE_LDAP_* env wiring + AUTH_MODE=ldap shorthand + Validate() gate on URL/UserBaseDN; cmd/server/main.go bootstrap mirrors OIDC/SAML pattern (gated on PG pool + Validate, degraded loudly); 14 unit tests with stub client + in-memory store cover validate / defaults / happy-path / orphan-disable / re-enable-on-return / unknown-member-DN / store-error-still-records-run / unwired-service / membership-shrink / scheduler-runs-immediately + 4 config tests. Pure Go (CGO_ENABLED=0 build clean)."
    },
    {
      "id": "US-253",
      "title": "MFA 多因素认证",
      "description": "作为用户，我想启用 TOTP 二次验证。",
      "acceptanceCriteria": [
        "集成 pquerna/otp 库",
        "启用 MFA 时生成 secret 和 QR 二维码",
        "登录后强制输入 6 位 code",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 54,
      "passes": true,
      "notes": "migration 000053 adds users.mfa_secret + users.mfa_enabled (partial idx on enabled=true); pkg/auth/mfa.go houses GenerateTOTPSecret (issuer/account opts, period 30, digits 6, sha1) + ValidateTOTPCode (skew=1) + ErrInvalidMFACode sentinel; pkg/auth/mfa_challenge.go is the in-memory single-use MFAChallengeStore (5min TTL default, SetNowFunc clock injection, Issue/Consume/Size, ErrMFAChallengeNotFound + ErrMFAChallengeExpired sentinels) bridging login mint → /verify consume; MFASecretStore narrow CRUD on *PGUserRepository (separate file mfa.go, kept off UserRepository to avoid mock cascade — same pattern as US-249/US-251/US-252); MFAHandler exposes /api/auth/mfa/{setup,enable,disable,verify} — setup persists secret + returns base64 PNG QR (256x256 via otp.NewKeyFromURL().Image), enable validates code then flips mfa_enabled=true, disable re-validates code then ClearMFA (idempotent for not-enrolled users), verify consumes the challenge token + validates code + mints LoginResponse identical to /login; LoginHandler now short-circuits at password-verify when user.MFAEnabled && deps.MFAChallenges != nil — issues challenge token + returns 202 Accepted with MFAChallengeResponse{mfa_required, challenge_token, expires_in} instead of access tokens; nil MFAChallenges falls through to direct token issuance so degraded-mode test routers don't lock out users; cmd/server/main.go bootstrap mirrors OIDC/SAML pattern (MFAStore=pgUserRepo captured BEFORE assigning to deps.UserRepo so reads-after-writes observe own mutations; MFAChallenges always constructed for the in-memory store; MFAHandler.RegisterRoutes mounted in the public auth route group gated on MFAStore!=nil && MFAChallenges!=nil); 23 unit tests across mfa_test.go (TOTP gen/validate/skew tolerance, challenge store round-trip + single-use + TTL eviction + unknown/empty/default-TTL) + mfa_handler_test.go (setup persists-but-not-enables, setup re-rolls + disables, setup auth required, enable success/bad-code/not-enrolled/missing-code, disable success/idempotent-no-op/bad-code, verify success/bad-code/unknown-challenge/expired-challenge/disabled-mid-flight/missing-fields) + login_handler_test.go (MFAEnabledReturnsChallenge, MFAEnabledButStoreUnwiredFallsThrough). Pure Go; pquerna/otp v1.4.0 + boombuler/barcode v1.0.1 are CGO-free."
    },
    {
      "id": "US-254",
      "title": "Session 管理",
      "description": "作为用户，我想查看和撤销其它会话。",
      "acceptanceCriteria": [
        "新增 sessions 表（user、ip、ua、created_at、last_seen）",
        "GET /auth/sessions 列表，DELETE /auth/sessions/{id} 撤销",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 55,
      "passes": true,
      "notes": "migration 000054 adds sessions (id UUID PK, user_id FK users ON DELETE CASCADE, refresh_token_id FK refresh_tokens ON DELETE SET NULL, ip, user_agent, created_at, last_seen) with idx_sessions_user + idx_sessions_last_seen(user_id, last_seen DESC); pkg/auth/session_store.go defines SessionRecord + SessionStore interface (Create/Get/ListByUser/Delete/DeleteAllForUser/Touch/RotateRefreshToken) + MemorySessionStore in-memory impl with sort-by-LastSeen-desc + owner-enforced Delete; pkg/auth/pg_session_store.go mirrors the interface over pgxpool with a two-step Delete that distinguishes ErrSessionNotFound from ErrSessionForbidden; ErrSessionNotFound / ErrSessionForbidden sentinels are the HTTP status hooks; pkg/auth/session_handler.go exposes GET /api/auth/sessions + DELETE /api/auth/sessions/{sessionID} under the standard MiddlewareFull auth group, emits SessionListResponse{sessions:[]SessionView} (empty-array-not-null), marks the current row via SessionIDAttributeKey on User.Attributes, and when RefreshService is wired the delete revokes the bound refresh_tokens.id with reason 'session_revoked' so the cookie becomes dead after the access TTL; LoginHandler/MFAHandler.Verify now optionally create a session row on success (IP + UA captured via clientIP/r.UserAgent); RefreshHandler calls RotateRefreshToken(oldID, newID, now) on rotate so the session row stays bound to the live chain member; cmd/server/main.go bootstraps NewPGSessionStore(pool) on the uncached pool and mounts SessionHandler.RegisterRoutes inside the auth group only when deps.SessionStore != nil (degraded-mode / contract-test routers skip both registration and the insert); 14 new unit tests across session_store_test.go (create validation + list-ordering + owner-scoped delete + touch + delete-all-for-user) + session_handler_test.go (401 unauth / 200 list filters to caller / current marker / 204 delete owner / 403 non-owner / 404 missing / refresh revoked on session delete / empty array sentinel)."
    },
    {
      "id": "US-255",
      "title": "SSO 单点登出",
      "description": "作为用户，我想在 IdP 登出后 Weave 也失效。",
      "acceptanceCriteria": [
        "OIDC back-channel logout 支持",
        "SAML SLO 端点",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 56,
      "passes": true,
      "notes": "OIDC back-channel logout: pkg/auth/oidc_logout_handler.go exposes POST /api/auth/oidc/back-channel-logout. Reuses the OIDCVerifier wired by NewOIDCDepsFromProvider so JWKS rotation applies to login + logout in lockstep. Validates: (1) JWT signature/iss/aud/exp via go-oidc, (2) events claim contains BackChannelLogoutEventKey ('http://schemas.openid.net/event/backchannel-logout'), (3) nonce claim absent (per RFC), (4) sub OR sid present. Resolves user via email-first → user:<email>-fallback chain (subjectCandidates helper); unknown subjects return 200 idempotently to avoid leaking subject existence. On match: RefreshService.RevokeAllForUser(reason='oidc_back_channel_logout') + SessionStore.DeleteAllForUser. Response: 200 + Cache-Control: no-store + {} body per spec §2.8. SAML SLO: pkg/auth/saml_slo_handler.go exposes POST /api/auth/saml/slo. Narrow SAMLLogoutVerifier interface (ValidateLogoutRequest + BuildLogoutResponse) is implemented by gosamlVerifier so a single SP instance powers both ACS (login) and SLO (logout). Validates IdP-initiated LogoutRequest signature via gosaml2.ValidateEncodedLogoutRequestPOST; maps NameID → user via email-first → user:<email>-fallback chain (samlNameIDCandidates); revokes the user's sessions + refresh tokens with reason='saml_slo'; returns base64-encoded LogoutResponse XML built via BuildLogoutResponseDocumentNoSig (signed responses require an SP keypair which we don't ship by default — operators wire one in by switching to BuildLogoutResponseDocument). Always emits StatusCodeSuccess to keep the IdP from looping. Plumbing: pkg/auth/refresh_service.go grew RevokeAllForUser(userID, reason) — bulk passthrough to RefreshStore.RevokeAllForUser keyed off the existing chain semantics. cmd/server/main.go wires deps.OIDCLogoutHandler / deps.SAMLSLOHandler conditionally inside the existing OIDC/SAML enable blocks — degraded-mode routers leave them nil and routes don't mount. Tests: 12 unit tests (oidc_logout_handler_test.go: happy path + signature reject + nonce reject + missing-events reject + wrong-events reject + missing-sub-and-sid reject + unknown-user idempotent + sid-only valid + sub-only resolves via user:<email> + only-affects-named-user + RegisterRoutes mounts path; saml_slo_handler_test.go: happy path + non-POST reject + missing-request reject + invalid-request reject + missing-NameID reject + unknown-NameID idempotent + only-affects-named-user + response-build-failure 500 + RegisterRoutes mounts path + production-builder smoke test verifies gosaml2 SP implements SAMLLogoutVerifier + samlNameIDCandidates table-driven test)."
    },
    {
      "id": "US-256",
      "title": "Row-Level Security",
      "description": "作为管理员，我想基于用户属性过滤可见对象。",
      "acceptanceCriteria": [
        "新增 row_policies 表（objectType、predicate、appliesTo）",
        "查询时追加 predicate 到 SQL/Bleve query",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 57,
      "passes": true,
      "notes": "migration 000055_row_policies (rid, object_type_rid FK→object_types, predicate JSONB where.WhereClause, applies_to JSONB {roles,groups,users}, description, created_by, created_at, updated_at + idx_row_policies_object_type). New pkg/rls package: RowPolicy model, narrow Store interface (Create/Get/List/ListByObjectType/Update/Delete) + in-memory MemoryStore, GroupMembershipLookup interface (adapter over auth.GroupRepository.ListUserGroups lives in cmd/server). Engine.Compile(ctx, user, otRID) resolves applicable policies (AppliesTo role/group/user intersection), converts each predicate via where.ConvertToBleveQuery, and OR-combines with DisjunctionQuery(MinShould=1). Admin bypass (PermUserManage) + nil-user short-circuit + permissive (policies that don't apply to caller add no filter). Integration: ServiceImpl.compilePolicyQuery AND-combines the existing security.Engine output with the new rls.Engine output; policyQueryAdapter on the ObjectSet path does the same so Load/Search + LoadObjectSet/Aggregate both enforce. Admin CRUD at /api/admin/row-policies (Create/List[?objectType=]/Get/Patch/Delete) behind PermUserManage; every write triggers engine.Reload so query-time cache stays fresh. PG store in cmd/server/row_policy_store.go (uncached — CRUD volume low, engine holds the hot path). Wiring conditional on deps.RowPolicyStore != nil — degraded-mode routers and contract-test harnesses skip registration. Tests: 10 engine tests (no policies, role match, role miss, group scope, user scope, admin bypass, nil user, invalid predicate error, scoped to ObjectType, OR-combined multi) + 4 model tests + 7 handler tests (Create/Create-401/Create-validation/List/List-filter-objectType/Get-Update-Delete/Get-404/WriteRefreshesEngine) + 2 ServiceImpl integration tests (FiltersListAndSearch, GetHidesDeniedObject)."
    },
    {
      "id": "US-257",
      "title": "Column-Level Masking",
      "description": "作为管理员，我想对敏感字段按用户角色掩码。",
      "acceptanceCriteria": [
        "新增 column_masks 表（property、maskRule: hash|redact|partial）",
        "响应序列化时执行 mask",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 58,
      "passes": true,
      "notes": "migration 000056_column_masks (rid, object_type_rid FK→object_types, property_api_name, mask_rule TEXT, applies_to JSONB {roles,groups,users}, description, created_by, created_at, updated_at + idx_column_masks_object_type). New pkg/masking package: ColumnMask model with MaskRule enum (hash|redact|partial) + IsKnownRule + Validate; narrow Store interface (Create/Get/List/ListByObjectType/Update/Delete) with in-memory MemoryStore; GroupMembershipLookup interface (same shape as pkg/rls, so cmd/server's groupLookupFromRepo satisfies both implicitly); rules.go with ApplyMaskRule (sha256:<hex> hash, [REDACTED], single-char-head+asterisks+single-char-tail partial) + ApplyTransforms mutator. Engine caches masks keyed by ObjectType RID; Compile(ctx, user, otRID) walks applicable masks — AppliesTo carries ALLOWED identities so callers inside the allow list see clear data, callers outside receive the mask; nil user / admin (PermUserManage) short-circuit to nil (bypass); empty AppliesTo means mask applies to everyone. ServiceImpl.SetColumnMaskEngine wired into GetObject, ListObjects, SearchObjects, linkedObjects list + getLinkedObject read paths — applyColumnMasking runs AFTER applyMarkingFilter + applyPropertyVisibility so masking operates on the surviving per-property subset. Admin CRUD at /api/admin/column-masks (Create/List[?objectType=]/Get/Patch/Delete) behind PermUserManage; every write triggers engine.Reload. PG store in cmd/server/column_mask_store.go (uncached — same rationale as RowPolicyStore). Wiring conditional on deps.ColumnMaskStore != nil so degraded-mode routers and contract-test harnesses skip registration. Tests: 7 model validate cases + 8 AppliesTo.IsApplicable cases + 8 rules cases (hash-string/hash-int/redact-string/redact-nil/partial-email/partial-short/partial-empty/unknown-passthrough) + 9 engine cases (no-masks/admin-bypass/nil-user/appliesTo-allows-caller/empty-appliesTo/multiple-props/scoped-by-OT/group-scope/Size) + 7 handler cases (Create/Create-401/Create-validation/List/List-filter-OT/GetUpdateDelete/Get-404/WriteRefreshesEngine) + 2 ServiceImpl integration tests (AppliedInListAndSearch covering viewer/finance/admin, GetObjectMasked with empty AppliesTo)."
    },
    {
      "id": "US-258",
      "title": "Cell-Level Security",
      "description": "作为管理员，我想按 object 实例 + 属性组合控制可见性。",
      "acceptanceCriteria": [
        "扩展 marking 系统到单 cell 级别",
        "查询/响应时应用 cell mask",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 59,
      "passes": true,
      "notes": "pkg/cellsec mirrors pkg/masking shape but adds primary_key dimension: Engine indexes masks by (objectTypeRID → primaryKey → []CellMask) for O(1) per-row lookup; reuses masking.MaskRule + masking.AppliesTo so admins see one rule vocabulary across column and cell surfaces; migration 000057 creates cell_masks with FK ON DELETE CASCADE + dual indexes (object_type + composite); wired into 5 read paths (GetObject/ListObjects/SearchObjects/ListLinkedObjects/GetLinkedObject) via applyCellMasking helper that runs AFTER applyColumnMasking so per-row rules sharpen column-wide defaults; admin CRUD at /api/admin/cell-masks with engine.Reload on every write; PG store in cmd/server/cell_mask_store.go follows pgColumnMaskStore pattern; 3 integration tests against real Bleve index prove targeted instance isolation, GetObject path, and composition with column masks."
    },
    {
      "id": "US-259",
      "title": "Marking 授予/撤销 UI",
      "description": "作为管理员，我想通过 UI 管理用户的 marking 授权。",
      "acceptanceCriteria": [
        "新增 /admin/markings 页面",
        "Grant/Revoke 操作含审计追踪",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 60,
      "passes": true,
      "notes": "pkg/auth/marking_handlers.go exposes /api/admin/markings (list defs), /api/admin/markings/{name}/grants (roster per marking), /api/admin/users/{id}/markings (grants per user), POST + DELETE grant/revoke — audits marking_grant / marking_revoke. Split repo interface: existing MarkingRepository stays the request-hot-path surface (GetUserMarkings on login/OIDC/SAML); new MarkingGrantAdminRepository adds ListGrantsByMarking + ListGrantsByUser with GrantedAt/GrantedBy envelope. Both implemented on the uncached *PGMarkingRepository — wired together in main.go so MarkingRepo + MarkingAdminRepo share one pool. Degraded mode (no PG) leaves both nil + skips registration. Frontend: /admin/markings page (MarkingAdminPage) renders marking-list + per-marking grants table with Grant-to-user modal and confirm-before-revoke dialog. Reachable from sidebar Admin section both with and without an active ontology. Gated via PermissionRoute permission=user.manage. 5 RTL tests + 13 Go handler tests all green; dev-browser skill not available in this harness — manual browser verification still pending."
    },
    {
      "id": "US-260",
      "title": "时间限制 Grants",
      "description": "作为管理员，我想授予临时访问（如 30 天）。",
      "acceptanceCriteria": [
        "user_markings 表新增 expires_at TIMESTAMPTZ 字段",
        "到期自动失效（查询时过滤）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 61,
      "passes": true,
      "notes": "迁移 000058 为 user_markings 增加 expires_at TIMESTAMPTZ (+ 部分索引 expires_at IS NOT NULL)。MarkingRepository.GrantMarking 接受 *time.Time 过期时间；GetUserMarkings / ListGrantsByMarking / ListGrantsByUser 在 SQL 层以 WHERE (expires_at IS NULL OR expires_at > NOW()) 过滤过期授予，无需后台扫描。API POST /api/admin/users/{userId}/markings 新增 expiresAt (RFC3339) 和 expiresInDays (便捷档) 入参，优先级 expiresInDays > expiresAt；响应 MarkingGrantResponse 新增 expiresAt。前端管理页在授权表单中加入 Expires in days 输入、在授予表格中显示 Expires At 列。新增 5 个 handler 单测 + 2 个 PG 集成测试 + 2 个 UI 测试；所有 marking filter / login / OIDC / SAML / MFA / session 测试继续通过。"
    },
    {
      "id": "US-261",
      "title": "Marking 继承",
      "description": "作为建模者，我想让子对象自动继承父对象的 marking。",
      "acceptanceCriteria": [
        "LinkType 声明 propagateMarkings: true",
        "创建时自动复制 parent marking",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 62,
      "passes": true,
      "notes": "迁移 000059 在 link_types 增加 propagate_markings BOOLEAN NOT NULL DEFAULT FALSE。LinkType.PropagateMarkings 字段（JSON omitempty）经 PG INSERT/SELECT/UPDATE 全部 round-trip。CreateLinkTypeRequest 增加 bool 入参；UpdateLinkTypeRequest 用 *bool 三态指针，省略=保留、false=禁用、true=启用，避免裸 bool 把现有 LinkType 静默关掉。pkg/funnel 新增 LinkPropagationResolver 窄接口；Consumer.applyLinkCreate 在边写入成功后调用 propagateMarkings：解析源/目标 ObjectType API name -> 从 Bleve fetch 源 _markings -> 与目标 _markings 取去重排序并集 -> reindex 目标。所有传播失败仅 log 不阻断 LINK_CREATE（best-effort 富化）。cmd/server/link_propagation_resolver.go 用窄接口（只需 GetLinkType + GetObjectType）适配 OMS 仓库；degraded mode（无 PG）resolver 不挂载、Consumer 静默 no-op。新增 7 个 funnel 单测（基础继承/合并保留/禁用/源无标记/未知 LinkType/目标未索引/resolver 错误吞掉）+ helper 形状单测，6 个 cmd/server resolver 单测，7 个 oms 管理 handler 单测覆盖 POST/PUT/JSON 输出三态。"
    },
    {
      "id": "US-262",
      "title": "数据分类 UI",
      "description": "作为管理员，我想给 ObjectType/Property 打分类标签（Public/Internal/Confidential/PII/Secret）。",
      "acceptanceCriteria": [
        "Admin 页面选择分类下拉",
        "搜索/导出时应用策略",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 63,
      "passes": true,
      "notes": "迁移 000060 在 object_types + properties 加 classification TEXT NULL（无默认）。pkg/oms.KnownClassifications()/IsKnownClassification 固定五个标签：Public/Internal/Confidential/PII/Secret；空串 = 未指定。ObjectType.Classification + Property.Classification（JSON omitempty），ToFullMetadataJSON 按需输出。CreateObjectTypeRequest / CreatePropertyRequest 增加 string Classification；UpdateObjectTypeRequest / UpdatePropertyRequest 用 *string 三态指针（nil=保留，\"\"=清空，known=赋值）。未知标签在 Create/Update 前返回 400 InvalidParameter:classification。pg_repository.go 的 object_types + properties 五个 CRUD 路径（Create/Get/List/ListInterfaceObjectTypes/Update）用 NULLIF/$N ''/COALESCE('') 搭配保持 Go 侧为 plain string。前端 web/src/api/types.ts 增加 Classification 联合类型 + CLASSIFICATION_VALUES 常量；ObjectTypeAdminPage Create + Edit 模态、PropertiesEditor Add + Edit 表单各加 Classification 下拉（'— Unspecified —' 哨兵 + 五个选项），PropertiesEditor 表格增加 Classification 列。测试：12 个 Go 分类单测（已知/未知矩阵 + Create/Update 三态 + 未知拒绝 + Property 同形）；4 个 React RTL 测试（Create 新建/Edit 预加载 + 转发 × 2）。v1 只是元数据层，未接入策略枚举——在 ObjectType.ToFullMetadataJSON + Property 属性条目按需输出后，搜索/导出响应自动携带 classification，管理员和 SDK 可消费该字段；后续 story 可将分类接入 marking / masking 引擎实现运行时强制。"
    },
    {
      "id": "US-263",
      "title": "PII 自动检测",
      "description": "作为合规员，我想系统自动扫描识别 PII 字段。",
      "acceptanceCriteria": [
        "正则扫描器识别 email/SSN/phone/credit card 模式",
        "发现 PII 自动打 PII marking",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 64,
      "passes": true,
      "notes": "新增 pkg/security/pii 纯 Go 正则扫描器（email / SSN XXX-XX-XXXX / NA 电话 / Luhn 校验信用卡），暴露 IsEmail/IsSSN/IsPhone/IsCreditCard + Scanner.DetectPII + Categories；33 个单测覆盖各类边界（embedded、format variants、Luhn invalid 等）。pkg/funnel.Consumer 增加 PIIDetector 接口 + SetPIIDetector 钩子 + autoTagPIIMarkings：扫描 CREATE/MODIFY 的 properties，命中即对 edit.Markings 追加 'PII'（去重；既有显式 PII 不重复）；DELETE/link 编辑跳过；nil detector 与未挂载等价。集成测试 6 个：正例命中、负例不写 _markings、与 CONFIDENTIAL 共存、显式 PII 不重复、未挂载 noop、DELETE 不调用 detector、真 Scanner 端到端 smoke。cmd/server/main.go 在 funnel 段无条件挂载 pii.NewScanner()（无 PG 依赖、纯 Go regex 路径成本恒定）。`go test -short ./pkg/... ./cmd/...` 全绿；`go vet ./...` / `go build ./...` 干净。"
    },
    {
      "id": "US-264",
      "title": "数据访问审计日志",
      "description": "作为审计员，我想知道谁查了哪些对象。",
      "acceptanceCriteria": [
        "loadObjectSet/getObject/search 路径记录 audit_events（action=data.access）",
        "按 objectType 可配置开关",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 65,
      "passes": true,
      "notes": "migration 000061 ObjectType.audit_data_access bool column; pkg/oss.DataAccessAuditor gates on the flag; SetDataAccessAuditor hook on ServiceImpl + objectset.Handler records data.access events for GetObject/ListObjects/SearchObjects/ListLinkedObjects/GetLinkedObject/loadObjectSet; audit.ClientInfoMiddleware + ClientInfoFromContext propagates IP+UserAgent from the HTTP layer; admin handlers Create/UpdateObjectType surface the flag via tri-state *bool on Update"
    },
    {
      "id": "US-265",
      "title": "审计日志导出",
      "description": "作为合规，我想将审计日志导出到 SIEM。",
      "acceptanceCriteria": [
        "支持 syslog、S3、stdout 三种 exporter",
        "可配置批量大小 + 重试策略",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 66,
      "passes": true,
      "notes": "pkg/audit/export — Exporter interface + Stdout/Syslog(UDP|TCP)/S3 sinks + BatchedExporter (RetryPolicy: MaxAttempts + Initial/MaxBackoff + Multiplier, ctx-aware backoff, buffer cleared after flush regardless) + TeeStore decorator (best-effort detached export, inner error propagates, nil exporter = passthrough). cmd/server/newAuditStoreWithExport wires via WEAVE_AUDIT_EXPORT_KIND + 14 WEAVE_AUDIT_EXPORT_* env vars, defaults to disabled. S3 uses narrow S3Uploader interface (operator wires AWS SDK adapter) to keep CGO_ENABLED=0 go.mod lean. 29 new tests; all green."
    },
    {
      "id": "US-266",
      "title": "审计日志签名（tamper-proof）",
      "description": "作为合规，我想审计日志防篡改。",
      "acceptanceCriteria": [
        "每条日志含前一条 hash，形成链式结构",
        "每日 root hash 发布到 append-only 文件",
        "校验工具可验证链完整性",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 67,
      "passes": true,
      "notes": "pkg/audit/chain.go — HashEvent (canonical-JSON envelope over id/actor/action/resource/diff/ip/ua/ts-UTC/prev_hash) + VerifyChain (contiguity + prev_hash linkage + content hash recompute) + ComputeRootHash. Migration 000062 adds chain_seq BIGSERIAL + prev_hash + entry_hash columns. PGStore.Insert wraps INSERT in a tx holding pg_advisory_xact_lock so concurrent inserts serialise into one coherent chain. pkg/audit/roothash.go publishes one `YYYY-MM-DD\\tHEX\\n` line per UTC day to an append-only file (O_APPEND|O_CREATE, 0600) via a Start(ctx)/Stop() scheduler that runs yesterday's anchor immediately at boot then every RootHashInterval. cmd/weave-audit-verify CLI walks ListChain, runs VerifyChain, optionally cross-checks the root-file. Wired via WEAVE_AUDIT_ROOTHASH_FILE + WEAVE_AUDIT_ROOTHASH_INTERVAL; degraded-mode (no PG) silently skips the publisher. 22 new tests (chain stability, tamper detection, root-file parse/verify, scheduler lifecycle); all green."
    },
    {
      "id": "US-267",
      "title": "GDPR 数据删除",
      "description": "作为用户，我想行使被遗忘权删除个人数据。",
      "acceptanceCriteria": [
        "POST /admin/gdpr/erase 按 userId 删除所有关联 object",
        "异步 job + 进度追踪",
        "审计记录不可被删除（但脱敏处理）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 68,
      "passes": true,
      "notes": "GDPR erase: POST /api/admin/gdpr/erase + GET /api/admin/gdpr/erase/{jobId}; async job + per-step progress; audit chain preserved via gdpr_redactions overlay applied at audit List time."
    },
    {
      "id": "US-268",
      "title": "GDPR 数据导出",
      "description": "作为用户，我想导出我的所有数据。",
      "acceptanceCriteria": [
        "POST /admin/gdpr/export 生成 ZIP",
        "ZIP 含 JSON 数据 + 附属 media 文件",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 69,
      "passes": true,
      "notes": "pkg/gdpr/export.go — Exporter composes over 6 narrow source interfaces (ProfileSource, RoleSource, OntologyRoleSource, AuditEventSource, MediaAssetSource, MediaBlobSource). WriteZip streams data.json first then media/<rid>/<filename> blobs; malicious filenames are sanitised (path.Base + strip leading dots + scrub separators) so the zip can't leak outside media/<rid>/. Missing blobs are logged in the bundle (RelativePath cleared) but don't abort the export — partial compliance > zero compliance, same philosophy as the erase orchestrator. POST /api/admin/gdpr/export is sync (application/zip response, Content-Disposition: attachment; filename=gdpr-export-<uid>.zip) mounted under RequirePermission(PermUserManage). Audit row (gdpr_export_request) stamped before streaming begins — mid-stream failure can't emit a structured 500 so the audit lands synchronously up front. cmd/server/gdpr_export.go owns 6 thin adapters (userProfileSource strips PasswordHash + MFASecret; auditActorSource wraps the already-redacted audit.Store so exports inherit the same PII-scrub semantics; mediaCatalogSource caps listing at 10_000). 10 new tests (4 exporter + 4 HTTP + 2 sanitisation/cancellation). pkg/gdpr stays free of pkg/oms / pkg/media imports — adapters own the translation. Degraded mode (no GDPRExporter wired) emits GDPRExportUnavailable 500."
    },
    {
      "id": "US-269",
      "title": "审计日志保留策略",
      "description": "作为合规，我想按策略自动归档/删除旧日志。",
      "acceptanceCriteria": [
        "配置 AUDIT_RETENTION_DAYS 环境变量",
        "Nightly job 归档到 S3 并删除 DB 行",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 70,
      "passes": true,
      "notes": "pkg/audit/retention.Service + Scheduler — archive-then-delete sweep paginates expired rows by chain_seq ASC through a pluggable Archiver, then issues a single DeleteBefore(cutoff). Archive failure aborts the sweep BEFORE delete so the next run retries; delete-only mode is the zero-config default. Scheduler captures stopCh/doneCh as locals (fixed a Start/Stop race the LDAP/roothash publishers share — Stop nil-ing s.stopCh could race with the goroutine's first select, hanging Stop forever on doneCh). PGStore gained ListBefore(before, cursor, limit) + DeleteBefore(before); both use the existing idx_audit_events_ts. AUDIT_RETENTION_DAYS=0 (default) retains forever; >0 enables the nightly scheduler. Archive wiring via cmd/server/audit_retention.go::startAuditRetention — reuses auditexport.NewS3Exporter (NDJSON batch upload) under a distinct RetentionS3Prefix so archive objects can life-cycle separately from SIEM exports. S3Uploader stays operator-injected (pluggable-SDK pattern from US-265); unwired archive kind falls back to delete-only with a warn. Retention deletion INTENTIONALLY breaks the US-266 tamper-proof chain for pre-retention history — archived NDJSON is the cold-path integrity record. 12 unit tests in pkg/audit/retention + 6 wiring tests in cmd/server + 1 integration test on PGStore helpers + 7 new config tests."
    },
    {
      "id": "US-270",
      "title": "合规报告生成",
      "description": "作为合规，我想一键生成 SOC2/ISO27001 风格的控制证据报告。",
      "acceptanceCriteria": [
        "POST /admin/compliance/report 生成 PDF/HTML",
        "报告含访问统计、Marking 分布、策略覆盖率",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 71,
      "passes": true,
      "notes": "pkg/compliance.Generator composes over four narrow source interfaces (AuditSource, MarkingSource, ObjectTypeSource, PolicySource) — every source optional, nil produces an empty but non-nil section so partial deployments still emit a useful report. Report shape is stable JSON (AccessStatistics + MarkingDistribution + PolicyCoverage); handler at POST /api/admin/compliance/report emits either application/json (default) or text/html (single-file printable template with inline CSS, no JS/external deps). Format selectable via request body `format` or `?format=` query param. Window fields `from`/`to` are RFC3339 optional; zero `to` defaults to `now`, zero `from` means \"since beginning of audit history\". Handler audits generation as `compliance_report_generated` BEFORE writing body (status-line-already-on-wire prevents mid-stream error emission). Mount gated on `deps.ComplianceGenerator != nil` in cmd/server/main.go; degraded-mode test routers with zero sources leave the generator nil and the route is not registered. Source adapters live in cmd/server/compliance_sources.go (same leaf-adapter pattern as gdpr_export.go). 22 unit tests: Generator empty/error-bubble/window-default/policy-coverage-math, HTML renders all three sections + nil-report error + empty-sections fallback copy, handler JSON/HTML/empty-body/unauthenticated/unconfigured/invalid-window/invalid-format/audit-row/query-string-format. No OpenAPI entry needed — admin-scoped endpoint mounted conditionally; contract-test router doesn't wire the deps so the route never appears in chi.Walk (same convention as /api/admin/gdpr/*)."
    },
    {
      "id": "US-271",
      "title": "OpenTelemetry 业务路径",
      "description": "作为 SRE，我想在 OMS/OSS/Actions 关键路径看到分布式追踪。",
      "acceptanceCriteria": [
        "每个 handler 和 repo 方法包裹 span",
        "Baggage 传递 requestId 和 userId",
        "配置 OTLP exporter（grpc 和 http）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 72,
      "passes": true,
      "notes": "tracing.HTTPMiddleware + tracing.BaggageMiddleware wired into NewFullRouter so every handler opens one server-kind span enriched with request_id + user_id baggage AND span attributes; pkg/tracing.PgxTracer wired via database.ConnectWithTracer + pgxpool.Config.ConnConfig.Tracer so every PG query opens a client-kind child span (repo methods inherit coverage without per-method instrumentation). OTLP gRPC exporter added alongside the existing HTTP one — Exporter=otlp defers to OTLPProtocol (http|grpc), Exporter=otlphttp/otlpgrpc are explicit shorthands. Init installs a composite TraceContext+Baggage propagator so inbound W3C headers stitch to the span and outbound clients carry both. Business-path sample spans on oss.GetObject / actions.Apply / oms.GetOntology demonstrate the StartSpan helper for callers that want finer granularity inside a handler. Pgx tracer is only attached when Tracing.Enabled=true so low-latency reads don't pay span-alloc cost in dev/test. New env knobs: WEAVE_OTLP_PROTOCOL, WEAVE_OTLP_INSECURE. 16 new tracing tests (OTLP grpc/http/auto, composite propagator, unknown exporter, BaggageMiddleware populate/omit/nil-resolver, baggage helpers, StartSpan, PgxTracer start/end/error/truncate/nil/firstSQLKeyword). Whole repo go test ./... and go vet ./... clean."
    },
    {
      "id": "US-272",
      "title": "SLO/SLI Dashboard",
      "description": "作为 SRE，我想看 Grafana dashboard 展示 p99 延迟、错误率、吞吐量。",
      "acceptanceCriteria": [
        "新增 grafana/dashboards/weave-slo.json",
        "面板：http_p99、error_rate、edit_batch_lag、nats_consumer_lag",
        "Docker compose 集成 grafana",
        "Typecheck passes"
      ],
      "priority": 73,
      "passes": true,
      "notes": "grafana/dashboards/weave-slo.json (schemaVersion=39, 5 panels: http_p99 on weave_http_request_duration_seconds_bucket, error_rate on weave_http_requests_total status=~5xx/4xx, edit_batch_lag on weave_nats_consume_duration_seconds_bucket p95/p99, nats_consumer_lag as publish−consume delta + consume error rate, + build_info stat). Datasource templated so operators can switch Prometheus instances. Deploy annotation keys off changes(weave_build_info[1m]). Auto-provisioning wiring: grafana/prometheus.yml scrapes weave:9117/metrics every 15s; grafana/provisioning/datasources/prometheus.yml pins a default Prometheus datasource; grafana/provisioning/dashboards/weave.yml loads every JSON under /var/lib/grafana/dashboards into a 'Weave' folder with 30s refresh. docker-compose.yml grew two services: prometheus (prom/prometheus:v2.55.0, 15d retention, port 9090, depends_on weave) and grafana (grafana/grafana:10.4.5, port 3000, anonymous Viewer, admin/admin default, mounts provisioning + dashboards read-only, depends_on prometheus). New internal/dashboards package with 4 Go tests that guard the whole stack — TestWeaveSLODashboard_Exists (JSON parses + schemaVersion≥30), TestWeaveSLODashboard_RequiredPanels (all four PRD panels present with the right metric substrings), TestDockerCompose_GrafanaWired (prom+grafana+dashboard file referenced), TestGrafanaProvisioning_DatasourceAndDashboardsPresent (all three provisioning files present). CGO_ENABLED=0 go build ./... + go vet ./... + focused tests clean."
    },
    {
      "id": "US-273",
      "title": "Grafana 告警规则",
      "description": "作为 SRE，我想在 SLO 超限时收到告警。",
      "acceptanceCriteria": [
        "新增 alerts/weave.yml PrometheusRule",
        "至少 5 条告警：HighErrorRate、HighLatency、NatsLag、DBConnExhaust、DiskFull",
        "Typecheck passes"
      ],
      "priority": 74,
      "passes": true,
      "notes": "alerts/weave.yml carries two rule groups — weave.slo (HighErrorRate 5xx ratio >1% for 5m on weave_http_requests_total, HighLatency p99 >500ms for 10m on weave_http_request_duration_seconds_bucket, NatsLag publish-consume delta >5 msg/s for 10m on weave_nats_publish_total/weave_nats_consume_total) and weave.infra (DBConnExhaust pg_stat_activity_count/pg_settings_max_connections >80% for 5m via postgres_exporter, DiskFull node_filesystem_avail_bytes/node_filesystem_size_bytes <10% for 10m via node_exporter). Every alert carries severity/service labels + summary/description/runbook_url annotations and references weave_* metrics already emitted by pkg/metrics. grafana/prometheus.yml grew a rule_files glob (/etc/prometheus/rules/*.yml) and docker-compose.yml mounts ./alerts:/etc/prometheus/rules:ro into the prometheus service so `docker compose up` picks up new rule files without extra wiring. 6 new Go tests in internal/dashboards/alerts_test.go — FileExistsAndParses (yaml.v3 round-trip through prometheusRuleFile envelope), RequiredAlertsPresent (all five PRD alert names + non-empty expr/for/severity/summary/description), TotalCountAtLeastFive (counts alerts across groups, guards the ≥5 criterion without hard-coding the list), ExpressionsReferenceWeaveMetrics (SLO alerts must reference the right weave_* metric family; infra alerts are exempt), WiredIntoPrometheus (accepts explicit weave.yml reference OR a glob that filepath.Match would match against weave.yml/weave.yaml), MountedInDockerCompose (./alerts mount present). CGO_ENABLED=0 go build ./... + go vet ./... clean; docker compose config --quiet exits 0."
    },
    {
      "id": "US-274",
      "title": "Backup/Restore 脚本 + PITR",
      "description": "作为运维，我想每日自动备份且可按时间点恢复。",
      "acceptanceCriteria": [
        "scripts/backup.sh：pg_dump + media tar + WAL archive",
        "scripts/restore.sh <timestamp> 实现",
        "scripts/test-restore.sh 验证脚本",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 75,
      "passes": true,
      "notes": "scripts/backup.sh emits BACKUP_DIR/<UTC timestamp>/{db.dump,media.tar.gz,wal/,manifest.json} — pg_dump custom-format + tar of $WEAVE_DATA_DIR/media + rsync of $WAL_ARCHIVE_DIR; manifest carries sha256 + sizes + WAL segment count; RETAIN_DAYS prune (default 14d) drops old backup directories. scripts/restore.sh <timestamp|latest> picks the newest backup dir ≤ target (string-compare on UTC stamp), validates db.dump sha256 against manifest, pg_restore --clean --if-exists into $PG_DSN, untars media into a sibling 'media.pre-restore.<stamp>' (no destructive overwrite); when PITR_TARGET_TIME is set it stages WAL into $WAL_RESTORE_DIR and prints the operator instructions for restore_command/recovery_target_time/recovery.signal (script does NOT auto-restart Postgres). scripts/test-restore.sh creates an isolated weave_restore_test_$$ DB on the same Postgres host, runs backup.sh + restore.sh against it, compares row counts on schema_migrations / ontologies / object_types / properties / link_types, drops the test DB on exit. 8 Go tests in internal/backup/scripts_test.go — Exist (3 files), Executable (0o111 bit on unix; skipped on windows), BashParse (exec.LookPath bash + bash -n; skip when bash absent), HaveShebang (#!/usr/bin/env bash on line 1), BackupContainsPgDump (pg_dump + media + WAL_ARCHIVE_DIR + manifest.json invariants), RestoreContainsPgRestore (pg_restore + PITR_TARGET_TIME + media.tar.gz + 'TARGET=\"${1:-latest}\"'), TestRestoreCallsBoth (scripts/{backup,restore}.sh + CREATE DATABASE + DROP DATABASE strings), SetEuoPipefail (strict-mode invariant on all three). CGO_ENABLED=0 go build ./... + go vet ./... clean; go test ./internal/backup/... PASS."
    },
    {
      "id": "US-275",
      "title": "Zero-Downtime Upgrade",
      "description": "作为运维，我想滚动升级不中断服务。",
      "acceptanceCriteria": [
        "迁移脚本支持 forward-compatible（旧版本能读新 schema）",
        "/health/ready 与 /health/live 分离",
        "双实例演练脚本",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 76,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-276",
      "title": "Feature Flags 系统",
      "description": "作为 PM，我想动态开关功能。",
      "acceptanceCriteria": [
        "新增 feature_flags 表 + admin UI",
        "封装 ctx.HasFlag(name, user) 工具",
        "支持 per-realm 和 per-user 作用域",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 77,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-277",
      "title": "多租户隔离与配额",
      "description": "作为 SaaS 运营，我想为每个 tenant 设置资源配额。",
      "acceptanceCriteria": [
        "新增 tenant_quotas 表（maxObjects、maxStorage、maxQPS）",
        "中间件执行配额检查",
        "超限返回 429",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 78,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-278",
      "title": "Secret Management (Vault)",
      "description": "作为运维，我想从 Vault 动态获取 secrets。",
      "acceptanceCriteria": [
        "集成 hashicorp/vault/api 库",
        "SECRET_PROVIDER=vault|env|file 可选",
        "Secret 轮换感知",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 79,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-279",
      "title": "AIP Threads 后端",
      "description": "作为 AI 用户，我想让与模型的对话持久化。",
      "acceptanceCriteria": [
        "新增 aip_threads 和 aip_messages 表",
        "CRUD API",
        "支持多 LLM provider（OpenAI、Anthropic、Mock）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 80,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-280",
      "title": "AIP Threads UI",
      "description": "作为 AI 用户，我想在 Web 中进行会话。",
      "acceptanceCriteria": [
        "新增 /threads 页面：会话列表 + 消息流 + 新建",
        "支持 streaming 响应显示",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 81,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-281",
      "title": "AIP Logic 后端",
      "description": "作为流程设计者，我想用 LLM 决策节点构建工作流。",
      "acceptanceCriteria": [
        "新增 aip_logic_flows 表（nodes: llm|tool|if|output）",
        "执行引擎按 DAG 顺序运行",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 82,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-282",
      "title": "AIP Logic 编辑器 UI",
      "description": "作为设计者，我想拖拽式编辑 Logic flow。",
      "acceptanceCriteria": [
        "使用 reactflow 库实现画布",
        "节点配置面板",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 83,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-283",
      "title": "RAG 工作流增强",
      "description": "作为 AI 用户，我想 LLM 回答时自动检索相关 objects。",
      "acceptanceCriteria": [
        "封装 Semantic retriever（ObjectSet + embedding）",
        "Prompt 模板注入 context",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 84,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-284",
      "title": "Function Calling Chain",
      "description": "作为 LLM 工具，我想在同一会话中调用多个 function 后再回答。",
      "acceptanceCriteria": [
        "OpenAI function calling loop（max 8 次）",
        "每次调用记录到 thread",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 85,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-285",
      "title": "LLM Tool 扩展",
      "description": "作为开发者，我想注册自定义 tool 供 LLM 调用。",
      "acceptanceCriteria": [
        "新增 aip_tools 表（name、schema、handler_function_rid）",
        "运行时 LLM 可见工具列表",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 86,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-286",
      "title": "MCP Resource 扩展",
      "description": "作为 MCP 客户端，我想访问 Weave 的 ontology/dataset 作为 resource。",
      "acceptanceCriteria": [
        "MCP resources/list 返回 ontologies 和 objectSets",
        "resources/read 返回 schema 或 object 内容",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 87,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-287",
      "title": "Pipeline DSL",
      "description": "作为数据工程师，我想用 YAML 定义 Pipeline。",
      "acceptanceCriteria": [
        "Pipeline schema：inputs、transforms、outputs、schedule",
        "新增 pipelines 表",
        "CRUD API",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 88,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-288",
      "title": "Pipeline DAG 编排",
      "description": "作为 Pipeline 执行引擎，我要按依赖图顺序执行节点。",
      "acceptanceCriteria": [
        "拓扑排序 + 并行度控制",
        "失败策略：continue/abort/retry",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 89,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-289",
      "title": "Pipeline 调度器",
      "description": "作为 Pipeline 维护者，我想用 cron 定时触发。",
      "acceptanceCriteria": [
        "集成 robfig/cron/v3 库",
        "pipelines.schedule 字段",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 90,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-290",
      "title": "Schema 推断",
      "description": "作为导入用户，我想让系统从 CSV/JSON 推断字段类型。",
      "acceptanceCriteria": [
        "扫描前 1000 行样本推断类型",
        "UI 预览 + 用户确认/调整",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 91,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-291",
      "title": "CDC (Change Data Capture)",
      "description": "作为 Pipeline，我想捕获源表变更增量同步。",
      "acceptanceCriteria": [
        "实现 PG logical replication 接收端",
        "增量应用 EditBatch",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 92,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-292",
      "title": "JDBC 连接器",
      "description": "作为用户，我想从外部 PG/MySQL 读数据。",
      "acceptanceCriteria": [
        "database/sql 抽象，支持 PG、MySQL、SQLite",
        "分页 + 增量字段",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 93,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-293",
      "title": "S3 连接器",
      "description": "作为用户，我想从 S3 批量导入 CSV/Parquet。",
      "acceptanceCriteria": [
        "AWS SDK v2 + MinIO 兼容",
        "Parquet 用 parquet-go 库",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 94,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-294",
      "title": "REST API 连接器",
      "description": "作为用户，我想从外部 REST API 轮询数据。",
      "acceptanceCriteria": [
        "配置：url、method、headers、jsonPath、auth",
        "分页策略支持 cursor/page/offset",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 95,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-295",
      "title": "Kafka 连接器",
      "description": "作为用户，我想订阅 Kafka topic 实时导入。",
      "acceptanceCriteria": [
        "集成 segmentio/kafka-go",
        "Consumer group + 自动 commit",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 96,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-296",
      "title": "数据质量检查",
      "description": "作为 Pipeline 维护者，我想定义 data quality rules。",
      "acceptanceCriteria": [
        "Rule DSL：notNull、range、unique、regex、foreign_key",
        "失败行记录到 quality_violations 表",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 97,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-297",
      "title": "Pipeline 监控 UI",
      "description": "作为 Pipeline 维护者，我想看执行状态和历史。",
      "acceptanceCriteria": [
        "/pipelines 列表页 + 执行图",
        "日志查看面板",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 98,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-298",
      "title": "Pipeline 执行历史 API",
      "description": "作为客户端，我想查询 Pipeline 过往执行记录。",
      "acceptanceCriteria": [
        "新增 pipeline_runs 表",
        "分页 API + 详情 API",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 99,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-299",
      "title": "Lineage 元数据模型",
      "description": "作为平台，我要记录 object 来源（dataset/pipeline/action）。",
      "acceptanceCriteria": [
        "新增 lineage_edges 表（upstream_rid、downstream_rid、operation、ts）",
        "写路径自动记录",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 100,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-300",
      "title": "Object 溯源 API",
      "description": "作为用户，我想查询 object 从哪里来。",
      "acceptanceCriteria": [
        "GET /objects/{rid}/lineage?direction=upstream|downstream&depth=N",
        "返回 graph 结构（nodes + edges）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 101,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-301",
      "title": "Action 变更追踪",
      "description": "作为审计员，我想看 action 引起的所有对象变更。",
      "acceptanceCriteria": [
        "Action 执行时写 lineage_edge（action_rid → object_rid）",
        "GET /actions/{rid}/impact 端点",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 102,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-302",
      "title": "Lineage 可视化 UI",
      "description": "作为用户，我想看血缘图。",
      "acceptanceCriteria": [
        "使用 react-force-graph 展示图",
        "点击节点展开更多",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 103,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-303",
      "title": "跨 ObjectSet 血缘",
      "description": "作为分析师，我想看 ObjectSet 派生链。",
      "acceptanceCriteria": [
        "GET /objectSets/{rid}/lineage 端点",
        "包含 filter/union/aggregation 操作链",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 104,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-304",
      "title": "ObjectSet 变更订阅",
      "description": "作为客户端，我想订阅 ObjectSet 内任意对象变更。",
      "acceptanceCriteria": [
        "WebSocket SUBSCRIBE_OBJECTSET 消息类型",
        "NATS EditBatch 路由到匹配订阅",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 105,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-305",
      "title": "Aggregation 增量更新",
      "description": "作为 dashboard 用户，我想聚合结果实时更新。",
      "acceptanceCriteria": [
        "订阅聚合 query",
        "增量计算而非每次全量",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 106,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-306",
      "title": "WebSocket 事件路由完善",
      "description": "作为 Hub，我要按过滤器路由事件到订阅者。",
      "acceptanceCriteria": [
        "订阅过滤器支持 objectType 和 whereClause",
        "扇出效率 O(log N)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 107,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-307",
      "title": "SSE 重连与恢复",
      "description": "作为客户端，断线重连后不丢事件。",
      "acceptanceCriteria": [
        "支持 Last-Event-ID 头",
        "服务端按 id 重放",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 108,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-308",
      "title": "订阅配额管理",
      "description": "作为平台，我要防止订阅失控。",
      "acceptanceCriteria": [
        "每用户最多 N 个订阅",
        "单连接最多 N 条/秒事件",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 109,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-309",
      "title": "Cmd+K 全局搜索",
      "description": "作为用户，我想按 Cmd+K 快速搜索任意对象/页面。",
      "acceptanceCriteria": [
        "集成 cmdk 库，模态弹出",
        "跨 ObjectType 搜索",
        "支持键盘导航",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 110,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-310",
      "title": "搜索 Facets UI",
      "description": "作为用户，我想在搜索结果侧栏按 facet 过滤。",
      "acceptanceCriteria": [
        "按 objectType/owner/date 展示 facet",
        "多选 + 计数显示",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 111,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-311",
      "title": "Saved Searches",
      "description": "作为用户，我想保存常用查询。",
      "acceptanceCriteria": [
        "保存 + 命名查询",
        "侧栏列表 + 一键加载",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 112,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-312",
      "title": "Activity Feed / Timeline",
      "description": "作为用户，我想看对象的活动时间轴。",
      "acceptanceCriteria": [
        "ObjectDetail 新增 Activity tab",
        "按时间倒序 + 分页",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 113,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-313",
      "title": "Object Diff Viewer",
      "description": "作为用户，我想对比 object 两个版本。",
      "acceptanceCriteria": [
        "集成 react-diff-viewer-continued",
        "字段级高亮差异",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 114,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-314",
      "title": "Rich Text / Markdown Editor",
      "description": "作为编辑用户，我想在 Markdown 属性中富文本编辑。",
      "acceptanceCriteria": [
        "集成 @uiw/react-md-editor",
        "预览切换 + 快捷按钮",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 115,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-315",
      "title": "Inline Property Editing",
      "description": "作为用户，我想在 ObjectDetail 中直接点击字段编辑。",
      "acceptanceCriteria": [
        "单击进入编辑，Esc 取消，Enter 保存",
        "乐观更新 + 错误回滚",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 116,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-316",
      "title": "文件/图片上传 UI",
      "description": "作为用户，我想拖拽上传附件到 media 属性。",
      "acceptanceCriteria": [
        "集成 react-dropzone",
        "进度条 + 取消上传",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 117,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-317",
      "title": "Action 执行历史",
      "description": "作为用户，我想看我执行过的 action 列表。",
      "acceptanceCriteria": [
        "新增 /actions/history 页面 + 过滤",
        "详情查看（参数/结果）",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 118,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-318",
      "title": "Batch Action 进度",
      "description": "作为用户，我想看批量 action 的实时进度。",
      "acceptanceCriteria": [
        "进度条 + 取消按钮",
        "WebSocket 订阅 job 状态",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 119,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-319",
      "title": "Action Undo UI",
      "description": "作为用户，我想一键撤销刚执行的 action。",
      "acceptanceCriteria": [
        "Toast 上有 Undo 按钮（5s 窗口）",
        "Action History 中也可 Undo",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 120,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-320",
      "title": "Action 参数模板",
      "description": "作为用户，我想保存常用参数集合。",
      "acceptanceCriteria": [
        "保存/加载模板",
        "用户私有 + 可分享",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 121,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-321",
      "title": "Approval Queue UI 前端",
      "description": "作为审批者，我想在专属页面看待审批 actions。",
      "acceptanceCriteria": [
        "新增 /approvals 页面显示待审核列表",
        "详情面板 + 批准/拒绝按钮",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 122,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-322",
      "title": "动态表单验证",
      "description": "作为 Action 表单用户，我想实时看到字段级错误。",
      "acceptanceCriteria": [
        "使用 react-hook-form + Zod",
        "错误与 API 422 响应对齐",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 123,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-323",
      "title": "Relationship Graph",
      "description": "作为用户，我想以力导图看对象关联。",
      "acceptanceCriteria": [
        "集成 react-force-graph",
        "点击展开邻居",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 124,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-324",
      "title": "Gantt Chart",
      "description": "作为项目用户，我想以 Gantt 看时间对象。",
      "acceptanceCriteria": [
        "集成 frappe-gantt 或 gantt-task-react",
        "根据 start/end 字段渲染",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 125,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-325",
      "title": "Sankey Diagram",
      "description": "作为分析师，我想看流向。",
      "acceptanceCriteria": [
        "使用 echarts Sankey 组件",
        "从 link 数据聚合生成",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 126,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-326",
      "title": "Pivot Table",
      "description": "作为分析师，我想透视表。",
      "acceptanceCriteria": [
        "集成 react-pivottable",
        "拖拽维度和度量",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 127,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-327",
      "title": "Dashboard Editor",
      "description": "作为用户，我想拖拽编辑自定义仪表盘。",
      "acceptanceCriteria": [
        "使用 react-grid-layout 做布局",
        "Widget 增删 + 配置",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 128,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-328",
      "title": "Widget 库",
      "description": "作为 Dashboard 用户，我想选不同类型 widget。",
      "acceptanceCriteria": [
        "支持类型：chart/table/stat/map/text",
        "每种 widget 有自身配置面板",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 129,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-329",
      "title": "Dashboard 保存/分享",
      "description": "作为用户，我想保存 dashboard 并分享链接。",
      "acceptanceCriteria": [
        "新增 dashboards 表",
        "/dashboards/{id} URL 路由",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 130,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-330",
      "title": "ObjectSet Diff",
      "description": "作为分析师，我想对比两个 ObjectSet 差异。",
      "acceptanceCriteria": [
        "显示 only-in-A / only-in-B / changed 三类",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 131,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-331",
      "title": "ObjectSet URL 分享",
      "description": "作为用户，我想生成可分享的 ObjectSet URL。",
      "acceptanceCriteria": [
        "定义序列化到 query string",
        "打开时还原状态",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 132,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-332",
      "title": "ObjectSet 版本管理",
      "description": "作为用户，我想命名保存 ObjectSet 版本。",
      "acceptanceCriteria": [
        "新增 /objectSets 列表页",
        "版本切换",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 133,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-333",
      "title": "Excel 导出",
      "description": "作为用户，我想导出结果为 Excel。",
      "acceptanceCriteria": [
        "使用 xlsx 库",
        "支持多 sheet（表 + 聚合）",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 134,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-334",
      "title": "评论系统后端",
      "description": "作为开发者，我要提供评论 API。",
      "acceptanceCriteria": [
        "新增 comments 表（targetRid、body、author、parent_id）",
        "CRUD + 分页 + 软删",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 135,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-335",
      "title": "评论 UI",
      "description": "作为用户，我想对 object 发评论。",
      "acceptanceCriteria": [
        "ObjectDetail 新增 Comments tab",
        "线程展开 + 编辑/删除",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 136,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-336",
      "title": "@Mention 自动完成",
      "description": "作为评论用户，我想 @ 其他人。",
      "acceptanceCriteria": [
        "输入 @ 弹出用户列表",
        "生成 mention link",
        "触发通知",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 137,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-337",
      "title": "Watch / Follow",
      "description": "作为用户，我想订阅 object 变更通知。",
      "acceptanceCriteria": [
        "Watch 按钮 + 状态指示",
        "新增 watches 表",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 138,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-338",
      "title": "活动订阅推送",
      "description": "作为 watcher，我想收到邮件 + 站内通知。",
      "acceptanceCriteria": [
        "NATS consumer 汇总事件到 notification_center",
        "可选 email 发送（SMTP）",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 139,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-339",
      "title": "分享链接与权限请求",
      "description": "作为用户，我想分享对象链接，对方无权时自动请求访问。",
      "acceptanceCriteria": [
        "权限请求 workflow + 审批",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 140,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-340",
      "title": "评论通知集成",
      "description": "作为 @ed 用户，我想收到即时通知。",
      "acceptanceCriteria": [
        "NotificationCenter 集成 mention 类型",
        "点击跳转到评论",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 141,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-341",
      "title": "评论 Markdown",
      "description": "作为评论用户，我想用 markdown 写评论。",
      "acceptanceCriteria": [
        "Markdown 渲染（含代码块、link）",
        "XSS 过滤",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 142,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-342",
      "title": "Emoji 反应",
      "description": "作为用户，我想给评论/对象加 emoji 反应。",
      "acceptanceCriteria": [
        "新增 reactions 表",
        "聚合计数 UI",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 143,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-343",
      "title": "Notification Center 扩展",
      "description": "作为用户，我想通知按类型筛选 + 批量已读。",
      "acceptanceCriteria": [
        "类型 tab：mention、watch、approval、system",
        "批量已读操作",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 144,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-344",
      "title": "深色模式",
      "description": "作为用户，我想切换深色主题。",
      "acceptanceCriteria": [
        "CSS 变量 + Tailwind dark 模式",
        "切换按钮 + 持久化到 localStorage",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 145,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-345",
      "title": "主题切换 UI",
      "description": "作为用户，我想选 light/dark/system。",
      "acceptanceCriteria": [
        "TopBar 下拉选择",
        "system 模式跟随 prefers-color-scheme",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 146,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-346",
      "title": "i18n 基础设施",
      "description": "作为全球用户，我想界面多语言。",
      "acceptanceCriteria": [
        "集成 i18next + react-i18next",
        "key 结构规范化 + 抽取脚本",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 147,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-347",
      "title": "中/英文翻译",
      "description": "作为中文/英文用户，我想看到对应语言。",
      "acceptanceCriteria": [
        "zh-CN 和 en 资源文件完整",
        "语言切换器",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 148,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-348",
      "title": "键盘快捷键系统",
      "description": "作为用户，我想用快捷键加速操作。",
      "acceptanceCriteria": [
        "集成 react-hotkeys-hook",
        "全局快捷键注册表",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 149,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-349",
      "title": "快捷键配置面板",
      "description": "作为用户，我想看所有快捷键列表。",
      "acceptanceCriteria": [
        "按 ? 打开 help modal",
        "按 group 显示",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 150,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-350",
      "title": "用户偏好中心",
      "description": "作为用户，我想一站式管理偏好。",
      "acceptanceCriteria": [
        "新增 /settings 页：主题、语言、通知、快捷键",
        "后端持久化到 user_preferences 表",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 151,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-351",
      "title": "WCAG 2.1 AA 无障碍审计",
      "description": "作为平台，我要通过无障碍审计。",
      "acceptanceCriteria": [
        "axe-core 跑通 Dashboard/Explorer/Browser 无 critical issue",
        "所有按钮/输入有 aria-label",
        "键盘完全可达",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 152,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-352",
      "title": "全局错误边界",
      "description": "作为开发者，我要防止子组件错误白屏。",
      "acceptanceCriteria": [
        "react-error-boundary 包 Route",
        "Fallback UI + 错误上报",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 153,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-353",
      "title": "Loading Skeleton 库",
      "description": "作为用户，我想看到渐进式加载占位。",
      "acceptanceCriteria": [
        "所有主要 Query 配 skeleton 组件",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 154,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-354",
      "title": "Offline 支持",
      "description": "作为用户，断网时能浏览已缓存数据。",
      "acceptanceCriteria": [
        "Service Worker + localforage 缓存",
        "OfflineIndicator 显示状态",
        "恢复时自动同步",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 155,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-355",
      "title": "Python async client",
      "description": "作为 Python 开发者，我想 async/await。",
      "acceptanceCriteria": [
        "WeaveAsyncClient 基于 httpx.AsyncClient",
        "所有 method 有 sync + async 变体",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 156,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-356",
      "title": "CLI REPL",
      "description": "作为 CLI 用户，我想交互模式。",
      "acceptanceCriteria": [
        "新增 weave repl 命令",
        "自动补全 + history",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 157,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-357",
      "title": "TS/Go Request Interceptor",
      "description": "作为 SDK 用户，我想注入 auth/log/retry。",
      "acceptanceCriteria": [
        "client.use(middleware) API",
        "TS + Go 实现对等",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 158,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-358",
      "title": "SDK 重试策略",
      "description": "作为 SDK 用户，我想网络抖动自动重试。",
      "acceptanceCriteria": [
        "指数退避 + jitter",
        "仅对 idempotent 方法",
        "可配置最大重试次数",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 159,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-359",
      "title": "SDK Telemetry Hooks",
      "description": "作为 SDK 用户，我想接入 OpenTelemetry。",
      "acceptanceCriteria": [
        "TS + Python + Go 生成含 tracing hook 点",
        "示例 instrumenter",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 160,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-360",
      "title": "Mock Server 生成器",
      "description": "作为 SDK 用户，我想离线测试。",
      "acceptanceCriteria": [
        "从 OpenAPI 自动生成 mock 服务器（Go binary）",
        "可自定义响应",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 161,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-361",
      "title": "Test Fixture 生成器",
      "description": "作为开发者，我想基于 ontology 生成测试数据。",
      "acceptanceCriteria": [
        "weave fixtures generate --ontology X --count 100",
        "尊重类型和约束",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 162,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-362",
      "title": "Contract Tests",
      "description": "作为 SDK 维护者，我要保证 SDK 与 backend API 一致。",
      "acceptanceCriteria": [
        "Pact-style 合约测试",
        "CI 集成",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 163,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-363",
      "title": "三语言快速启动模板",
      "description": "作为新用户，我想 clone 一个 hello-world 项目。",
      "acceptanceCriteria": [
        "examples/ts-quickstart、py-quickstart、go-quickstart",
        "README 5 分钟跑通",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 164,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-364",
      "title": "SDK Cookbook",
      "description": "作为高级用户，我想看最佳实践 recipes。",
      "acceptanceCriteria": [
        "新增 docs/cookbook/ 目录",
        "章节：async、retry、batching、subscription、rag",
        "每节含可运行代码",
        "Typecheck passes"
      ],
      "priority": 165,
      "passes": false,
      "notes": ""
    }
  ]
}