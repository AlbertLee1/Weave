{
  "project": "Weave",
  "branchName": "ralph/osv2-full-parity",
  "description": "Palantir OSv2 Full Parity — 补齐核心功能缺口、本体分支治理、自动化引擎、WebSocket 订阅、OSDK 代码生成、Developer Console、Ontology Manager UI、前端增强",
  "userStories": [
    {
      "id": "US-100",
      "title": "Action CreateLink rule type",
      "description": "As a developer, I want to create many-to-many link relationships through Actions so that I can establish object connections declaratively.",
      "acceptanceCriteria": [
        "rules.go adds createLink rule type with linkTypeApiName, sourceObjectPrimaryKey, targetObjectPrimaryKey fields",
        "ExecuteRules generates LinkEdge upsert operation when createLink rule is encountered",
        "funnel.Edit supports LINK_CREATE edit type; Consumer.applyBatch handles it by calling UpsertLinkEdge",
        "CollapseEdits handles LINK_CREATE correctly (no duplicate links)",
        "Integration test: action with createLink rule → searchAround finds linked object",
        "go test ./pkg/actions/... passes",
        "go test ./pkg/funnel/... passes",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-101",
      "title": "Action DeleteLink rule type",
      "description": "As a developer, I want to delete many-to-many link relationships through Actions so that I can remove obsolete connections.",
      "acceptanceCriteria": [
        "rules.go adds deleteLink rule type with linkTypeApiName, sourceObjectPrimaryKey, targetObjectPrimaryKey",
        "ExecuteRules generates LinkEdge deletion operation",
        "funnel.Edit supports LINK_DELETE edit type; Consumer handles it by calling DeleteLinkEdge",
        "CollapseEdits handles LINK_CREATE + LINK_DELETE cancellation on same link",
        "Integration test: create link → delete link → searchAround returns empty",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-102",
      "title": "Action CreateOrModifyObject rule type (upsert)",
      "description": "As a developer, I want a createOrModify rule type so that I can implement upsert behavior in Actions.",
      "acceptanceCriteria": [
        "rules.go adds createOrModifyObject rule type",
        "At runtime: check if object exists in Bleve index; if exists → MODIFY edit, if not → CREATE edit",
        "CollapseEdits merges createOrModify with subsequent edits correctly",
        "Integration test: upsert non-existent → CREATE; upsert existing → MODIFY",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-103",
      "title": "Interface-backed Action rules",
      "description": "As a developer, I want Action rules that reference Interface types so that one Action can operate on any implementing ObjectType.",
      "acceptanceCriteria": [
        "rules.go adds createInterfaceObject, modifyInterfaceObject, deleteInterfaceObject rule variants",
        "Rule config accepts interfaceApiName instead of objectType; objectType resolved from parameters at runtime",
        "Validation: target ObjectType must implement the specified Interface (check via ListObjectTypeInterfaces)",
        "Integration test: action with createInterfaceObject rule creates object of correct concrete type",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-104",
      "title": "Action Undo — PrevState recording in ActionLog",
      "description": "As a developer, I need ActionLog to record PrevState of modified/deleted objects so that undo is possible.",
      "acceptanceCriteria": [
        "ActionLog model adds PrevEdits field (JSONB) storing pre-edit object state for each edit",
        "Migration 000023 adds prev_edits column to action_logs table",
        "Executor.Prepare fetches current object state from Bleve before generating edits",
        "PrevEdits populated for MODIFY (full previous properties) and DELETE (full previous object) edits",
        "CREATE edits have null PrevEdits entry (nothing to revert to)",
        "Existing tests still pass with new field (backward compatible)",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-105",
      "title": "Action Undo — Revert endpoint",
      "description": "As a user, I want to undo a previously executed Action so that I can recover from mistakes.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/actions/revert endpoint accepts { actionLogId }",
        "Handler loads ActionLog by ID, reads PrevEdits, generates reverse EditBatch",
        "Reverse logic: CREATE → DELETE, MODIFY → restore PrevState, DELETE → CREATE with PrevState",
        "Reverted ActionLog marked with status=reverted; double-revert returns 409 Conflict",
        "Integration test: create object → revert → object no longer in index",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Depends on US-104"
    },
    {
      "id": "US-106",
      "title": "Where clause — withinPolygon and intersectsPolygon operators",
      "description": "As a developer, I want polygon-based geospatial query operators so that I can support complex spatial analysis.",
      "acceptanceCriteria": [
        "where/converter.go adds withinPolygon, intersectsPolygon, doesNotIntersectPolygon, doesNotIntersectBoundingBox operators",
        "Polygon operators accept GeoJSON Polygon coordinates array",
        "Implementation uses Bleve GeoShape query or point-in-polygon in-memory fallback",
        "Unit tests: point inside polygon → match; point outside → no match; polygon intersection → match",
        "go test ./pkg/oss/where/... passes",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-107",
      "title": "Where clause — containsAllTermsInOrderPrefixLastTerm (autocomplete)",
      "description": "As a developer, I want prefix-match on the last search term so that I can implement autocomplete/typeahead search.",
      "acceptanceCriteria": [
        "where/converter.go adds containsAllTermsInOrderPrefixLastTerm operator",
        "Splits input into terms; first N-1 terms use MatchPhraseQuery, last term uses PrefixQuery",
        "Single-term input uses PrefixQuery only",
        "Unit test: 'John S' matches 'John Smith' but not 'Smith John'",
        "Unit test: 'J' matches 'John', 'Jane', 'James'",
        "go test ./pkg/oss/where/... passes",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-108",
      "title": "Aggregation — exactDistinct metric",
      "description": "As a developer, I want exact distinct count aggregation so that I can get precise unique value counts.",
      "acceptanceCriteria": [
        "aggregation/engine.go adds exactDistinct metric type",
        "Uses map-based deduplication for exact count (not HyperLogLog approximation)",
        "Unit test: exactDistinct on field with 5 unique values returns exactly 5",
        "Unit test: compare exactDistinct vs approximateDistinct — exact is always precise",
        "go test ./pkg/oss/aggregation/... passes",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-109",
      "title": "Aggregation — collectList metric",
      "description": "As a developer, I want to collect field values into a list during aggregation so that I can retrieve grouped raw values.",
      "acceptanceCriteria": [
        "aggregation/engine.go adds collectList metric type",
        "Returns []interface{} of collected values per group",
        "Supports maxItems config (default 100) to limit list length",
        "Unit test: groupBy category + collectList names → returns name arrays per category",
        "go test ./pkg/oss/aggregation/... passes",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-110",
      "title": "Fuzzy Search support",
      "description": "As a developer, I want fuzzy search with configurable fuzziness so that I can handle typos and spelling variations.",
      "acceptanceCriteria": [
        "SearchObjects request schema accepts optional fuzzy field: { maxEdits: 1 | 2 }",
        "When fuzzy is set, wrap text query terms with Bleve FuzzyQuery (Levenshtein distance)",
        "Default maxEdits = 1 if fuzzy is present but maxEdits omitted",
        "Unit test: search 'Jonh' with maxEdits=1 matches document containing 'John'",
        "Unit test: search 'Jonh' without fuzzy does NOT match 'John'",
        "go test ./pkg/oss/... passes",
        "Typecheck passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-111",
      "title": "Value Type constraint enforcement at write time",
      "description": "As a developer, I want Value Type constraints (regex, range, length) to be enforced at write time so that data quality is guaranteed.",
      "acceptanceCriteria": [
        "New pkg/types/constraints.go: ValidateConstraints(value, constraints) function",
        "Supports constraint types: regex, minLength, maxLength, min, max, enum",
        "Action executor calls ValidateConstraints for each property that has an associated ValueType",
        "Constraint violation returns 400 APIError with field name and violation details",
        "Unit tests: regex pass/fail, length pass/fail, range pass/fail, enum pass/fail",
        "go test ./pkg/types/... passes",
        "go test ./pkg/actions/... passes",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-112",
      "title": "Ontology branches — migration and model",
      "description": "As a developer, I need database tables for ontology branches and branch changes so that schema branching is possible.",
      "acceptanceCriteria": [
        "Migration 000024 creates ontology_branches table: id UUID PK, ontology_rid FK, name TEXT, base_version BIGINT, status TEXT CHECK (open/merged/closed), created_by TEXT, created_at/updated_at",
        "Migration 000024 creates ontology_branch_changes table: id UUID PK, branch_id FK, change_type TEXT CHECK (ADDED/MODIFIED/DELETED), entity_type TEXT, entity_rid TEXT, before_state JSONB, after_state JSONB, created_at",
        "UNIQUE constraint on (ontology_rid, name) for branches",
        "OMS models: OntologyBranch and BranchChange structs in models.go",
        "Repository interface adds: CreateBranch, GetBranch, ListBranches, CloseBranch, CreateBranchChange, ListBranchChanges",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Phase 2: Ontology Branching"
    },
    {
      "id": "US-113",
      "title": "Ontology branches — CRUD API endpoints",
      "description": "As an ontology owner, I want to create and manage ontology branches via API.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/branches creates branch (snapshots current version as base_version)",
        "GET /api/v2/ontologies/{ontology}/branches lists open branches",
        "GET /api/v2/ontologies/{ontology}/branches/{branch} gets branch details including change count",
        "DELETE /api/v2/ontologies/{ontology}/branches/{branch} closes a branch (sets status=closed)",
        "PG repository implements all branch CRUD methods",
        "Integration test: create → list → get → close lifecycle",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Depends on US-112"
    },
    {
      "id": "US-114",
      "title": "Ontology branches — write operations with ?branch= overlay",
      "description": "As an ontology owner, I want OMS write operations to accept a branch parameter so that changes are isolated.",
      "acceptanceCriteria": [
        "All OMS write handlers (Create/Update/Delete ObjectType, Property, LinkType, ActionType) accept optional ?branch= query parameter",
        "When branch is specified: write goes to ontology_branch_changes table (not main tables)",
        "BranchChange records before_state (fetched from main) and after_state",
        "Delete operations on branch record change_type=DELETED with before_state",
        "Unit test: create ObjectType on branch → not visible on main",
        "go test ./pkg/oms/... passes",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": true,
      "notes": "Depends on US-113"
    },
    {
      "id": "US-115",
      "title": "Ontology branches — read operations merge main + overlay",
      "description": "As an ontology owner, I want read operations on a branch to return merged view of main + branch changes.",
      "acceptanceCriteria": [
        "All OMS read handlers accept optional ?branch= query parameter",
        "When branch is specified: fetch main data, then apply branch overlay (ADDED entries added, MODIFIED entries replaced, DELETED entries removed)",
        "BranchedRepository decorator wraps base Repository to provide merged views",
        "Unit test: create ObjectType on branch → read on branch returns it; read on main does not",
        "Unit test: modify property on branch → branch read returns modified version; main read returns original",
        "go test ./pkg/oms/... passes",
        "Typecheck passes"
      ],
      "priority": 16,
      "passes": true,
      "notes": "Depends on US-114"
    },
    {
      "id": "US-116",
      "title": "Ontology branch diff API",
      "description": "As an ontology owner, I want to see the diff between a branch and main so that I can review changes.",
      "acceptanceCriteria": [
        "GET /api/v2/ontologies/{ontology}/branches/{branch}/diff returns list of changes",
        "Each change includes: entity_type, entity_rid, change_type (ADDED/MODIFIED/DELETED), before (null for ADDED), after (null for DELETED)",
        "Changes sorted by entity_type then change_type",
        "Integration test: make 3 changes on branch → diff returns all 3 with correct types",
        "go test ./pkg/oms/... passes",
        "Typecheck passes"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Depends on US-114"
    },
    {
      "id": "US-117",
      "title": "Ontology proposals — migration and CRUD",
      "description": "As an ontology owner, I want to create merge proposals from branches so that schema changes are governed.",
      "acceptanceCriteria": [
        "Migration 000025 creates ontology_proposals table: id UUID PK, branch_id FK, ontology_rid FK, title, description, status CHECK (pending/approved/rejected/merged), author, created_at/updated_at",
        "Migration 000025 creates proposal_reviews table: id UUID PK, proposal_id FK, reviewer, decision CHECK (approve/reject), reason TEXT, created_at",
        "POST /api/v2/ontologies/{ontology}/proposals creates proposal from branch (validates branch is open)",
        "GET /api/v2/ontologies/{ontology}/proposals lists proposals with status filter",
        "GET /api/v2/ontologies/{ontology}/proposals/{id} gets proposal with review list",
        "Integration test: create proposal from branch → list returns it",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 18,
      "passes": true,
      "notes": "Depends on US-113"
    },
    {
      "id": "US-118",
      "title": "Proposal review workflow",
      "description": "As a reviewer, I want to approve or reject ontology proposals so that schema changes are governed.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/proposals/{id}/approve adds approval review",
        "POST /api/v2/ontologies/{ontology}/proposals/{id}/reject adds rejection review with reason",
        "Proposal status updated: any rejection → rejected; at least 1 approval and no rejections → approved",
        "Only non-author users can review (self-review returns 403)",
        "Integration test: create proposal → approve → status is approved",
        "Integration test: create proposal → reject → status is rejected",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 19,
      "passes": true,
      "notes": "Depends on US-117"
    },
    {
      "id": "US-119",
      "title": "Branch merge with conflict detection",
      "description": "As an ontology owner, I want to merge an approved proposal into main so that schema changes take effect.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/proposals/{id}/merge applies branch changes to main tables",
        "Only approved proposals can be merged (returns 409 otherwise)",
        "Conflict detection: if entity was modified on both main (since base_version) and branch → return conflict list",
        "On success: apply all branch changes to main, increment ontology version, create snapshot, mark branch as merged",
        "Integration test: create branch → add ObjectType → approve → merge → ObjectType visible on main",
        "Integration test: concurrent main edit → merge returns conflict",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 20,
      "passes": true,
      "notes": "Depends on US-118"
    },
    {
      "id": "US-120",
      "title": "Branch rebase",
      "description": "As an ontology owner, I want to rebase a branch onto latest main so that I can resolve conflicts.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/branches/{branch}/rebase updates branch base_version to current main version",
        "Non-conflicting main changes are transparently incorporated (branch before_state updated)",
        "Conflicting changes returned as conflict list for manual resolution",
        "Integration test: main adds ObjectType after branch created → rebase succeeds → branch sees new type",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 21,
      "passes": true,
      "notes": "Depends on US-115"
    },
    {
      "id": "US-121",
      "title": "Ontology Export API",
      "description": "As an ontology owner, I want to export full ontology definitions as JSON so that I can migrate between environments.",
      "acceptanceCriteria": [
        "GET /api/v2/ontologies/{ontology}/export returns complete JSON with all metadata",
        "Export includes: ontology, objectTypes (with properties), linkTypes, actionTypes, interfaces, sharedProperties, valueTypes, typeGroups, functions, queryTypes",
        "Export format matches existing OntologyExport model structure",
        "Integration test: populate ontology → export → JSON contains all entities",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 22,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-122",
      "title": "Ontology Import API",
      "description": "As an ontology owner, I want to import ontology definitions from JSON so that I can restore or migrate ontologies.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/import accepts JSON body with mode: merge | replace",
        "merge mode: create missing entities, update existing, skip conflicts",
        "replace mode: drop all existing entities for ontology, recreate from import",
        "RID conflict handling: generate new RIDs for imported entities, remap references",
        "Integration test: export → import to new ontology → data matches original",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 23,
      "passes": true,
      "notes": "Depends on US-121"
    },
    {
      "id": "US-123",
      "title": "Branch diff frontend view",
      "description": "As an ontology owner, I want to see branch diffs visually in the browser so that I can review schema changes.",
      "acceptanceCriteria": [
        "Frontend route /explorer/:ontology/branches/:branch/diff page",
        "Fetches diff from GET /api/v2/ontologies/{ontology}/branches/{branch}/diff",
        "Displays changes grouped by entity_type with color coding: green=ADDED, yellow=MODIFIED, red=DELETED",
        "MODIFIED entries show before/after side-by-side comparison",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 24,
      "passes": true,
      "notes": "Depends on US-116"
    },
    {
      "id": "US-124",
      "title": "Automate — migration and rule model",
      "description": "As a developer, I need database tables for automation rules and executions so that the Automate engine has persistence.",
      "acceptanceCriteria": [
        "Migration 000026 creates automation_rules table: id UUID PK, ontology_rid FK, name, description, status CHECK (active/paused/disabled), trigger_type CHECK (schedule/dataChange/manual), trigger_config JSONB, effects JSONB, created_by, created_at/updated_at",
        "Migration 000026 creates automation_executions table: id UUID PK, rule_id FK, trigger_event JSONB, started_at, completed_at, status CHECK (running/success/error/retrying), error TEXT, retry_count INT DEFAULT 0",
        "OMS models: AutomationRule and AutomationExecution structs",
        "Repository interface adds: CreateAutomationRule, GetAutomationRule, ListAutomationRules, UpdateAutomationRule, DeleteAutomationRule, InsertExecution, ListExecutions",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 25,
      "passes": true,
      "notes": "Phase 3: Automate Engine"
    },
    {
      "id": "US-125",
      "title": "Automate — CRUD API endpoints",
      "description": "As a user, I want to create and manage automation rules via API.",
      "acceptanceCriteria": [
        "POST /api/v2/ontologies/{ontology}/automationRules creates rule",
        "GET /api/v2/ontologies/{ontology}/automationRules lists rules (with ?status= filter)",
        "GET /api/v2/ontologies/{ontology}/automationRules/{id} gets rule details",
        "PUT /api/v2/ontologies/{ontology}/automationRules/{id} updates rule",
        "DELETE /api/v2/ontologies/{ontology}/automationRules/{id} deletes rule",
        "POST /api/v2/ontologies/{ontology}/automationRules/{id}/pause sets status=paused",
        "POST /api/v2/ontologies/{ontology}/automationRules/{id}/resume sets status=active",
        "Integration test: full CRUD lifecycle",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 26,
      "passes": true,
      "notes": "Depends on US-124"
    },
    {
      "id": "US-126",
      "title": "Automate — Schedule Trigger (cron)",
      "description": "As a user, I want automation rules to run on cron schedules so that routine tasks are automated.",
      "acceptanceCriteria": [
        "New pkg/automate/scheduler.go using github.com/robfig/cron/v3",
        "Scheduler loads all active schedule rules at startup",
        "trigger_config format: { cron: '0 */6 * * *' }",
        "Cron fires → look up rule effects → execute them (initially just log execution)",
        "Execution record written to automation_executions with status and timing",
        "Rule status changes (pause/resume/delete) update scheduler dynamically",
        "Unit test: mock cron tick → execution record created",
        "go test ./pkg/automate/... passes",
        "Typecheck passes"
      ],
      "priority": 27,
      "passes": false,
      "notes": "Depends on US-125"
    },
    {
      "id": "US-127",
      "title": "Automate — Data Change Trigger",
      "description": "As a user, I want automation rules to trigger on object data changes so that I can react to real-time events.",
      "acceptanceCriteria": [
        "New pkg/automate/watcher.go subscribing to NATS ChangeEvent callback",
        "trigger_config format: { objectType: 'Employee', where: {...}, editTypes: ['CREATE','MODIFY'] }",
        "Watcher evaluates each ChangeEvent against active dataChange rules",
        "Match = objectType matches AND editType in editTypes AND (where clause matches OR no where clause)",
        "Debounce support: trigger_config.debounceMs (default 0, no debounce)",
        "Integration test: create object matching rule trigger → execution recorded",
        "go test ./pkg/automate/... passes",
        "Typecheck passes"
      ],
      "priority": 28,
      "passes": false,
      "notes": "Depends on US-125"
    },
    {
      "id": "US-128",
      "title": "Automate — Action Effect",
      "description": "As a user, I want automation rules to execute Actions automatically so that routine operations are hands-free.",
      "acceptanceCriteria": [
        "Effect type executeAction: { type: 'executeAction', actionTypeApiName, parameters: {...} }",
        "Parameters support template variables: ${event.primaryKey}, ${event.properties.fieldName}, ${event.editType}",
        "Template resolution from trigger event data before passing to ActionExecutor",
        "Execution delegates to existing actions.Executor.Apply, records ActionLog",
        "On failure: execution marked as error with error message",
        "Unit test: data change trigger → executeAction effect → ActionLog created",
        "go test ./pkg/automate/... passes",
        "Typecheck passes"
      ],
      "priority": 29,
      "passes": false,
      "notes": "Depends on US-127"
    },
    {
      "id": "US-129",
      "title": "Automate — Function Effect",
      "description": "As a user, I want automation rules to execute Functions so that custom logic runs automatically.",
      "acceptanceCriteria": [
        "Effect type executeFunction: { type: 'executeFunction', functionRid, parameters: {...} }",
        "Delegates to GojaDispatcher or HTTPDispatcher based on functionRid prefix",
        "Function result stored in automation_executions.result JSONB",
        "Chain support: effect can reference previous effect output via ${effects[0].result}",
        "Unit test: schedule trigger → executeFunction → result recorded",
        "go test ./pkg/automate/... passes",
        "Typecheck passes"
      ],
      "priority": 30,
      "passes": false,
      "notes": "Depends on US-126"
    },
    {
      "id": "US-130",
      "title": "Automate — Notification Effect (backend)",
      "description": "As a user, I want automation rules to send notifications so that I'm alerted about important events.",
      "acceptanceCriteria": [
        "Migration 000027 creates notifications table: id UUID PK, user_id TEXT, title, body, type TEXT, link TEXT, read BOOLEAN DEFAULT false, created_at",
        "Effect type notification: { type: 'notification', channel: 'platform'|'email', template: '...', recipients: [...] }",
        "Platform channel: insert into notifications table",
        "Email channel: send via configurable SMTP (WEAVE_SMTP_HOST, WEAVE_SMTP_PORT env vars); graceful skip if not configured",
        "Template supports ${event.*} variable substitution",
        "GET /api/v2/notifications lists user's notifications (with ?unread=true filter)",
        "POST /api/v2/notifications/{id}/read marks notification as read",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 31,
      "passes": false,
      "notes": "Depends on US-125"
    },
    {
      "id": "US-131",
      "title": "Automate — retry and monitoring",
      "description": "As an admin, I want automation execution history with retry support so that failures are recoverable.",
      "acceptanceCriteria": [
        "AutomationRule config adds retry policy: { maxRetries: 3, backoffMs: 1000 }",
        "On effect failure: if retries remaining → schedule retry with exponential backoff; update execution status=retrying",
        "After max retries exhausted: status=error with final error message",
        "GET /api/v2/ontologies/{ontology}/automationRules/{id}/executions returns execution history",
        "Execution list supports pagination and ?status= filter",
        "Unit test: effect fails → retried 3 times → final status=error",
        "go test ./pkg/automate/... passes",
        "Typecheck passes"
      ],
      "priority": 32,
      "passes": false,
      "notes": "Depends on US-128"
    },
    {
      "id": "US-132",
      "title": "WebSocket — server infrastructure",
      "description": "As a developer, I need a WebSocket server so that clients can subscribe to real-time object changes.",
      "acceptanceCriteria": [
        "New pkg/subscriptions/ package with Hub and Connection types",
        "Hub manages active connections and subscription routing",
        "Connection wraps nhooyr.io/websocket with read/write goroutines",
        "go get nhooyr.io/websocket added to go.mod",
        "Route: GET /api/v2/ontologies/{ontology}/subscriptions/ws upgrades to WebSocket",
        "Auth: Bearer token in ?token= query parameter validated before upgrade",
        "Graceful shutdown: Hub.Close() drains all connections",
        "Unit test: connect → receive welcome message → disconnect",
        "go test ./pkg/subscriptions/... passes",
        "Typecheck passes"
      ],
      "priority": 33,
      "passes": false,
      "notes": "Phase 4: WebSocket Subscriptions"
    },
    {
      "id": "US-133",
      "title": "WebSocket — object change subscription",
      "description": "As a client, I want to subscribe to object changes on a specific ObjectType so that I receive real-time updates.",
      "acceptanceCriteria": [
        "Subscribe message: { type: 'subscribe', objectType: '...', where?: {...}, select?: [...] }",
        "Success response: { type: 'subscribed', subscriptionId: 'uuid' }",
        "Change event: { type: 'objectChanged', subscriptionId, state: 'ADDED_OR_UPDATED'|'DELETED', object: {...} }",
        "Where clause filtering: only push events matching the filter",
        "Select clause: only include specified properties in pushed object",
        "Max 10 subscriptions per connection (returns error on 11th)",
        "Unit test: subscribe → trigger object change → receive event with correct format",
        "go test ./pkg/subscriptions/... passes",
        "Typecheck passes"
      ],
      "priority": 34,
      "passes": false,
      "notes": "Depends on US-132"
    },
    {
      "id": "US-134",
      "title": "WebSocket — NATS event routing and lifecycle",
      "description": "As a system, I need to route NATS ChangeEvents to matching WebSocket subscriptions efficiently.",
      "acceptanceCriteria": [
        "Hub subscribes to funnel.Consumer ChangeEvent callback",
        "Each ChangeEvent matched against active subscriptions by objectType",
        "Where clause evaluated per subscription (reuse where/converter logic)",
        "Unsubscribe: { type: 'unsubscribe', subscriptionId } removes subscription",
        "Connection disconnect auto-cleans all subscriptions",
        "Heartbeat: 30s ping/pong; no pong within 60s → close connection",
        "onOutOfDate event sent when subscription buffer overflows",
        "Unit test: 3 subscriptions on same connection, change matches 2 → 2 events sent",
        "go test ./pkg/subscriptions/... passes",
        "Typecheck passes"
      ],
      "priority": 35,
      "passes": false,
      "notes": "Depends on US-133"
    },
    {
      "id": "US-135",
      "title": "WebSocket — frontend integration",
      "description": "As a frontend user, I want the Browser page to use WebSocket for real-time updates.",
      "acceptanceCriteria": [
        "New web/src/hooks/useWebSocketSubscription.ts hook",
        "Connects to /api/v2/ontologies/{ontology}/subscriptions/ws",
        "Sends subscribe message with objectType and optional where/select",
        "On objectChanged event: invalidates TanStack Query cache for affected object",
        "Auto-reconnect with exponential backoff (1s → 2s → 4s → max 30s)",
        "Browser page toggle: 'Live' button uses WebSocket when available, SSE as fallback",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 36,
      "passes": false,
      "notes": "Depends on US-134"
    },
    {
      "id": "US-136",
      "title": "SDK Generator — framework and CLI",
      "description": "As a developer, I need a code generation framework so that SDKs can be generated from ontology metadata.",
      "acceptanceCriteria": [
        "New pkg/sdkgen/ package with Generator interface: Generate(schema OntologySchema, lang string, outputDir string) error",
        "OntologySchema struct loads full metadata from OMS: ObjectTypes with Properties, LinkTypes, ActionTypes, Interfaces",
        "CLI entrypoint: cmd/sdkgen/main.go with flags --ontology, --lang (ts|python|go), --output, --server-url",
        "API endpoint: POST /api/v2/ontologies/{ontology}/sdkgen?lang=ts returns zip file",
        "BaseType → language type mapping tables defined per language",
        "Unit test: load test ontology → verify OntologySchema populated correctly",
        "go test ./pkg/sdkgen/... passes",
        "Typecheck passes"
      ],
      "priority": 37,
      "passes": false,
      "notes": "Phase 5: OSDK Code Generation"
    },
    {
      "id": "US-137",
      "title": "TypeScript SDK generation",
      "description": "As a TypeScript developer, I want an auto-generated SDK so that I can interact with ontology objects type-safely.",
      "acceptanceCriteria": [
        "Each ObjectType → TypeScript interface with property types mapped (String→string, Integer→number, Boolean→boolean, Date→string, etc.)",
        "Generated client class with methods: get(pk), list(opts), search(where, opts), for each ObjectType",
        "Action methods: apply{ActionName}(params) with typed parameter interface",
        "Link traversal: {objectType}.linkedObjects.{linkType}(pk) methods",
        "Output includes package.json (name: @weave/{ontology}-sdk) and tsconfig.json",
        "Integration test: generate SDK → npx tsc --noEmit → passes",
        "go test ./pkg/sdkgen/... passes",
        "Typecheck passes"
      ],
      "priority": 38,
      "passes": false,
      "notes": "Depends on US-136"
    },
    {
      "id": "US-138",
      "title": "Python SDK generation",
      "description": "As a Python developer, I want an auto-generated SDK so that I can interact with ontology objects with type hints.",
      "acceptanceCriteria": [
        "Each ObjectType → Pydantic BaseModel with field types (String→str, Integer→int, Boolean→bool, Date→datetime.date, etc.)",
        "Generated WeaveClient class (based on httpx) with methods per ObjectType",
        "Action methods with typed parameter dataclass",
        "Output includes pyproject.toml with dependencies (httpx, pydantic)",
        "Generated __init__.py exports all models and client",
        "Integration test: generate SDK → python -m mypy → passes",
        "go test ./pkg/sdkgen/... passes",
        "Typecheck passes"
      ],
      "priority": 39,
      "passes": false,
      "notes": "Depends on US-136"
    },
    {
      "id": "US-139",
      "title": "Go SDK generation",
      "description": "As a Go developer, I want an auto-generated SDK so that I can interact with ontology objects idiomatically.",
      "acceptanceCriteria": [
        "Each ObjectType → Go struct with json tags and correct types (String→string, Integer→int32, Long→int64, etc.)",
        "Generated Client struct (based on net/http) with typed methods per ObjectType",
        "Action methods with typed parameter structs",
        "Output includes go.mod (module weave-sdk/{ontology})",
        "Generated code formatted with gofmt",
        "Integration test: generate SDK → go vet → passes",
        "go test ./pkg/sdkgen/... passes",
        "Typecheck passes"
      ],
      "priority": 40,
      "passes": false,
      "notes": "Depends on US-136"
    },
    {
      "id": "US-140",
      "title": "SDK version tracking",
      "description": "As a developer, I want SDK versions to track ontology versions so that I know when to regenerate.",
      "acceptanceCriteria": [
        "Generated SDK includes metadata file with ontologyVersion, generatedAt, serverUrl",
        "CLI --diff mode: compare current ontology version with last generated version, print changed types",
        "Generated CHANGELOG.md lists added/modified/deleted ObjectTypes and Properties",
        "API response includes X-Ontology-Version header on sdkgen endpoint",
        "Unit test: generate → modify ontology → diff shows changes",
        "go test ./pkg/sdkgen/... passes",
        "Typecheck passes"
      ],
      "priority": 41,
      "passes": false,
      "notes": "Depends on US-137"
    },
    {
      "id": "US-141",
      "title": "Developer Console — Application registration",
      "description": "As a developer, I want to register applications and get OAuth credentials.",
      "acceptanceCriteria": [
        "Migration 000028 creates applications table: id UUID PK, name, description, client_id TEXT UNIQUE, client_secret_hash TEXT, redirect_uris TEXT[], scopes JSONB, created_by, created_at/updated_at",
        "POST /api/v2/developer/applications creates app (auto-generates client_id + client_secret, returns secret once)",
        "GET /api/v2/developer/applications lists caller's applications",
        "GET /api/v2/developer/applications/{id} gets app details (without secret)",
        "DELETE /api/v2/developer/applications/{id} deletes app",
        "Integration test: create app → list → get → delete",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 42,
      "passes": false,
      "notes": "Phase 6: Developer Console"
    },
    {
      "id": "US-142",
      "title": "OAuth 2.0 authorization code flow with PKCE",
      "description": "As a developer, I want OAuth 2.0 flows so that users can authorize my app to access Weave.",
      "acceptanceCriteria": [
        "Migration 000029 creates oauth_authorization_codes and oauth_tokens tables",
        "GET /oauth/authorize renders consent screen (HTML page showing app name, requested scopes)",
        "POST /oauth/authorize approves → redirects with authorization code",
        "POST /oauth/token grant_type=authorization_code → returns access_token + refresh_token",
        "POST /oauth/token grant_type=client_credentials → returns access_token (app-level)",
        "PKCE: code_challenge + code_verifier validation (S256 method)",
        "Access tokens include scopes claim; auth middleware checks scope intersection",
        "Integration test: full authorization_code flow → valid token → scoped API access",
        "go test ./... passes",
        "Typecheck passes"
      ],
      "priority": 43,
      "passes": false,
      "notes": "Depends on US-141"
    },
    {
      "id": "US-143",
      "title": "Developer Console — API Playground frontend",
      "description": "As a developer, I want an interactive API playground to test API calls without writing code.",
      "acceptanceCriteria": [
        "Frontend route /developer/playground page",
        "Loads OpenAPI spec from /swagger endpoint and parses it",
        "Left panel: endpoint list grouped by tag, with search filter",
        "Right panel: parameter form auto-generated from OpenAPI schema, Send button, response viewer",
        "Code snippet tabs: curl, TypeScript, Python, Go (auto-generated from current request)",
        "Request history saved to localStorage (last 50 requests)",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 44,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-144",
      "title": "Developer Console — Usage Metrics middleware",
      "description": "As a developer, I want API usage metrics tracked per application so that I can monitor performance.",
      "acceptanceCriteria": [
        "New pkg/metrics/usage.go: UsageMiddleware records per-request metrics",
        "Prometheus counters: weave_api_requests_total{endpoint, method, status, app_id}",
        "Prometheus histogram: weave_api_request_duration_seconds{endpoint, method, app_id}",
        "App ID extracted from OAuth token claims (or 'anonymous' for unauthenticated)",
        "Middleware inserted in chi router chain after auth middleware",
        "GET /api/v2/developer/applications/{id}/usage returns aggregated metrics (24h/7d/30d) via PromQL or in-memory",
        "go test ./pkg/metrics/... passes",
        "Typecheck passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": "Depends on US-141"
    },
    {
      "id": "US-145",
      "title": "Developer Console — Metrics dashboard frontend",
      "description": "As a developer, I want to see API usage metrics in a visual dashboard.",
      "acceptanceCriteria": [
        "Frontend route /developer/metrics page",
        "Request volume time-series chart (using SimpleChart component or uPlot)",
        "Endpoint latency table: P50, P95, P99 per endpoint",
        "Error rate bar chart with top errors",
        "Time range selector: 24h / 7d / 30d",
        "Per-application filter dropdown",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 46,
      "passes": false,
      "notes": "Depends on US-144"
    },
    {
      "id": "US-146",
      "title": "Ontology Manager — ObjectType CRUD page",
      "description": "As an ontology owner, I want a visual interface to create and edit Object Types.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/objectTypes page",
        "ObjectType list with search, status filter, sort by name",
        "Create form: displayName input → auto-generates apiName (camelCase) and pluralDisplayName",
        "Edit form: all fields editable (displayName, description, icon, color, status, visibility)",
        "Delete button with confirmation modal showing impacted LinkTypes and ActionTypes count",
        "Uses existing OMS API endpoints for CRUD operations",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 47,
      "passes": false,
      "notes": "Phase 7: Ontology Manager UI"
    },
    {
      "id": "US-147",
      "title": "Ontology Manager — Property visual editor",
      "description": "As an ontology owner, I want a visual property editor so that I can define properties without JSON.",
      "acceptanceCriteria": [
        "Embedded in ObjectType edit page as Properties tab",
        "Add property form: apiName, displayName, BaseType dropdown, isArray toggle, isNullable toggle, isSearchable toggle, isSortable toggle",
        "Struct type: sub-form for field definitions (name + type rows, add/remove)",
        "Mark primaryKey and titleProperty with radio buttons (one each per ObjectType)",
        "Edit existing property inline; delete with confirmation",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 48,
      "passes": false,
      "notes": "Depends on US-146"
    },
    {
      "id": "US-148",
      "title": "Ontology Manager — LinkType visual management",
      "description": "As an ontology owner, I want to create and manage link types visually.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/linkTypes page",
        "List view: source → target with cardinality badge",
        "Create form: select source ObjectType, target ObjectType, cardinality dropdown (ONE_TO_ONE/ONE_TO_MANY/MANY_TO_MANY)",
        "Foreign key config form for ONE_TO_* cardinality",
        "Delete with confirmation showing impacted Actions and searchAround queries",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 49,
      "passes": false,
      "notes": "Depends on US-146"
    },
    {
      "id": "US-149",
      "title": "Ontology Manager — ActionType visual builder",
      "description": "As an ontology owner, I want a visual action builder to define parameters and rules without JSON.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/actionTypes page",
        "Parameter builder: add/edit/remove parameters with name, type dropdown, required toggle, description",
        "Rule builder: add rules with type dropdown (createObject/modifyObject/deleteObject/createLink/deleteLink/createOrModify)",
        "Property binding: dropdown to select parameter or enter static value for each rule property",
        "JSON preview panel showing generated rules/parameters JSON",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 50,
      "passes": false,
      "notes": "Depends on US-146"
    },
    {
      "id": "US-150",
      "title": "Ontology Manager — Interface management",
      "description": "As an ontology owner, I want to manage interfaces visually.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/interfaces page",
        "Create form: apiName, displayName, description, parent interface (extendsRID) dropdown",
        "SharedProperties editor: select from available shared properties",
        "OutgoingLinkTypes editor: add interface link types with target type and cardinality",
        "Implementing ObjectTypes list with Attach/Detach buttons",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 51,
      "passes": false,
      "notes": "Depends on US-146"
    },
    {
      "id": "US-151",
      "title": "Ontology Manager — force-directed schema graph",
      "description": "As an ontology owner, I want a full schema graph view to see all types and relationships.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/graph page",
        "Force-directed graph layout using d3-force (npm install d3-force @types/d3-force)",
        "Nodes = ObjectTypes with icon and color from metadata",
        "Edges = LinkTypes with cardinality labels on edges",
        "Click node → navigate to ObjectType edit page",
        "Zoom (scroll wheel), pan (drag background), reset view button",
        "Filter by TypeGroup or Interface dropdown",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 52,
      "passes": false,
      "notes": "Depends on US-146"
    },
    {
      "id": "US-152",
      "title": "Ontology Manager — audit history page",
      "description": "As an admin, I want to see a timeline of all ontology metadata changes.",
      "acceptanceCriteria": [
        "Frontend route /admin/:ontology/history page",
        "Fetches from existing GET /api/v2/ontologies/{ontology}/audit endpoint (audit.Store)",
        "Timeline list: actor, action, resource_type, resource_rid, timestamp",
        "Expand row to see before/after diff (JSON diff view)",
        "Filters: entity type dropdown, actor text input, date range picker",
        "Pagination: load more button (cursor-based)",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 53,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-153",
      "title": "Frontend — CSV / JSON data export",
      "description": "As a user, I want to export object data as CSV or JSON from the Browser page.",
      "acceptanceCriteria": [
        "Browser page adds Export dropdown button (CSV | JSON) in header toolbar",
        "Export applies current search/filter (not full table dump)",
        "CSV: header row with property apiNames, data rows with values, downloaded as {objectType}-export.csv",
        "JSON: { data: [...], metadata: { objectType, exportedAt, count } } downloaded as {objectType}-export.json",
        "Large exports (>1000 rows): paginate through all results before generating file",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 54,
      "passes": false,
      "notes": "Phase 8: Frontend Enhancements"
    },
    {
      "id": "US-154",
      "title": "Frontend — bulk operations UI",
      "description": "As a user, I want to select multiple objects for batch operations.",
      "acceptanceCriteria": [
        "Browser ObjectTable adds checkbox column (per-row + select-all header checkbox)",
        "Selection state managed in component (selectedPrimaryKeys set)",
        "Floating toolbar appears when items selected: shows count + Delete + Export Selected buttons",
        "Delete Selected: confirmation modal → calls ApplyBatch with delete actions",
        "Export Selected: downloads CSV/JSON of selected rows only",
        "Clear selection button",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 55,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-155",
      "title": "Frontend — time-series chart in ObjectDetail",
      "description": "As a user, I want to see time-series properties as interactive line charts.",
      "acceptanceCriteria": [
        "ObjectDetail panel detects TimeSeries type properties and renders chart instead of raw value",
        "npm install uplot (lightweight ~35KB chart library)",
        "Time range selector: 1h / 24h / 7d / 30d buttons",
        "Fetches data from GET /timeseries/{property}/streamPoints with time range",
        "Chart shows time on X-axis, value on Y-axis with hover tooltip",
        "Multiple TimeSeries properties shown as separate charts",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 56,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-156",
      "title": "Frontend — map view for geospatial data",
      "description": "As a user, I want to see Geopoint/Geoshape properties on a map.",
      "acceptanceCriteria": [
        "npm install leaflet react-leaflet @types/leaflet",
        "Browser page adds 'Map' toggle button next to table/list view",
        "Map view renders OpenStreetMap tiles (no API key needed)",
        "Geopoint properties displayed as circle markers",
        "Geoshape properties displayed as GeoJSON polygons/lines",
        "Click marker opens popup with object primaryKey + title property",
        "Click popup link opens ObjectDetail panel",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 57,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-157",
      "title": "Frontend — notification center",
      "description": "As a user, I want a notification center to see alerts from Automate and system events.",
      "acceptanceCriteria": [
        "Topbar adds bell icon with unread count badge (fetches from GET /api/v2/notifications?unread=true)",
        "Click bell opens SlidePanel with notification list",
        "Each notification shows: title, body, time (relative), link (clickable)",
        "Mark as read: click notification or 'Mark all read' button (POST /api/v2/notifications/{id}/read)",
        "Auto-refresh every 30s via TanStack Query refetchInterval",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 58,
      "passes": false,
      "notes": "Depends on US-130 (notifications table)"
    },
    {
      "id": "US-158",
      "title": "Frontend — CSV data import wizard",
      "description": "As a user, I want to import data from CSV files to quickly populate object types.",
      "acceptanceCriteria": [
        "Frontend route /import/:ontology page with 4-step wizard",
        "Step 1: drag-and-drop CSV file upload (parse with FileReader + manual CSV split)",
        "Step 2: select target ObjectType dropdown; auto-map CSV columns to Properties by name match",
        "Step 3: preview first 10 rows in table; show type conversion warnings (yellow badge per cell)",
        "Step 4: execute import via Action batch (create objects); progress bar showing rows processed",
        "Error summary: list of failed rows with error messages",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 59,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-159",
      "title": "Admin navigation — sidebar and routing",
      "description": "As an ontology owner, I want admin pages integrated into the main navigation so that I can access management tools.",
      "acceptanceCriteria": [
        "Sidebar adds 'Admin' section (visible only to ontology-owner and admin roles)",
        "Admin sub-links: Object Types, Link Types, Action Types, Interfaces, Schema Graph, History",
        "Each link navigates to /admin/:ontology/{section}",
        "App.tsx adds routes for all /admin/:ontology/* pages",
        "PermissionGate wraps admin routes (requires ontology.write permission)",
        "npx tsc --noEmit passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 46,
      "passes": false,
      "notes": "Must be implemented before other admin pages (US-146+). Re-prioritized to run before admin pages."
    }
  ]
}
