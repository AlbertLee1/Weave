{
  "project": "Weave-Vertex",
  "branchName": "ralph/vertex-replication-v1",
  "description": "Stream A: Vertex replication — 18 stories",
  "tddConvention": "Red → Green → Refactor。每个 story 必须先写失败测试（Red commit，CI 红）→ 写最小实现（Green commit，测试绿）→ 可选重构 commit。后端 Go 测试命名 Test{Feature}_Given_{State}_When_{Action}_Then_{Outcome}；前端用 web/src/test/bdd.ts 的 given().when().then() 链式 helper。",
  "userStories": [
    {
      "id": "VTX-010",
      "name": "Vertex 专用 Link Type Classes",
      "phase": 2,
      "active": false,
      "depends_on": [],
      "dependencies": "pkg/oms LinkType 元数据；migrations",
      "description": "调研报告 §2.1 要求每个 LinkType 在 Ontology 中带上 vertex:link_primary_direction / link_undirectional / link_bidirectional 之一，决定边箭头方向。",
      "bdd_acceptance": [
        "Given OMS 的 LinkType 元数据 When 增加 type_classes []string 字段并 migration Then 可存 vertex 三类标签",
        "Given 一条 LinkType 标 vertex:link_primary_direction When Vertex graph render 边 Then 用 primary 箭头样式",
        "Given 一条标 vertex:link_bidirectional When render Then 双向箭头",
        "OMS Admin UI 可勾选 type class",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "TypeClasses field on LinkType + Vertex tag validation + admin UI checkboxes + pickEdgeArrowStyle helper."
    },
    {
      "id": "VTX-011",
      "name": "SystemGraph Payload JSON Schema 校验",
      "phase": 2,
      "active": false,
      "depends_on": [
        "VTX-009"
      ],
      "dependencies": "github.com/xeipuuv/gojsonschema 或类似；pkg/oms 元数据查询",
      "description": "保存图时强校验 payload 结构，避免脏数据。",
      "bdd_acceptance": [
        "Given payload 缺少 layers 字段 When POST Then 返回 400 + 明确错误 \"layers field required\"",
        "Given layer.objectTypeRid 引用不存在的 ObjectType When POST Then 返回 422 + \"objectType not found\"",
        "Given edge.linkTypeRid 不存在 When POST Then 返回 422",
        "Given positions 中含非数值坐标 When POST Then 返回 400",
        "schema 文件在 pkg/vertex/graphsvc/schema/graph.schema.json",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": "PayloadValidator wraps embedded graph.schema.json (Draft-07) + OMS reference checks. Schema failures → 400 InvalidGraphPayload; unknown objectType/linkType RIDs → 422 WEAVE_VALIDATION_SCHEMA. Wired on POST + PUT /api/vertex/v1/graphs."
    },
    {
      "id": "VTX-012",
      "name": "Graph Template 资源",
      "phase": 2,
      "active": false,
      "depends_on": [
        "VTX-009"
      ],
      "dependencies": "VTX-009 REST 基础",
      "description": "调研报告 §2.4 — Save as Template 后参数化。",
      "bdd_acceptance": [
        "Given 已有图 When POST /save-as-template 传入 parameterizedFields:[\"layers[0].filter.objectRid\"] Then 写入 graph_templates(rid, source_graph_rid, parameters JSONB)",
        "Given 模板 + 参数 {objectRid:\"ri...Airport.JFK\"} When POST /templates/{rid}/instantiate Then 返回 instantiated graph payload（参数替换完成）",
        "Given 模板含 search around 函数 RID When instantiate 时传入新对象 Then 自动调用 search around 扩展图",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Backend-only: pure Instantiate(payload, parameterizedFields, params) + POST /api/vertex/v1/templates/{rid}/instantiate handler. Path syntax `layers[0].filter.objectRid`; param binding by leaf segment. searchAroundFnRid + objectRids params → searchAroundCalls plan injected into payload (matches web/.../parameterizedSearchAround.ts shape). Bad path → 400 InvalidTemplateField; unknown template RID → 404 TemplateNotFound."
    },
    {
      "id": "VTX-013",
      "name": "共享链接 / 权限模型",
      "phase": 2,
      "active": false,
      "depends_on": [
        "VTX-009"
      ],
      "dependencies": "pkg/auth；pkg/rls；pkg/masking",
      "description": "调研报告 §7.6 — Graph 资源走 Project-level RBAC + Action policy + 共享链接（缺权限可见结构不可见值）。",
      "bdd_acceptance": [
        "Given Graph A 由 user1 创建 When user2 无 read 权限访问 Then 返回 403",
        "Given user1 生成 share link When user3 用 link 访问 Then 返回 graph 结构但 layers 内对象属性值替换为 \"***\"（结构可见值不可见）",
        "Given share link 已 revoke When user3 再访问 Then 返回 410 Gone",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Owner-based read ACL on GET /graphs/{rid}: ownerless graphs stay public (legacy compat), otherwise CreatedBy == user.ID OR admin role required (else 403 PERMISSION_DENIED GraphReadForbidden). New ShareLinkStore (Mem + PG-backed via migration 000203 graph_share_links) + 3 routes: POST /graphs/{rid}/share-links (owner-only, returns opaque base64url token), DELETE /share-links/{token} (owner-only revoke, idempotent → 204), GET /share-links/{token}/graph (404 unknown, 410 revoked, 200 masked). maskLayerPropertyValues walks payload.layers and replaces every scalar leaf under any `properties` subtree with \"***\" while preserving keys + non-property structure. New apierror.NewGone (410, errorCode NOT_FOUND) added for the revoked case."
    },
    {
      "id": "VTX-014",
      "name": "Workshop 嵌入 widget API",
      "phase": 2,
      "active": false,
      "depends_on": [
        "VTX-009"
      ],
      "dependencies": "pkg/apps 既有 widget 机制",
      "description": "调研报告 §6.5 — Workshop 嵌入 Vertex Graph。",
      "bdd_acceptance": [
        "Given Workshop module 引用 vertex_graph widget 传入 graphRid 参数 When Workshop 渲染 widget Then 调用 Weave 后端 /api/vertex/v1/graphs/{rid}/widget 返回紧凑 payload（无历史、无 saved selections）",
        "Given widget 支持 overrideGraphRid URL 参数 When Save action 触发 Then 把当前 graph state 存到 overrideGraphRid 对应资源",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Backend-only: GET /api/vertex/v1/graphs/{rid}/widget returns the same Graph shape with payload run through widgetCompactPayload (strips top-level `savedSelections` + `history`; everything else byte-for-byte). POST /api/vertex/v1/graphs/{rid}/widget/save accepts `{payload, overrideGraphRid?}`; empty/omitted override falls back to the source rid in the path. ACL enforced on the *target* graph (not source) — owner-or-admin via canReadGraph. Unknown target → 404 GraphNotFound. Both routes ride the contract allow-list until OpenAPI lands."
    },
    {
      "id": "VTX-015",
      "name": "Vertex Control Panel（Admin 配置）",
      "phase": 2,
      "active": false,
      "depends_on": [],
      "dependencies": "pkg/auth admin role",
      "description": "调研报告 §7.6 — 默认时窗、轮询间隔、search-around limits、active icon categories。",
      "bdd_acceptance": [
        "Given 初始未配置 When GET /api/vertex/v1/admin/control-panel Then 返回默认值 {defaultWindowDays:30, pollingIntervalSec:5, searchAroundMaxNodes:200, searchAroundMaxDepth:3, missingDataWarningHours:24}",
        "Given admin 调 PUT 修改 pollingIntervalSec=10 When 普通用户 GET Then 看到 10",
        "Given 非 admin When PUT Then 403",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 15,
      "passes": true,
      "notes": "Backend-only: new pkg/vertex/controlpanel package with Config struct + DefaultConfig() + Store interface (MemStore + PGStore via migration 000204 vertex_control_panel). GET /api/vertex/v1/admin/control-panel is open to any caller and returns DefaultConfig when no row has been written. PUT requires the 'admin' role (anonymous/non-admin → 403 ControlPanelUpdateForbidden); body is a sparse map (pointer fields) merged onto current state so partial updates are first-class. Store-level Validate enforces strictly-positive integers; rejected writes never leak into Get (Set returns ErrInvalidConfig → 400 InvalidControlPanelConfig). Routes ride the contract allow-list."
    },
    {
      "id": "VTX-016",
      "name": "SystemGraph E2E 冒烟测试",
      "phase": 2,
      "active": false,
      "depends_on": [
        "VTX-007",
        "VTX-008",
        "VTX-009",
        "VTX-011",
        "VTX-012",
        "VTX-013",
        "VTX-014",
        "VTX-015"
      ],
      "dependencies": "test/e2e/vertex/",
      "description": "闭环验证 Phase 2。",
      "bdd_acceptance": [
        "Given 全栈启动 When 创建图 → 保存 → 加版本 → duplicate → 拉历史 Then 全流程 200 OK，时间 ≤ 1 s",
        "E2E 测试在 test/e2e/vertex/graph_lifecycle_test.go",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 16,
      "passes": true,
      "notes": "test/e2e/vertex/graph_lifecycle_test.go drives the full Phase-2 chain (POST create → PUT save v=2 → PUT bump v=3 → POST duplicate (fresh rid + v=1 reset) → GET history) through the full chi router over MemRepo + MemTemplateStore + MemShareLinkStore — same shape as cmd/server.main degraded-mode boot, no PG required. ≤ 1s wall-clock budget enforced; observed ~240µs. Used a schema-clean validGraphPayload helper so a later default-on validator wire-up doesn't break the smoke. No production sources changed; the test is the only new asset."
    },
    {
      "id": "VTX-017",
      "name": "前端依赖与画布最小框架",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-009"
      ],
      "dependencies": "npm: sigma, graphology, @react-sigma/core；React Router；Vite",
      "description": "作为前端开发者，我需要在 web/ 引入 Sigma.js + Graphology，并搭出 Vertex 工作区路由 /vertex/:graphRid。",
      "bdd_acceptance": [
        "Given npm install sigma graphology @react-sigma/core When make web-build Then 构建成功，bundle 大小不超过 +500 KB",
        "Given 访问 /vertex/new When 页面挂载 Then 显示空 canvas + TopBar（save/share/layout/timeSelection/run）",
        "Given 访问 /vertex/{rid} 不存在的 rid When 加载 Then 显示 \"Graph not found\" + 返回首页按钮",
        "含 Vitest snapshot 测试",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Lazy-loaded VertexWorkspacePage (web/src/vertex/VertexWorkspacePage.tsx) wired to /vertex/:rid via React.lazy + Suspense in App.tsx so the heavy Sigma/Graphology bundle is split off the initial chunk (main bundle gzip delta = +0.22 kB; new VertexWorkspacePage chunk = 164 kB raw / 39.81 kB gzip — well under the +500 KB budget). /vertex/new mounts an empty Graph in <SigmaContainer>; /vertex/{unknownRid} fetches /api/vertex/v1/graphs/{rid}, maps 404 → Graph not found + back-to-Dashboard <Link to=\"/\">. Tests at web/src/vertex/VertexWorkspacePage.test.tsx (4 cases incl. snapshot) mock @react-sigma/core to dodge jsdom WebGL. Manual browser verification still needed (no dev-browser skill in this environment)."
    },
    {
      "id": "VTX-018",
      "name": "节点渲染（圆形节点 + 标题 + subtitle）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-017"
      ],
      "dependencies": "VTX-017 画布；TanStack Query 拉 payload",
      "description": "从后端拉图 payload，渲染节点 + 边。",
      "bdd_acceptance": [
        "Given payload 含 1 layer + 50 个 Airport 对象 When 页面加载 Then Sigma 画布显示 50 个节点，每节点带 name 作为 subtitle",
        "Given 边 50 条 When 渲染 Then 边按 primary direction 显示箭头",
        "Given 滚轮 When 用户滚动 Then 缩放生效；Given 拖拽空白 When 拖动 Then 平移生效",
        "Benchmark：5000 节点首屏 ≤ 2 s（在 dev 机）",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 18,
      "passes": true,
      "notes": "Pure payloadToGraph(payload) projection in web/src/features/vertex/render/payloadToGraph.ts: dedupes objects by objectRid across layers, reads payload.positions[rid] when set (otherwise a deterministic radius-scaled circle so first paint has something to draw before VTX-022 layouts), maps typeClasses through pickEdgeArrowStyle → Sigma edge type ('arrow' for primary, 'line' for undirectional, 'arrow'+bothArrows:true for bidirectional), and drops edges whose source/target aren't in the node set. VertexWorkspacePage now feeds the projection into Sigma via a GraphLoader (useLoadGraph) so each render builds a fresh graphology Graph + bulk-adds nodes/edges in one effect. Zoom (scroll) and pan (drag-empty) come from Sigma's default camera controls — no custom handlers. Bundle delta vs VTX-017: main +0.22 kB raw / +0.05 kB gzip; VertexWorkspacePage chunk +2.03 kB raw / +0.81 kB gzip — well under the 500 KB BDD budget. 11 Vitest tests added (10 projection + perf gate of <500 ms on 5000 nodes vs the 2 s BDD spec, plus 2 page-level renderer tests asserting graphology Graph order/size/labels/edge types). Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-019",
      "name": "节点 DOM overlay（卡片式 Extended Labels）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018"
      ],
      "dependencies": "React Portal；Sigma 相机变换 API",
      "description": "调研报告 §10.1 — 用 Canvas 绘节点底版 + DOM 绝对定位卡片显示扩展标签。",
      "bdd_acceptance": [
        "Given 一个节点 + 3 个扩展标签（property/timeseries/measure） When render Then 节点上方有 absolute-positioned div 显示 3 个标签",
        "Given 用户缩放/平移 When 相机变换 Then DOM overlay 跟随",
        "Given 节点离屏 When 计算可见性 Then overlay 隐藏（性能优化，virtualization）",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 19,
      "passes": true,
      "notes": "Frontend-only: extractExtendedLabels(payload, objectRid) in web/src/features/vertex/render/extendedLabels.ts (pure JS, dedupes via first-layer-wins like payloadToGraph, returns {key, kind, label, value?} with property values stringified + missing → '—', timeSeries/measure values land in VTX-059+). VertexNodeOverlay in web/src/vertex/VertexNodeOverlay.tsx mounts inside <SigmaContainer> next to GraphLoader: useSigma() + useRegisterEvents() → re-render trigger on afterRender; reads each node's x/y via graph.getNodeAttribute, projects to viewport via sigma.graphToViewport, virtualization clips nodes outside sigma.getDimensions(). Each card is absolute-positioned with translate(-50%, calc(-100% - 14px)) so it floats above the node. Per-kind rows: property shows label + value, timeSeries shows sparkline placeholder ▁▂▃▄▅, measure shows ellipsis (real renderers in VTX-059+). Page wires labelsByRid via useMemo over projection.nodes. 13 new tests (8 helper + 5 overlay) + 1 integration test on the page. Bundle delta vs VTX-018: VertexWorkspacePage chunk +4.06 kB raw / +1.14 kB gzip; main bundle unchanged. Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-020",
      "name": "选择交互（单击 / 框选）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018"
      ],
      "dependencies": "Sigma 事件 API；Zustand selection state",
      "description": "选中节点 + 触发 sidebar；shift 框选；Ctrl/Cmd 多选。",
      "bdd_acceptance": [
        "Given 用户单击节点 When 触发 Then 节点高亮 + 右侧 sidebar 打开显示属性",
        "Given 用户按住 shift + 框选 When 释放 Then 框内节点全选 + sidebar 显示批量信息",
        "Given Ctrl/Cmd + 单击 When 触发 Then 加/减选",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 20,
      "passes": true,
      "notes": "Frontend-only. Pure selection state (web/src/features/vertex/selections/selectionState.ts: ReadonlySet<string> + selectionSingle/Toggle/Replace/Add/Clear) + pure box-select math (boxSelect.ts: rectFromCorners + nodesInRect inclusive-edge containment). VertexSelectionLayer.tsx mounts inside <SigmaContainer>, subscribes via useRegisterEvents to clickNode (single-click → selectionSingle; ctrl/meta or shift+clickNode → selectionToggle), clickStage (no modifier → selectionClear), downStage/mousemovebody/mouseup (shift held → render dashed-blue overlay rect → selectionReplace on release via nodesInRect over sigma.graphToViewport). VertexSelectionSidebar.tsx: hidden when selection is empty; single shows label + property list; ≥2 shows count + label list. SelectionHighlighter Sigma child mutates `highlighted` + `color` node attributes on each selection change so the next paint draws selected nodes in #3B82F6 — no graph rebuild. payloadToObjectSummaries(payload) projects payload.layers[*].objects[*] into Map<rid, {rid,label,properties}> for the sidebar (first-layer-wins dedupe matching payloadToGraph/extractExtendedLabels). 30 new Vitest tests (9 selectionState + 4 boxSelect + 4 objectSummaries + 6 SelectionLayer + 4 Sidebar + 3 page-level VTX-020 integration). Page test mock now merges handlers across useRegisterEvents calls (overlay + selection layer + highlighter share one captured map) so clickNode/clickStage can be fired without losing afterRender. Bundle delta vs VTX-019: VertexWorkspacePage chunk +5.54 kB raw / +1.45 kB gzip — well under the 500 KB BDD budget. Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-021",
      "name": "右侧 Selection Sidebar",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-020"
      ],
      "dependencies": "OSS get / search API",
      "description": "调研报告 §6.1 — 选中对象自动打开 sidebar 显示 Properties / Series / Linked Events / Derived Funcs。",
      "bdd_acceptance": [
        "Given 选中一个 Airport 对象 When sidebar 加载 Then 显示 4 个 tab：Properties / Series / Linked Events / Derived Funcs",
        "Given Properties tab When 渲染 Then 列出对象所有属性 + 值（用 OSS get API）",
        "Given Series tab + 对象有 2 个 time series property When 切换 Then 显示 sparkline mini chart",
        "Given Linked Events tab When 加载 Then 显示该对象的最近 50 个事件",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 21,
      "passes": true,
      "notes": "Frontend-only. Right sidebar gains a 4-tab single-object panel (Properties / Series / Linked Events / Derived Funcs) while batch mode (VTX-020) is unchanged. VertexObjectSummary picks up optional ontologyApiName / objectType / primaryKey, sourced by payloadToObjectSummaries from layer.ontology + layer.objectType + an explicit properties.primaryKey (falls back to the last `.`-segment of objectRid). PropertiesTab calls OSS get via useGetObject (path `/api/v2/ontologies/<ont>/objects/<type>/<pk>`) and merges fresh values over the snapshot bag (strips `__rid`/`__primaryKey`/`__apiName` wire keys); falls back gracefully to the snapshot when api-names are absent. SeriesTab filters extendedLabels for kind `timeSeries`, derives the property key from the label's stable key, and renders a per-series MiniSparkline (SVG path scaled to 120×24) via useTimeSeriesPoints — empty state shows the ▁▂▃▄▅ placeholder until ≥2 numeric points arrive. LinkedEventsTab uses useObjectActivity with pageSize=50 (matches BDD '最近 50 个事件'), flattens all loaded pages, and caps at 50; explicit unavailable/loading/error/empty rows. DerivedFuncsTab lists `measure` extendedLabels as placeholder cards (real evaluation lands in VTX-064). VertexWorkspacePage feeds `labelsByRid` into the sidebar prop `extendedLabelsByRid`. Tests: 11 new (4 VTX-020 baseline migrated to QueryClientProvider + 7 VTX-021 BDD: 4-tabs visible / OSS get fetched + listed / fallback when no api-names / 2 sparklines on series tab / activity API w/ pageSize=50 / measure cards on derived funcs / tab switch preserves state). objectSummaries.test.ts +3 cases for the new metadata fields. Page test wrapper now mounts QueryClientProvider so the single-selected sidebar can resolve useQuery. Bundle delta vs VTX-020: VertexWorkspacePage chunk 175.71 → 181.32 kB raw (+5.61 kB) / 43.21 → 44.62 kB gzip (+1.41 kB); main bundle 3,652.77 → 3,653.06 kB raw (+0.29 kB) / 1,075.76 → 1,075.74 kB gzip (-0.02 kB). Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-022",
      "name": "布局算法 — Hierarchical（dagre）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018"
      ],
      "dependencies": "npm: dagre, graphology-layout-dagre",
      "description": "调研报告 §2.3 + §10.1。",
      "bdd_acceptance": [
        "Given 安装 dagre + graphology-layout-dagre When 用户点 TopBar Layout → Hierarchical Then 节点按层次排列",
        "Given 用户配置 reverse=true 翻转方向 When Apply Then 上下反转",
        "Given 用户指定 root nodes When Apply Then 以指定根开始分层",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 22,
      "passes": true,
      "notes": "Hierarchical layout via @dagrejs/dagre. Pure helper hierarchicalLayout({nodes,edges,reverse,rootNodes}) returns Map<id,{x,y}>; rankdir TB by default, BT when reverse; explicit rootNodes forced to top tier by stripping every edge whose target is a root before dagre.layout(). PRD's named npm 'graphology-layout-dagre' does not exist on the registry — installed @dagrejs/dagre@3.0.0 directly and wrote the adapter in-tree. TopBar Layout button now opens a popover (reverse checkbox + comma-separated root-rids input + Apply) that hands a HierarchicalLayoutSpec to the page-level state; a LayoutApplier Sigma child writes positions back via setNodeAttribute(x|y) + sigma.refresh()."
    },
    {
      "id": "VTX-023",
      "name": "布局算法 — Force-directed + Circular",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-022"
      ],
      "dependencies": "npm: graphology-layout-forceatlas2, graphology-layout-circular",
      "description": "ForceAtlas2 + Circular + Auto 启发式（< 100 节点用 Force，否则 Hierarchical）。",
      "bdd_acceptance": [
        "Given graphology-layout-forceatlas2 When Layout → Force-directed Then 节点按 ForceAtlas2 收敛",
        "Given graphology-layout-circular When Layout → Circular Then 节点环形排列",
        "Given Auto 模式 When 节点数 < 100 Then 用 Force，否则 Hierarchical",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 23,
      "passes": true,
      "notes": "Frontend-only. Two pure helpers (`forceAtlas2Layout`, `circularLayout`) + an `pickAutoLayoutKind(nodeCount)` heuristic (< 100 → force, otherwise hierarchical). The PRD's named `graphology-layout-circular` does NOT exist on npm — `graphology-layout` already ships circular as `graphology-layout/circular`, so we depend on the umbrella package and import the subpath. ForceAtlas2 uses 150 iterations + a deterministic seed circle (radius 100) so nodes never share an origin (the algorithm degenerates with co-located points); falls back to the seed when the lib produces NaN/Infinity. Circular's helper rewraps the upstream `scale`/`center` (where `center` is a normalized 0..1 offset multiplied by `scale`) into a saner `radius` + explicit `centerX`/`centerY` contract by running upstream at `center: 0.5` (offset = 0) and applying our own translation. VertexWorkspacePage's LayoutMenu now has a radio group (`vertex-layout-kind-{hierarchical|force|circular|auto}`) — hierarchical's reverse/root controls hide for the other kinds. LayoutApplier dispatches via a pure `computeLayoutPositions(spec, nodes, edges)` helper. 19 new tests (8 force + 5 circular + 2 auto-pick + 5 page-level: force apply / circular radius / auto < 100 → force / auto ≥ 150 → hierarchical / hierarchical-controls toggle). Bundle delta vs VTX-022: VertexWorkspacePage chunk 224.89 → 239.05 kB raw (+14.16 kB) / 59.44 → 63.69 kB gzip (+4.25 kB); main bundle unchanged. Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-024",
      "name": "手动拖拽 + position 持久化",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018",
        "VTX-009"
      ],
      "dependencies": "Sigma drag plugin；PATCH /graphs/{rid}/layout",
      "description": "调研报告 §2.3。",
      "bdd_acceptance": [
        "Given 用户拖动节点 A When 释放 Then 节点新坐标 + pinned=true 自动 PATCH 到 /graphs/{rid}/layout",
        "Given pinned 节点 When 重跑布局算法 Then pinned 节点不动，其他重排",
        "Given 用户右键 → Unpin Then pinned=false，下次布局参与计算",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 24,
      "passes": true,
      "notes": "Backend: PATCH /api/vertex/v1/graphs/{rid}/layout now gates writes through canReadGraph (owner-or-admin; ownerless legacy graphs stay anonymously patchable) and the request body MERGES into payload.positions key-by-key (jsonb '||' in PG / mergePositionsBytes in MemRepo) so a single-node drag doesn't clobber siblings. New handler_layout_test.go covers stranger→403 / owner→200 / admin→200 / legacy-anon→200 / subset PATCH preserves other keys / pinned bool round-trips. Frontend: pure dragPersistence helpers (pinnedPositionsFromPayload + formatLayoutPatchBody). Each layout helper (hierarchicalLayout / forceAtlas2Layout / circularLayout) now accepts pinnedPositions: Map<id,{x,y}> via a shared applyPinnedOverrides(out, knownIds, pinned) post-step that overwrites layout-computed coords for pinned ids; unknown ids are ignored so callers pass the full map without filtering. VertexDragLayer subscribes to Sigma's downNode/mousemovebody/mouseup, writes live x/y to graphology + sigma.refresh, then PATCHes /layout on release with pinned:true. VertexNodeContextMenu listens to rightClickNode only (clickNode/clickStage owned by VertexSelectionLayer — multi-subscriber pattern of the existing useRegisterEvents mock would otherwise overwrite handlers); dismisses via document-level mousedown. Page state uses a seed (useMemo over payload.positions) + user-diff ({set, cleared}) pair to dodge react-hooks/set-state-in-effect on the fetch→ready transition; merged pinnedPositions flows into LayoutApplier so the helpers see the override on every Apply. Bundle delta: VertexWorkspacePage chunk 239.05 → 243.35 kB raw (+4.30 kB) / 63.69 → 64.84 kB gzip (+1.15 kB). 1647/1647 Vitest pass, +13 vs VTX-023 (4 pinnedLayout + 5 dragPersistence + 4 page VTX-024). Pre-existing baseline TestContract_AllRoutesDocumented failure for VTX-030 timeseries OpenAPI gap is unrelated. Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-025",
      "name": "Link Merging（同向同类型边合并）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018",
        "VTX-010"
      ],
      "dependencies": "Graphology 自定义 reducer",
      "description": "调研报告 §2.5 + §10.1。",
      "bdd_acceptance": [
        "Given A→B 有 5 条同 LinkType 边 + Vertex 设置 merge=true When render Then 显示 1 条粗边 + 标 ×5",
        "Given merge=false When render Then 5 条边平行显示",
        "graphology 自定义 reducer 实现",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 25,
      "passes": true,
      "notes": "Frontend-only. Pure graphology-style edge reducer `mergeEdgesByLinkType(edges, {merge})` in web/src/features/vertex/render/mergeEdges.ts: groups by (source, target, linkTypeRid) triple — direction-aware (A→B never merges with B→A), edges without linkTypeRid stream through ungrouped, singletons (count=1) get no ×N label / no size bump so merge=on visually matches merge=off for already-unique edges. Multi-member groups collapse to one edge with `count: number`, `label: \"×N\"`, `size: 1 + log2(count) * 1.5` (log2 keeps thickness sane at high N). VertexEdge gained optional `linkTypeRid` (preserved by payloadToGraph), `count`, `label`, `size`. GraphLoader switched from `new Graph()` to `new MultiGraph()` so parallel edges can co-exist when merge=off; the reducer is what collapses them when merge=on. TopBar gains a Merge links toggle (`data-testid=\"vertex-topbar-merge-toggle\"`, aria-pressed reflects state) wired to page-level `mergeEnabled` state (default OFF — keeps existing dashboards / test fixtures unchanged until users opt in). 14 new Vitest tests (10 mergeEdges helper + 1 payloadToGraph linkTypeRid preservation + 3 page-level VTX-025: default→5 distinct edges / toggle on→1 edge with label ×5 + count=5 + size>1 / toggle off→back to 5). Snapshot updated for the new TopBar button. Bundle delta vs VTX-024: VertexWorkspacePage chunk 243.35 → 244.64 kB raw (+1.29 kB) / 64.84 → 65.27 kB gzip (+0.43 kB); main bundle unchanged. Manual browser verification still needed (no dev-browser skill wired)."
    },
    {
      "id": "VTX-026",
      "name": "节点 Context Menu（右键菜单）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-018"
      ],
      "dependencies": "Radix UI ContextMenu 或自研 popover",
      "description": "调研报告 §2.5 — 右键 → Search Around / Open in Quiver / Open in Object Explorer / Pin / Hide。",
      "bdd_acceptance": [
        "Given 用户右键节点 When 弹出菜单 Then 至少 5 项：Search Around、Open in Object Explorer、Pin/Unpin、Hide、Copy RID",
        "Given 点击 Open in Object Explorer When 触发 Then 新 tab 打开 /explorer/{ontology}?objectRid=...",
        "Given 点击 Hide When 触发 Then 该节点 + 其唯一邻居孤立的边一并隐藏（不删数据，仅 UI 状态）",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    },
    {
      "id": "VTX-027",
      "name": "节点 add 对话框（+ Add objects）",
      "phase": 3,
      "active": false,
      "depends_on": [
        "VTX-026"
      ],
      "dependencies": "OSS search API",
      "description": "调研报告 §2.1 — 手工方式：从 + Add objects 搜索对话框检索对象类型/对象实例加入图。",
      "bdd_acceptance": [
        "Given TopBar 有 + Add objects 按钮 When 点击 Then 打开 dialog，含 ObjectType 下拉 + 对象搜索框",
        "Given 搜索 JFK When 用户输入 Then 显示匹配对象列表，可勾选",
        "Given 用户选中 5 个对象 + 点击 Add When 触发 Then 5 个节点出现在 graph 上，默认位置",
        "Verify in browser using dev-browser skill",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 27,
      "passes": false,
      "notes": ""
    }
  ]
}