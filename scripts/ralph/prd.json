{
  "project": "Weave Self-Iteration PR CI Formatting Repair",
  "branchName": "ralph/self-iteration-20260518-164046",
  "description": "Small Ralph self-iteration focused on repairing the current pull-request CI golangci-lint gofmt failure for Weave's OSv2 service and upper-layer experience work. PR CI run 26031952938 failed after the prior coverage repair because cmd/covercheck/main_test.go was not gofmt-clean.",
  "userStories": [
    {
      "id": "SELF-405",
      "name": "Restore gofmt-clean covercheck CI test",
      "phase": "PR CI Repair",
      "active": true,
      "priority": 1,
      "passes": true,
      "dependencies": [],
      "description": "The current PR Go CI job fails during golangci-lint because cmd/covercheck/main_test.go contains struct field alignment that gofmt would rewrite. Repair the formatting-only regression so the CI lint step can proceed to the already-covered build, vet, test, and coverage gates.",
      "bdd_acceptance": [
        "Given cmd/covercheck/main_test.go contains the SELF-404 coverage floor test, When gofmt evaluates the file, Then it reports no formatting diff or filename.",
        "Given golangci-lint enforces new gofmt issues in PR CI, When the covercheck tests are linted after the repair, Then the previous gofmt issue at cmd/covercheck/main_test.go:161 is absent.",
        "Given the covercheck coverage-floor contract test is still present, When the targeted covercheck tests run, Then the SELF-404 behavior remains covered while the file stays formatted."
      ],
      "ci_evidence": [
        "GitHub PR #16 run 26031952938 on 2026-05-18 failed Go / golangci-lint.",
        "CI log reported `cmd/covercheck/main_test.go:161:1: File is not properly formatted (gofmt)`.",
        "Local reproduction on 2026-05-18: `gofmt -l cmd/covercheck/main_test.go` printed `cmd/covercheck/main_test.go`.",
        "Local red on 2026-05-18: `sh -c 'files=$(gofmt -l cmd/covercheck/main_test.go); if [ -n \"$files\" ]; then printf \"%s\\n\" \"$files\"; exit 1; fi'` failed and printed `cmd/covercheck/main_test.go`.",
        "Green on 2026-05-18: `sh -c 'files=$(gofmt -l cmd/covercheck/main_test.go); if [ -n \"$files\" ]; then printf \"%s\\n\" \"$files\"; exit 1; fi'` passed.",
        "Green on 2026-05-18: `go test ./cmd/covercheck -run TestLoadConfig_CICoverageFloorsTrackCurrentBroadPackageReality -count=1 -v` passed.",
        "Green on 2026-05-18: `go test ./cmd/covercheck -count=1` passed.",
        "Green on 2026-05-18: `go build ./...` passed.",
        "Green on 2026-05-18: `go vet ./...` passed.",
        "Green on 2026-05-18: `go test ./...` passed.",
        "Green on 2026-05-18: `jq empty scripts/ralph/prd.json` passed.",
        "Green on 2026-05-18: `git diff --check` passed.",
        "Local note on 2026-05-18: `command -v golangci-lint` found no local binary, so the gofmt-specific CI issue was verified with the exact gofmt gate."
      ],
      "quality_gates": [
        "`test -z \"$(gofmt -l cmd/covercheck/main_test.go)\"`",
        "`go test ./cmd/covercheck -run TestLoadConfig_CICoverageFloorsTrackCurrentBroadPackageReality -count=1 -v`",
        "`go test ./cmd/covercheck -count=1`",
        "`go build ./...`",
        "`go vet ./...`",
        "`go test ./...`",
        "`jq empty scripts/ralph/prd.json`",
        "`git diff --check`"
      ]
    }
  ]
}
