# Ralph Progress Log
Started: Wed May 20 12:32:19 CST 2026

Archived previous passing PRD:
- scripts/ralph/archive/self-iteration-20260520-123219/prd.json
- scripts/ralph/archive/self-iteration-20260520-123219/progress.txt

Fresh health evidence:
- main: `gh run list --branch main --limit 5` showed latest CI run 26109104836 success and latest Codespaces prebuild 26109104180 success.
- branch: `gh run list --branch ralph/self-iteration-20260520-002052 --limit 8` showed latest pull_request CI run 26141075643 success.
- local inspection: `dogfood-output/weave-webui-dogfood-20260517.md` remains the latest web dogfood output; no newer dogfood-output file targets this review blocker.

LLM review auto-fix evidence:
- PR: https://github.com/AlbertLee1/Weave/pull/39
- Branch: ralph/self-iteration-20260520-002052
- Reviewed HEAD: 25cfd23ae809
- Review status: request_changes
- Summary: Request changes: the main SDKs mostly follow start-and-poll, but web scenario-run helpers/tests still preserve stale terminal-response semantics and the stream REST fallback is not aligned with the mounted run-record schema.
- Blocking findings:
```json
[
  {
    "severity": "warning",
    "file": "web/src/features/vertex/scenarioPane/scenarioRun.ts",
    "line": 305,
    "issue": "The sync scenario-run helper still models POST /api/vertex/v1/scenarios/{rid}/runs as a terminal 200 response with {status:'success', durationMs, scenario}. The same helper's request builder now targets the mounted /runs start route, whose contract is 202 {runRid,status} followed by GET /runs/{runRid} polling. Any caller using this helper against the real route will parse the accepted response as malformed instead of polling, and the BDD test at scenarioRun.test.ts:474 still documents the impossible 200 OK flow.",
    "suggestion": "Replace or quarantine the old sync terminal response helpers. Make the web run flow parse the 202 start response and poll GET /runs/{runRid} until terminal, or route callers through scenarioRunAsync. Update scenarioRun.test.ts to cover the real start-and-poll contract instead of a mocked 200 success payload."
  },
  {
    "severity": "warning",
    "file": "web/src/vertex/scenarioRunStream.ts",
    "line": 176,
    "issue": "pollFinalState now calls the mounted GET /runs/{runRid} route, but it reads body.runRid/body.scenarioRunRid. The documented/server VertexScenarioRun record uses rid, so the fallback returns null for real terminal records. It also treats any non-'succeeded' status as failed, which would misreport valid pending/running/canceled records.",
    "suggestion": "Parse the mounted run-record shape: use body.rid for the run RID, return completed only for succeeded, failed only for failed (and handle canceled explicitly or return null), and leave pending/running as null. Update scenarioRunStream.test.ts fixtures to use {rid, status} from the OpenAPI contract."
  }
]
```
- Required repair: fix only the stale sync scenario-run helper/tests and `pollFinalState` run-record parsing described above.

Codebase Patterns:
- CI pins golangci-lint in `.github/workflows/ci.yml` and uses `only-new-issues: true`; local reproductions should use the same pinned version when the binary is not installed.
- Go SDK Vertex scenario-run routes are covered in `sdk/go/vertex_test.go` and should stay aligned with `/api/vertex/v1/scenarios/{scenarioRid}/runs`.
- Mounted Vertex scenario-run server contract is start/poll/cancel only: `POST /api/vertex/v1/scenarios/{scenarioRid}/runs`, `GET /api/vertex/v1/scenarios/{scenarioRid}/runs/{runRid}`, and `POST /api/vertex/v1/scenarios/{scenarioRid}/runs/{runRid}/cancel`.
- OpenAPI schemas `VertexScenarioRunStartResponse` and `VertexScenarioRun` define start as `{runRid,status}` and terminal polling as the persisted run record.

## Selected Story
- `SELF-473` - Repair web scenario-run contract drift.

## SELF-473 Evidence
- Root cause: the web single-function scenario-run helper still documented and parsed POST `/api/vertex/v1/scenarios/{scenarioRid}/runs` as a terminal 200-style response with `{durationMs,scenario}`, while the mounted route accepts the run as `202 {runRid,status}` and requires GET `/runs/{runRid}` for terminal state. The stream fallback was already polling the mounted GET route, but it read stale `runRid`/`scenarioRunRid` response fields instead of the OpenAPI/server `VertexScenarioRun.rid` field and collapsed every non-`succeeded` status into failure.
- RED:
  - `npm test -- scenarioRun.test.ts scenarioRunStream.test.ts` from `web/` failed as expected: 9 failures. `scenarioRun.test.ts` lacked `parseScenarioRunAcceptedResponse`/`buildGetRunScenarioRequest`; `scenarioRunStream.test.ts` returned `null` for `{rid,status:'succeeded'|'failed'}` fixtures.
- GREEN:
  - `web/src/features/vertex/scenarioPane/scenarioRun.ts` now exposes accepted-response parsing for `202 {runRid,status}` and a mounted GET `/runs/{runRid}` request builder. The stale terminal POST parser/types/comments were removed from the helper and tests.
  - `web/src/vertex/scenarioRunStream.ts` now parses `VertexScenarioRun.rid`, returns completed only for `succeeded`, failed only for `failed`, and returns `null` for `pending`, `running`, and `canceled`.
- Verification:
  - `npm test -- scenarioRun.test.ts scenarioRunStream.test.ts` from `web/` passed: 2 files, 62 tests.
  - `npm test -- scenarioRun.test.ts scenarioRunAsync.test.ts scenarioRunStream.test.ts` from `web/` passed: 3 files, 144 tests.
  - `npm run typecheck` from `web/` passed.
  - `git diff --check` passed.
- Files changed:
  - `web/src/features/vertex/scenarioPane/scenarioRun.ts`
  - `web/src/features/vertex/scenarioPane/scenarioRun.test.ts`
  - `web/src/vertex/scenarioRunStream.ts`
  - `web/src/vertex/scenarioRunStream.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning: web scenario-run helpers should treat POST `/runs` as start-only. Any terminal UI transition must be driven by the persisted run record from GET `/runs/{runRid}`; stream fallbacks must parse the same `VertexScenarioRun` shape as the mounted REST endpoint.
