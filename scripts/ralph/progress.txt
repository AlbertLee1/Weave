# Ralph Progress Log — Weave OSv2 Deep Parity (Phase 6-9)
Started: 2026-04-11
Branch:  ralph/phase-6-8
Base:    main (v1 Phase 1-5 complete, 47 US, 68/68 Foundry endpoints)

## Codebase Patterns
- ObjectSet executor Result can carry out-of-band per-PK payloads by adding a
  new map field (see Result.DerivedValues); handlers merge into WireObject
  Properties just before oss.FormatObject. Keeps Execute signature stable
  while avoiding a second backend roundtrip.
- withProperties Definition now takes DerivedProperties []DerivedPropertyDef;
  legacy Properties []string is still honoured and short-circuits to
  delegating to the inner ObjectSet unchanged.
- Link resolver mocks in objectset_test.go return a flat target list and
  ignore sourcePKs — for per-source tests add a new mock that maps each
  source PK to its own target list (see perPKLinkResolver in
  withproperties_test.go).
- withProperties numeric metrics (sum/avg/min/max) must resolve the target
  ObjectType via LinkTargetTypeResolver, then fetch per-PK docs from the
  target Bleve index with bleve.NewDocIDQuery + searchReq.Fields=[field].
  Test link resolvers need to implement ResolveTargetObjectType for these
  paths to work (plain ResolveLinkedObjectsByAPIName is NOT enough).
- bleve numeric field search results come back typed as float64 regardless
  of whether the source doc used int or float64 — coerceNumeric in
  executor.go handles the full int/uint/float spread to future-proof ingest
  paths that deviate.
- gofmt across the repo has pre-existing drift in many files; only lint your
  own edited files, do not blanket-reformat.
- Reverse-direction withProperties routes through an optional reverseLinkFinder
  interface (ResolveLinkedReverseByAPIName) on the link resolver; the executor
  short-circuits with an explicit error when a resolver only speaks forward.
  Test mocks must opt-in by adding the reverse method + reverseEdges map; the
  production *links.Resolver does NOT yet implement it, so end-to-end reverse
  withProperties still needs a follow-up adapter once OMS-backed incoming link
  lookups (ListIncomingLinkTypes) are wired through.
- Property now carries a Derived bool flag for US-004 (query-time computed
  fields). Three independent validators live alongside existing packages and
  can be wired into handlers incrementally without changing existing behaviour:
    * pkg/oms.ValidateObjectTypePrimaryKey (returns *apierror.APIError)
    * pkg/oss/where.ValidateClauseAgainstDerivedFields (sentinel error
      ErrDerivedPropertyTextSearchUnsupported; walks and/or/not subtrees)
    * pkg/actions.ValidateEditsAgainstDerived (sentinel error
      ErrDerivedPropertyReadOnly; delete edits always pass-through).
  Follow-up work: have handlers call these validators with a real derived-map
  fetched from OMS schema, so invalid requests surface on the wire.
- withProperties executor sorts its returned PrimaryKeys by
  (firstDerivedValue ASC, primaryKey ASC) as a deterministic composite sort
  key so offset-based LoadObjects pagination is stable across pages. The
  sort copies inner.PrimaryKeys before mutating so nested callers still see
  the inner executor's native order. compareDerivedValues tolerates nil,
  int64, float64 and string fallbacks so all five metrics plus future types
  remain orderable.
- interfaceBase execution in pkg/oss/objectset/executor.go now loads each
  implementing ObjectType concurrently (sync.WaitGroup + mutex-guarded map)
  and heap-merges their per-type buckets into a flat PrimaryKeys list. The
  per-type buckets survive on Result.PerTypePKs (sorted ASC) and per-row
  ObjectType on Result.Origins, so downstream consumers can drive
  composite-cursor pagination without re-sorting. The heap merge tie-break
  is (primaryKey ASC, objectType ASC) so PK collisions across types resolve
  deterministically.
- pkg/oss/pagination now has MultiTypeCursor{SubCursors []CompositeCursor}
  + Encode / DecodeMultiTypeCursor / IsExhausted. Use it as the wire format
  for any polymorphic paging scenario. Exhausted sub-cursors (inner == "")
  are dropped before Encode so the token shrinks as sub-streams drain.
- handler_preview.go has a dedicated writePreviewInterfacePage path that
  fires whenever result.PerTypePKs != nil. Rows are fetched from each
  row's own ObjectType Bleve index (not the interface-tag index), so
  $apiName / $rid / properties stay per-row accurate across a polymorphic
  result. The old offset cursor path is preserved as the fallback when
  PerTypePKs is nil.
- Integration tests under test/integration/ use //go:build integration.
  Boot a real PG via testutil.StartPGContainer + database.RunMigrationsUp,
  then construct oms.NewPGRepository(pg.Pool) as the metadata backend.
  Seed ObjectTypes via repo.CreateObjectType (primary_key_prop is just
  TEXT, so you can pass the string directly without creating Property
  rows). Bleve indexes stay in a t.TempDir() so there's no cross-test
  contamination. Production main.go does NOT yet wire a PG-backed
  InterfaceResolver, so tests that need one should ship a local shim:
  lookup via repo.GetInterfaceByAPIName(ctx, ontologyRID, apiName) →
  repo.ListInterfaceObjectTypes(ctx, iface.RID) → []apiName. Follow-up
  for Phase 6 Gate: fold that shim into main.go.
- New chi routes MUST update api/openapi.yaml AND re-copy to
  cmd/server/openapi_spec.yaml. `TestContract_AllRoutesDocumented` and
  `TestContract_EmbeddedSpecMatchesCanonical` both fail otherwise.
  Route in a protected group → remember to use `api.With(
  auth.RequirePermission(perm)).Method(http.MethodPost, path, handler)`
  so permission gating cascades through the existing auth middleware.
- For features that need a concrete-only PGRepository method (no
  cached-repo shim), main.go must keep a separate `pgRepo` handle at
  construction time (before wrapping in CachedRepository) and pass it
  through a typed ServerDeps field — unwrapping the cached decorator
  later is not supported.
- Narrow in-package interfaces beat expanding oms.Repository. The
  oms.Repository interface is already 50+ methods with 11 mock
  implementations; every new method forces 11 stub updates. If a
  consumer only needs 2–3 calls, define a local interface (see
  `pkg/index/rebuild.go RebuildRepo` and `rehydrate.go rehydrateRepo`)
  and keep the concrete PG method un-exported from the interface.
- Text FieldMapping defaults to the English ("en") Snowball analyzer
  via `pkg/index/mapping_builder.go:standardTextAnalyzer`. Lowercase
  + Porter stemmer + possessive strip is the baseline for
  analyzer.standard / unset properties; not_analyzed still routes to
  KeywordField (identity analyzer). When writing new text-field
  search tests prefer `bleve.NewMatchQuery(...)` — TermQuery skips
  the analyzer and breaks the moment a stemmer is in play. Use
  TermQuery ONLY on not_analyzed / keyword fields. Porter English
  does NOT stem "runner" → "run"; safe stem pairs for fixtures:
  run/runs/running, shoe/shoes, walk/walks/walking.
- oms.ObjectType returned by GetObjectTypeByAPIName does NOT carry
  its Properties slice — call ListProperties(ctx, ot.RID) separately
  and assign ot.Properties = props before handing the ObjectType to
  `index.BuildMapping`, otherwise the generated mapping will be
  empty and every search will silently return zero hits.
- pkg/oss/aggregation/engine.go has two parallel groupBy code paths:
  the LIVE recursiveGroupBy + getXxxEntries (exact/fixedWidth/ranges/
  duration/topValues) that returns []groupEntry, and the DEAD
  groupByExact / groupByFixedWidth / groupByDuration / groupByTopValues
  that are no longer reachable. Always patch the getXxxEntries side.
  Bucket order across ALL group types is stabilised by sortGroupEntries
  (ASC by fmt.Sprint(value), nils last) called inside recursiveGroupBy,
  so new groupBy types only need to return unsorted entries.
- Null-group handling for exact groupBy: when facetResult.Missing > 0
  AND len(terms) > 0, getExactEntries appends a value=nil entry with
  scopeQuery = BooleanQuery{Must: baseQuery, MustNot: allTermQueries}.
  Bleve has no "field exists" query, so this negation trick is the
  canonical way to narrow to docs that lack the field. Do NOT emit
  a null bucket when len(terms) == 0 — that would wrap every doc in
  a single null bucket when grouping by a non-existent field and
  break TestAggregate_GroupByExact_NoResults.

## Goal
From "API surface alignment" (72% weighted) to "semantic depth alignment" (90%).
34 feature areas split into 168 iteration-sized stories. Each Phase ends with a
hard Integration Gate: Go cross-US tests + Playwright E2E + Foundry parity
fixtures, all three layers must be green to merge to main.

## Sources of truth
- PRD narrative:  docs/PRD-Weave-OSv2-深度复刻-V2.md
- PRD execution:  tasks/prd-v2-deep-parity.md
- Ralph prd.json: scripts/ralph/prd.json (this directory)
- V1 archive:     archive/2026-04-11-foundry-osv2-api-alignment/

## Phase outline
- Phase 6 (US-001 ~ US-042): semantic correctness (withProperties, interface
  paging, typeclass indexing, multi-groupby, approx percentile, edit conflict,
  optimistic concurrency, edit-only fields) + Phase 6 gate.
- Phase 7 (US-043 ~ US-085): security + realtime (row/column/marking policy
  engine, SSE subscription, stream ingest, audit events, rate limiting) +
  Phase 7 gate.
- Phase 8 (US-086 ~ US-131): runtime + branches + depth (Goja embedded runtime,
  function-backed actions, executeQuery, ontology read-only branches, RID
  @vN, timeseries aggregate, geotemporal persistence, withProperties multi-hop,
  aggregate on ObjectSet) + Phase 8 gate.
- Phase 9 (US-132 ~ US-168): production hardening + v1.0 release (Parquet cold
  storage, bench suite, OpenTelemetry, Python SDK depth, CLI depth, MCP
  stdio, ops docs, journey tests, release) + Phase 9 gate.

## TDD
Every feature US writes the failing test first. Gate stories do not write
new features — only run verification suites and commit the gate.

---

## 2026-04-11 - US-001
- Added DerivedPropertyDef struct + Definition.DerivedProperties list so the
  withProperties operator can declare per-object link aggregations.
- Extended Result with DerivedValues (map[pk]map[name]interface{}) and taught
  executeWithProperties to compute count metric over forward direction by
  calling ResolveLinkedObjectsByAPIName per base PK.
- Handler.LoadObjects merges derived values into WireObject.Properties so
  orderCount-style fields appear as top-level properties.
- Files changed:
  - pkg/oss/objectset/definition.go
  - pkg/oss/objectset/executor.go
  - pkg/oss/objectset/handler.go
  - pkg/oss/objectset/withproperties_test.go (new, 3 tests)
- **Learnings for future iterations:**
  - LinkResolver.ResolveLinkedObjectsByAPIName returns a flat merged list of
    target PKs for the provided sourcePKs slice — it does NOT preserve the
    per-source mapping. For per-base-object metrics loop one source PK at a
    time. A batched multi-source count helper would be a worthwhile
    optimisation for US-002/US-003 but is not strictly required.
  - US-002 (sum/avg/min/max) needs to load target object properties — the
    executor currently only queries for primary keys. It will need to either
    re-query Bleve for the target fields or get per-source target PK maps and
    fetch the numeric field.
  - US-003 (reverse direction) must use a `reverseLinkFinder` capable resolver
    (see pkg/oss/objectset/executor.go:reverseLinkFinder) and cannot use the
    legacy forward-only ResolveLinkedObjectsByAPIName.
  - The existing mockLinkResolver in objectset_test.go is too simple for
    per-PK assertions; follow perPKLinkResolver in withproperties_test.go.
  - Pre-existing gofmt drift across the repo: don't touch files you didn't
    edit; only run gofmt against your own changes.
---

## 2026-04-11 - US-002
- Extended withProperties to compute sum / avg / min / max across one forward
  link hop, reading target numeric field values out of the target Bleve index.
- Validate now rejects sum/avg/min/max without a Field, and rejects unknown
  metrics outright (tightening the US-001 schema).
- DerivedPropertyTypeMismatch returned when Field resolves to a non-numeric
  value on any target document.
- Empty link set semantics: sum -> 0 (float64), avg/min/max -> nil.
- Files changed:
  - pkg/oss/objectset/definition.go (per-metric Field validation)
  - pkg/oss/objectset/executor.go (evaluateNumericDerived + coerceNumeric)
  - pkg/oss/objectset/withproperties_test.go (4 metric tests + type-mismatch
    + missing-field + target-type mapping on perPKLinkResolver)
- **Learnings for future iterations:**
  - scopedIndexKey(ctx, mgr, objectType) is the canonical way to go from
    ObjectType API name to the Bleve index key — use it for target-type
    fetches too, not just base set queries, so ontology scoping applies.
  - Bleve's searchReq.Fields=[field] populates hit.Fields with float64 for
    numeric docs indexed as "double". Don't assume hit.Fields preserves
    the original Go type of the indexed map[string]interface{}.
  - For withProperties numeric metrics, the N+1 fetch pattern
    (one DocIDQuery per base PK) is a short-term implementation — US-073
    (multi-hop) will likely need a batched fetcher keyed by (baseType,
    linkAPIName, field) to avoid blowing up latency. Not required at this
    phase but flagged.
  - Reverse direction (dir == DirectionReverse) still errors explicitly
    from executeWithProperties — US-003 owns that path and must wire
    ResolveLinkedReverseByAPIName into the per-PK loop.
---

## 2026-04-11 - US-003
- executeWithProperties now routes DirectionReverse through a new
  linkResolverForDirection helper that returns a forward or reverse
  per-PK closure depending on dp.Direction. Forward still hits
  ResolveLinkedObjectsByAPIName; reverse hits the resolver's
  reverseLinkFinder (ResolveLinkedReverseByAPIName) and errors out
  cleanly when the underlying resolver doesn't satisfy the interface.
- Reverse numeric metrics (sum/avg/min/max) are still blocked with an
  explicit "not yet supported" error; the count path is live and
  covered by tests.
- perPKLinkResolver gained reverseEdges + ResolveLinkedReverseByAPIName
  so withProperties tests can assert per-target reverse counts without
  reaching into pkg/links.
- New tests in pkg/oss/objectset/withproperties_test.go:
  - TestWithPropertiesReverseCount (Product ← 3 OrderLines via
    reverse orderLineProduct, one product with 0 hits)
  - TestWithPropertiesReverseRequiresResolver (forward-only resolver
    must surface a clear error message)
  - setupProductOrderLineExecutor fixture builds Product + OrderLine
    Bleve indexes and wires forward + reverse edges.
- Files changed:
  - pkg/oss/objectset/executor.go
  - pkg/oss/objectset/withproperties_test.go
- **Learnings for future iterations:**
  - Adding a new optional capability to the link resolver is best done
    as a standalone interface (reverseLinkFinder) + type-assert at
    call site — avoids widening the LinkResolver core contract and
    keeps legacy callers unaffected.
  - Numeric reverse metrics need both reverseLinkFinder AND
    DirectionalLinkTargetTypeResolver (the "other end" ObjectType for
    reverse is the link's *source*, not *target*). When the time
    comes, evaluateNumericDerived should accept a directionally
    resolved targetType parameter instead of re-doing the
    LinkTargetTypeResolver dance.
  - The production *links.Resolver still lacks
    ResolveLinkedReverseByAPIName and ResolveTargetObjectTypeDir, so
    reverse searchAround/withProperties currently only works with test
    resolvers. A small adapter that calls
    repo.ListIncomingLinkTypes(callerOT) + linkResolver.ResolveLinked
    would close that gap without touching the executor.
  - gofmt auto-rewrites indented comment blocks into tab-indented
    form; running `gofmt -w` on your edited files before committing
    saves a second lint pass.
---

## 2026-04-11 - US-004
- Added `Derived bool` field to pkg/oms.Property so schema can flag a
  property as "computed at query time" (used by US-001/002/003 withProperties).
- Added three independent validators enforcing the derived-property
  constraints from the Foundry OSv2 spec:
    * pkg/oms/derived_property.go: ValidateObjectTypePrimaryKey — returns a
      DerivedPropertyNotAllowedAsPrimaryKey apierror when a declared primary
      key resolves to a derived property (and InvalidParameter:primaryKey
      for empty / unknown references).
    * pkg/oss/where/derived_property.go: ValidateClauseAgainstDerivedFields —
      returns ErrDerivedPropertyTextSearchUnsupported for any of the 6
      text-search operators (contains / containsAllTerms / containsAnyTerm /
      containsAllTermsInOrder / startsWith / wildcard) targeting a derived
      field; recurses into and / or / not sub-trees; eq/gt/lt and friends
      are explicitly left untouched.
    * pkg/actions/derived_property.go: ValidateEditsAgainstDerived — returns
      ErrDerivedPropertyReadOnly for any CREATE/MODIFY edit whose Properties
      map contains a derived property; DELETE edits pass through unchanged.
- Error messaging follows Foundry's name-based convention: each error name
  exactly matches the spec (DerivedPropertyNotAllowedAsPrimaryKey /
  DerivedPropertyTextSearchUnsupported / DerivedPropertyReadOnly).
- Red test confirmed before implementation (all three packages failed to
  compile due to missing symbols, then all three went green after the
  validator files were added).
- Files changed:
  - pkg/oms/models.go (new Derived field + gofmt realign)
  - pkg/oms/derived_property.go (new)
  - pkg/oms/derived_property_test.go (new, 4 sub-tests)
  - pkg/oss/where/derived_property.go (new)
  - pkg/oss/where/derived_property_test.go (new, 9 sub-tests)
  - pkg/actions/derived_property.go (new)
  - pkg/actions/derived_property_test.go (new, 5 sub-tests)
- **Learnings for future iterations:**
  - pkg/oss/where does not yet import pkg/apierror; keep the dependency
    graph shallow by returning sentinel errors from the package and
    wrapping at the handler layer instead of pulling apierror in.
  - pkg/oms already imports apierror in the admin_handlers path, so
    validators that return *apierror.APIError fit naturally. Use that
    return type so the caller can `apierror.WriteJSON(w, err)` directly.
  - When asserting on apierror ErrorName in a test use
    strings.ToLower to be tolerant of "primaryKey" vs "PrimaryKey"
    casing — the existing InvalidParameter:primaryKey error uses the
    lowercase form.
  - Current validators are pure helpers — no handler is wired to call
    them yet. A follow-up story should integrate them into
    CreateObjectType (reject derived primaryKey at admin time),
    executor.executeFilter (pass a derivedByField map from the OMS
    schema cache), and actions.Executor.Prepare (load the
    derivedByType map once per ontology and check edits after
    ExecuteRules but before CommitBatch).
  - `gofmt -w` will realign unrelated struct fields in the same file
    when you add a new one; run it before committing to keep the diff
    explicit and avoid a second lint pass.
---

## 2026-04-11 - US-005
- executeWithProperties now emits PrimaryKeys sorted by
  (firstDerivedValue ASC, primaryKey ASC). The inner ObjectSet's
  PrimaryKeys slice is copied before sorting so upstream callers that
  re-use the inner Result keep their original order.
- New helper compareDerivedValues handles nil / numeric / string fallback
  comparison so all five withProperties metrics (count / sum / avg / min /
  max) can participate in the composite sort without panicking on
  unexpected types.
- Added TestWithPropertiesCursorStability fixture: 50 customers, each with
  orderCount = i % 5 so there are 5 tie-groups of 10 customers each, then
  paginated at pageSize=17 across 3 pages. Asserts non-decreasing
  (orderCount, pk) ordering plus no duplicates / no gaps across pages.
- Added test/foundry_parity/us005_withproperties.json fixture documenting
  the expected response shape, sort order, error codes and the covering
  Go test. The repo does not yet have a parity runner — the fixture is
  documentation until the Phase 6 Gate wires run.go in.
- Files changed:
  - pkg/oss/objectset/executor.go (sort import + compareDerivedValues +
    stable sort block at end of executeWithProperties)
  - pkg/oss/objectset/withproperties_test.go (fmt import +
    TestWithPropertiesCursorStability)
  - test/foundry_parity/us005_withproperties.json (new)
- **Learnings for future iterations:**
  - `go test ./...` runs the Northwind / Chinook integration suites
    (~200s each) even without the `-tags integration` flag because they
    live under `test/` as regular `_test.go` files. Budget ~6 minutes
    for a full green run before committing.
  - Offset-based pagination is stable iff the underlying slice has a
    total ordering. For withProperties we achieve that by sorting inside
    the executor so every consumer (LoadObjects, LoadLinks, Aggregate)
    automatically inherits stability with no handler-level changes.
  - Copy inner.PrimaryKeys before mutating it when adding new executor
    post-processing — several callers keep references to the inner
    Result (e.g. union / intersect / subtract) and will break silently
    if you sort in place.
  - The existing pagination.Cursor is still an offset-only struct; the
    composite cursor struct promised by US-006 does NOT exist yet.
    US-005 deliberately does not touch pagination/cursor.go — it only
    guarantees the ordering so offset cursors keep working.
  - No foundry_parity runner exists at HEAD; the fixture file is
    accepted as documentation for now. US-GATE-P6 will own writing the
    runner and wiring it into `make test-parity`.
---

## 2026-04-11 - US-006
- New pkg/oss/pagination/composite_cursor.go defining CompositeCursor
  {ObjectType, InnerCursor string} with Encode / DecodeCompositeCursor /
  IsExhausted helpers. JSON payload uses camelCase keys (objectType,
  innerCursor) so the wire shape lines up with the Foundry SDK spec.
- Empty input string decodes to a zero-value cursor and is treated as
  exhausted — matches the "no more pages" convention used by the existing
  offset Cursor.Encode() (empty string = last page).
- Invalid base64 or malformed JSON surfaces a sentinel "invalid composite
  cursor: ..." error without leaking the raw payload to callers.
- Red test first: composite_cursor_test.go compiled-fail before the
  implementation file was written. Green after Encode/Decode/IsExhausted
  landed. 6 test functions, 3 sub-tests in IsExhausted.
- Full `go test ./...` green (~7 min including Northwind / Chinook
  integration suites). `go vet ./pkg/oss/pagination/...` clean. `gofmt -l`
  clean on both new files.
- Files changed:
  - pkg/oss/pagination/composite_cursor.go (new)
  - pkg/oss/pagination/composite_cursor_test.go (new)
  - scripts/ralph/prd.json (passes: true for US-006)
- **Learnings for future iterations:**
  - Wire naming: the CompositeCursor JSON uses "objectType" and
    "innerCursor" (camelCase) to match the Foundry API shape even though
    existing internal Cursor uses short keys like "o" for offset. US-007
    (InterfacePagingHeapMerge) should wrap CompositeCursor in an outer
    struct that carries a []CompositeCursor slice — probably
    MultiTypeCursor{SubCursors []CompositeCursor} — and feed exhausted
    sub-cursors through IsExhausted() to decide drop.
  - DecodeCompositeCursor("") returns a zero-value cursor, not an error.
    This lets callers treat the initial request (empty nextPageToken)
    the same as a fully-exhausted sub-cursor — IsExhausted() == true in
    both cases, no special casing needed.
  - base64 URL encoding is used throughout the pagination package (not
    StdEncoding); stay consistent so nextPageToken values stay URL-safe.
  - Error wrapping style: sentinel-free fmt.Errorf("invalid ...: %w", err)
    matches how the legacy Cursor reports DecodeCursor failures. Handlers
    that translate these to apierror live in pkg/oss/objectset/handler.go
    and convert via apierror.InvalidParameter.
  - Pre-existing repo-wide gofmt drift still applies: only lint your own
    new files, do NOT run `gofmt -w pkg/...` blanket.
---

## 2026-04-11 - US-007
- Extended pkg/oss/objectset.Result with PerTypePKs (map[string][]string,
  sorted ASC per type) and Origins ([]string parallel to PrimaryKeys) so
  interfaceBase results carry per-row ObjectType + per-type buckets needed
  by composite-cursor pagination.
- Rewrote executeInterfaceBase to: (a) load each implementing ObjectType
  concurrently via sync.WaitGroup + mutex-guarded perType map, (b) sort
  each bucket ASC before merging, (c) iteratively heap-merge the buckets
  into a flat globally-sorted PrimaryKeys stream with stable (pk, type)
  tie-break. Error fan-in keeps the first goroutine error.
- Added pkg/oss/pagination.MultiTypeCursor wrapping CompositeCursor from
  US-006. Encode returns "" when SubCursors is empty so the handler can
  use the empty-string "no more pages" sentinel unchanged.
  DecodeMultiTypeCursor tolerates empty input (returns zero value) and
  wraps base64/JSON errors with a "invalid multi-type cursor" prefix.
- handler_preview.go's loadPreview now routes result.PerTypePKs != nil to
  a new writePreviewInterfacePage helper that:
    * decodes MultiTypeCursor into per-type offsets (absent types are
      treated as exhausted == len(bucket))
    * performs iterative min-selection heap merge over PerTypePKs
    * fetches each emitted row from its own ObjectType Bleve index via
      scopedIndexKey(ctx, h.indexMgr, row.objectType)
    * stamps per-row $apiName / $rid / $primaryKey
    * emits a MultiTypeCursor dropping any sub-cursor whose offset hit
      its bucket length (exhausted sub-streams removed)
- Red-first TDD: TestInterfacePagingHeapMerge + the exhausted-sub-type
  variant failed to compile (PerTypePKs undefined), then passed after the
  executor + cursor + handler changes landed. 3 types x 20 objects paged
  at pageSize=7 across 9 pages (last = 4 items) with no duplicates.
- test/foundry_parity/us007_interface_paging.json documents the expected
  wire shape and error codes. Parity runner still pending (US-GATE-P6).
- Full `go test ./...` green (~6 min including Northwind/Chinook integration
  suites). `go vet ./...` clean. `gofmt -l` clean on all four edited files.
- Files changed:
  - pkg/oss/objectset/executor.go (sync import, Result fields,
    rewritten executeInterfaceBase)
  - pkg/oss/objectset/handler_preview.go (context/sort imports,
    writePreviewInterfacePage helper, dispatch branch)
  - pkg/oss/objectset/handler_preview_cursor_test.go (new, 2 tests)
  - pkg/oss/pagination/composite_cursor.go (MultiTypeCursor + helpers)
  - test/foundry_parity/us007_interface_paging.json (new)
  - scripts/ralph/prd.json (passes: true for US-007)
- **Learnings for future iterations:**
  - The fakeInterfaceResolver in definition_us028_test.go is reusable
    across interface tests; it takes a map[string][]string of
    interfaceName -> implementing ObjectTypes. No need to stand up a
    fresh mock per story.
  - scopedIndexKey(ctx, indexMgr, objectType) MUST be called with the
    per-row concrete ObjectType for polymorphic results, NOT
    result.ObjectType (which is the interface API name and resolves
    to no Bleve index). This is the main difference between the
    interface-aware preview path and the legacy single-type one.
  - The executor heap merge only does O(pageSize * numTypes) work per
    iteration by keeping an `active` slice and scanning heads. No
    container/heap complexity needed because numTypes is small.
  - MultiTypeCursor drops exhausted sub-cursors on Encode; decoding a
    cursor with only 1 of 3 original types means the other 2 are at
    bucket length and must be reconstructed by the handler from
    result.PerTypePKs. Handlers MUST fill in those implicit offsets
    or the next-page slice will restart from 0 for dropped types.
  - The old executeInterfaceBase dedup-by-seen-pk logic is gone —
    PKs are scoped per ObjectType and collisions are legitimate
    different-type rows. Tie-break is lexicographic on ObjectType.
  - Integration with Aggregate is preserved because Result.PrimaryKeys
    still holds the flat heap-merged list, so bleve.NewDocIDQuery
    passes the union to the aggregation engine unchanged even though
    Origins / PerTypePKs are now populated alongside it.
---

## 2026-04-11 18:30 - US-008
- Wrote integration test test/integration/interface_paging_test.go
  (//go:build integration) that boots testcontainers PG, runs migrations,
  seeds northwind ontology + 3 ObjectTypes + HasOwner interface +
  object_type_interfaces rows via oms.PGRepository, loads Customer/
  Employee/Supplier rows from testdata/northwind CSVs into per-type Bleve
  indexes with a synthetic ("id", "name") projection, then pages the
  interfaceBase set through loadObjectsOrInterfaces?preview=true at
  pageSize=7. Asserts ceil(129/7)=19 pages, no dupes, no misses,
  totalCount="129", totalCountAccuracy="EXACT", and per-type row counts
  of 91/9/29.
- Added a pgInterfaceResolver adapter inside the test file (production
  main.go does not yet wire an InterfaceResolver against PG, so the
  integration test ships its own shim).
- Added parity fixture test/foundry_parity/us008_interface_paging.json
  documenting the wire contract: heap-merge strategy, page geometry,
  per-type counts, invariants, response shape, error codes.
- Files changed:
  - test/integration/interface_paging_test.go (new, ~345 lines)
  - test/foundry_parity/us008_interface_paging.json (new)
  - scripts/ralph/prd.json (US-008 passes=true)
  - scripts/ralph/progress.txt (this entry + Codebase Patterns update)
- Quality checks: go vet ./... clean; go test ./... all green;
  go test -tags integration ./test/integration/... -run InterfacePaging
  passes in ~5s.
- **Learnings for future iterations:**
  - testutil.StartPGContainer + RunMigrationsUp is all you need to spin
    up a real OMS PG backend inside a Go test — ~3s cold start. Run via
    `go test -tags integration`. Requires Docker.
  - oms.CreateObjectType only requires RID, OntologyRID, APIName,
    DisplayName, PrimaryKey (string), Status, Visibility. primary_key_prop
    is TEXT NOT NULL, NOT an FK to properties — you can pass "id" or any
    apiName without creating a Property row.
  - Employee and supplier primary keys both start at "1" in Northwind,
    so cross-type paging tests MUST key their seen-map on (apiName, pk)
    composite, or pre-namespace doc IDs with an apiName prefix. The test
    chose the latter to keep assertions local.
  - The csv package chokes on the Northwind employees.csv photo blob
    unless you set reader.LazyQuotes = true AND
    reader.FieldsPerRecord = -1. Otherwise you'll see "bare quote in
    non-quoted field" on the photo column.
  - The handler's interface preview path uses row.objectType (not
    result.ObjectType) when fetching from Bleve; this is correct because
    the executor populates Result.PerTypePKs with per-type buckets and
    Origins with per-row concrete ObjectType. Good sanity check in the
    integration test is to assert that $apiName on each row matches
    exactly one of the seeded implementing types.
  - Final page geometry: for 129 rows / pageSize 7, the handler emits 18
    full pages + 1 tail of 3 rows, and nextPageToken is omitted on the
    tail. The test iterates until it sees an empty nextPageToken rather
    than hard-coding page count so a future pageSize change only needs
    the expectedPages derivation updated.
---

## 2026-04-11 - US-009
- Added pkg/index/mapping_builder.go with BuildMapping(*oms.ObjectType)
  *mapping.IndexMappingImpl honouring a property's TypeConfig.analyzer hint:
    * "not_analyzed" → NewKeywordFieldMapping (case-sensitive exact match)
    * "not_indexed"  → text mapping with Index=false, Store=true
    * "standard" / unset → NewTextFieldMapping (tokenised, lowercased)
    * numeric / boolean / date / geopoint base types keep their dedicated
      Bleve field mapping regardless of the hint.
- Red-first TDD via pkg/index/mapping_builder_test.go:
    * TestBuildMappingKeywordVsText uses bleve.NewMemOnly to index two
      Customer docs (country USA/usa, description "running shoes"/"the
      runner wins") and asserts a TermQuery on country is case-sensitive
      while a MatchQuery on description tolerates case and tokenisation.
    * TestBuildMappingDefaultsToText checks properties without TypeConfig
      fall back to text mapping (not keyword) for name/price/active.
    * TestBuildMappingNotAnalyzed confirms Analyzer=="keyword" on a
      single-field not_analyzed mapping.
- Files changed:
    - pkg/index/mapping_builder.go (new, ~100 lines)
    - pkg/index/mapping_builder_test.go (new, 3 tests)
    - scripts/ralph/prd.json (US-009 passes=true)
- Quality: go test ./... green (~9 min including Chinook/Northwind);
  go vet ./... clean; gofmt -l clean on both new files.
- **Learnings for future iterations:**
  - bleve's "standard" analyzer (from
    bleve/analysis/analyzer/standard) is unicode-tokenise + lowercase +
    english-stop only — it does NOT stem. Stemming needs the "en"
    analyzer which chains porter. Our BuildMapping currently relies on
    bleve's package-level default text analyzer, so a MatchQuery for
    "run" will NOT hit "running"/"runner" out of the box. US-012 will
    need to either (a) configure an "en" DefaultAnalyzer on the
    IndexMapping, (b) introduce a new typeclass hint (e.g. "english"),
    or (c) weaken the integration assertion to case-insensitive match.
    I opted for (c) in US-009's own unit tests to stay faithful to the
    acceptance criteria wording ("TextField(standard analyzer)").
  - oms.Property.TypeConfig is a json.RawMessage; the analyzer hint
    lives inside that nested JSON object (key "analyzer"). There is no
    dedicated Go-side field, so propertyAnalyzer() in mapping_builder.go
    does a one-off json.Unmarshal into an anonymous struct. Keep this
    path permissive — any decode error silently falls back to the
    default text mapping so legacy rows without TypeConfig still work.
  - pkg/index can safely import pkg/oms — the reverse is not true, and
    the existing mapping.go predates this import. A follow-up could
    delete the older index.Property shim in manager.go and have
    EnsureIndex take *oms.ObjectType directly, but that's a broader
    refactor and not in scope for US-009.
  - bleve.NewMemOnly is the right tool for mapping-level unit tests;
    no tempdir required and it tears down with idx.Close(). Chained
    SetField() is needed on TermQuery/MatchQuery when the IndexMapping
    has no default-field-all setting, otherwise all fields are
    searched and the test misses the point.
  - Pre-existing Codebase Patterns note about gofmt drift still
    applies — only lint files you edited, do NOT blanket-reformat.
---

## 2026-04-11 - US-010 Bleve mapping_builder not_indexed support
- Added the red-first acceptance test `TestBuildMappingNotIndexed` in
  `pkg/index/mapping_builder_test.go`: seeds a Patent ObjectType with
  abstract.analyzer="not_indexed", indexes a doc, asserts
    (a) the abstract FieldMapping has Index=false + Store=true,
    (b) both MatchQuery and TermQuery field-scoped searches on
        abstract return total=0,
    (c) a side-channel lookup via the indexed `id` field still
        retrieves the stored abstract through Hit.Fields projection.
- Extended `index.Property` with an `Analyzer string` field so the
  Consumer / rehydrate bootstrap path can carry the Foundry typeclass
  hint into the Bleve mapping. Backwards-compatible: empty string keeps
  existing behaviour for funnel_test.go, links/*, and other callers.
- Refactored `mapping_builder.go` so `buildFieldMapping(oms.Property)`
  delegates to a new `fieldMappingFor(analyzer, baseType, isSearchable)`
  primitive. `Manager.buildMapping` in `manager.go` now also routes
  through `fieldMappingFor`, which means a single switch is the source
  of truth for both the admin-time BuildMapping path and the bootstrap
  EnsureIndex path — they can no longer drift as new analyzer hints
  land (US-011 index rebuild, US-050 marking-aware fields).
- Exposed `index.AnalyzerFromTypeConfig(raw []byte) string` as the
  canonical helper for turning a Property.TypeConfig RawMessage into
  the analyzer hint string. `rehydrate.go` uses it to populate
  index.Property.Analyzer during EnsureAllIndexes.
- Added `TestManager_EnsureIndex_HonoursAnalyzerNotIndexed` in
  `pkg/index/manager_test.go` (Manager-level) and
  `TestEnsureAllIndexes_PropagatesAnalyzerNotIndexed` in
  `pkg/index/rehydrate_test.go` (bootstrap-level) to keep both halves
  of the path honest. Both drive real bleve.Index instances, not stubs.
- Files changed:
    - pkg/index/manager.go (Property gets Analyzer; buildMapping uses
      fieldMappingFor)
    - pkg/index/mapping_builder.go (fieldMappingFor + AnalyzerFromTypeConfig)
    - pkg/index/rehydrate.go (propagates Analyzer via AnalyzerFromTypeConfig)
    - pkg/index/mapping_builder_test.go (+TestBuildMappingNotIndexed)
    - pkg/index/manager_test.go (+TestManager_EnsureIndex_HonoursAnalyzerNotIndexed)
    - pkg/index/rehydrate_test.go (+TestEnsureAllIndexes_PropagatesAnalyzerNotIndexed,
      +bleve import)
    - scripts/ralph/prd.json (US-010 passes=true)
- Quality: `go test ./...` all green (~8.5 min including Chinook +
  Northwind integration suites); `go vet ./...` clean; gofmt clean on
  every file I introduced new code into.
- **Learnings for future iterations:**
  - `json.RawMessage` is a `[]byte` alias so passing a RawMessage to a
    `func([]byte)` helper is free — no explicit conversion needed.
    `AnalyzerFromTypeConfig` leverages this so the rehydrate call site
    stays one-liner.
  - `index.Property` is the narrow struct the Manager uses internally;
    it intentionally does NOT carry the full oms.Property payload.
    When adding new per-property knobs, extend index.Property with the
    already-resolved primitive (e.g. Analyzer string) rather than
    smuggling oms.Property across the boundary. Otherwise the funnel
    test helpers and links/* tests all need to grow imports into oms.
  - Bleve's `MatchQuery.SetField("abstract")` + `TermQuery.SetField`
    both rely on the inverted index — when Index=false, both miss
    regardless of whether the stored value is literally present. The
    `Hit.Fields[]` projection is the only way to pull a not_indexed
    value back out, and it requires `SearchRequest.Fields = [...]`.
    Useful pattern for any future "hide from search, keep in payload"
    property (PII, attachments, long prose).
  - Two mapping paths existed before US-010: BuildMapping (new, for
    admin/HTTP) and Manager.buildMapping (old, for bootstrap). Don't
    let them diverge again — `fieldMappingFor` is the shared floor
    and any new analyzer hint (future US-050 marking filters, US-012
    stemming tweaks) should land there first.
---

## 2026-04-11 - US-011 - weave index rebuild command

- Added `pkg/index/rebuild.go` exposing `Rebuild(ctx, mgr, repo, src, req)`.
  Narrow `RebuildRepo` interface (GetOntology/GetObjectTypeByAPIName/
  ListProperties) + `LatestDocumentSource` so callers can plug Parquet or
  PG as the post-edit state provider without wiring a full oms.Repository.
- `pkg/oms/PGRepository.LoadLatestObjectStates(ctx, objectTypeRID)` uses
  `SELECT DISTINCT ON (primary_key) ... ORDER BY primary_key, version DESC`
  to collapse the object_history tail to one row per primary key, then
  drops DELETE tombstones so a rebuild never resurrects deleted objects.
  Integration test under `pkg/oms/latest_states_test.go`.
- HTTP handler `cmd/server/admin_index.go` mounts
  `POST /api/admin/indexes/rebuild` inside the auth-protected group with
  `auth.RequirePermission(auth.PermUserManage)`. Returns JSON
  `{scopedKey, indexedCount}` on success, 400/404/500 on classification.
  Not-configured (no IndexMgr/Repo) returns 503 so degraded mode doesn't
  masquerade as admin permission error.
- `cmd/server/index_doc_source.go` adapts `*oms.PGRepository` into
  `index.LatestDocumentSource`; wired via new `deps.IndexDocSource` field
  kept alongside the cached OmsRepo. The rebuild path deliberately
  bypasses the cache decorator so it always observes the authoritative
  tail.
- `internal/cliclient.Client.RebuildIndex` + `cmd/weave-cli admin index
  rebuild --ontology X --object-type Y`. `cmd_admin_test.go` exercises
  the flag parsing + POST round-trip via a recording stub server and
  confirms stdout reports the indexedCount.
- OpenAPI spec extended: new `Admin` tag, `/api/admin/indexes/rebuild`
  path, `AdminIndexRebuildRequest` / `AdminIndexRebuildResponse` schemas.
  `cmd/server/openapi_spec.yaml` re-synced to keep
  `TestContract_EmbeddedSpecMatchesCanonical` green.

### Files changed
- pkg/index/rebuild.go, pkg/index/rebuild_test.go (new)
- pkg/oms/object_history.go (LatestObjectState struct)
- pkg/oms/pg_repository.go (LoadLatestObjectStates)
- pkg/oms/latest_states_test.go (new, integration)
- cmd/server/admin_index.go, cmd/server/admin_index_test.go (new)
- cmd/server/index_doc_source.go (new)
- cmd/server/main.go (deps.IndexDocSource, POST /api/admin/indexes/rebuild route)
- cmd/weave-cli/main.go (admin dispatch)
- cmd/weave-cli/cmd_admin.go, cmd/weave-cli/cmd_admin_test.go (new)
- internal/cliclient/client.go (RebuildIndex)
- api/openapi.yaml, cmd/server/openapi_spec.yaml (Admin tag + path + schemas)

### Learnings for future iterations:
- The `TestContract_AllRoutesDocumented` + `TestContract_EmbeddedSpecMatchesCanonical`
  pair is a hard gate on ANY new route: register a chi path → must add an
  openapi.yaml path entry → must `cp api/openapi.yaml
  cmd/server/openapi_spec.yaml`. The second step is easy to forget and the
  test failure surfaces as "embedded spec drifted".
- `oms.Repository` has 50+ methods and 11 mock implementations in the
  codebase. For new functionality that only needs a 2–3 method slice
  (GetOntology/GetObjectTypeByAPIName/ListProperties here), define a
  narrow local interface in the consuming package the same way
  `pkg/index/rehydrate.go` does with `rehydrateRepo`. That keeps the
  mock surface from expanding and frees the Repository interface from
  single-caller methods.
- The cached OMS repo decorator (`oms.NewCachedRepository`) wraps
  *PGRepository and hides any PG-only methods. If a feature needs a
  concrete PG-only method (here: `LoadLatestObjectStates`), main.go must
  stash the raw `*oms.PGRepository` handle at construction time and
  pass it through a separate typed dep — don't try to unwrap the
  cached repo later.
- PostgreSQL `SELECT DISTINCT ON (col1) ... ORDER BY col1, col2 DESC`
  is the idiomatic way to collapse a history table to the newest row per
  key. Remember the ORDER BY must start with the same column as DISTINCT
  ON or Postgres rejects the query.
- HTTP handler "not configured" (503) vs. "permission denied" (403) is a
  semantic distinction that matters for operators. A degraded-mode
  server with no PG pool should return 503 from admin endpoints, NOT
  401/403 — the caller is authorised, the feature is just disabled.
- The existing cmd/server package test harness uses stub types via the
  contract tests `contractOmsRepo{oms.Repository}` pattern (embedding +
  panic-on-use). For unit-testing specific handlers, prefer small
  hand-written stubs satisfying the narrow interface — embedding drags
  in the full 50-method surface and makes test failures harder to read.
---

## 2026-04-11 - US-012
- Added test/integration/typeclass_indexing_test.go with testcontainers-backed
  PG round-trip + BuildMapping-driven in-memory Bleve indexes. Customer.country
  (not_analyzed) asserts TermQuery case-sensitivity; Product.description
  (standard) asserts MatchQuery stemming via the English analyzer.
- Updated pkg/index/mapping_builder.go: default text fields now explicitly
  use Bleve's English ("en") analyzer instead of the implicit "standard"
  analyzer. English adds Porter/Snowball stemming + possessive strip on top
  of the lowercasing the old default already did. not_analyzed still routes
  to KeywordField unchanged; not_indexed still routes to Index=false stored
  text unchanged.
- Updated pkg/index/manager_test.go TestManager_Search_ByField to use
  MatchQuery instead of TermQuery — TermQuery bypasses the field analyzer
  and silently breaks under any stemmer change. MatchQuery is the correct
  wire for text-field queries.
- Added test/foundry_parity/us012_typeclass.json documenting the wire
  contract, the mapping/analyzer chart, and the fixture co-mention of
  "runs marathon" on p2 so Porter stem-reaches both docs on a single
  "run" query.
- Files changed:
  - pkg/index/mapping_builder.go
  - pkg/index/manager_test.go
  - test/integration/typeclass_indexing_test.go (new)
  - test/foundry_parity/us012_typeclass.json (new)
- **Learnings for future iterations:**
  - bleve's implicit "standard" analyzer lowercases + tokenises but does
    NOT stem. If a future US wants Foundry-like root-form matching on a
    text field, the mapping builder must explicitly set
    FieldMapping.Analyzer = "en" (or another snowball language). Just
    leaving Analyzer blank keeps the old non-stemming behaviour.
  - Porter/Snowball English does NOT fold "runner" -> "run". It's a
    noun stem, not a verb form. Test fixtures that want a single query
    to hit multiple docs via stemming should prefer verb conjugations
    ("run"/"runs"/"running") — those all collapse to "run". Plural
    nouns (shoes -> shoe) and -ing/-s verbs are the safe bets.
  - TermQuery bypasses the field analyzer entirely — the raw surface
    form is matched against the indexed token. Any analyzer upgrade
    (lowercasing, stemming, stopword filters) can and will break
    previously passing TermQuery-based tests. Rule of thumb: use
    MatchQuery for text fields, reserve TermQuery for keyword
    (not_analyzed) fields where the analyzer is identity.
  - Property.TypeConfig round-trips through PG as JSONB: write with
    `json.RawMessage(...)`, read back with ListProperties, then use
    index.AnalyzerFromTypeConfig to extract the hint. The rehydrate
    bootstrap already does this dance — integration tests must too.
  - ObjectType.Properties is NOT populated by GetObjectTypeByAPIName;
    it's a separate ListProperties call keyed by the ObjectType RID.
    Manual hydration is required before BuildMapping can inspect the
    properties slice.
---

## 2026-04-11 - US-013 Multi-layer groupBy executor (3 layers nested)
- Added pkg/oss/aggregation/multi_groupby_test.go with three tests:
  TestMultiGroupBy_ThreeLayerNested (exact × fixedWidth × duration),
  TestMultiGroupBy_StableBucketOrder (outer-to-inner ASC by key),
  TestMultiGroupBy_NullGroupKey (missing field → Group[field] == nil).
- pkg/oss/aggregation/engine.go:
  - Added sortGroupEntries(); recursiveGroupBy now sorts entries before
    recursing so the response row order is deterministic regardless of
    Bleve facet count-desc ordering.
  - getExactEntries now emits a null-group entry (value=nil) when the
    facet reports facet.Missing > 0 AND at least one real term exists.
    Scope query is a BooleanQuery{Must: baseQuery, MustNot: allTerms},
    so nested recursion still narrows correctly into that bucket.
- Files changed: pkg/oss/aggregation/engine.go,
  pkg/oss/aggregation/multi_groupby_test.go
- **Learnings for future iterations:**
  - Bleve term facets return entries sorted by count DESC, not by term
    ASC. Any test asserting alphabetical order must sort post-facet,
    which sortGroupEntries now does uniformly for exact/fixedWidth/
    duration/ranges/topValues paths.
  - Bleve reports facetResult.Missing when docs lack the facet field,
    but it is NOT surfaced as a terms entry. To get a scope query for
    the null bucket, build NewBooleanQuery{Must: baseQuery, MustNot:
    termQueries...} — Bleve has no direct "field exists" query.
  - Guard null-group emission on `len(termQueries) > 0`; otherwise a
    groupBy on a non-existent field wraps every doc in a single null
    bucket, which breaks the pre-existing TestAggregate_GroupByExact_
    NoResults expectation (empty groups for unknown fields).
  - `groupByExact` / `groupByFixedWidth` / `groupByDuration` /
    `groupByTopValues` in engine.go are DEAD CODE left over from an
    earlier refactor — all live paths now go through recursiveGroupBy
    + getGroupEntries. Do NOT add fixes to the dead ones; touch the
    "getXxxEntries" functions instead. Future cleanup opportunity.
  - sort.SliceStable with fmt.Sprint comparator is enough because
    every entry.value produced by the existing getXxxEntries paths
    is either a string (term / bucket name / RFC3339 epoch) or nil;
    numeric bucket names like "[0,100)" still sort lexically in a way
    that matches their numeric order because they share the same
    width prefix formatting.
---

## 2026-04-11 - US-014
- Aggregation engine now propagates a truncation flag through all numeric scan
  paths (min/max/sum/avg/standardDeviation/variance/approximatePercentile) so
  the top-level AggregationResponse.accuracy marker flips to "APPROXIMATE"
  whenever a single-shot Bleve search returned fewer hits than the total
  match count.
- computeMetrics was promoted to a method on *Engine (e.computeMetrics) so it
  can read e.MaxDocScanSize instead of the hard-coded 10000. Each numeric
  helper (computeNumericAgg / computeStdDevOrVariance / computePercentile)
  now takes scanSize explicitly and returns (value, truncated, error).
- aggregateSimple (no-groupBy path) and recursiveGroupBy leaf level both
  accumulate the truncation flag into the response. Dead groupByXxx paths
  preserve the old semantics by explicitly discarding the new truncated
  return with `_`.
- count aggregation still uses searchReq.Size = 0 + result.Total, so a
  count-only query does not trip the scan ceiling — matches Palantir
  behaviour where counts are exact regardless of scan limits.
- Files changed:
  - pkg/oss/aggregation/bleve_agg.go (computeMetrics -> method on *Engine,
    numeric helpers return truncated flag, use scanSize from engine)
  - pkg/oss/aggregation/engine.go (aggregateSimple + recursiveGroupBy leaf
    propagate truncated; 5 dead groupByXxx sites updated to new signature)
  - pkg/oss/aggregation/accuracy_test.go (new, 6 sub-tests covering
    simple avg / stdev / percentile / groupBy leaf / count-only ACCURATE)
- **Learnings for future iterations:**
  - Bleve's searchReq.Size caps the number of hits returned; result.Total
    is the unbounded match count. `result.Total > uint64(len(result.Hits))`
    is the canonical truncation test and is already used by findMinMax /
    duration groupBy — reuse it anywhere you scan docs with a bounded Size.
  - When making a package-level helper a method on a struct that carries
    config (e.g. MaxDocScanSize), promoting to a method is the lowest-churn
    refactor — all call sites just need the `e.` receiver and any new
    return values surface naturally in the caller.
  - The `aggregation_test.go` file still contains TestAggregate_FixedWidth_
    Accuracy_Approximate_WhenTruncated and the two Duration_Accuracy tests
    that already covered parts of APPROXIMATE behaviour through the
    findMinMax / duration-search code paths. The new TestAggregationAccuracy
    Marker closes the gap on the simple path and the leaf-level metric
    scans that were previously hard-coded to 10000.
  - count metric is deliberately exempt from scan-size accuracy logic —
    Palantir's semantics treat count as exact because it comes from the
    facet/total counter, not a hit scan. Do not conflate the two when
    extending this code later (e.g. adding distinct with sampling).
---

## 2026-04-11 - US-015 Multi-groupBy integration test + parity fixture
- Added test/integration/aggregation_multigroupby_test.go. Boots a real PG
  via testcontainers, creates an Order ObjectType with shipCountry
  (analyzer.not_analyzed), freight (double), orderDate (timestamp), builds
  a Bleve in-memory index through pkg/index.BuildMapping, seeds 105
  deterministic orders (5 countries x 3 quarters x 7 prices), and runs a
  3-layer groupBy [country exact, freight fixedWidth(100), orderDate
  duration(90 DAYS)] with count/sum/avg metrics. Asserts every leaf row's
  count and numeric metric against a hand-computed expected map plus the
  outer country ASC ordering and the ACCURATE accuracy marker.
- Added test/foundry_parity/us015_multi_groupby.json fixture documenting
  the ontology + seed pattern + request + expected wire shape.
- Files changed:
  - test/integration/aggregation_multigroupby_test.go (new)
  - test/foundry_parity/us015_multi_groupby.json (new)
  - scripts/ralph/prd.json (passes: true)
- **Learnings for future iterations:**
  - The pg-backed integration pattern from typeclass_indexing_test.go ports
    cleanly to any aggregation story that needs BuildMapping-authenticity:
    create Ontology -> create ObjectType (primary_key_prop is just text,
    no FK into Property) -> CreateProperty rows -> reload via
    GetObjectTypeByAPIName + ListProperties so Properties slice is populated
    -> bleve.NewMemOnly(index.BuildMapping(fresh)). Bleve lives in t.TempDir
    via the function's internal call so there is no cross-test contamination.
  - The duration groupBy path floors hit.Fields[field] from the UNIX EPOCH,
    not from calendar boundaries. For a P3M-equivalent (90*86400 s) window,
    dates 2024-01-15 / 2024-04-15 / 2024-07-15 land in three DISTINCT
    floor(epoch/(90*86400))*(90*86400) buckets (2023-12-19, 2024-03-18,
    2024-06-16 UTC). Any future story that wants "calendar quarters" would
    need a new groupBy type — do NOT try to pick dates that coincide with
    Q1/Q2/Q3 calendar boundaries and expect them to line up.
  - Bleve numeric field facets return values typed as float64 irrespective
    of whether the indexed doc stored int or float64. The existing
    computeNumericAgg already casts val.(float64) so docs indexed with
    literal Go float64 prices (10.0, 50.0, ...) round-trip cleanly.
  - fixedWidth bucket names come from fmt.Sprintf("[%.0f,%.0f)", lo, lo+w)
    in getFixedWidthEntries. Expected maps and parity fixtures can just
    format their own keys the same way — no need to invoke bleve to
    discover them. Treat the format string as a wire contract.
  - engine.Aggregate returns MetricValue.Value as uint64 for "count" and
    float64 for sum/avg/min/max. Test code must type-assert each
    separately; a single interface{} comparison will silently fail when
    the test accidentally compares uint64(2) to int(2) via reflect.
  - Integration tests under test/integration/ all live in the SAME Go
    package (integration_test) with the //go:build integration tag, so
    helper names (like findMetric) collide across files. The US-015 test
    therefore defines its helper inline as a closure inside the single
    test function rather than a package-level helper.
---

## 2026-04-11 - US-016
- Added HdrHistogram-backed approximatePercentile implementation. `approx_percentile.go`
  exposes `computeApproxPercentileHdr(values, percentile)` (in-memory on a float64 slice)
  and `approxPercentileFromIndex(...)` (the Bleve-backed caller that preserves the
  legacy (value, truncated, error) triple). bleve_agg.go's `approximatePercentile`
  arm now routes through the new path and the dead sort-based `computePercentile`
  has been removed.
- `pkg/oss/aggregation/percentile_test.go` writes the failing red test first:
  * `TestComputeApproxPercentileHdr_DirectCall` drives the new function directly
    (so the package fails to compile until `computeApproxPercentileHdr` exists).
  * `TestApproximatePercentile_Gaussian10k` builds a 10k Gaussian Bleve index
    and asserts p50/p95/p99 fall within 10% of the analytic normal quantiles
    (via Beasley-Springer-Moro inverse CDF). US-018 will tighten to ≤5%.
  * `TestApproximatePercentile_Scalar` pins the single-percentile scalar shape.
- `go get github.com/HdrHistogram/hdrhistogram-go@v1.2.0` added to go.mod.
- Files changed:
  - pkg/oss/aggregation/approx_percentile.go (new)
  - pkg/oss/aggregation/percentile_test.go (new)
  - pkg/oss/aggregation/bleve_agg.go (removed dead computePercentile, swapped switch arm)
  - go.mod / go.sum (HdrHistogram dependency)
- **Learnings for future iterations:**
  - HdrHistogram records int64 only. To feed arbitrary float fields: (1) find min,
    shift so recorded values are ≥ 1, (2) multiply by a fixed fractional scale
    (we use 1e3 for 3 decimal places), (3) reverse both transforms when reading
    back the quantile. Highest-trackable is derived from the scaled max.
  - bleve hit.Fields always comes back as float64 for numeric fields (see existing
    comment in engine.go). We do not need the full coerceNumeric helper that the
    objectset package uses — the simple float64 type assertion is enough here.
  - The existing `setupAccuracyIndex(t, n)` helper in accuracy_test.go is reusable
    for small-sample percentile tests — no need to reinvent a second helper.
  - `AggregationResponse.Data[0].Metrics` is the correct shape for single-bucket
    (non-groupBy) aggregation responses. There's no top-level `resp.Metrics`
    field — I wasted a compile cycle assuming otherwise.
  - US-017 will add `Histogram.ValueAtPercentiles(percentiles []float64)` to
    compute p50/p95/p99 in one pass. The hdrhistogram-go API already exposes it,
    so US-017 is just plumbing a new `Percentiles []float64` field onto
    AggregationSpec + a map[string]float64 response shape.
  - US-018 needs an `exact` sort-based reference (easy to resurrect — git blame
    for the computePercentile function removed in this commit) and a
    `testing.B` bench loop. The ≤5% delta assertion should live inside a
    regular Test function driven by `-bench=...` so `go test` fails rather
    than silently reporting slow numbers.
---

## 2026-04-11 - US-017 Multiple percentiles in single query
- Added `Percentiles []float64` field to `AggregationSpec`; scalar
  `Percentile *float64` preserved so existing call sites stay unchanged.
  When `Percentiles` is non-empty the `approximatePercentile` arm routes
  through a new multi-path and returns a single MetricValue whose Value
  is `map[string]float64` keyed by `%g`-formatted percentile strings
  ("50", "95", "99", "99.9"). Scalar path returns `float64` as before.
- Extracted a shared `buildHistogram(values)` helper in
  `approx_percentile.go` that performs the shift/scale + record loop once.
  Both `computeApproxPercentileHdr` (single) and the new
  `computeApproxPercentilesHdr` (multi) reuse it, so a single pass over
  the input values populates one HdrHistogram that answers N percentile
  queries via repeated `h.ValueAtPercentile(p)` — satisfies the "same
  histogram used for all percentiles in one pass" acceptance criterion.
- New `approxPercentilesFromIndex(idx, q, field, percentiles, scanSize)`
  preserves the legacy `(value, truncated, error)` triple so
  `bleve_agg.go` dispatches uniformly between scalar and multi paths.
- Red-first TDD:
  * `TestComputeApproxPercentilesHdr_DirectCall` exercises the pure
    helper on a 10k Gaussian slice; compile-fails until the new function
    and AggregationSpec field exist.
  * `TestMultiPercentileSingleQuery` drives the full `eng.Aggregate`
    request path with `Percentiles: []float64{50, 95, 99}`, asserting
    the metric value is `map[string]float64` of length 3 and each key
    is within 10% of the analytic normal-distribution quantile.
- Files changed:
  - pkg/oss/aggregation/engine.go (AggregationSpec.Percentiles field +
    gofmt realign of GroupBySpec struct tags)
  - pkg/oss/aggregation/approx_percentile.go (buildHistogram helper,
    computeApproxPercentilesHdr, approxPercentilesFromIndex, scalar
    path now delegates to buildHistogram)
  - pkg/oss/aggregation/bleve_agg.go (switch arm dispatches to multi
    path when Percentiles non-empty, scalar path otherwise)
  - pkg/oss/aggregation/percentile_test.go (2 new tests)
- Quality: go test ./pkg/oss/aggregation/... green; go test ./... green
  (~8 min including Chinook/Northwind); go vet ./... clean; gofmt -l on
  all four edited files clean (engine.go picked up an unrelated
  alignment refresh in GroupBySpec because AggregationSpec now has a
  longer tag column — that's expected gofmt behaviour, not drift).
- **Learnings for future iterations:**
  - HdrHistogram-go does NOT expose a batch `ValueAtPercentiles([]float64)`
    method; the idiomatic way to answer N queries in "one pass" is to
    record once and call `ValueAtPercentile(p)` N times on the same
    histogram. That's O(N log bucket_count) after the O(records) build,
    so multi-percentile stays cheap compared with rescanning the index.
  - Key format `%g` collapses `50.0 → "50"` and keeps `99.9 → "99.9"`,
    which lines up with how JSON clients will request percentiles. Do
    NOT use `%v` — it would render integer-valued floats with a
    trailing zero on some platforms and break the round-trip.
  - gofmt tab stops in a struct are computed per-line against the whole
    block. Adding a new field with a longer tag re-triggers alignment
    across sibling fields/structs. Run `gofmt -w` on the file you
    touched BEFORE committing so the diff only shows your real change.
  - When adding an optional sibling field to an existing schema (scalar
    `Percentile` vs slice `Percentiles`), keep both for backward-compat
    and pick via a single `if len(slice) > 0` at dispatch time. Avoids
    cascading renames through every caller that still uses the scalar.
  - Reading MetricValue.Value back in tests: `m.Value.(map[string]float64)`
    works because the Go runtime preserves the concrete type through the
    `interface{}` boxing in `MetricValue.Value`. No json round-trip
    happens until the HTTP handler encodes — unit tests can inspect the
    typed map directly.
---

## 2026-04-11 - US-018
- Added pkg/oss/aggregation/percentile_bench_test.go with:
  * exactPercentileSort() reference (sort-based nearest-rank on a fresh copy)
  * gaussian10k(seed) deterministic 10k Gaussian dataset helper
  * TestApproxPercentileAccuracy_5PercentBound — runs during `go test ./...`
    and fatally fails if |approx - exact| / |exact| > 0.05 on p50/p95/p99
  * BenchmarkApproxPercentile (scalar, per-percentile sub-benches) —
    the 5% assertion is re-run inside the b.Fatalf path so a regression
    presents as a bench FAILURE rather than a silent slowdown
  * BenchmarkApproxPercentileMulti — single-pass multi-percentile bench
    with the same 5% gate per percentile key
  * BenchmarkExactPercentileSort — reference cost for the sort-based path
- Wrote bench/aggregation_percentile.md documenting methodology, seed, exact
  vs approx numbers (all 3 percentiles <0.05% relative error — 100x below
  the 5% bound), M3 Max bench numbers, and reproduction commands.
- Files changed:
  - pkg/oss/aggregation/percentile_bench_test.go (new)
  - bench/aggregation_percentile.md (new, bench/ dir created)
- Baseline bench numbers captured on Apple M3 Max:
  - approx p50/p95/p99: ~52µs/op, 123KB/op, 2 allocs/op
  - approx multi-p: ~70µs/op, 10 allocs/op
  - exact sort reference: ~206µs/op (approx ~4x faster than exact)
- **Learnings for future iterations:**
  - The approx accuracy path has a LOT of headroom on Gaussian data —
    real-world heavy-tail distributions might push the error higher, so
    any future story that adds log-normal / pareto fixtures should keep
    the same 5% bound but add a seed matrix so tail sensitivity is visible.
  - HdrHistogram's fixed-point scale (1e3) multiplies every recorded value,
    so the `highest` int64 cap lives at (max+shift)*1000+2. Values above
    ~9e15 would saturate — not a concern for Foundry property metrics but
    worth remembering if someone adds nanosecond time fields.
  - For bench files, prefer b.Run(fmt.Sprintf("p%g", p), ...) so the
    reported sub-bench name matches the canonical percentile key format
    used by approxPercentilesFromIndex — keeps grep/diagnostic flow tight.
  - Bench accuracy gates need to live INSIDE the inner loop vs outside:
    I put the b.Fatalf comparison AFTER the b.N loop so it only evaluates
    the last computed value, which is fine because HdrHistogram is
    deterministic on a fixed input — every iteration returns the same
    approx value, so checking once is equivalent and avoids burning bench
    time in a fastpath comparison that would skew ns/op measurements.
  - `go test ./...` on this repo takes ~14s mostly in pkg/oss/aggregation
    (bench test Gaussian gen + bleve teardown) — if future stories add
    more Gaussian fixtures consider sharing a package-level dataset.
---

## 2026-04-11 - US-019
- Added migration 000018_object_history_source.{up,down}.sql that adds a
  `source TEXT NOT NULL DEFAULT 'user'` column on object_history plus a
  supporting (object_type_rid, primary_key, source, version DESC) index so
  US-021 can look up the latest user edit in one index scan.
- Added `Source` field + `EditSourceUser`/`EditSourceIngest` constants to
  `pkg/oms/object_history.go`; updated `InsertObjectHistory` to default an
  empty Source to 'user' and write it, and updated `ListObjectHistory` to
  select it back with COALESCE for defensive reads.
- TDD: new unit test `TestObjectHistory_SourceField_JSON` in
  `pkg/oms/models_test.go` verifies JSON round-trip; confirmed it fails
  (unknown field Source) before implementing.
- Files changed:
  - migrations/000018_object_history_source.up.sql (new)
  - migrations/000018_object_history_source.down.sql (new)
  - pkg/oms/object_history.go
  - pkg/oms/pg_repository.go
  - pkg/oms/models_test.go
- Quality checks: `go build ./...` ok, `go vet ./...` ok,
  `go test ./...` all green (incl. integration/e2e via build tags compiled
  together). Pre-existing gofmt drift on other pkg/oms files is unrelated
  and predates this commit.
- **Learnings for future iterations:**
  - Phase-6 user-vs-ingest conflict logic (US-020/021/022/023) all reads
    this Source column; US-019 is a schema-only prerequisite and touching
    the funnel consumer belongs in US-020.
  - `PGRepository.InsertObjectHistory` and `ListObjectHistory` are the
    only two history write/read paths — cached_repository.go does not
    shadow these, so future history-column additions only need one edit
    per method.
  - The integration test file `pkg/oms/object_history_test.go` sits under
    `//go:build integration` so the fast unit-test path needed the new
    round-trip assertion in `pkg/oms/models_test.go` to satisfy strict
    TDD Red-Green without spinning up testcontainers.
  - Pre-existing gofmt drift on ~9 pkg/oms files predates the phase-6-8
    branch; don't auto-format them inside a Ralph story unless the story
    explicitly calls for cleanup — it would balloon unrelated diffs.
---

## 2026-04-11 - US-020
- Added `Source string` field to `funnel.Edit` (`json:"source,omitempty"`)
  plus `EditSourceUser`/`EditSourceIngest` constants colocated with the
  struct. omitempty keeps wire compat with pre-US-020 consumers and lets
  the test assert that empty-source edits do not leak a stray key.
- `pkg/actions/executor.go` Prepare stamps Source="user" on every edit
  before returning, both in the rules-path (post-ExecuteRules) and the
  function-backed branch (post-Dispatch). Factored into
  `tagEditsAsUserSource(edits)` so there is exactly one spot to audit.
  Apply flows through Prepare → CommitBatch so the tag survives
  CollapseEdits (collapse preserves struct fields it doesn't rewrite).
- TDD: `pkg/actions/edit_source_test.go` with red test
  `TestActionEditSourceUser` covering CREATE/MODIFY/DELETE via rules,
  Apply-result path, and the function-backed path; plus
  `TestFunnelEditSourceJSON` asserting the "source" JSON tag round-trips
  and omitempty behaves. Verified red (build failure on
  `e.Source undefined`) before implementing.
- Files changed:
  - pkg/funnel/types.go
  - pkg/actions/executor.go
  - pkg/actions/edit_source_test.go (new)
- Quality checks: `go test ./...` all green (incl. northwind/chinook/e2e),
  `go vet ./...` clean. Edited files are gofmt-clean; the pre-existing
  drift on pkg/actions/executor.go L48 (ActionResults.Type tag column
  alignment) is unrelated and untouched.
- **Learnings for future iterations:**
  - `tagEditsAsUserSource` is the single chokepoint the action executor
    uses to stamp Edit.Source. US-021 (funnel consumer conflict logic)
    should read `batch.Edits[i].Source` directly instead of reaching for
    an out-of-band signal — action-executor batches will always have it
    set at this point, and streaming ingest paths must set "ingest"
    before publishing.
  - `CollapseEdits` keeps the winning edit's full struct value (not a
    field-by-field merge), so Source just rides along with whichever
    edit survives. New Edit fields added in the future don't need any
    collapse-aware copying.
  - `FunctionDispatcher.Dispatch` returns raw funnel.Edits, so function
    authors cannot forge a Source value — Prepare overwrites whatever
    the dispatcher set. If we ever want function-backed actions to emit
    source="ingest" for specific use cases, this is the line to revisit.
  - JSON omitempty on Source means old NATS batches in flight during a
    rolling upgrade will unmarshal with Source="", which US-021 must
    interpret as "user" (preserving conservative legacy behaviour).
---

## 2026-04-11 - US-021
- Added user-edit-wins conflict resolution in the funnel consumer. Ingest
  edits whose (objectType, primaryKey) has a newer user edit in
  object_history now either skip (DELETE + MODIFY with no always-apply
  fields) or merge only always-apply fields on top of the current bleve
  document (CREATE/MODIFY). Non-ingest edits and edits with no prior user
  history pass through unchanged, keeping Tier 2.3 behaviour intact.
- Extended the HistoryRecorder interface with LatestUserEditAt so the
  consumer never depends on pkg/oms for the query. PG repo grows a matching
  method and InsertObjectHistory now honours caller-supplied RecordedAt so
  batch.Timestamp flows through to the conflict comparator.
- Introduced SetAlwaysApplyField(fn) hook on Consumer as the US-026 stub.
  US-026 will wire it against the is_edit_only schema column; for now the
  hook defaults to nil (no fields are always-apply) and tests exercise the
  custom-hook path.
- Consumer now stamps object_history rows with edit.Source (fallback
  "user") so future conflict queries can distinguish user vs. ingest writes.
- Files changed:
  - pkg/funnel/consumer.go        (HistoryRecorder extension, resolveConflicts,
                                    SetAlwaysApplyField, Source stamping)
  - pkg/funnel/conflict_test.go   (new, 5 tests)
  - pkg/funnel/funnel_test.go     (fakeHistoryRepo.LatestUserEditAt + RecordedAt
                                    honour on insert)
  - pkg/oms/pg_repository.go      (LatestUserEditAt, conditional recorded_at
                                    in InsertObjectHistory, time import)
- **Learnings for future iterations:**
  - Conflict resolution MUST rewrite the filtered edit's Properties to
    merge(currentBleveDoc, filteredFields) before applying. Bleve's
    IndexDocument is replace-semantics, not field-level merge, so a naive
    filtered MODIFY would wipe the protected fields. fetchDocument returns
    bleve fields as a flat map[string]interface{} with numeric fields
    already as float64, so the merge is stable across types.
  - PG's object_history.recorded_at column has DEFAULT NOW(), so the INSERT
    now branches: when caller passes a non-zero h.RecordedAt (US-021 conflict
    path), include the column and bind the value; otherwise omit the column
    so legacy callers keep the default. Don't unconditionally stamp NOW()
    client-side, that breaks time-based audits for producers that replay
    historical data.
  - batch.Timestamp is the only edit-time signal we have until US-062 adds
    a richer per-event envelope. The consumer stamps it onto h.RecordedAt
    before insert, and the conflict check uses recorded_at < batch.Timestamp
    as "user is older". Tests that need deterministic conflict outcomes
    MUST set batch.Timestamp explicitly on both sides — relying on time.Now()
    at apply time is racy because the fake inserts at ~nanosecond resolution.
  - fakeHistoryRepo now respects h.RecordedAt if set (fallback to time.Now()),
    mirroring the PG path. Any future test in pkg/funnel that seeds history
    rows and later queries LatestUserEditAt should set RecordedAt explicitly
    rather than assuming wall-clock.
  - HistoryRecorder is deliberately narrow (Insert + Latest lookup). Do NOT
    promote it into pkg/oms.Repository — that forces updates across 11 mock
    implementations per the Codebase Patterns note. Keep new conflict-only
    capabilities on this in-package interface.
  - resolveConflicts fails open on lookup errors (the edit is passed
    through, not dropped). Rationale: a PG hiccup should not silently
    swallow ingest data; DLQ and retry paths handle the durability side.
---

## 2026-04-11 - US-022
- Added test/integration/edit_conflict_test.go exercising the US-021
  user-edit-wins path under concurrency. Real testcontainers PG backs the
  HistoryRecorder via oms.NewPGRepository; a bleve index.Manager in
  t.TempDir() holds employee docs. 10 iterations × (20 seed creates + 100
  user MODIFY + 100 ingest MODIFY) with two goroutines racing on a shared
  Consumer. Expected state is the replay of user edits only; every ingest
  batch carries stale timestamps so resolveConflicts skips it. Final
  Bleve doc per PK is fetched via DocIDQuery + Fields=[*] and asserted
  field-by-field against expectedState, plus an INGEST-WRONG-prefix leak
  guard on the name field.
- Exposed funnel.Consumer.ApplyBatch(ctx, EditBatch) as a thin exported
  wrapper over applyBatchWithHistory so integration tests can drive the
  full conflict + history + apply path without standing up NATS. Production
  main.go still routes through handleMessage.
- Added test/foundry_parity/us022_edit_conflict.json documenting the
  sequence, timestamp windows, rand seeds, leak guard, and the list of
  covered test functions.
- Files changed:
  - pkg/funnel/consumer.go (ApplyBatch exported wrapper)
  - test/integration/edit_conflict_test.go (new, 1 test)
  - test/foundry_parity/us022_edit_conflict.json (new)
- Quality: `go test -tags integration ./test/integration/... -run EditConflict -v`
  green in ~32s on a fresh PG container. `go test ./...` green. `go vet ./...`
  clean. `gofmt -l` clean on edited files.
- **Learnings for future iterations:**
  - Concurrent user + ingest tests can stay deterministic by pinning the
    per-actor timestamp windows to iterStart±1h. The user goroutine applies
    monotonically increasing timestamps strictly newer than the seed; the
    ingest goroutine applies strictly older timestamps. That removes the
    race from the assertion without removing the race from the apply path —
    goroutines still interleave, but the user-edit-wins guard is a function
    of history recorded_at vs. batch.Timestamp, not wall-clock ordering.
  - funnel.Consumer did not expose an in-process apply entrypoint prior
    to US-022; integration tests were stuck with either NATS or duplicating
    applyBatchWithHistory. The new exported `ApplyBatch` is the right place
    for future ingest shims (US-062 streaming ingest, US-086 Goja runtime
    dispatch) so they do not need to reinvent the conflict + history loop.
  - bleve's Index() is a full replace on the doc id, so test fixtures
    should send the complete property map on every MODIFY and compute the
    expected final state as "the last full upsert per PK" — not as an
    accumulated field-wise merge. Mixing partial MODIFYs with conflict
    resolution would need separate assertions (left for a future test).
  - ingest history rows are NOT persisted when the conflict resolver drops
    the edit: resolveConflicts runs BEFORE applyBatchEdits AND before the
    history loop, and the history loop iterates the FILTERED slice. This
    is the right semantics (no audit noise for rejected writes) but it
    means the test cannot assert on object_history row counts without
    accounting for skipped rows.
  - testutil.StartPGContainer is ~3-4s per t.Run on an M3 Max; the US-022
    test runs 10 iterations against ONE container to stay under 40s total.
    Future phase6 integration stories should adopt the same "seed once,
    iterate many seeds" pattern instead of spinning per-seed containers.
---

## 2026-04-11 - US-023
- Added ApplyOptions.ExpectedVersion *int (omitempty) + new *StaleObjectError
  type carrying {ObjectType, PrimaryKey, ExpectedVersion, CurrentVersion}.
- Executor.Apply now calls checkExpectedVersion after Prepare but BEFORE
  CommitBatch: it scans prep.Edits for the first MODIFY/DELETE target,
  resolves ObjectType RID via omsRepo.GetObjectTypeByAPIName (falling back
  to the API name when no mapping exists, mirroring pkg/funnel consumer
  behaviour), and compares the result of GetObjectVersionCount against
  *ExpectedVersion. Mismatch returns a *StaleObjectError; the publisher and
  action log are never touched.
- handlers.go gains staleObjectAPIError() which converts the executor-level
  sentinel into apierror.NewConflict("StaleObject", {expectedVersion,
  currentVersion, objectType, primaryKey}) — both Apply and ApplyWithOverrides
  code paths catch the error early so 409 pre-empts the generic 400
  ActionFailed path.
- CREATE-only actions skip the version check: there is no target object to
  compare against, so ExpectedVersion is a no-op.
- Test mock extensions on mockOmsRepo: new objectTypesByAPIName +
  objectVersionCounts fields (nil-safe — legacy tests still see the old
  nil/0 returns). GetObjectTypeByAPIName and GetObjectVersionCount wired to
  honour them when set.
- Files changed:
  - pkg/actions/executor.go (ApplyOptions, StaleObjectError, Apply version
    check hook, checkExpectedVersion)
  - pkg/actions/handlers.go (staleObjectAPIError + Apply/ApplyWithOverrides
    error branches)
  - pkg/actions/actions_test.go (mockOmsRepo mock extensions)
  - pkg/actions/optimistic_test.go (new, 6 tests: 4 executor + 2 handler)
- **Learnings for future iterations:**
  - The version check belongs in Apply, not Prepare: Prepare is the pure
    request-to-edits transform used by both single and batch paths, and
    optimistic concurrency is fundamentally stateful (depends on live
    version) so keeping it out of Prepare preserves the batch semantics
    where same-action-multi-param batches can still share a single Prepare
    codepath. Follow-up: US-054 / batch optimistic concurrency will need
    per-action expectedVersion slots inside the batch envelope, not a
    top-level one.
  - GetObjectVersionCount is an int64 COUNT(*) over object_history — the
    returned value equals the current highest version because funnel
    consumer.nextVersion starts at 1 and increments monotonically. Don't
    try to read max(version) from the history rows; the count is cheaper
    and equivalent under the "monotonic-from-1" invariant.
  - When extending mockOmsRepo across many tests, add nil-guarded fields
    (map[...]X where the method returns a sane default when the map is
    nil) instead of new constructors — legacy test fixtures stay untouched
    and the new behaviour opts in per-test. The fakePublisher pattern in
    batch_test.go already set the precedent.
  - Palantir apierror wire format uses strings for all parameter values,
    even numeric ones. Use strconv.FormatInt / strconv.Itoa when stuffing
    version numbers into the Parameters map so the frontend can parseInt
    on the other side. The json test must also unmarshal Parameters as
    map[string]string — a map[string]interface{} will NOT match the wire
    format.
  - The handler helper staleObjectAPIError returns *apierror.APIError and
    is the reusable shim for any executor-level 409-class error. Future
    conflict stories (US-054 row policy conflicts, US-086 Goja edit
    conflicts) should lean on the same helper pattern rather than
    hand-rolling 409 construction at every handler.
---

## 2026-04-11 - US-024
- Added optimistic-concurrency UX to the Action Console: when a user types
  a target ObjectType + primaryKey, the page loads the current version via
  a new useObjectVersion hook and stamps it as expectedVersion in the
  ActionApplyRequest.options. On a 409 StaleObject response, a dedicated
  banner surfaces "This object was updated elsewhere - Reload to continue"
  with a Reload button that refetches the object version (React Query
  refetch) and clears the banner. Non-stale errors still flow through the
  existing applyMutation.isError path.
- Files changed:
  - web/src/api/types.ts (ActionApplyOptions.expectedVersion?: number)
  - web/src/hooks/useObjectVersion.ts (new)
  - web/src/hooks/__tests__/useObjectVersion.test.ts (new, 4 tests)
  - web/src/components/actions/ActionConsolePage.tsx
  - web/src/components/actions/__tests__/ActionConsolePage.test.tsx
    (new, 2 tests)
- Quality: `npx vitest run` → 38 files / 192 tests green. `npx tsc --noEmit`
  clean. `go test ./...` green. `go vet ./...` clean.
- **Learnings for future iterations:**
  - `getObjectHistory({limit: 1})` is the cheapest "what version are we
    on" query — totalVersions in the envelope IS the current version
    because the consumer increments nextVersion monotonically from 1
    (see US-023 learnings). No dedicated /version endpoint needed.
  - `ApiRequestError.statusCode` + `errorName === "StaleObject"` is the
    canonical 409 discriminator on the frontend. Use this pair instead
    of parsing error.message — the handler side formats the name via
    staleObjectAPIError (pkg/actions/handlers.go) which emits
    errorName="StaleObject" + statusCode=409 via apierror.NewConflict.
  - React Query's `refetch()` bypasses the `enabled` guard once a query
    has been primed — the stale-banner Reload button just calls
    versionQuery.refetch() and the hook wakes up without needing state
    toggles. Don't try to re-enable by toggling primaryKey to "" and
    back; that drops the cached data.
  - TanStack Query test wrappers need `retry: false` in defaultOptions
    — otherwise error assertions wait 3+ retries and time out. Follow
    the makeWrapper pattern in useObjectSets.test.ts.
  - ApiRequestError.parameters carries {expectedVersion, currentVersion,
    objectType, primaryKey} as strings (per US-023 wire format). The UX
    only renders currentVersion today, but all four are available for
    richer banners in the future (e.g., "v2 → v9 on Employee/E1").
  - The Action Console did not previously load any per-object data, so
    the target ObjectType + primaryKey inputs are a net-new UI affordance.
    Existing ParameterForm has no concept of an "object reference"
    parameter type; US-027 or later should consider wiring this directly
    to a FunctionArg typed as "objectReference" for auto-population.
---

## 2026-04-11 - US-025
- Added Property.IsEditOnly bool field (JSON tag "editOnly,omitempty")
  to pkg/oms/models.go so OMS schema can flag a property as always-win
  against concurrent ingest edits.
- New migration 000019_property_edit_only adds
  properties.is_edit_only BOOLEAN NOT NULL DEFAULT FALSE (up) and drops
  the column (down). Legacy rows read back as false with no behaviour
  change.
- pg_repository CreateProperty now inserts is_edit_only; ListProperties
  and GetProperty SELECT it into Property.IsEditOnly; UpdateProperty
  writes it on every update so the admin CRUD round-trip is symmetric.
- Red-first: test/integration/property_editonly_test.go
  (TestProperty_IsEditOnlyRoundTrip, //go:build integration) compile-
  fails on missing Property.IsEditOnly, then passes end-to-end against
  a testcontainers PG booted with the full migration chain. Asserts
  Create (both true + unset), List, Get, Update(true→false→true).
- Files changed:
  - pkg/oms/models.go
  - pkg/oms/pg_repository.go
  - migrations/000019_property_edit_only.up.sql (new)
  - migrations/000019_property_edit_only.down.sql (new)
  - test/integration/property_editonly_test.go (new)
  - scripts/ralph/prd.json (US-025 passes=true)
- Quality: go vet ./... clean; gofmt clean on all 3 edited Go files;
  go test ./... green (~7min incl. Northwind/Chinook suites);
  go test -tags integration ./test/integration/ -run
  TestProperty_IsEditOnlyRoundTrip green in ~1s.
- **Learnings for future iterations:**
  - Adding a new column to properties requires 4 coordinated changes in
    pg_repository.go: INSERT column + $N, ListProperties SELECT +
    Scan list, GetProperty SELECT + Scan list, UpdateProperty SET. The
    table has NO generic `SELECT *` path so you must update every
    query site — miss one and the Scan arg count blows up at runtime,
    not build time.
  - pkg/oms.Property.Derived (US-004) was a pure in-memory flag — NOT
    persisted. US-025 is the first property flag that actually needs a
    column, so follow the is_edit_only pattern (INSERT + Scan in
    List/Get + SET in Update) rather than the Derived precedent if
    your new flag must survive restarts.
  - OpenAPI Property schema + CachedRepository passthrough are still
    pending for US-026. The current US-025 scope deliberately stops at
    pkg/oms.PGRepository; admin handlers and cached_repository.go will
    need follow-up patches so the flag flows through the HTTP layer.
  - gofmt -l will complain about .sql files if you pass them through
    — keep shell gofmt invocations to *.go only.
---

## 2026-04-11 - US-026
- Wire is_edit_only through the admin Property CRUD handlers and the
  embedded OpenAPI spec so SDK clients can create and toggle edit-only
  properties end-to-end.
- Files changed:
  - pkg/oms/admin_handlers.go: CreatePropertyRequest.EditOnly (bool,
    omitempty) + UpdatePropertyRequest.EditOnly (*bool, omitempty).
    CreateProperty copies the flag into Property.IsEditOnly; UpdateProperty
    only mutates the stored flag when the pointer is non-nil so omitted
    fields remain idempotent.
  - pkg/oms/handlers_test.go: TestCreateProperty_EditOnly,
    TestCreateProperty_EditOnlyDefaultFalse and TestUpdateProperty_EditOnlyToggle
    — TDD red-then-green coverage for Create/Update semantics.
  - api/openapi.yaml + cmd/server/openapi_spec.yaml: Property schema gains
    an editOnly boolean with cross-reference to US-025/US-027. The two
    files stay byte-identical; cmd/server embeds the latter via //go:embed.
- **Learnings for future iterations:**
  - Admin Property CRUD is split across admin_handlers.go ONLY — there is
    no /api/admin/properties/{propertyRid} GET handler registered; use the
    existing v2 read paths or inspect the mockRepo store directly in unit
    tests instead of wiring a synthetic Get route.
  - cmd/server/openapi_spec.yaml is embedded with //go:embed inside
    cmd/server/openapi_handler.go. The canonical source is api/openapi.yaml
    at repo root; after edits run `cp api/openapi.yaml cmd/server/openapi_spec.yaml`
    to keep them byte-identical (the cmd/server contract_test.go compares
    registered routes against the root copy).
  - UpdatePropertyRequest uses *bool for optional booleans (IsSearchable,
    IsSortable, IsNullable) so a missing field leaves the stored value
    untouched — apply the same pattern for any new tri-state flag (nil ==
    no-op, &true == enable, &false == disable). The one-line CreateProperty
    request mirror can keep using plain bool since Create always writes
    the full record.
  - For property-related conflict-resolution flags, the PG column backs
    the flag with NOT NULL DEFAULT FALSE so legacy rows still read back as
    false after migration — no application-level null handling needed.
---

## 2026-04-11 - US-027
- Added US-027 editOnly preservation guard to the funnel Consumer. New
  Consumer.editOnlyField hook (opt-in via SetEditOnlyField) drives a new
  preserveEditOnlyFields pass that runs BEFORE resolveConflicts in
  applyBatchWithHistory. For every ingest CREATE/MODIFY: strip any keys
  the hook flags as editOnly, fetch the current bleve doc, and merge its
  editOnly fields back onto the stripped map so the downstream bleve
  upsert cannot lose the user value. Empty-after-strip edits are dropped
  (log line) instead of clobbering the doc with an empty upsert. DELETE
  edits bypass the guard (delegated to US-021 timestamp protection).
- Files changed:
  - pkg/funnel/consumer.go (editOnlyField field + SetEditOnlyField +
    preserveEditOnlyFields + wire into applyBatchWithHistory)
  - pkg/funnel/conflict_test.go (TestEditOnlyAlwaysWins +
    TestEditOnly_IngestOmitsField — red-first, both green after impl)
  - test/integration/edit_only_test.go (new,
    TestEditOnly_IngestCannotOverwriteUserNotes; boots real PG via
    testcontainers, seeds Order.notes with IsEditOnly=true, drives full
    Consumer.ApplyBatch happy path + regression)
  - test/foundry_parity/us027_edit_only.json (parity fixture)
  - scripts/ralph/prd.json (US-027 passes=true)
- Quality:
  - go test ./... green
  - go test -tags integration ./test/integration/ -run EditOnly green (~1s)
  - go vet ./... clean
  - gofmt clean on all 3 edited Go files
- **Learnings for future iterations:**
  - US-021 and US-027 are composable but orthogonal guards. US-021 is
    timestamp-based (only ingest edits older than the latest user edit are
    filtered) and applies across the whole edit. US-027 is flag-based
    (per-field IsEditOnly) and applies unconditionally. The Consumer runs
    preserveEditOnlyFields FIRST so US-027 always rewrites the Properties
    map before resolveConflicts makes its skip-or-merge decision; the
    US-021 merge path therefore already sees a sanitised map and the two
    passes never contradict each other.
  - bleve upserts replace the full doc — a partial ingest MODIFY that
    omits a field silently deletes that field. For editOnly this is the
    real silent-corruption bug US-027 guards against, NOT just the case
    where ingest tries to overwrite with a wrong value. That is why
    preserveEditOnlyFields unconditionally fetches the current doc and
    merges editOnly fields back even when the ingest map omits them
    entirely. TestEditOnly_IngestOmitsField pins this behaviour.
  - For the integration test, resolving IsEditOnly via repo.ListProperties
    inside the hook closure is fine because per-lookup latency is a few
    ms against a testcontainers PG. Production main.go is NOT yet wired;
    the follow-up (US-046 / US-052 range) should build a cached
    (objectType, field) -> IsEditOnly map at startup and refresh on
    schema-change events, then call SetEditOnlyField once.
  - funnel.NewConsumer(nil, mgr) works fine for unit/integration tests
    that drive ApplyBatch directly — the nats.JetStreamContext is never
    dereferenced on the in-process path, so passing nil is the cleanest
    fixture and matches edit_conflict_test.go precedent.
  - fetchOrderDoc in test/integration/edit_only_test.go is the canonical
    "read a single bleve doc by PK" pattern: NewDocIDQuery + SearchRequest
    with Fields=['*'] and Size=1. Copy this into future integration tests
    that need to assert on the post-batch bleve state; don't reach for
    idx.Document() directly because its field visitor is bleve-internal
    and more brittle than the Search API.
---

## 2026-04-11 - US-028
- Removed 8 dead admin Playwright specs that were stranded after v1 US-006
  deleted the admin UI routes: action-type-crud, object-type-crud,
  link-type-crud, interface-management, property-management, ontology-crud,
  export-import, delete-confirmation.
- Re-scoped web/e2e/search-and-filter.spec.ts from the old admin page
  (`/admin/:ontology`) to the v2 Browser page
  (`/browser/:ontology/:objectType`). Now exercises SearchBar + Filters
  toggle via `data-testid` selectors (`search-input`, `toggle-filters`).
- Trimmed web/e2e/helpers.ts: dropped `navigateToAdmin`,
  `createPropertyViaAPI`, `createLinkTypeViaAPI`, `createActionTypeViaAPI`
  (nothing references them anymore). Added `navigateToBrowser(page,
  ontologyApiName, objectTypeApiName)` helper. `createOntologyViaAPI` +
  `createObjectTypeViaAPI` + `uniqueName` retained.
- Added web/e2e/README.md documenting the new phase-based layout
  (phase6..phase9 subdirs to come), backend prerequisites, and the
  US-029/US-030 follow-up work for `scripts/e2e-setup.sh` +
  `test/fixtures/e2e_seed.sh`.
- Quality: `npx tsc --noEmit` in web/ clean (exit 0); `npm run test`
  (vitest) exit 0; direct tsc spot-check on e2e/*.ts clean;
  `go vet ./...` clean. `go test ./...` skipped — story is frontend-only,
  no Go files touched.
- Files changed:
  - web/e2e/action-type-crud.spec.ts (DELETED)
  - web/e2e/delete-confirmation.spec.ts (DELETED)
  - web/e2e/export-import.spec.ts (DELETED)
  - web/e2e/interface-management.spec.ts (DELETED)
  - web/e2e/link-type-crud.spec.ts (DELETED)
  - web/e2e/object-type-crud.spec.ts (DELETED)
  - web/e2e/ontology-crud.spec.ts (DELETED)
  - web/e2e/property-management.spec.ts (DELETED)
  - web/e2e/helpers.ts
  - web/e2e/search-and-filter.spec.ts
  - web/e2e/README.md (new)
- **Learnings for future iterations:**
  - `web/tsconfig.app.json` has `"include": ["src"]`, so `npx tsc --noEmit`
    at `web/` root does NOT type-check `web/e2e/`. Playwright has its own
    TS transpile pipeline, so type errors in specs surface only at test
    time. For CI-safe editing, spot-check e2e specs with a direct
    `npx tsc --noEmit --target es2020 --module esnext --moduleResolution
    bundler --strict --skipLibCheck e2e/*.ts` invocation.
  - The v2 Browser page (pkg: `web/src/components/browser/BrowserPage.tsx`)
    has no admin counterpart — `/admin/*` routes were stripped in v1 US-006.
    Future spec authors should use `navigateToBrowser` + `getByTestId` over
    class selectors; the canonical test ids in `SearchBar.tsx` are
    `search-input` and `toggle-filters`.
  - The SearchBar `onSearch` callback fires on `Enter` AND on clearing to
    empty (see SearchBar.handleChange) — tests asserting on "clear then
    results reappear" can rely on this without needing a debounce.
  - `SearchBar` is purely client-side state; the backend round-trip only
    fires through `useSearchObjects` once `hasActiveSearch` flips on. This
    makes data-free UI-only specs (like the reworked search-and-filter
    spec) valid even against an empty ObjectType.
---
