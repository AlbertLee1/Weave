# Ralph Progress Log
Started: 2026-05-12
Branch: ralph/osv2-parity-and-coverage-iteration
Source PRD: tasks/prd-osv2-parity-and-coverage-iteration.md
Total stories: 56 (US-001 ~ US-056)
Phases:
  - Phase 1 (US-001~US-002): BDD framework + Playwright support
  - Phase 2 (US-003~US-012): Backend TDD for weak packages
  - Phase 3 (US-013~US-020): Backend BDD .feature scenarios (godog)
  - Phase 4 (US-021~US-038): UI Playwright BDD for existing pages
  - Phase 5 (US-039~US-045): P0 new UI features (Automation / Proposals / Security / Saga / Audit)
  - Phase 6 (US-046~US-050): P1 new UI features (FunctionRepo / Interface / TimeTravel / Lineage / Notifications)
  - Phase 7 (US-051~US-055): P2 new UI features (ValueTypes / Bindings / Rollback / Marketplace / QueryTypes)
  - Phase 8 (US-056): CI coverage gating
---

## Codebase Patterns
- 后端 BDD 入口：`make test-bdd` → `go test -tags bdd -count=1 -v ./test/bdd/...`。
  `bdd` 构建标签让套件不污染默认 `go test ./...` / `make test-unit`。
- 复用 `internal/testutil.StartPGContainer(t)` + `database.RunMigrationsUp(dsn, testutil.MigrationsDir())`
  是后端 PG 真实容器测试的标准入口；godog 步骤通过它拿到 `*pgxpool.Pool` 与 `*oms.PGRepository`。
- godog v0.15.1 API：`godog.TestSuite{ScenarioInitializer: ..., Options: &godog.Options{Format: "pretty",
  Paths: []string{"features"}, TestingT: t}}`；步骤注册用 `sc.Given/When/Then(regex, fn)`。
- 不要把 `*testing.T` 在多 scenario 共享时假设为同一个 — godog 给每个 scenario 走 subtest，
  且 `ScenarioInitializer` 每个 scenario 调一次，所以 per-scenario state（含容器句柄）独立。
- `pkg/oms.Repository` 当前没有 `DeleteOntology`；BDD/集成场景要硬删除时直接走 `pool.Exec` 即可，
  但请同时校验 FK（snapshots/branches）级联前确认。

## Open Questions
(参考 tasks/prd-osv2-parity-and-coverage-iteration.md 第 9 节)
1. playwright-bdd 第三方库 vs 自建 Given/When/Then helper — 当前选自建避免新依赖
2. godog scenarios 是否并行运行？默认串行最稳；待评估隔离成本
3. 覆盖率门禁阈值是否严格 fail PR 还是仅 warn？建议先 warn 一个月再 enforce
4. PC-A07（Security Policies）与 PC-A13（Lineage 图）复杂度高，是否先出低保真草图？
5. BDD scenarios 是否中文化 Gherkin？暂定英文

## 2026-05-12 17:25 - US-001 引入 godog BDD 框架
- 引入 `github.com/cucumber/godog v0.15.1`（间接拉入 cucumber/gherkin、messages、go-memdb）。
- 新建 `test/bdd/`：
  - `features/ontology_crud.feature`：Create / Update / Delete ontology 三个 scenarios + Background。
  - `steps/ontology_steps.go`（//go:build bdd）：定义 8 个 step regex（Given/When/Then），用
    `internal/testutil.StartPGContainer` + `database.RunMigrationsUp` 起 pgvector:pg16，并通过
    `*oms.PGRepository` 验证 Create/Get/Update 路径；Delete 直接 SQL（Repository 无 DeleteOntology）。
  - `bdd_test.go`（//go:build bdd）：`TestBDD(t)` 用 godog.TestSuite + TestingT=t，"pretty" formatter。
- `Makefile` 新增 `make test-bdd → go test -tags bdd -count=1 -v ./test/bdd/...`，与 `test` / `test-unit`
  解耦（默认 `go test ./...` 仍不会跑 BDD）。
- 验证：
  - `go build ./...` / `go vet ./...` 双绿。
  - `go test -count=1 -short ./pkg/oms/... ./pkg/rid/... ./internal/...` 全绿（未引发回归）。
  - `make test-bdd`：`3 scenarios (3 passed) / 13 steps (13 passed) / 4.66s`。
- 文件改动：
  - `go.mod` + `go.sum`（godog 链）
  - `Makefile`（.PHONY、test-bdd target）
  - `test/bdd/features/ontology_crud.feature` (新)
  - `test/bdd/steps/ontology_steps.go` (新)
  - `test/bdd/bdd_test.go` (新)
- **Learnings for future iterations:**
  - godog v0.15 不要漏掉 `Options.TestingT`，否则 scenario 失败不会让 `go test` 失败；本 suite 走子测试更易诊断。
  - `pkg/oms.Repository.GetOntology(ctx, ridOrApiName)` 同时接 `rid` 与 `api_name`（`WHERE rid=$1 OR api_name=$1`），
    BDD 步骤直接传 apiName 即可。
  - 每个 scenario 都重新起一个 pg 容器是 godog `ScenarioInitializer` 的默认行为；如果后续 BDD scenarios 多了，
    应改成 `TestSuiteContext.BeforeSuite` 起一次、`Before` 用 TRUNCATE 重置（容器复用），节省 ~1s/scenario。
  - `migrations/000004_ontology_snapshots.up.sql` 把 `current_version` 默认为 0；CreateOntology 不写它，
    所以 `BDD` 断言 `currentVersion=0`（不是 1）。
  - 编写 godog step regex 用 `Given/When/Then`（keyword-aware）比 `Step` 通用版更精确，避免 "And" 误绑。
---
