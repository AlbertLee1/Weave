# Ralph Progress Log - Parallel2 Stream A
Started: 2026-05-23T10:26:03+08:00
Integration branch: ralph/self-iteration-20260523-102602
Stream branch: ralph/self-iteration-20260523-102602-A
Focus: backend/API/MCP/ontology services, server-side reliability, migrations, Go tests

## Codebase Patterns
- JSON body hardening on write surfaces: replace `json.NewDecoder(r.Body).Decode(&req)` with `httputil.ReadJSON(r, &req)` (pkg/httputil/json.go). The helper enforces the shared 1 MiB `MaxBodySize` cap via `http.MaxBytesReader` AND rejects trailing JSON values (single-JSON-value invariant). Wrap the resulting error in `apierror.NewInvalidParameter("Invalid<Surface>Request", {"reason": err.Error()})`.
- For BDD coverage of body-validation hardening, drive the existing handler harness through `httptest.NewRequest` + `bytes.NewReader` / `strings.NewReader`, then assert (1) `rec.Code == http.StatusBadRequest`, (2) decoded `errorName` matches the surface (e.g. "InvalidLoginRequest"), and (3) the response's `parameters.reason` mentions "single JSON value" for the trailing-tokens case. Name files `*_bdd_test.go` and functions `TestBDD_*` per CLAUDE.md.
- Existing handlers using raw `json.NewDecoder(r.Body).Decode(&req)` (as of P2A-A001) still in scope for future hardening: pkg/auth/refresh_handler.go, pkg/auth/logout_handler.go, pkg/auth/mfa_handler.go, pkg/auth/role_handlers.go, pkg/auth/group_handlers.go, pkg/auth/api_key_handlers.go, pkg/auth/service_account_handlers.go, pkg/auth/user_role_handlers.go, pkg/auth/marking_handlers.go, pkg/aip/handlers.go, pkg/aip/tool_handlers.go, pkg/aip/handlers_fork.go, pkg/aip/logic/handlers.go, pkg/apps/handlers.go, pkg/dashboards/handlers.go, pkg/featureflags/handlers.go, pkg/gdpr/handlers.go, pkg/oms/handlers_v2_*.go, pkg/oss/handlers_*.go, pkg/pipeline/*.go, pkg/developer/application_handlers.go, pkg/userprefs/handlers.go, pkg/quiver/handlers.go, pkg/actiontemplates/handlers.go.

---

## 2026-05-23T11:30+08:00 - P2A-A001
- Implemented: switched POST /api/auth/login to decode through pkg/httputil.ReadJSON so ambiguous (concatenated) JSON bodies and oversized payloads are rejected with `400 InvalidLoginRequest` before the credential lookup runs.
- Files changed:
  - pkg/auth/login_handler.go (replaced raw json.NewDecoder + adopted shared helper)
  - pkg/auth/login_handler_bdd_test.go (new BDD scenarios: ambiguous body, oversized body, well-formed happy-path regression)
  - scripts/ralph/prd.json (story P2A-A001, passes:true)
- Tests:
  - `go test ./pkg/auth/ -run TestBDD_Login -v` → 3/3 PASS (initially failed for the right reasons before the fix — ambiguous body decoded silently into a 401, 2 MiB body issued tokens)
  - `go test ./pkg/auth/...` → ok
  - `go test ./...` → ok (full unit suite)
- **Learnings for future iterations:**
  - The login handler previously accepted a 2 MiB+ JSON body and issued tokens — the absence of `http.MaxBytesReader` on the body was a genuine reliability gap, not just a hypothetical. Whenever a write surface still uses raw `json.NewDecoder(r.Body)`, assume both the trailing-value AND oversized-body issues exist and cover both in the BDD test.
  - `pkg/auth/role_resolver_test.go` defines the shared `fakeUserRepo` used by every auth handler test; it does NOT count `GetUserByEmail` invocations. For "rejection runs before credential lookup" assertions, prefer asserting the response `errorName` (e.g. `InvalidLoginRequest` vs `InvalidCredentials`) over instrumenting the repo — the error-name distinction is enough because each path emits a different `errorName`.
  - `pkg/apierror.NewInvalidParameter(name, params)` writes 400s with `errorCode: "INVALID_ARGUMENT"` and the provided `errorName`. Assert on `errorName` to pin the exact surface failure, not `errorCode` (multiple paths share the latter).
---
