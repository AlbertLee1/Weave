# Ralph Progress Log
Started: Tue May 19 05:58:41 CST 2026

Branch: ralph/self-iteration-20260519-055841

Initial inspection:
- Archived stale Ralph state for branch `ralph/self-iteration-20260519-044235` to `scripts/ralph/archive/self-iteration-20260519-060220/`.
- Prior PRD belonged to `ralph/self-iteration-20260519-044235` and all stories had `passes: true`, so this branch required a fresh small PRD.
- No PR CI auto-fix context was present in the current progress file.
- `gh pr status` reported no pull request for this branch.
- `gh run list --branch ralph/self-iteration-20260519-055841 --limit 5` returned no workflow runs.
- `gh run list --branch main --limit 5` showed the latest main `CI` run completed successfully for PR #22; the previous main CI failure from PR #21 was repaired by the prior branch.
- Local baseline checks before PRD selection:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 5 tests.
  - `npm --prefix web exec playwright test -- --list us444/08-app-builder.spec.ts` passed: 1 test listed.
  - `git diff --check` passed.
- Named fresh-CI risks checked:
  - `cmd/server/embed.go` embeds `cmd/server/web/dist`, and the committed `cmd/server/web/dist` tree is present in this checkout.
  - US-453 streamless binary download coverage is present in `web/src/components/admin/__tests__/ComplianceReportsPage.test.tsx` and the component uses `generateComplianceReport` / `triggerBlobDownload`.
- Dogfood issues DOG-002 through DOG-005 have current code/tests, so the new PRD avoids duplicating those fixed gaps.

Durable Codebase Patterns:
- Web `src` tests are compiled under `tsconfig.app.json`; when a contract test needs source text, prefer Vite `?raw` imports over Node filesystem APIs.
- US-444 Playwright smoke specs use `skipWhenBackendDown(request)` for missing backend only; full-stack core endpoint absence should be asserted as a failure when the route is now expected to be wired.
- Seed-package integration tests are behind the `integration` build tag, so `go test ./test/fixtures/seed_northwind` alone only compiles the package and reports `[no test files]`.
- Browser realtime E2E should observe the subscription stream first, then make table assertions deterministic with primary-key lookup/search rather than relying on the current page.

Story SELF-422 - Make the US-444 App Builder gate reject unwired Apps APIs:
- Selected as the highest-priority story with `passes != true`.
- RED:
  - Added a raw-source contract check to `web/src/lib/__tests__/playwrightSpecContracts.test.ts` requiring `web/e2e/us444/08-app-builder.spec.ts` to stay free of `test.skip` / `test.fixme`, to call `/api/v2/apps`, and to assert create/get/delete wired endpoint messages.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected: the new App Builder contract found existing `test.skip(...)` guards in `08-app-builder.spec.ts`.
- Root cause investigation:
  - `08-app-builder.spec.ts` skipped when app creation returned 404/503 and skipped all other non-OK create responses, so a reachable backend with unwired Apps APIs could pass as degraded mode.
  - `pkg/apps/handlers.go` defines create/get/delete routes under `/api/v2/apps`, and `cmd/server/main.go` wires the PG-backed Apps store during full-stack PG boot.
  - The existing Playwright list check already discovered the App Builder scenario, so the missing signal was endpoint-status assertion rather than test discovery.
- GREEN implementation:
  - Updated `web/e2e/us444/08-app-builder.spec.ts` to keep only the backend-down portability skip, require create/get/delete to return 2xx, assert the returned RID is non-empty, and fail 404/503 through assertion messages.
  - Updated the Playwright spec contract test to pin the App Builder spec to mandatory endpoint probes and skip-free discoverability.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 6 tests.
  - `npm --prefix web exec playwright test -- --list us444/08-app-builder.spec.ts` passed: 1 test listed.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/us444/08-app-builder.spec.ts` returned no matches (exit 1 as expected for the no-match gate).
  - `npm --prefix web test -- --run` passed: 268 files, 2801 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/us444/08-app-builder.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - US-444 upper-layer smoke specs for PG-backed services should fail on reachable-backend 404/503 once the service is wired in full-stack boot; reserve `test.skip` for backend-down portability only.

Story SELF-423 - Make the US-444 Quiver gate reject unwired dashboard APIs:
- Selected as the highest-priority story with `passes != true`.
- RED:
  - Added a raw-source contract check to `web/src/lib/__tests__/playwrightSpecContracts.test.ts` requiring `web/e2e/us444/09-quiver.spec.ts` to stay free of `test.skip` / `test.fixme`, to call `/api/v2/quiver/save` and `/api/v2/quiver/dashboards`, and to assert save/list/delete wired endpoint messages.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected: the new Quiver contract found existing `test.skip(...)` guards in `09-quiver.spec.ts`.
- Root cause investigation:
  - `09-quiver.spec.ts` skipped when dashboard save returned 404/503 and skipped all other non-OK save responses, so a reachable backend with unwired Quiver dashboard APIs could pass as degraded mode.
  - `cmd/server/main.go` wires the PG-backed `QuiverStore` for full-stack PG boot and registers `/api/v2/quiver/*` routes when the store is present.
  - The existing Playwright list check already discovered the Quiver scenario, so the missing signal was endpoint-status assertion rather than test discovery.
- GREEN implementation:
  - Updated `web/e2e/us444/09-quiver.spec.ts` to keep only the backend-down portability skip, require save/list/delete to return 2xx, assert the returned RID is non-empty, and fail 404/503 through assertion messages.
  - Updated the Playwright spec contract test to pin the Quiver spec to mandatory endpoint probes and skip-free discoverability.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 7 tests.
  - `npm --prefix web exec playwright test -- --list us444/09-quiver.spec.ts` passed: 1 test listed.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/us444/09-quiver.spec.ts` returned no matches (exit 1 as expected for the no-match gate).
  - `npm --prefix web test -- --run` passed: 268 files, 2802 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/us444/09-quiver.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - Quiver full-stack E2E coverage should fail on reachable-backend missing store routes now that the PG-backed store is wired; list/delete lifecycle assertions catch partial endpoint regressions after save succeeds.
