# Ralph Progress Log
Started: Mon May 18 12:56:23 CST 2026
---

## Codebase Patterns
- ObjectSet `searchAround` uses API-name link traversal interfaces; production `links.Resolver` must expose the same optional reverse and target-type methods that ObjectSet mocks use.
- M2M ObjectSet coverage should wire `links.NewResolverWithEdges` and an EdgeRepository-shaped fixture so the test protects the join-table path, not only mock fanout.

## [2026-05-18 13:04 CST] - SELF-103
- Root cause: lower-level M2M join-table resolution existed in `pkg/links`, but the production `links.Resolver` did not satisfy ObjectSet's optional reverse-by-API-name and direction-aware target-type interfaces. Forward ObjectSet M2M traversal could return linked PKs but lost the target ObjectType, while reverse ObjectSet M2M traversal failed before calling the edge repository.
- Red test:
  - `go test ./pkg/oss/objectset -run TestBDD_ObjectSetSearchAround_M2MJoinEdges -count=1 -v` failed before implementation because forward `searchAround` returned `ObjectType == ""` and reverse `searchAround` returned `link resolver does not support reverse searchAround`.
- Implementation:
  - Added `ResolveLinkedReverseByAPIName`, `ResolveTargetObjectType`, and `ResolveTargetObjectTypeDir` to `pkg/links.Resolver`.
  - Added a focused BDD test that executes ObjectSet `searchAround` forward and reverse over an M2M LinkType through `links.NewResolverWithEdges`, including duplicate/converging join rows.
- Verification evidence:
  - `go test ./pkg/oss/objectset -run TestBDD_ObjectSetSearchAround_M2MJoinEdges -count=1 -v`: passed.
  - `go test ./pkg/oss/objectset -run 'SearchAround.*M2M' -count=1 -v`: passed.
  - `go test ./pkg/oss/objectset ./pkg/links -count=1`: passed.
- Files changed:
  - `pkg/links/resolver.go`
  - `pkg/oss/objectset/searcharound_m2m_bdd_test.go`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
