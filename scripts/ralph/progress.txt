# Ralph Progress Log
Started: 2026-05-22T13:32:51+08:00
Branch: ralph/self-iteration-20260522-123354
Archived previous state: scripts/ralph/archive/self-iteration-20260522-133251/

## Codebase Patterns
- `internal/testprofile.Instrumented(testing.CoverMode())` selects wider test profiles when race or coverage instrumentation distorts wall-clock performance gates.
- `pkg/security` keeps CEL RLS performance coverage in normal Go tests and exposes focused benchmarks for raw hot-path timing.
- Hot-path hash changes should preserve canonical FNV byte order with explicit tests when cache keys may already be materialized or compared indirectly.
- Instrumented wall-clock performance gates should reduce iteration count enough to leave CI-runner contention margin, not only widen duration budgets; keep the strict high-iteration profile for non-instrumented runs and keep a benchmark for raw ns/op signal.

## Baseline Evidence
- Branch verified: `ralph/self-iteration-20260522-123354`.
- Root symlinks verified: `prd.json -> scripts/ralph/prd.json`, `progress.txt -> scripts/ralph/progress.txt`.
- Previous PRD had all stories passing, so it was archived and replaced with a focused P0 CI repair PRD for this branch.
- Current GitHub CI health: run `26269888489` for HEAD `4fd1a209e79d5b0960b7222bb13f174d331f5ef0` failed in `Go (build, vet, lint, test, vulncheck)` during `go test -race -coverprofile=coverage.out -covermode=atomic $(./scripts/ci/go-packages.sh)`.
- GitHub failure evidence: `pkg/security` `TestRLSEvaluator_PerformanceGate` selected profile `instrumented`, iterations `200000`, cold `3.874807789s`, warm `4.126205606s`, cache stats `{Hits:399950 Misses:50 Size:50}`, compile stats `compileHits=10 compileMisses=10`, exceeding cold budget `1.5s` and warm budget `1s`.
- Known fresh-CI embed issue checked locally: `go test ./cmd/server -run '^TestBDD_WebEmbedDoesNotRequireGeneratedDist$' -count=1` passed.
- Known US-453 web test issue checked locally: `npm --prefix web test -- src/components/admin/__tests__/ComplianceReportsPage.test.tsx --run` passed (8 tests).
- Dogfood follow-up checks passed locally:
  - DOG-002/DOG-005/DOG-004 frontend BDD set passed (3 files, 10 tests).
  - DOG-003 OMS/OSS targeted BDD checks passed.
- Local RED reproduction for SELF-501: `go test -race -coverprofile=/tmp/weave-security-full.out -covermode=atomic ./pkg/security -count=1 -v` failed in `TestRLSEvaluator_PerformanceGate`; profile `instrumented`, iterations `200000`, cold `1.023372792s`, warm `1.020082666s`, cache stats `{Hits:399950 Misses:50 Size:50}`, warm exceeded `1s`.
- Control checks:
  - `go test -race -coverprofile=/tmp/weave-security-coverage.out -covermode=atomic ./pkg/security -run '^TestRLSPerfProfile|^TestRLSEvaluator_PerformanceGate$' -count=1 -v` passed locally but with warm `989.832542ms`, leaving almost no margin.
  - `go test ./pkg/security -run '^TestRLSPerfProfile|^TestRLSEvaluator_PerformanceGate$' -count=1 -v` passed with standard profile, cold `33.429334ms`, warm `32.812083ms`.
  - `go test ./pkg/security -run '^$' -bench '^BenchmarkRLSEvaluator_Warm$' -benchmem -count=3` reported `31.53ns/op` to `39.89ns/op`, `0 B/op`, `0 allocs/op`.

## Current Story
- SELF-501 Repair RLS Race Coverage Performance Gate: completed.
---

## 2026-05-22T13:47:00+08:00 - SELF-501
- What was implemented:
  - Reduced the `pkg/security` RLS instrumented profile from `200000` iterations to `25000`, with `1s` cold and `750ms` warm budgets, so race+coverage runs still exercise cache-heavy behavior without sitting on a CI wall-clock edge.
  - Renamed the profile guard to `TestBDD_RLSPerfProfile_KeepsStrictStandardGateAndAllowsInstrumentedVariance` and updated its instrumented acceptance expectations first.
  - Kept the standard non-instrumented profile unchanged at `1000000` iterations, `500ms` cold budget, and `50ms` warm budget.
- Files changed:
  - `pkg/security/rls_bench_test.go`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- RED evidence:
  - GitHub CI run `26269888489` failed in `go test -race -coverprofile=coverage.out -covermode=atomic $(./scripts/ci/go-packages.sh)` with `pkg/security` `TestRLSEvaluator_PerformanceGate`: profile `instrumented`, iterations `200000`, cold `3.874807789s`, warm `4.126205606s`, budgets `1.5s` and `1s`.
  - Local reproduction before the fix: `go test -race -coverprofile=/tmp/weave-security-full.out -covermode=atomic ./pkg/security -count=1 -v` failed with profile `instrumented`, iterations `200000`, cold `1.023372792s`, warm `1.020082666s`, warm budget `1s`.
  - Test-first RED after changing only the expected profile contract: `go test -race -coverprofile=/tmp/weave-security-red.out -covermode=atomic ./pkg/security -run '^TestBDD_RLSPerfProfile' -count=1 -v` failed with `profile.iterations = 200000, want 25000`.
- GREEN evidence:
  - `go test -race -coverprofile=/tmp/weave-security-profile.out -covermode=atomic ./pkg/security -run '^TestBDD_RLSPerfProfile' -count=1 -v` passed.
  - `go test -race -coverprofile=/tmp/weave-security-coverage.out -covermode=atomic ./pkg/security -run '^TestBDD_RLSPerfProfile|^TestRLSEvaluator_PerformanceGate$' -count=1 -v` passed; profile `instrumented`, iterations `25000`, cold `126.964375ms`, warm `126.2625ms`, cache stats `{Hits:49950 Misses:50 Size:50}`.
  - `go test -race -coverprofile=/tmp/weave-security-full.out -covermode=atomic ./pkg/security -count=1 -v` passed; package coverage `73.9%`, RLS gate profile `instrumented`, cold `126.151334ms`, warm `124.901875ms`.
  - `go test ./pkg/security -run '^TestBDD_RLSPerfProfile|^TestRLSEvaluator_PerformanceGate$' -count=1 -v` passed; profile `standard`, iterations `1000000`, cold `52.791083ms`, warm `37.623417ms`.
  - `go test ./pkg/security -run '^$' -bench '^BenchmarkRLSEvaluator_Warm$' -benchmem -count=3` passed; `34.68ns/op` to `37.77ns/op`, `0 B/op`, `0 allocs/op`.
  - `go test -race -coverprofile=/tmp/weave-ci-coverage.out -covermode=atomic $(./scripts/ci/go-packages.sh)` passed locally; `pkg/security` passed with `73.9%` coverage.
- Learnings for future iterations:
  - The RLS benchmark still provides the raw hot-path signal (`~35ns/op`, zero allocs) while the test gate proves profile selection and cache-hit behavior. Under race+coverage, use the smaller profile to avoid mistaking instrumentation overhead and runner contention for a hot-path regression.
  - The synthetic workload's paired modulo pattern produces 50 unique decision keys, so `25000` iterations still gives many warm-cache hits (`49950` hits over cold+warm passes) without needing 200000 instrumented iterations.
---
