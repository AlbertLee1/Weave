# Ralph Progress Log
Started: 2026-05-22T18:47:04+08:00
Branch: ralph/self-iteration-20260522-165538

Archived completed prior Ralph state:
- scripts/ralph/archive/self-iteration-20260522-184704/prd.json
- scripts/ralph/archive/self-iteration-20260522-184704/progress.txt

Initial inspection:
- Branch verified with `git branch --show-current`: ralph/self-iteration-20260522-165538.
- Prior PRD matched this branch, but all stories had `passes: true`.
- A newer `LLM review auto-fix context` was appended to the active progress file and is the strict highest-priority target for this PRD.
- Latest GitHub CI entries checked with `gh run list --limit 8 --json ...`: recent runs for this branch and main completed with conclusion success.
- Relevant code inspected: `web/src/lib/aggregationCsv.ts`, `web/src/lib/objectSetDiffCsv.ts`, and existing browser export CSV helper `web/src/lib/exportObjects.ts`.
- Relevant tests inspected: `web/src/components/aggregation/__tests__/AggregationPage.csvExport.bdd.test.tsx`, `web/src/components/objectsets/__tests__/ObjectSetDiffPage.csvExport.bdd.test.tsx`, and `web/src/lib/__tests__/exportObjects.test.ts`.
- Docs and dogfood output were scanned with `rg --files`; no existing CSV formula-injection hardening contract was found there.

Preserved LLM review auto-fix context - 2026-05-22T18:45:26+08:00:
- PR: https://github.com/AlbertLee1/Weave/pull/46
- Branch: ralph/self-iteration-20260522-165538
- Reviewed HEAD: c3e035db9ee5
- Review status: request_changes
- Summary: Request changes: the previous share-link and one-bucket pie issues appear addressed, but the newly added CSV export surfaces need formula-injection hardening before auto-merge.
- Blocking findings:
```json
[
  {
    "severity": "warning",
    "file": "web/src/lib/aggregationCsv.ts",
    "line": 68,
    "issue": "The new aggregation CSV exporter only applies RFC4180 quoting. User-controlled group/header values that begin with spreadsheet formula prefixes such as =, +, -, @, tab, or CR will be emitted as executable formulas when opened in Excel/LibreOffice, creating a CSV injection risk on the new export surface.",
    "suggestion": "Add a formula-injection guard to the CSV cell serializer, ideally shared with the object-set diff exporter. Preserve numeric metric values as numbers, but prefix dangerous string cells after leading-whitespace handling with an apostrophe or another accepted text-forcing escape, and add tests for formula-like group/header values."
  },
  {
    "severity": "warning",
    "file": "web/src/lib/objectSetDiffCsv.ts",
    "line": 129,
    "issue": "The new object-set diff CSV exporter has the same formula-injection gap for primary keys, field names, and object values. Diff data can come from object records controlled by other users, so a downloaded CSV can carry executable spreadsheet formulas.",
    "suggestion": "Use the same hardened CSV serializer here and cover primaryKey/valueA/valueB cases that start with formula trigger characters, including values that contain leading whitespace before the trigger."
  }
]
```
- Recommended fix prompt:
```text
Harden the new aggregation and object-set diff CSV exporters against spreadsheet formula injection. Introduce a shared CSV cell serializer or equivalent local helpers that RFC4180-quote cells and force dangerous string cells beginning with =, +, -, @, tab, CR, or leading whitespace before those characters to be treated as text, while preserving numeric values. Add Vitest coverage for formula-like group values, primary keys, field names, and diff values, then run the relevant web tests.
```

Codebase Patterns:
- Web CSV exporters currently build strings in `web/src/lib` and are exercised through component-level BDD tests that inspect the downloaded `Blob` text.
- Aggregation CSV rows derive string group keys and values from user-controlled bucket data, while metric values can be numeric and should remain number-shaped in the emitted CSV.
- Object-set diff CSV rows include section, primaryKey, field, valueA, and valueB; primary keys, field names, and object values can originate from object data and need the same cell-level hardening.
- RFC4180 escaping is local to exporter helpers today; a shared helper is appropriate for the two new CSV export surfaces so formula hardening and quote handling stay consistent.

Selected story:
- LLM-003 - Harden CSV exports against formula injection

## 2026-05-22T18:51:57+08:00 - LLM-003
- Hardened the reviewed aggregation and object-set diff CSV export surfaces with shared cell serialization in `web/src/lib/csvCell.ts`.
- Dangerous string cells beginning with `=`, `+`, `-`, `@`, tab, CR, or benign leading whitespace before those triggers are prefixed with an apostrophe before RFC4180 escaping.
- Numeric metric/object values remain emitted as number-shaped CSV cells, so negative numbers such as `-42` are not text-prefixed.
- Aggregation CSV now applies the shared serializer to group headers, metric headers, group values, and metric values.
- Object-set diff CSV now applies the shared serializer to section, primaryKey, field, valueA, and valueB cells.
- Files changed:
  - `web/src/lib/csvCell.ts`
  - `web/src/lib/aggregationCsv.ts`
  - `web/src/lib/objectSetDiffCsv.ts`
  - `web/src/lib/__tests__/aggregationCsv.test.ts`
  - `web/src/lib/__tests__/objectSetDiffCsv.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- TDD evidence:
  - RED targeted: `npm test -- aggregationCsv.test.ts objectSetDiffCsv.test.ts` failed with both new tests showing dangerous formula-like cells emitted unchanged.
  - GREEN targeted: `npm test -- aggregationCsv.test.ts objectSetDiffCsv.test.ts` passed with 2 files and 2 tests.
  - Relevant CSV export gate: `npm test -- src/lib AggregationPage.csvExport.bdd.test.tsx ObjectSetDiffPage.csvExport.bdd.test.tsx` passed with 27 files and 247 tests.
  - TypeScript gate: `npm run typecheck` passed.
- Codebase Patterns:
  - Keep CSV hardening before RFC4180 escaping so the apostrophe itself is included in the quoted cell when commas, quotes, CR, or LF require quoting.
  - Preserve value types until serialization; stringify objects and protect only raw string cells so numeric values remain numeric in exported CSV.

Remaining story:
- All stories in the active PRD pass.
