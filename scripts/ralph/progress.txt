# Ralph Progress Log
Started: Tue May 19 04:42:35 CST 2026

Branch: ralph/self-iteration-20260519-044235

Initial inspection:
- Archived stale Ralph state for branch `ralph/self-iteration-20260519-034928` to `scripts/ralph/archive/self-iteration-20260519-044626/`.
- Prior PRD belonged to `ralph/self-iteration-20260519-034928` and all stories had `passes: true`, so this branch required a fresh small PRD.
- No PR CI auto-fix context was present in the current progress file.
- `gh pr status` reported no pull request for this branch.
- `gh run list --branch ralph/self-iteration-20260519-044235 --limit 5` returned no workflow runs.
- GitHub main health at PRD creation: latest main push CI for PR #21 was still in progress; the previous main CI for PR #20 completed successfully.
- Local health probes before PRD selection:
  - `npm --prefix web test -- --run` passed: 268 files, 2798 tests.
  - `npm --prefix web run build` passed; Vite production build completed and bundle budgets passed.
  - `go test $(./scripts/ci/go-packages.sh)` passed.
- Named fresh-CI risks checked:
  - `cmd/server/embed.go` embeds `cmd/server/web/dist` via the committed server web tree.
  - US-453 streamless binary download coverage is present in `web/src/components/admin/__tests__/ComplianceReportsPage.test.tsx` and passed in the full web test run.
- Dogfood issues DOG-001 through DOG-005 have current code/tests, so the new PRD avoids duplicating those fixed gaps.

Durable Codebase Patterns:
- Web `src` tests are compiled under `tsconfig.app.json`; when a contract test needs source text, prefer Vite `?raw` imports over Node filesystem APIs.
- US-444 Playwright smoke specs use `skipWhenBackendDown(request)` for missing backend only; full-stack core endpoint absence should be asserted as a failure when the route is now expected to be wired.
- Seed-package integration tests are behind the `integration` build tag, so `go test ./test/fixtures/seed_northwind` alone only compiles the package and reports `[no test files]`.
- Browser realtime E2E should observe the subscription stream first, then make table assertions deterministic with primary-key lookup/search rather than relying on the current page.

Story SELF-419 - Repair Vertex workspace coverage-mode graph title gate:
- Selected as the highest-priority story after fresh main CI completed with a Web job failure while this iteration was running.
- CI failure evidence:
  - `gh run view 26059225943 --json name,status,conclusion,createdAt,updatedAt,event,headBranch,headSha,jobs` showed the main `CI` run failed in job `Web (typecheck, test, build)` while the Go job succeeded.
  - `gh run view 26059225943 --log-failed` showed `npx vitest run --coverage` failed in `src/vertex/VertexWorkspacePage.test.tsx:170`: expected `ri.vertex.main.graph.alpha` to contain `Alpha`.
- Root cause investigation:
  - The failing test waited for `vertex-canvas-mock`, but `/vertex/{rid}` renders the canvas shell immediately while the graph fetch is still pending.
  - `VertexWorkspacePage` intentionally renders the RID fallback in `vertex-topbar-graph-name` until fetch resolution populates `state.kind === "ready"` with `graph.name`.
  - Coverage instrumentation can slow the async state update enough for the assertion to observe the transient RID fallback.
  - A local pre-fix full coverage run did not reproduce the timing failure, so the GitHub CI failure is the RED evidence for this test-race repair.
- GREEN implementation:
  - Updated `web/src/vertex/VertexWorkspacePage.test.tsx` so the resolving `/vertex/{rid}` case still checks the canvas and TopBar, then waits specifically for `vertex-topbar-graph-name` to contain `Alpha` before asserting the loaded title.
- Verification:
  - `npm --prefix web test -- --run src/vertex/VertexWorkspacePage.test.tsx` passed: 1 file, 36 tests.
  - `npx vitest run --coverage` from `web/` passed: 268 files, 2798 tests.
  - `npm --prefix web test -- --run` passed: 268 files, 2798 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/src/vertex/VertexWorkspacePage.test.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - Vertex workspace tests should not use the Sigma canvas as graph-fetch readiness; the canvas is an initial shell. Wait on the loaded UI field or graph state being asserted.

Story SELF-420 - Make the US-444 lineage gate assert wired endpoints:
- Selected as the highest-priority story with `passes != true`.
- RED:
  - Added a raw-source contract check to `web/src/lib/__tests__/playwrightSpecContracts.test.ts` requiring `web/e2e/us444/13-lineage-view.spec.ts` to stay free of `test.skip` / `test.fixme`, to call the property-lineage and dataset-column impact endpoints, and to assert the `impacted` response shape.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected: the new lineage contract found existing `test.skip(...)` guards in `13-lineage-view.spec.ts`.
- Root cause investigation:
  - The Playwright spec skipped when customer full metadata was unavailable, when no property RID was surfaced, and when the property-lineage or dataset-column impact endpoints returned 404.
  - `pkg/lineage/handler.go` shows the wired property-lineage response shape is `{ propertyRid, upstream, truncated }` and the dataset-column impact response shape is `{ datasetRid, column, impacted, truncated }`.
  - `pkg/oms/handlers_v2_objecttype.go` requires `?preview=true` on object type fullMetadata, so the probe needed to request the preview contract before deriving a seeded property RID.
- GREEN implementation:
  - Updated `web/e2e/us444/13-lineage-view.spec.ts` to keep only the backend-down portability skip, request customer fullMetadata with `?preview=true`, fail if metadata or property RIDs are missing, require 2xx responses from both lineage endpoints, and assert the structured response bodies.
  - Updated the Playwright spec contract test to pin the US-444 lineage spec to the mandatory endpoint probes and skip-free discoverability.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 4 tests.
  - `npm --prefix web exec playwright test -- --list us444/13-lineage-view.spec.ts` passed: 2 tests listed.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/us444/13-lineage-view.spec.ts` returned no matches (exit 1 as expected for the no-match gate).
  - `npm --prefix web test -- --run` passed: 268 files, 2799 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/us444/13-lineage-view.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - Full-stack US-444 Playwright specs may skip only for a down backend; once the backend is reachable, expected core OSv2 surfaces should assert wired response shapes so 404/5xx regressions fail loudly.

Story SELF-421 - Make US-444 branch lifecycle gates reject unwired branches:
- Selected as the highest-priority story with `passes != true`.
- RED:
  - Added a raw-source contract check to `web/src/lib/__tests__/playwrightSpecContracts.test.ts` requiring `web/e2e/us444/06-branch.spec.ts` and `web/e2e/us444/07-merge.spec.ts` to stay free of `test.skip` / `test.fixme`, to reject 404 acceptance, and to keep branch, diff, merge, and close probes discoverable.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected: the new branch lifecycle contract found existing `test.skip(...)` guards in `06-branch.spec.ts`.
- Root cause investigation:
  - `06-branch.spec.ts` skipped when branch creation returned 404 and skipped all non-OK creation responses, so a reachable backend with an unwired branch route could pass as degraded mode.
  - `07-merge.spec.ts` skipped when branch creation returned 404 and explicitly accepted 404 from both branch diff and branch merge, masking unwired US-385 routes.
  - `cmd/server/routes.go` wires branch create/list/get/close plus GET/POST diff and merge routes, and `pkg/oms/handlers_branch_merge.go` shows a quiet branch POST diff returns structured `added`/`modified`/`deleted` arrays and merge returns 200 with applied/skipped counts.
- GREEN implementation:
  - Updated `web/e2e/us444/06-branch.spec.ts` to keep only the backend-down portability skip, require create/list/close to return 2xx, assert the created branch name, and assert the created branch appears in the list.
  - Updated `web/e2e/us444/07-merge.spec.ts` to require branch creation and POST diff to return 2xx, assert structured empty diff/conflict state for a quiet branch, and require merge to return 200 instead of accepting degraded statuses.
  - Updated the Playwright spec contract test to pin both branch lifecycle specs to mandatory endpoint probes and skip-free discoverability.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 5 tests.
  - `npm --prefix web exec playwright test -- --list us444/06-branch.spec.ts us444/07-merge.spec.ts` passed: 2 tests listed.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/us444/06-branch.spec.ts web/e2e/us444/07-merge.spec.ts` returned no matches (exit 1 as expected for the no-match gate).
  - `npm --prefix web test -- --run` passed: 268 files, 2800 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/us444/06-branch.spec.ts`
  - `web/e2e/us444/07-merge.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - US-444 full-stack branch smoke specs should only skip when the backend is down; once `/health` is reachable, branch, diff, and merge route absence must fail through status/body assertions rather than degraded-mode skips.
