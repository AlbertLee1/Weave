# Ralph Progress Log
Started: 2026-05-21T12:07:17+08:00
Branch: ralph/self-iteration-20260521-110910
Archived previous Ralph files: scripts/ralph/archive/self-iteration-20260521-120717/

## Initial inspection
- Branch verified: `ralph/self-iteration-20260521-110910`.
- Previous PRD belonged to this branch but all stories had `passes: true`, so a new small PRD was created.
- No `PR CI auto-fix context` or `LLM review auto-fix context` was present in the working progress file.
- GitHub PR #41 check rollup was inspected with `gh pr list --head ralph/self-iteration-20260521-110910 --json number,headRefName,statusCheckRollup,url,state`; both CI checks were successful:
  - Go (build, vet, lint, test, vulncheck), completed 2026-05-21T03:55:21Z.
  - Web (typecheck, test, build), completed 2026-05-21T03:55:26Z.
- Known issue check:
  - `cmd/server/embed.go` already has `cmd/server/embed_ci_bdd_test.go` guarding fresh CI builds without generated `web/dist`.
  - `npm --prefix web test -- --run src/components/admin/__tests__/ComplianceReportsPage.test.tsx` passed; the US-453 `object.stream is not a function` regression is covered and not active.
- Dogfood findings checked:
  - DOG-001 Northwind seed replay has `scripts/e2e-setup.sh` object-type probing and seed tests from P2B-503.
  - DOG-003 stream ingest index readiness has DOG-003 bootstrap/fail-fast coverage in OMS/OSS.
  - DOG-004 browser search where-contract and DOG-005 explorer flags have BDD coverage.

## Codebase Patterns
- MCP discovery methods should prefer deterministic full results over silent partial catalogues; `resources/list` is the local precedent for surfacing upstream OMS listing failures as JSON-RPC internal errors.
- Quiver editor state lives in `web/src/components/quiver/QuiverPage.tsx`; persisted dashboard payloads use the frontend-owned `QuiverDashboardConfig` in `web/src/api/quiver.ts`.
- Existing Quiver Playwright feature tests intentionally document gaps using absence assertions; replace those assertions with positive BDD checks when the UI capability is implemented.
- Playwright actual browser runs can silently reuse a Vite server from another checkout because `web/playwright.config.ts` sets `reuseExistingServer: !process.env.CI`; inspect `lsof -nP -iTCP:5173 -sTCP:LISTEN` before treating browser-run output as worktree-local evidence.
- Aggregation `where` can reuse `web/src/components/browser/FilterBuilder.tsx` and `web/src/lib/whereBuilder.ts`; this keeps object browser and aggregation request bodies on the same WhereClause contract.
- Northwind E2E seed replay depends on both layers: `test/fixtures/seed_northwind.Seed` owns DB wipe ordering for non-cascading ontology dependents (`functions`, `query_types`, `ontology_snapshots`, `automation_rules`, and object-type scoped `datasource_bindings`), while `scripts/e2e-setup.sh` should probe the live `/api/v2/ontologies/northwind/objectTypes` endpoint after the seeder returns so a clean exit cannot mask an empty browser fixture.

## Selected story
- SELF-601 - Record Scheduled Pipeline Ticks as Run History.
- Reason: `cmd/server/main.go` currently wires a scheduled pipeline runner that only logs each cron tick. The existing Pipeline run-history API and store contract are already present, so scheduled ticks can be made observable in product surfaces with a small, testable server-side change before broader runtime execution work.

## SELF-601 - Record Scheduled Pipeline Ticks as Run History
Status: PASS

BDD/TDD red step:
- Added `cmd/server/pipeline_scheduler_runner_bdd_test.go` with two scenarios:
  - A scheduled tick records a failed run-history row when the pipeline runtime is not configured.
  - Run-history append failures propagate back to the scheduler runner.
- Command: `go test ./cmd/server -run TestBDD_ScheduledPipelineTick -count=1 -v`
- Result: exited 1 as expected because `newScheduledPipelineRunRecorder` did not exist.

Implementation:
- `cmd/server/pipeline_scheduler_runner.go`
  - Added `scheduledPipelineRunRecorder`, a `pipeline.PipelineRunner` implementation.
  - On each cron tick, it appends a terminal `PipelineRun` with `status=failed`, `triggeredBy=schedule`, `startedAt`, `finishedAt`, and `errorMessage="pipeline runtime not configured"`.
  - It returns the runtime-not-configured error after a successful append so the scheduler logger still records that the runtime executor has not been wired.
  - It wraps store append errors so `errors.Is` still detects store-level causes such as `pipeline.ErrPipelineNotFound`.
- `cmd/server/main.go`
  - Replaced the log-only scheduled pipeline placeholder with `newScheduledPipelineRunRecorder(deps.PipelineStore)`.

Verification:
- RED: `go test ./cmd/server -run TestBDD_ScheduledPipelineTick -count=1 -v` exited 1 with `undefined: newScheduledPipelineRunRecorder`.
- GREEN targeted: `go test ./cmd/server -run TestBDD_ScheduledPipelineTick -count=1 -v` exited 0.
- Related packages: `go test ./cmd/server ./pkg/pipeline -count=1` exited 0.
- CI helper tests: `go test ./scripts/ci -count=1` exited 0.
- Fast Go gate: `go test $(./scripts/ci/go-packages.sh) -count=1` exited 0.

Files changed:
- `cmd/server/main.go`
- `cmd/server/pipeline_scheduler_runner.go`
- `cmd/server/pipeline_scheduler_runner_bdd_test.go`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`

Reusable learning:
- Scheduled pipeline ticks should be observable through the existing Pipeline run-history API before the full DAG runtime is wired. A failed run row with an explicit runtime-not-configured message is less misleading than a success row and more useful than a server log line.
---

## Selected story
- SELF-602 - Surface Pipeline Run History in the Pipeline Log Panel.
- Reason: SELF-601 now records scheduled ticks through the existing Pipeline run-history store, but the Web Pipeline page still showed placeholder copy in the log panel. Surfacing `/api/v2/pipelines/{pipelineId}/runs` closes the operator visibility loop with a narrow frontend change.

## SELF-602 - Surface Pipeline Run History in the Pipeline Log Panel
Status: PASS

BDD/TDD red step:
- Added SELF-602 scenarios to `web/src/components/pipelines/__tests__/PipelinesPage.test.tsx`:
  - A selected pipeline with recent runs lists run id, status, trigger, timestamp, and error text in the log panel.
  - A selected pipeline with no runs shows an empty run-history state and removes the old placeholder copy.
- Command: `npm --prefix web test -- --run src/components/pipelines/__tests__/PipelinesPage.test.tsx`
- Result: exited 1 as expected; the new tests could not find `pipeline-run-history` or `pipeline-run-history-empty`.

Implementation:
- `web/src/api/pipelines.ts`
  - Added `PipelineRun`, `ListPipelineRunsResponse`, `ListPipelineRunsOptions`, and `listPipelineRuns`.
  - `listPipelineRuns` calls the mounted `/api/v2/pipelines/{pipelineId}/runs` endpoint with optional `limit` and `cursor` query parameters.
- `web/src/hooks/usePipelines.ts`
  - Added `pipelineRuns` query keys and `usePipelineRuns(pipelineId, limit = 10)`.
- `web/src/components/pipelines/PipelinesPage.tsx`
  - Fetches recent runs for the selected pipeline.
  - Replaced the placeholder log-panel copy with a run-history section.
  - Shows loading, error, empty, and non-empty run-history states.
  - Displays run status, trigger, UTC timestamp, and error message for recent runs.
- `web/src/components/pipelines/__tests__/PipelinesPage.test.tsx`
  - Stubs the run-history fetch for existing Pipeline page tests.
  - Adds SELF-602 BDD coverage for recent-run and no-run states.

Verification:
- RED: `npm --prefix web test -- --run src/components/pipelines/__tests__/PipelinesPage.test.tsx` exited 1 with missing run-history test ids.
- GREEN targeted: `npm --prefix web test -- --run src/components/pipelines/__tests__/PipelinesPage.test.tsx` exited 0 with 8/8 tests passing.
- Package typecheck: `npm --prefix web run typecheck` exited 0.
- Web build: `npm --prefix web run build` exited 0; Vite built 1680 modules and bundle budgets passed.
- CI-shaped typecheck: `npx tsc --noEmit` from `web/` exited 0.
- Final targeted rerun: `npm --prefix web test -- --run src/components/pipelines/__tests__/PipelinesPage.test.tsx` exited 0 with 8/8 tests passing.
- Note: `npm --prefix web exec tsc -- --noEmit` exited 1 because the command syntax sent no options to `tsc`; reran the workflow-equivalent `npx tsc --noEmit` from `web/`, which exited 0.

Files changed:
- `web/src/api/pipelines.ts`
- `web/src/hooks/usePipelines.ts`
- `web/src/components/pipelines/PipelinesPage.tsx`
- `web/src/components/pipelines/__tests__/PipelinesPage.test.tsx`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`

Reusable learning:
- Pipeline run-history UI should use the existing frontend API/hook pattern from AIP Logic runs: add a typed API client, wrap it in a React Query hook keyed by the selected resource id, and pass query state into the page panel so empty/error/loading states are explicit.
---
