# Ralph Progress Log
Started: 2026-05-21T12:58:11+08:00
Branch: ralph/self-iteration-20260521-110910
Archived previous Ralph files: scripts/ralph/archive/self-iteration-20260521-125811/

## Initial inspection
- Branch verified: `ralph/self-iteration-20260521-110910`.
- Previous PRD belonged to this branch and all stories had `passes: true`, so a new small PRD was created.
- Root symlinks remain in place: `prd.json -> scripts/ralph/prd.json`, `progress.txt -> scripts/ralph/progress.txt`.
- `LLM review auto-fix context` was present and is the highest-priority target for this iteration. Scope is limited to the blocking finding: aggregation where filters are exposed by the UI but ignored by the backend.
- Root cause found before implementation: `web/src/api/types.ts` already includes `AggregationRequest.where`, but `pkg/oss/aggregation.AggregationRequest` has no `Where` field, so `json.Decoder` drops the request member before `pkg/oss.Handler.AggregateObjects` calls the aggregation engine.
- Existing backend affordances:
  - `pkg/oss/where.ConvertToBleveQuery` converts canonical WhereClause values to Bleve queries.
  - `pkg/oss/aggregation.Engine.AggregateWithQuery` already accepts a base query and conjoins downstream metric/groupBy work to that scope.
  - `pkg/oss/where.MatchClause` evaluates WhereClause trees in memory for non-Bleve paths such as scenario overlays.
  - `pkg/oss/handlers_aggregate.go` already enforces column policy for aggregation metric and groupBy fields.

## LLM review auto-fix context - 2026-05-21T12:55:43+08:00
- PR: https://github.com/AlbertLee1/Weave/pull/41
- Branch: ralph/self-iteration-20260521-110910
- Reviewed HEAD: 6d7bf207b533
- Review status: request_changes
- Summary: Request changes: aggregation filters are exposed in the UI but ignored by the backend.
- Blocking findings:
```json
[
  {
    "severity": "warning",
    "file": "web/src/components/aggregation/AggregationPage.tsx",
    "line": 55,
    "issue": "The new Aggregation where filter is UI/request-body only. The frontend now sends `where`, but the server decodes into `pkg/oss/aggregation.AggregationRequest`, which has no `where` field, so `json.Decoder` drops it and the aggregate endpoint still runs over all rows. The added tests only assert the POST body, not filtered aggregate results, so this regression is invisible to the current coverage.",
    "suggestion": "Implement the server-side where contract end to end: add/parse a where clause for aggregation, convert it to a Bleve query and conjoin it with the aggregate base query, cover scenario overlay if applicable, enforce property policy on where fields, and add a backend or browser test that proves filtered results change."
  }
]
```
- Recommended fix prompt:
```text
Fix P2B-502 end to end: make `/api/v2/ontologies/{ontology}/objects/{objectType}/aggregate` apply `where` filters in the backend, enforce property visibility on where fields, and add result-level tests proving filtered aggregates differ from unfiltered results.
```

## Codebase Patterns
- MCP discovery methods should prefer deterministic full results over silent partial catalogues; `resources/list` is the local precedent for surfacing upstream OMS listing failures as JSON-RPC internal errors.
- Quiver editor state lives in `web/src/components/quiver/QuiverPage.tsx`; persisted dashboard payloads use the frontend-owned `QuiverDashboardConfig` in `web/src/api/quiver.ts`.
- Existing Quiver Playwright feature tests intentionally document gaps using absence assertions; replace those assertions with positive BDD checks when the UI capability is implemented.
- Playwright actual browser runs can silently reuse a Vite server from another checkout because `web/playwright.config.ts` sets `reuseExistingServer: !process.env.CI`; inspect `lsof -nP -iTCP:5173 -sTCP:LISTEN` before treating browser-run output as worktree-local evidence.
- Aggregation `where` can reuse `web/src/components/browser/FilterBuilder.tsx` and `web/src/lib/whereBuilder.ts`; this keeps object browser and aggregation request bodies on the same WhereClause contract.
- Backend aggregation `where` must live on `pkg/oss/aggregation.AggregationRequest` and be applied by conjoining `pkg/oss/where.ConvertToBleveQuery` with the aggregation engine base query; scenario overlays need `pkg/oss/where.MatchClause` because they run in memory after fold.
- Northwind E2E seed replay depends on both layers: `test/fixtures/seed_northwind.Seed` owns DB wipe ordering for non-cascading ontology dependents (`functions`, `query_types`, `ontology_snapshots`, `automation_rules`, and object-type scoped `datasource_bindings`), while `scripts/e2e-setup.sh` should probe the live `/api/v2/ontologies/northwind/objectTypes` endpoint after the seeder returns so a clean exit cannot mask an empty browser fixture.

## Selected story
- SELF-604 - Apply Aggregation Where Filters Server-Side.
- Reason: This directly addresses the blocking LLM review finding and keeps the repair isolated to backend where execution, policy enforcement, and scenario-overlay parity.

## SELF-604 - Apply Aggregation Where Filters Server-Side
Status: PASS

BDD/TDD red step:
- Added `pkg/oss/handlers_aggregate_where_bdd_test.go` with three scenarios:
  - Backend aggregate results differ when `where.active == true` filters the employee fixture.
  - Column policy rejects a field-less count aggregation when its `where` references hidden `active`.
  - Scenario overlay aggregation applies `where.status == "shipped"` after edits are folded and before metrics/groupBy buckets are computed.
- Command: `go test ./pkg/oss -run TestBDD_SELF604 -count=1 -v`
- Result: exited 1 as expected. The filtered count remained 3 instead of 2, the hidden `where` field returned 200 instead of 403, and the scenario overlay returned both pending and shipped buckets instead of only the filtered shipped bucket.

Implementation:
- `pkg/oss/aggregation/engine.go`
  - Added `AggregationRequest.Where *where.WhereClause`.
  - Converts `Where` with `where.ConvertToBleveQuery` and conjoins it with the existing aggregation base query before exclusions, metrics, facets, cube/rollup, and sub-aggregation scopes run.
- `pkg/oss/handlers_aggregate.go`
  - Walks `where` clause trees and includes referenced fields in the existing property visibility gate.
  - Returns the existing `PropertyNotAccessible` permission error for hidden `where` fields.
- `pkg/oss/scenario_aggregate.go`
  - Filters post-overlay rows with `where.MatchClause` before the in-memory scenario aggregation buckets and metrics are computed.

Verification:
- RED: `go test ./pkg/oss -run TestBDD_SELF604 -count=1 -v` exited 1 with the expected ignored-filter failures.
- GREEN targeted: `go test ./pkg/oss -run TestBDD_SELF604 -count=1 -v` exited 0 with all three SELF-604 BDD tests passing.
- Related packages: `go test ./pkg/oss ./pkg/oss/aggregation ./pkg/oss/where -count=1` exited 0.
- CI helper tests: `go test ./scripts/ci -count=1` exited 0.
- Fast Go gate: `go test $(./scripts/ci/go-packages.sh) -count=1` exited 0.

Files changed:
- `pkg/oss/aggregation/engine.go`
- `pkg/oss/handlers_aggregate.go`
- `pkg/oss/scenario_aggregate.go`
- `pkg/oss/handlers_aggregate_where_bdd_test.go`
- `scripts/ralph/prd.json`
- `scripts/ralph/progress.txt`

Reusable learning:
- Aggregation request filtering belongs at the shared aggregation-engine base-query boundary so simple metrics, groupBy, cube/rollup, excludedItems, and sub-aggregations inherit the same scope. Scenario overlays are the exception because they bypass Bleve and must evaluate the same WhereClause against folded in-memory rows.
---
