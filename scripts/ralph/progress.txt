# Ralph Progress Log
Started: Tue May 19 20:19:09 CST 2026
Reset: Tue May 19 20:22:41 CST 2026
Branch: ralph/self-iteration-20260519-201909

## Codebase Patterns
- Status-doc drift stories should pin both sides of the contract: forbidden stale wording in `docs/PRD-Weave-OSv2-深度复刻-V2.md` plus required live code fragments from route registration, backend implementation files, and regression tests.
- Vertex legacy/deep-link routes should resolve through explicit redirect components in `App.tsx`; keep stale placeholder copy out of `web/src/vertex` once the live workspace route exists.
- Frontend typecheck is exposed as `npm --prefix web run typecheck`, backed by `tsc -b`.

## Kickoff Inspection
- Archived stale branch PRD/progress from ralph/self-iteration-20260519-192439 to `scripts/ralph/archive/self-iteration-20260519-202241/`.
- No `PR CI auto-fix context` was present in the active progress file.
- GitHub main health: latest CI run `26096351095` completed success on commit `5c51315`; Codespaces prebuild `26096359892` was still in progress.
- Known local risk checks:
  - `go test ./scripts/ci -count=1` => passed.
  - `go test ./cmd/server -run 'TestBDD_CIEmbedPackageSurvivesFreshCheckoutWithoutGeneratedWebDist|TestBuildEmbeddingProvider|TestEmbeddedWebDist|TestSPA|TestCI' -count=1` => passed.
  - `npm --prefix web test -- --run src/components/admin/__tests__/ComplianceReportsPage.test.tsx` => passed, 1 file / 8 tests.
- Dogfood issues DOG-002 through DOG-005 are already covered by live tests/code on this branch (`DashboardPage.totalObjectTypes`, `pkg/oms/index_bootstrap_bdd_test.go`, `pkg/oss/where/converter_dog004_bdd_test.go`, `PropertiesTable.dog005.bdd.test.tsx`).
- New PRD created for three narrow OSv2 upper-layer contract gaps. SELF-459 is first because `pkg/vertex/controlpanel/handler.go` registers GET/PUT `/api/vertex/v1/admin/control-panel`, but `cmd/server/contract_test.go` still allow-lists those routes as undocumented, leaving API Playground/OpenAPI clients blind to live Vertex operator knobs.
---

## Tue May 19 20:27:30 CST 2026 - SELF-459
- Implemented a BDD OpenAPI contract for the live Vertex control-panel routes and published GET/PUT `/api/vertex/v1/admin/control-panel` in both `api/openapi.yaml` and `cmd/server/openapi_spec.yaml`.
- RED evidence: `go test ./cmd/server -run TestBDD_VertexControlPanelOpenAPIContract -count=1` failed because GET/PUT `/api/vertex/v1/admin/control-panel` were still allow-listed as undocumented, absent from `api/openapi.yaml`, and `VertexControlPanelConfig` was missing from components schemas.
- GREEN evidence:
  - `go test ./cmd/server -run TestBDD_VertexControlPanelOpenAPIContract -count=1` => passed.
  - `go test ./cmd/server -run 'TestBDD_VertexControlPanelOpenAPIContract|TestContract_' -count=1` => passed.
  - `go test ./pkg/vertex/controlpanel -count=1` => passed.
  - `python3 -m json.tool scripts/ralph/prd.json >/dev/null` => passed.
  - `git diff --check` => passed.
- Files changed:
  - `api/openapi.yaml`
  - `cmd/server/openapi_spec.yaml`
  - `cmd/server/contract_test.go`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - `cmd/server/contract_test.go` has an explicit undocumented-route allow-list for live chi routes that are not yet in OpenAPI. Removing an entry plus adding canonical and embedded spec paths is the smallest way to convert a live route into a documented API contract.
  - The running server serves `cmd/server/openapi_spec.yaml`; `TestContract_EmbeddedSpecMatchesCanonical` requires it to be byte-identical to `api/openapi.yaml`.
  - Vertex control-panel behavior already lives in `pkg/vertex/controlpanel`; this story only changed discoverability and contract coverage, not runtime semantics.
---

## Tue May 19 20:32:37 CST 2026 - SELF-460
- Implemented a BDD OpenAPI contract for the live Vertex share-link and Workshop widget routes, removed those five routes from `undocumentedRouteAllowList`, and published their path operations in both `api/openapi.yaml` and `cmd/server/openapi_spec.yaml`.
- Added OpenAPI schemas for:
  - `VertexGraph`, including the shared graph/widget response shape and payload semantics.
  - `VertexShareLinkCreateResponse`, including `token`, `graphRid`, `createdBy`, and `createdAt`.
  - `VertexWidgetSaveRequest`, including `payload` and optional `overrideGraphRid`.
- RED evidence: `go test ./cmd/server -run TestBDD_VertexShareLinksAndWidgetOpenAPIContract -count=1` failed because POST/DELETE/GET share-link routes and GET/POST widget routes were still allow-listed as undocumented and absent from `api/openapi.yaml`.
- GREEN evidence:
  - `go test ./cmd/server -run TestBDD_VertexShareLinksAndWidgetOpenAPIContract -count=1` => passed.
  - `go test ./cmd/server -run 'TestContract_|TestGraphShare|TestGraphWidget' -count=1` => passed.
  - `go test ./pkg/vertex/graphsvc -run 'Test.*Share|Test.*Widget' -count=1` => passed.
  - `git diff --check` => passed.
- Files changed:
  - `api/openapi.yaml`
  - `cmd/server/openapi_spec.yaml`
  - `cmd/server/contract_test.go`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Share-link and widget runtime behavior was already covered in `pkg/vertex/graphsvc`; this story only promoted the live routes into the discoverable API contract.
  - For OpenAPI drift work, add a focused BDD contract test that asserts both removal from `undocumentedRouteAllowList` and schema refs on the documented operations.
  - Copy `api/openapi.yaml` to `cmd/server/openapi_spec.yaml` after spec edits; the embedded-spec contract test treats byte drift as a server failure.
---

## Tue May 19 20:39:39 CST 2026 - SELF-461
- Replaced the stale Vertex diagramming placeholder with an explicit redirect from `/vertex/{rid}/diagramming` to `/vertex/{rid}`, wired the redirect into `App.tsx`, and removed the retired `vertex.diagramming.*` i18n placeholder keys from both locales.
- Added `web/package.json` `typecheck` script so the PRD quality gate runs `tsc -b` directly.
- RED evidence:
  - `npm --prefix web test -- --run src/vertex/DiagrammingStubPage.test.tsx src/vertex/vertexI18n.test.tsx` => failed because `DiagrammingStubPage` still rendered `Diagramming · Coming soon` instead of redirecting, and both locale resource trees still contained `diagramming.*` keys.
  - `npm --prefix web run typecheck` => failed with `Missing script: "typecheck"`.
- GREEN evidence:
  - `npm --prefix web test -- --run src/vertex/DiagrammingStubPage.test.tsx src/vertex/vertexI18n.test.tsx` => passed, 2 files / 20 tests.
  - `npm --prefix web test -- --run src/vertex/DiagrammingStubPage.test.tsx src/vertex/vertexI18n.test.tsx src/vertex/VertexWorkspacePage.test.tsx` => passed, 3 files / 56 tests.
  - `npm --prefix web run typecheck` => passed.
  - `git diff --check` => passed.
- Files changed:
  - `web/package.json`
  - `web/src/App.tsx`
  - `web/src/i18n/resources/en.ts`
  - `web/src/i18n/resources/zh-CN.ts`
  - `web/src/vertex/DiagrammingStubPage.test.tsx`
  - `web/src/vertex/DiagrammingStubPage.tsx`
  - `web/src/vertex/VertexDiagrammingRedirect.tsx`
  - `web/src/vertex/vertexI18n.test.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - React Router paths like `vertex/:rid` do not match deeper URLs such as `/vertex/{rid}/diagramming`; add an explicit redirect route before the wildcard to keep deep links out of `NotFoundPage`.
  - Retiring a placeholder page needs both route/test updates and i18n resource cleanup; `vertexI18n.test.tsx` now guards against reintroducing stale `vertex.diagramming.*` keys.
  - The `web` package did not previously expose the PRD's typecheck command; `npm --prefix web run typecheck` now maps to `tsc -b`.
---
