# Ralph Progress Log
Started: Tue May 19 03:49:28 CST 2026

Branch: ralph/self-iteration-20260519-034928

Initial inspection:
- Archived stale Ralph state for branch `ralph/self-iteration-20260519-024456` to `scripts/ralph/archive/self-iteration-20260519-035412/`.
- Prior PRD belonged to `ralph/self-iteration-20260519-024456` and all stories had `passes: true`, so this branch required a fresh small PRD.
- No PR CI auto-fix context was present in the current progress file.
- `gh pr checks --watch=false` reported no pull request for this branch.
- `gh run list --branch ralph/self-iteration-20260519-034928 --limit 5` returned no workflow runs.
- Local health probes before PRD selection:
  - `npm --prefix web test -- --run` passed: 267 files, 2795 tests.
  - `npm --prefix web run build` passed; Vite production build completed and bundle budgets passed.
  - `go test $(./scripts/ci/go-packages.sh)` passed.
- Named fresh-CI risks checked:
  - `cmd/server/embed.go` embeds `cmd/server/web/dist` via the committed server web tree; `cmd/server/embed_ci_bdd_test.go` covers the placeholder path.
  - US-453 streamless binary download coverage is present in `web/src/components/admin/__tests__/ComplianceReportsPage.test.tsx` and passed in the full web test run.
- Dogfood issues DOG-002, DOG-003, DOG-004, and DOG-005 have current code/tests, so the new PRD avoids duplicating those fixed gaps.

Durable Codebase Patterns:
- Browser realtime mode currently owns transport selection in `web/src/components/browser/BrowserPage.tsx`; WebSocket is primary via `useWebSocketSubscription`, and SSE fallback uses `useObjectSetSubscription` with a temporary ObjectSet RID.
- Browser object refresh should invalidate TanStack Query cache with `queryKey: ['objects']` so list and search result hooks refetch through their canonical paths.
- Web tests use MSW plus hook-level mocks in `web/src/components/browser/__tests__/BrowserPage.test.tsx`; hook transport behavior is isolated in `web/src/hooks/__tests__/useWebSocketSubscription.test.ts` and `web/src/hooks/__tests__/useObjectSetSubscription.test.ts`.
- Browser realtime E2E should observe the subscription stream first, then make the table assertion deterministic with a primary-key query instead of polling the current page for a newly inserted row.
- Browser Live toolbar health should be derived from transport callbacks (`connected`, `connecting`, `reconnecting`) and not from the requested Live toggle state.

Story SELF-416 - Make SSE reconnect Playwright gate deterministic:
- Selected as the highest-priority incomplete story because local CI was clean and `web/e2e/phase7/sse-reconnect.spec.ts` still hid the reconnect scenario behind `test.fixme`, weakening the realtime recovery signal.
- RED:
  - Added `web/src/lib/__tests__/playwrightSpecContracts.test.ts` to assert that the SSE reconnect Playwright spec has no `test.fixme`, performs a primary-key search through the Browser search endpoint, and verifies the current `live-status` toolbar surface rather than a stale selector-only indicator.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected: the source still matched `test.fixme(`.
- GREEN implementation:
  - Re-enabled `web/e2e/phase7/sse-reconnect.spec.ts` by changing `test.fixme(...)` to an active `test(...)`.
  - Added `waitForLiveConnected` so the scenario waits on `data-testid="live-status"` and its accessible connected label before and after the offline window.
  - Added `searchForCustomerById` so each inserted customer is searched by primary key before the row assertion, keeping the test deterministic when previous specs have accumulated more than one table page.
  - Fixed the new contract test to use Vite `?raw` imports after `npm --prefix web run build` exposed that `node:fs`, `node:path`, and `process` are not available in `tsconfig.app.json`.
- Verification:
  - RED command: `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed for the expected `test.fixme` match.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 1 test.
  - `npm --prefix web exec playwright test -- --list phase7/sse-reconnect.spec.ts` passed: listed 1 active test.
  - `rg -n "test\\.fixme\\(" web/e2e/phase7/sse-reconnect.spec.ts` returned no matches (exit 1).
  - `npm --prefix web run build` passed; Vite production build completed and bundle budgets passed.
  - `npm --prefix web test -- --run` passed: 268 files, 2796 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/phase7/sse-reconnect.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - Web `src` tests are compiled under `tsconfig.app.json`; when a contract test needs source text, prefer Vite `?raw` imports over Node filesystem APIs.

Story SELF-417 - Restore optimistic concurrency browser gate:
- Selected as the highest-priority incomplete story because local/remote CI health was already clean and `web/e2e/phase6/optimistic-concurrency.spec.ts` still guarded the stale-object browser recovery flow behind `test.skip`.
- Root cause investigation:
  - `object_history.ontology_rid` was added by migration `000017_ontology_isolation_columns.up.sql`, but `oms.ObjectHistory` and `PGRepository.InsertObjectHistory` did not persist that scope.
  - `test/fixtures/seed_northwind.Seed()` wrote Northwind history through `InsertObjectHistory`, so all seeded rows had an empty `ontology_rid`.
  - `useObjectVersion()` still called the removed `/objects/{type}/{pk}/history` route; the supported per-object history surface is `/activity`, whose newest row carries the current version.
- RED:
  - Added `TestBDD_SeedObjectHistoryRowsCarryOntologyScope` to assert every seeded history row, especially `customer/ALFKI`, carries the same ontology scope as its ObjectType.
  - Updated `useObjectVersion` tests to expect `getObjectActivity({ pageSize: 1 })` and version derivation from the newest activity row.
  - Extended `playwrightSpecContracts.test.ts` to reject `test.skip`/`test.fixme` in the optimistic-concurrency Playwright spec.
  - `go test -tags integration ./test/fixtures/seed_northwind -run TestBDD_SeedObjectHistoryRowsCarryOntologyScope -count=1` failed as expected with empty `object_history ... ontology_rid`.
  - `npm --prefix web test -- --run src/hooks/__tests__/useObjectVersion.test.ts` failed as expected because the hook still used `getObjectHistory`.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected because the spec still contained `test.skip(`.
- GREEN implementation:
  - Added `OntologyRID` to `oms.ObjectHistory` and persisted it in both `InsertObjectHistory` paths while keeping empty values nullable for legacy callers.
  - Returned `ontologyRid` from `ListObjectHistory` and `ListObjectHistoryPage` so activity/history readers preserve scope when it exists.
  - Stamped the Northwind ontology RID on every seeded object history row.
  - Switched `useObjectVersion()` to read the newest `/activity` row with `pageSize: 1`.
  - Removed the permanent skip from `web/e2e/phase6/optimistic-concurrency.spec.ts`.
- Verification:
  - RED commands above failed for the expected reasons before implementation.
  - `go test -tags integration ./test/fixtures/seed_northwind -run TestBDD_SeedObjectHistoryRowsCarryOntologyScope -count=1` passed.
  - `npm --prefix web test -- --run src/hooks/__tests__/useObjectVersion.test.ts` passed: 1 file, 4 tests.
  - `npm --prefix web test -- --run src/components/actions/__tests__/ActionConsolePage.test.tsx` passed: 1 file, 7 tests.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 2 tests.
  - `go test ./test/fixtures/seed_northwind` passed with `[no test files]` under the default non-integration build tag.
  - `go test ./pkg/oms ./pkg/oss` passed.
  - `npm --prefix web exec playwright test -- --list phase6/optimistic-concurrency.spec.ts` passed: listed 1 active test.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/phase6/optimistic-concurrency.spec.ts` returned no matches (exit 1).
  - `npm --prefix web test -- --run` passed: 268 files, 2797 tests.
  - `npm --prefix web run build` passed; Vite production build completed and bundle budgets passed.
  - `go test $(./scripts/ci/go-packages.sh)` passed.
  - `git diff --check` passed.
- Files changed:
  - `pkg/oms/object_history.go`
  - `pkg/oms/pg_repository.go`
  - `test/fixtures/seed_northwind/seed.go`
  - `test/fixtures/seed_northwind/seed_test.go`
  - `web/e2e/phase6/optimistic-concurrency.spec.ts`
  - `web/src/components/actions/__tests__/ActionConsolePage.test.tsx`
  - `web/src/hooks/useObjectVersion.ts`
  - `web/src/hooks/__tests__/useObjectVersion.test.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - The legacy object-history endpoint is intentionally removed by US-006; Action Console version reads should use the current activity timeline endpoint and derive the version from the newest row.
  - Seed-package integration tests are behind the `integration` build tag, so `go test ./test/fixtures/seed_northwind` alone only compiles the package and reports `[no test files]`.

Story SELF-418 - Make the US-444 action gate assert seeded actions:
- Selected as the only remaining incomplete story because SELF-416 and SELF-417 were already passing, and the US-444 action Playwright probe still skipped the dispatcher contract when the Northwind action catalog was empty.
- RED:
  - Extended `web/src/lib/__tests__/playwrightSpecContracts.test.ts` to assert that `web/e2e/us444/04-action.spec.ts` has no `test.skip`/`test.fixme`, does not dispatch `types[0].apiName`, requires the known `createCustomer` seed action, and posts to the named action dispatcher path.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` failed as expected because the source still matched `test.skip(`.
- GREEN implementation:
  - Changed `web/e2e/us444/04-action.spec.ts` to require `createCustomer` in the seeded Northwind action catalog instead of skipping on an empty list.
  - Posted deliberately invalid parameters to `/actions/createCustomer/apply` and asserted a structured 4xx ApiError envelope with `errorCode` or `errorName`.
- Verification:
  - RED command above failed for the expected `test.skip(` match before implementation.
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 1 file, 3 tests.
  - `npm --prefix web exec playwright test -- --list us444/04-action.spec.ts` passed: listed 1 active test.
  - `rg -n "test\\.(skip|fixme)\\(" web/e2e/us444/04-action.spec.ts` returned no matches (exit 1).
  - `npm --prefix web test -- --run` passed: 268 files, 2798 tests.
  - `git diff --check` passed.
- Files changed:
  - `web/e2e/us444/04-action.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Reusable learning:
  - US-444 action gate coverage should bind to a known Northwind seed action, not the first listed action, so an empty or malformed action catalog fails before the dispatcher contract can be skipped.
