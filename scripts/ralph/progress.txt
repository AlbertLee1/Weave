# Ralph Progress Log
Started: Tue May 19 08:22:47 CST 2026
Branch: ralph/self-iteration-20260519-082247
---

## Codebase Patterns
- US-444 full-stack probes should only use `test.skip` for the shared backend-down portability guard. If the backend is reachable, quiet, missing, 404/503, or shallowly wired service behavior should be an assertion failure guarded by a source-level contract test.

## Kickoff
- Current branch verified with `git branch --show-current`: `ralph/self-iteration-20260519-082247`.
- Previous active PRD belonged to `ralph/self-iteration-20260519-073710` and all stories had `passes: true`; archived old `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` to `scripts/ralph/archive/self-iteration-20260519-082749/`.
- Root symlinks verified before refresh: `prd.json -> scripts/ralph/prd.json`, `progress.txt -> scripts/ralph/progress.txt`.
- No `PR CI auto-fix context` was present in the active progress file.
- GitHub CI at kickoff: PR CI run 26067989274 completed success; main CI run 26066339689 completed success; fresh main CI run 26068337937 was in progress with Web job success and Go job running test/coverage.
- Local health sampled:
  - `go test ./...` passed.
  - `npm --prefix web test -- --run` passed: 268 files, 2809 tests.
- Known-risk inspection:
  - Fresh-CI `cmd/server/embed.go` risk is covered by `cmd/server/embed_ci_bdd_test.go` plus `cmd/server/web/embed-placeholder.txt`.
  - US-453 streamless binary-response risk is covered by `web/src/components/admin/__tests__/ComplianceReportsPage.test.tsx`.
  - Dogfood DOG-001 through DOG-005 have committed-looking implementation and regression coverage in this checkout.

## Selected Story
- Selected SELF-431 because it is the highest-priority incomplete story and the remaining `us444/20-subscribe.spec.ts` skip can hide a reachable-but-unwired WebSocket subscription surface.

## SELF-431 - Make US-444 subscribe gate require the WebSocket subscribe handshake
- TDD red:
  - Added `us444SubscribeSpecSource` coverage to `web/src/lib/__tests__/playwrightSpecContracts.test.ts`.
  - Ran `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts`.
  - Expected failure observed: new contract rejected `no welcome frame within 3s` in `web/e2e/us444/20-subscribe.spec.ts`.
- Implementation:
  - Updated `web/e2e/us444/20-subscribe.spec.ts` to parse the welcome frame, assert `type === "welcome"` and numeric `lastEventId`, send `{type:"subscribe", data:{objectType:"customer"}}`, and require a `subscribed` acknowledgement with non-empty `subscriptionId`.
  - Removed the quiet/unwired endpoint `test.skip(firstMessage === null, ...)` path.
  - Added small frame read/parse helpers inside the spec so missing, closed, non-string, or timeout frames fail the Playwright test.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/playwrightSpecContracts.test.ts` passed: 15 tests.
  - `npm --prefix web exec playwright test -- --list us444/20-subscribe.spec.ts` passed: 1 discoverable test.
  - `rg -n "no welcome frame|endpoint unwired or quiet|test\\.skip\\(firstMessage" web/e2e/us444/20-subscribe.spec.ts` returned no matches.
  - `npm --prefix web run build` passed, including `tsc -b`, Vite build, and bundle budget check.
  - `git diff --check` passed.
  - Fresh main GitHub CI run 26068337937 completed success while this story was in progress.
- Files changed:
  - `web/e2e/us444/20-subscribe.spec.ts`
  - `web/src/lib/__tests__/playwrightSpecContracts.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Result: SELF-431 marked `passes: true`; SELF-432 and SELF-433 remain open for later Ralph iterations.
