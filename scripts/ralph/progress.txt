# Ralph Progress Log
Started: Tue May 19 23:05:04 CST 2026
Branch: ralph/self-iteration-20260519-230504

## Cycle Setup
- Archived stale PRD/progress from branch `ralph/self-iteration-20260519-215825` to `scripts/ralph/archive/self-iteration-20260519-230855/` because all stories were already marked `passes: true`.
- No `PR CI auto-fix context` was present in the active progress file.
- GitHub health inspected with `gh run list --limit 8 --json ...`: main CI run `26105215653` on `91985d2` completed success; main Codespaces prebuild `26105216657` was in progress; previous branch PR CI `26103928834` completed success.
- Local known-risk checks passed:
  - `go test ./cmd/server -run 'TestBDD_WebEmbedDoesNotRequireGeneratedDist|TestContract_' -count=1`
  - `npm --prefix web test -- --run src/components/admin/__tests__/ComplianceReportsPage.test.tsx src/components/browser/__tests__/BrowserPage.search.bdd.test.tsx src/components/explorer/__tests__/PropertiesTable.dog005.bdd.test.tsx`
  - `go test ./pkg/oss ./pkg/oms -run 'TestBDD_StreamIngest|TestBDD_ImportOntology|TestBDD_CreateObjectType|Test.*DOG|Test.*dog' -count=1`

## Selected Story
- `SELF-465` — Preserve Vertex added-object primary keys.

## SELF-465 Evidence
- RED: `npm --prefix web test -- --run src/vertex/VertexAddObjectsDialog.test.tsx src/vertex/VertexWorkspacePage.test.tsx` failed as expected before implementation.
  - `VertexAddObjectsDialog.test.tsx` received `[undefined, ...]` for `primaryKey` instead of `A0..A4`.
  - `VertexWorkspacePage.test.tsx` selected `ri.airport.NEW0` but `calls.objectGets` stayed empty, proving the sidebar could not fetch `/api/v2/ontologies/main/objects/Airport/NEW0`.
- GREEN: after carrying `__primaryKey` through `AddedObjectInput` and `addedSummariesByRid`, the same command passed with 2 test files and 41 tests.
- Files changed:
  - `web/src/vertex/VertexAddObjectsDialog.tsx`
  - `web/src/vertex/VertexAddObjectsDialog.test.tsx`
  - `web/src/vertex/VertexWorkspacePage.tsx`
  - `web/src/vertex/VertexWorkspacePage.test.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`

## Codebase Patterns
- Vertex workspace tests mock `@react-sigma/core` in `web/src/vertex/VertexWorkspacePage.test.tsx` and fire captured Sigma handlers directly for selection/context-menu behavior.
- Vertex Add Objects dialog result rows come from OSS search `WireObject` rows; `__primaryKey` is already selected from the backend and should be carried through page state when downstream tabs need fresh OSS calls.
- For transient Vertex objects added outside the saved graph payload, keep `VertexObjectSummary` metadata complete enough for existing sidebar hooks (`useGetObject`, `useObjectActivity`, `useTimeSeriesPoints`) to work exactly like payload-resident nodes.
