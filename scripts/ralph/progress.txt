# Ralph Progress Log - Stream A
Started: Fri May 15 09:45:33 CST 2026
Stream: A

## Codebase Patterns
- LinkType admin handlers use **tri-state pointer** for partial PUT fields (nil = preserve, &value = set). Mirror this for any new optional field; bare value collapses "omit" and "clear" into one (see UpdateLinkTypeRequest.PropagateMarkings & TypeClasses).
- LinkType wire format uses `omitempty` so the JSON shape stays backwards compatible — emit new fields from ToWireJSON only when non-zero.
- PostgreSQL TEXT[] columns: pgx v5 scans into `[]string` directly. SELECT with `COALESCE(col, '{}'::text[])` keeps nil rows safe; INSERT pass `[]string{}` (not nil) to avoid pgx encoding gotchas.
- Frontend Vertex pure helpers live under `web/src/features/vertex/<feature>/` with `<helper>.ts` + `<helper>.test.ts` siblings (Vitest, no React).
- Frontend admin UI for OMS resources lives in `web/src/components/admin/*.tsx`; co-located `__tests__/*.test.tsx` use a `installFetch(state)` stub pattern to mock the v2 REST endpoints.
- `cmd/server/embed.go` requires `cmd/server/web/dist/*` to exist before `go build ./...` works — keep a placeholder file when running Go tests without first building the frontend.
- Migration numbering: VTX stream uses 000200+. Add a `down.sql` even if it just drops the column.
---

## Fri May 15 ~10:02 CST 2026 - VTX-010
- Implemented Vertex Link Type Classes per BDD acceptance.
- Backend
  - pkg/oms/models.go: added `LinkType.TypeClasses []string` with `omitempty`, plus constants `VertexLinkTypeClassPrimaryDirection/Undirectional/Bidirectional`, helpers `KnownVertexLinkTypeClasses()` and `IsKnownVertexLinkTypeClass()`. Extended `ToWireJSON` to emit the slice when non-empty.
  - pkg/oms/admin_handlers.go: added `TypeClasses []string` to `CreateLinkTypeRequest`, `*[]string` (tri-state) to `UpdateLinkTypeRequest`; introduced `validateLinkTypeClasses` (returns 400 InvalidParameter:typeClasses) + `normaliseLinkTypeClasses` (dedup); wired both into POST + PUT handlers.
  - pkg/oms/pg_repository.go + pkg/oms/link_edges.go: extended every SELECT / INSERT / UPDATE on link_types to read/write `type_classes` column (TEXT[]).
  - migrations/000202_link_type_type_classes.up.sql / down.sql: ADD COLUMN IF NOT EXISTS type_classes TEXT[] NOT NULL DEFAULT '{}'.
  - pkg/oms/admin_handlers_type_classes_test.go: 8 new tests (Red → Green) covering create/persist, unknown-tag rejection 400, omit-on-wire when empty, tri-state PUT (preserve/clear/replace), ToWireJSON shape.
- Frontend
  - web/src/api/types.ts: added optional `typeClasses?: string[]` to `LinkType`.
  - web/src/api/ontologies.ts: added `typeClasses` to Create/UpdateLinkTypeRequest.
  - web/src/features/vertex/links/edgeArrowStyle.ts + .test.ts: pure helper `pickEdgeArrowStyle(tags)` returning 'primary'|'none'|'both'. Exports `VERTEX_LINK_TYPE_CLASSES` constant for admin UI. 7 Vitest tests cover each tag + unknown-tag + bidirectional-wins-over-primary fallback.
  - web/src/components/admin/LinkTypeAdminPage.tsx: added a `TypeClassCheckboxes` sub-component used by both Create and Edit modals; Create sends the array on submit (or omits when empty), Edit uses a `typeClassesDirty` flag so omitted fields stay untouched (tri-state mirror).
  - web/src/components/admin/__tests__/LinkTypeAdminPage.test.tsx: +2 tests for Create with checkbox → POST body, Edit preserves prior tag → toggle → PUT body.
- Quality
  - `go test ./pkg/oms/... ./pkg/oss/... ./pkg/vertex/...` PASS (1 line cached, rest fresh)
  - `go vet ./...` clean
  - `go build ./...` clean (after temporary `cmd/server/web/dist` placeholder)
  - `npx vitest run` — 1527/1527 tests pass
  - `npx tsc --noEmit` — clean
  - `npx eslint <touched files>` — clean
- **Learnings for future iterations:**
  - Pattern: when adding an optional field to a LinkType-style resource, replicate ALL three repo touchpoints (`pg_repository.go` Create+Get+ListOutgoing+ListIncoming+ListLinkTypes+UpdateLinkType, plus `link_edges.go::GetLinkTypeByAPIName`). Easy to miss the last one.
  - Pattern: for PG arrays use `COALESCE(col, '{}'::text[])` in SELECTs and pass `[]string{}` (never nil) on INSERT — pgx v5 scan into `[]string` works directly.
  - Pattern: tri-state pointer on `Update<Resource>Request` is the project-wide idiom for "preserve / clear / replace" partial updates. New fields should follow it, not bare value.
  - Gotcha: `go build ./...` fails out of the box because `cmd/server/embed.go` references `web/dist`. CI presumably runs `make web-build` first; locally drop a placeholder file under `cmd/server/web/dist/` (gitignored) before running Go-only checks.
  - Gotcha: `make` is not available here directly — use `go build ./...` after the dist placeholder, and `npx vitest run` for frontend.
  - Useful: BDD test naming `Test{Feature}_Given_{State}_When_{Action}_Then_{Outcome}` matches the PRD convention; existing `admin_handlers_propagate_markings_test.go` is the closest template.
---
