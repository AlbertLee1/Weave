# Ralph Progress Log - Stream B
Started: Fri May 15 09:45:35 CST 2026
Stream: B
---

## Codebase Patterns
- 每个前端 story 是一个独立纯逻辑模块：`web/src/features/vertex/<feature>/<name>.ts` + `<name>.test.ts`，不依赖 React，让 vitest 在 happy-dom 之外直接驱动。
- 测试命名沿用 BDD：`describe('VTX-### <feature>')` + `it('given_<State>_when_<Action>_then_<Outcome>')`；时间常量用 `new Date(...).getTime()` 写绝对时间戳，可读且稳定。
- compare windows 已在 VTX-079 (`web/src/features/vertex/timeSeries/compareWindows.ts`) 提供，新 story 直接复用，不要重复实现。
- vitest 配置已支持 `vi.useFakeTimers()`，定时器/防抖类 helper 用假时钟测试。
- 项目根目录 `tsconfig` 有 `erasableSyntaxOnly`，预先存在的两处错误 (`sdk/typescript/src/vertex.ts`、`src/vertex/SdkVertexClient.test.ts`) 与本 stream 无关。
- uPlot 类型用 `import type uPlotCtor from 'uplot'`，options 写 `uPlotCtor.Options`（沿用 `MultiSeriesChart` / `TimeSeriesChart` 现有模式）。注意 `select` 配置如果只写 `{ show: false }` 会 TS2739（必须给 left/top/width/height），干脆省略 `select`，默认就是禁用。
- `src/components/__tests__/admin-removed.test.tsx` 是已知 flaky：在完整 vitest 套件里偶尔超时（5s import App.tsx），单独跑稳定通过。本 stream 不要为它修代码。

---

## 2026-05-15 - VTX-031
- 实现 TopBar Time Selection 状态模型 + 副作用工具的纯逻辑层。
- 新增文件：
  - `web/src/features/vertex/timeSelection/timeSelection.ts` — 状态工厂 `createTimeSelectionState`、reducer 风格 helper（`setRange`、`setSelectedTime`、`toggleLiveMode`），以及两个工厂：`makeDebouncedNotifier`（默认 100ms 防抖）和 `createLivePoller`（按 pollingIntervalSec 周期 tick）。
  - `web/src/features/vertex/timeSelection/timeSelection.test.ts` — 19 个测试覆盖：from/to 校验、selectedTime clamp、liveMode toggle、debounce（含 cancel）、polling start/stop/interval-change。
- 质量检查：
  - vitest：201 files / 1537 tests 全绿。
  - eslint：通过。
  - tsc：本 story 新增文件 0 错误；仓库内残留两处 pre-existing 错误（与本 stream 无关）。
- **Learnings for future iterations:**
  - `compareWindows.ts`（VTX-079）已经实现了多时窗 add/remove/aggregate，VTX-031 的 "Compare button" 接受度只需在 React 接线层调用现有 helper。
  - `pollingIntervalSec` 默认值（10s）来自 `web/src/features/vertex/admin/controlPanel.ts` 的 `defaultControlPanelConfig`，未来 Live mode UI 接线时从该 store 读。
  - `selectedTime` 默认值约定为 `to`（窗口右沿），与 OSS `?asOf=...` 默认行为一致；不要默认成 `(from+to)/2` 否则会和 Live mode 语义冲突。
  - dev-browser skill 不可用 — 浏览器手工验证（slider 拖动、Live toggle 点击）需在 React 组件接线 story 时单独完成；本 story 仅交付逻辑层。
---

## 2026-05-15 - VTX-032
- 实现节点 Sparkline Extended Label 的纯逻辑层（uPlot.Options 工厂 + selectedTime 高亮辅助 + 批量重绘调度器）。
- 新增文件：
  - `web/src/features/vertex/sparkline/sparkline.ts` — 导出：
    - `SPARKLINE_DEFAULT_DIMS` (80×20)、`toAlignedData`（毫秒 → 秒、parallel xs/ys）
    - `findNearestIndex` / `computeSelectedMark`（二分查找；tie 取较早），`selectedTimeRatio`（0..1 用于绝对定位高亮 overlay 而不依赖 uPlot 实例）
    - `buildSparklineOptions({ series, selectedTime, dims?, stroke? })` — 单 y 系列、隐藏 legend/cursor/axes、`x.time=true`、stroke 默认 `#3B82F6`
    - `createSparklineScheduler(raf?, cancelRaf?)` — 把 N 个 redraw 合并到单帧（schedule / flushSync / cancel / stop）；同 id 复用最新回调，达成 50 节点 1 RAF coalesce
  - `web/src/features/vertex/sparkline/sparkline.test.ts` — 31 个测试覆盖：转换、二分边界（空 / exact / 之前 / 之后 / tie 取早 / 两侧距离）、ratio clamp、Options（默认/自定义/隐藏 legend 等/系列样式/x scale time/stroke 注入）、scheduler（合并、覆盖、cancel、flushSync 取消 raf、stop、空 flushSync）。
- 质量检查：
  - vitest（src/features/vertex 子集）：28 files / 237 tests 全绿。
  - sparkline 单测 31/31 绿。
  - tsc：本 story 新增文件 0 错误；仅余 2 处 pre-existing 错误（与本 stream 无关）。
  - eslint：新增目录无 warning。
- **Learnings for future iterations:**
  - 浏览器层接线 sparkline 时：dynamic-import uPlot 后用 `buildSparklineOptions` 构造 opts、`toAlignedData(series)` 喂 data；selectedTime 重渲走 `SparklineScheduler.schedule(nodeId, () => chart.redraw(false))` 把 N 节点合并到 1 帧；高亮当前点可读 `computeSelectedMark`，或用 `selectedTimeRatio * width` 在 DOM overlay 上画一根 vertical line（不依赖 uPlot 实例，更省）。
  - uPlot `select` 配置类型严格，省略即默认不显示；不要写 `{ show: false }` 占位（TS2739）。
  - `≥ 30 fps` 验收无法在 vitest 里测；浏览器接线 story 用 Performance.now()+RAF 计数器验真。本 story 通过 scheduler API 保证理论可达 ≥ 30 fps（1 RAF / 50 节点）。
  - findNearestIndex tie 行为约定取早 index — 配合 step-after style sparkline（值在区间右沿改变）语义最贴合。
---

## 2026-05-15 - VTX-033
- 实现 Series Panel（底部时间线面板）的纯逻辑层：状态 reducer + subplot 布局派生 + 双向游标换算。
- 新增文件：
  - `web/src/features/vertex/seriesPanel/seriesPanel.ts` — 导出：
    - 类型 `SeriesPanelEntry { key, seriesRid, label, property? }` / `SeriesPanelState { expanded, entries }`
    - `createSeriesPanelState(init?)` — 默认 collapsed + 空 entries；init.entries 复制一份避免外部 mutation
    - `openSeries(state, entry)` — 按 `key` 幂等（不重复插入），并强制 `expanded=true`，按插入序堆叠
    - `closeSeries(state, key)` — 删除；entries 清空时自动 collapse；不存在的 key 返回原 state
    - `togglePanel` / `setPanelExpanded`
    - `computeSubplotLayouts(entries, totalHeight, gap?)` — N 个 subplot 等分剩余高度（扣除 (N-1)*gap），高度 clamp ≥ 0；空数组返回 `[]`
    - `timestampFromX(x, width, from, to)` / `xFromTimestamp(t, width, from, to)` — 双向换算，`width<=0` 或 `to<=from` 安全降级（分别返回 from / 0）
  - `web/src/features/vertex/seriesPanel/seriesPanel.test.ts` — 35 个 BDD 测试覆盖：状态工厂（init 复制）、open/close（幂等 / collapse-on-empty / 中间删 / nonexistent）、toggle/setExpanded、布局（0/1/N、gap、挤压、负 total）、换算（边界 / clamp / 零宽度 / 零窗口 / 往返稳定）。
- 质量检查：
  - vitest 子集（src/features/vertex）：29 files / 272 tests 全绿。
  - vitest 全量：203 files / 1603 tests 全绿。
  - tsc（web/）：0 错误。
  - eslint（seriesPanel）：0 warning。
- **Learnings for future iterations:**
  - React 接线层用法：sidebar Series tab 的 "Open in series view" 按钮 → `openSeries(state, { key, seriesRid, label })`；底部面板按 `entries.length` 渲染 N 个 uPlot subplot，高度从 `computeSubplotLayouts(entries, panelHeight, gap)` 取。
  - 游标同步：拖拽 cursor 时 `onPointerMove` 计算容器内 `x`，调 `timestampFromX(x, width, from, to)` 喂回 TopBar 的 `setSelectedTime`；反向（TopBar 滑块动时）用 `xFromTimestamp` 在每个 subplot 上画一根 vertical line。两个方向都不依赖 uPlot 实例。
  - openSeries 选择"按 key 幂等不动 entries 顺序"而不是"bring to top"，避免用户重复点击同一个按钮导致 subplot 闪烁重排；如果后续 UX 要 bring-to-top，加专门的 `bringToFront` API 而不是改 openSeries 语义。
  - closeSeries 删完最后一个自动 collapse 是为了避免空 Panel 占空间；用户主动 togglePanel 不影响 entries，所以"折叠但有 entries"是合法状态（再 open 同 key 会重新展开）。
  - computeSubplotLayouts 把 `Math.max(0, totalHeight - totalGap)` 用于挤压场景：测试里 `totalHeight=10, gap=50, n=2` 不会产生负 height（防止 DOM 报错或 uPlot 异常）。
  - `to <= from` 的退化窗口（live mode 刚开始或时间窗未初始化）在换算 helper 里直接降级返回 from/0，避免除零；React 层不需要再做防御。
---

## 2026-05-15 - VTX-034
- 实现 Timeline（事件 + 时序统一视图）的纯逻辑层：子模式状态 reducer + selectedTime 自动推进 + event bar first-fit 布局。
- 新增文件：
  - `web/src/features/vertex/timeline/timeline.ts` — 导出：
    - `TIMELINE_SPEEDS` = [0.5, 1, 2, 5]，`TimelineSpeed` 类型
    - `TimelineState { enabled, playing, speed, loop }` + reducer 全套：`createTimelineState` / `setTimelineEnabled` / `toggleTimelineEnabled` / `setTimelinePlaying` / `toggleTimelinePlaying` / `setTimelineSpeed`（非法 throw）/ `setTimelineLoop` / `toggleTimelineLoop`；invariants：disable 时 playing 强制 false，禁用状态下 play 操作 no-op
    - `advanceSelectedTime({ current, from, to, speed, loop, realDtMs, baseAdvancePerSecond? })` — 纯函数；delta = realDtMs * speed * base / 1000；loop=true 时 mod wrap = from + (next-from) % span；loop=false 越界 clamp 至 to；退化窗口 (to≤from) 返回 from + reachedEnd
    - `createPlaybackController({ getState, onAdvance, tickIntervalMs?, baseAdvancePerSecond? })` — setInterval 副作用工厂；每 tick 调 getState() → advance → onAdvance(r)；start 幂等；stop 清 interval；isRunning() 反映状态。controller 不持有 speed/loop/selectedTime —— 真理留在 Zustand store；loop=false reachedEnd 后不自动 stop，让调用方决定（onAdvance 看 reachedEnd 自行 dispatch pause）
    - `layoutEventBars(events, { from, to }, pixelWidth, opts?)` — first-fit 多行布局：按 start 排序后贪心查行；越界事件剔除；横跨边界 clamp 到窗口内；瞬时事件（无 end）按 `minBarWidthPx` 兜底；`maxRows` 截断；pixelWidth≤0 / to≤from 返回 []
  - `web/src/features/vertex/timeline/timeline.test.ts` — 45 个 BDD 测试覆盖：状态工厂（init / invariants / 非法 speed）、reducer 全部分支、advance（speed 三档 / loop wrap / 多 span wrap / 退化窗口 / dt=0 / 默认 baseAdvancePerSecond）、controller（未 start no-op / start 后 tick / stop / 幂等 start / loop reach end / 拉取最新 state 反映 speed 改变）、layout（空 / 1 个 / 不重叠 / 重叠多行 / 边界贴边同行 / 瞬时事件 minBarWidth / 越窗剔除 / 左右溢出 clamp / pixelWidth=0 / 退化窗口 / maxRows 截断 / color 透传）。
- 质量检查：
  - vitest（timeline）：45/45 绿。
  - vitest 子集（src/features/vertex）：30 files / 317 tests 全绿。
  - vitest 全量：失败的 14 个测试全部是已知 flaky（admin-removed / Accessibility / ObjectTypeAdminPage / route-aliases / ExportButton / ObjectDiffPanel / CommandPalette / SlidePanel / SettingsPage — 都是 React 渲染测试，5s 超时；单独跑全部稳定通过；和 progress.txt Codebase Patterns #6 记载的同根 issue）。
  - tsc（web/）：本 story 新增文件 0 错误；仓库残留 pre-existing 2 处（与本 stream 无关，详见 Codebase Patterns）。
  - eslint（timeline）：0 warning。
- **Learnings for future iterations:**
  - VTX-082 `eventBars.ts` 已经提供 RawEvent → AnnotatedEvent + filter（颜色 + 分类过滤），VTX-034 复用其类型不要重新发明 RawEvent；layoutEventBars 直接吃 AnnotatedEvent，让上游 React 层先 `annotateEventsWithColor` → `filterEventsByCategory` → `layoutEventBars` 三段管线。
  - React 接线层用法：Play 按钮点击 → `toggleTimelinePlaying` → 触发 `pc.start()`；onAdvance 收到 `{ selectedTime, reachedEnd, looped }`：dispatch `setSelectedTime(r.selectedTime)`，如果 `r.reachedEnd && !state.loop` 顺手 dispatch `setTimelinePlaying(false)` 让 UI 自动暂停（与 VTX-031 `setSelectedTime` clamp 行为天然兼容）。
  - speed 用 union literal `0.5 | 1 | 2 | 5` 而不是 enum/numeric range，配合 erasableSyntaxOnly + verbatimModuleSyntax；`assertSpeed` 在运行时校验防止任意数字穿透（Zustand 持久化反序列化场景）。
  - controller "不持有自有状态" 的设计：调用方持有真理（Zustand store），controller 每 tick 拉 getState() —— 用户改 speed/loop 不需要通知 controller，下一个 tick 自然生效。代价是要传 getState 闭包。这与 VTX-031 `createLivePoller`（持有 intervalSec 内部副本）不同，因为 poller 只关心 tick 节奏，advance 没有"调用方写 selectedTime"的反向数据流。
  - loop=false reachedEnd 不自动 stop —— 一开始我考虑在 controller 内 stop，但这样 onAdvance 之外又多了一个副作用入口；改让调用方在 onAdvance 看 reachedEnd 主动 stop，更符合"controller 是哑泵"的设计。
  - layout first-fit：每行只记 `rowEnds[i]`（最后一个 bar 的 end），新事件 `start >= rowEnds[i]` 即可放入该行；测试 `BackToBackEvents`（mid 同时是 e1 end 和 e2 start）用 `<=` 判定让它们共行。
  - layoutEventBars 把 `end ?? start` 回填进返回结果的 `end` 字段，让 React 层无需再做 nullish 处理；同时保留 raw `start` 供 hover tooltip 显示原始时间戳。
  - 全套 vitest 偶发 timeout 的范围比想象中大：除了已知的 admin-removed.test.tsx，至少还有 Accessibility / ObjectTypeAdminPage / SettingsPage / ObjectDiffPanel 等 10+ 个 React 渲染测试在完整套件并发下会 5s 超时，但单独跑稳。这是套件级 flaky 而非个别测试 bug，本 stream 不为它修代码（同 VTX-031 学习项）。
---

## 2026-05-15 - VTX-035
- 实现 Open in Quiver / Object Explorer 跳转的纯逻辑层：URL builder + 新 tab 副作用 helper。
- 新增文件：
  - `web/src/features/vertex/contextMenu/openLinks.ts` — 导出：
    - `buildOpenInQuiverUrl({ ontology, objectRid, property?, objectType?, primaryKey? })` → `/quiver/{ontology}?objectRid=...&property=...&objectType=...&primaryKey=...`；ontology 用 `encodeURIComponent` 嵌入路径段，其他参数走 `URLSearchParams` 自动编码；空 ontology/objectRid 抛错；可选字段缺省时不写入 query。
    - `buildOpenInExplorerUrl({ ontology, objectRid, objectType? })` → `/explorer/{ontology}?objectRid=...`，带 objectType 时变 `/explorer/{ontology}/{objectType}?objectRid=...`（沿用 App.tsx `/explorer/:ontology/:objectType` 现有嵌套路由）。
    - `openInNewTab(url, win?)` — 默认从 `globalThis.window` 取，缺省或 `win.open` 不是函数时安全 noop；空 url 抛错；调用形式 `win.open(url, '_blank', 'noopener,noreferrer')`，符合 react-router 外跳 + 防 reverse-tabnabbing 最佳实践。
  - `web/src/features/vertex/contextMenu/openLinks.test.ts` — 16 个 BDD 测试覆盖：buildOpenInQuiverUrl（base + 无 property + objectType/primaryKey 透传 + 特殊字符编码 + ontology 路径段编码 + 空校验）、buildOpenInExplorerUrl（base + objectType 嵌套 + 路径段编码 + 空校验）、openInNewTab（fake window.open / undefined window noop / 空 url 抛错 / win 无 open 方法 noop）。
- 质量检查：
  - vitest（contextMenu）：16/16 绿。
  - vitest 子集（src/features/vertex）：31 files / 333 tests 全绿（+1 file / +16 tests）。
  - tsc（web/）：0 错误，pre-existing 错误均已被其他 commit 修复。
  - eslint（contextMenu）：0 warning。
- **Learnings for future iterations:**
  - BDD spec 给的 URL 路径形式（`/quiver/{objectRid}/{property}`）与现有 React Router 路由（`/quiver/:ontology/:rid`，rid 是 dashboard rid）冲突，不能直译。采用 query param 形式 `/quiver/{ontology}?objectRid=...&property=...&objectType=...&primaryKey=...`，让 QuiverPage 接 `useSearchParams` 在 mount 时 prefill 一个 SeriesSpec，等价语义但保留现有路由不变。这种 spec-vs-codebase 冲突优先选 query param 而不是加新路由 — 改 routing 是更大 blast radius，应由专门 routing story 决定。
  - encodeURIComponent 对路径段、URLSearchParams 对 query：两者编码规则不同（` ` 在路径里编 `%20`、在 query 里编 `+`）。测试断言时如果跨路径/query 比较，分别用对应 API 解码，不要直接字符串比较。
  - `openInNewTab` 写成"win 可注入 + win 缺省/无 open 方法时 noop"而非 throw，原因是 SSR / Node 测试环境 / 内嵌 iframe 沙箱里 window.open 可能不存在，导致整个右键菜单 click handler 抛错；UI 层的"开新 tab 失败"应该是无声降级（最差用户体验是没跳转），不是 React error boundary 炸开。
  - `noopener,noreferrer` 是 window.open 第三参 features 字符串而非选项对象 — 之前在 deeplink/buildVertexUrl 模块里也没设这两个，单纯用 `<a target="_blank">` 隐式继承；这里因为是 imperative `window.open`，必须显式加，否则新 tab 可通过 `window.opener` 反向操作源 tab（reverse tabnabbing）。
  - dev-browser skill 不可用 — 右键菜单 React 接线 + 实际 new-tab 行为验证留到组件接线 story；本 story 仅交付纯逻辑 + 副作用 helper。
---

## 2026-05-15 - VTX-036
- 实现 Scenario Pane（侧栏纵向表格 UI）的纯逻辑层：reducer + 工具栏/表头/分组派生 helper。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/scenarioPane.ts` — 导出：
    - 类型 `CaseStudyRef` / `ScenarioRef` / `ScenarioPaneActionRow` / `ScenarioPaneModelRow` / `ScenarioPaneInputOutputColumn` / `ScenarioPaneState`
    - reducer：`createScenarioPaneState(init?)`（深拷贝 init 集合防外部 mutation）/ `togglePane` / `setPaneExpanded` / `setCaseStudy`（rid 变更/置空 → 重置 scenarios/rows/inputOutputColumns；同 rid 仅刷新对象不动依赖状态）/ `addScenario`（无 caseStudy 抛错，按 rid 幂等，按插入序追加列）/ `removeScenario` / `addActionRow` / `addModelRow`（按 rid 幂等，不重复插入）/ `removeRow` / `addInputOutputColumn`（按 key 幂等）/ `removeInputOutputColumn`
    - 派生：`getToolbarButtons`（空 pane=['addCaseStudy']，有 caseStudy=['addScenario','addAction','addInputOrOutput','run']）/ `getColumnHeaders`（['Baseline', ...scenarios.name]，无 caseStudy 返回 []）/ `getRowGroups`（按 kind 拆 models/actions 两个数组，分别保留插入序）/ `isEmpty`
  - `web/src/features/vertex/scenarioPane/scenarioPane.test.ts` — 34 个 BDD 测试覆盖：工厂（默认值 / init 深拷贝防 mutation）、toggle/setPaneExpanded、setCaseStudy（首次/切换/置空/同 rid 保留依赖状态）、addScenario（无 caseStudy 抛错 / 顺序追加 / 幂等）/ removeScenario（顺序 / unknown 返回原 state）、rows（addModelRow / addActionRow / 幂等 / mixed remove / unknown 返回原 state）、inputOutputColumns（add/remove/幂等/unknown）、getToolbarButtons（空/有 caseStudy/有 2 scenarios 仍然全 toolbar）、getColumnHeaders（无 cs/仅 baseline/baseline + N/immutable scenario label 不变）、getRowGroups（空/mixed 保留插入序）、isEmpty。
- 质量检查：
  - vitest（scenarioPane）：34/34 绿。
  - vitest 子集（src/features/vertex）：32 files / 367 tests 全绿（+1 file / +34 tests）。
  - tsc（web/）：0 错误。
  - eslint（scenarioPane）：0 warning。
- **Learnings for future iterations:**
  - React 接线层用法：Scenarios 按钮点击 → `togglePane`；空 caseStudy 渲染只 `+ Add Case Study` 大按钮（`getToolbarButtons` 返回 `['addCaseStudy']`）；TanStack Table headerGroup 直接取 `getColumnHeaders(state)` 当 `<th>`（多语言 i18n 时把 Baseline 字面值挪到 React 层映射，本模块保持英文常量稳定）；行渲染按 `getRowGroups(state)` 分成 Models / Actions 两个 section（垂直表的"行 = 单个 Model/Action"），每行下面 Input/Output 列各自再开一行（这是调研报告 §6.4 的 "类 Excel 纵向网格"）。
  - 单 scenario "+ Add input or output" 加列时调 `addInputOutputColumn({ key: paramId, label })`，key 用 `${actionRid}::${paramName}` 这种 composite 避免不同 action/model 同名参数冲撞（在测试里仅用 'capacity'/'demand' 简化，React 层负责构造 composite key）。
  - setCaseStudy 同 rid no-op-on-dependent-state 的设计：避免用户重命名 Case Study 时把已加载的 Scenarios/Rows 清空。但传入的对象引用是新的（caseStudy 字段更新），React 层用 `useMemo` 依赖 `state.caseStudy.rid` 而不是 `state.caseStudy` 引用，否则会触发不必要重渲。
  - "Run" 按钮的精细禁用规则（需要 ≥1 scenario，且任何 scenario.immutable=false 等）不在 `getToolbarButtons` 里判断 — 该 helper 只回答"是否暴露按钮"，"按钮是否可点"留给 React 层组合 `state.scenarios.length` / `scenarios.some(s => !s.immutable)`。避免 helper 膨胀为业务规则枢纽。
  - Row 类型用 discriminated union（`kind: 'model'|'action'`）而不是两个独立数组，`state.rows` 单列存储是为了未来插入顺序（"先加 M1，再加 A1，再加 M2" 用户可能希望按时间顺序看到混排），`getRowGroups` 才在派生时分组。如果 spec 之后要求"Models 永远在 Actions 上面"，改 `getRowGroups` 不动 state 即可。
  - immutable scenario 的视觉差异（灰色列头 / 锁图标）由 React 层根据 `scenario.immutable` 自行装饰，本模块的 `getColumnHeaders` 只回 name 字面值；VTX-037 的"Scenario is immutable. Duplicate to modify."提示走同样路径。
  - dev-browser skill 不可用 — TanStack Table 接线 + + Add Case Study 对话框浏览器验证留到 React 组件接线 story。
---

## 2026-05-15 - VTX-037
- 实现 Create Case Study / Create Scenario 对话框的纯逻辑层：双对话框 reducer + 名称校验 + POST 请求构造器 + immutable Scenario 编辑守卫。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/createDialogs.ts` — 导出：
    - 类型 `CreateCaseStudyDialogState { open, name, submitting, error }` / `CreateScenarioDialogState { open, caseStudyRid, name, submitting, error }` / `ApiRequest { method, path, body }` / `DialogNameValidation` / `ScenarioLike`
    - 工厂 + reducer：`createCaseStudyDialogState` / `openCreateCaseStudyDialog`（无参；总是返回 blank open）/ `closeCreateCaseStudyDialog`（无参）/ `setCreateCaseStudyName` / `setCreateCaseStudySubmitting`（true 顺手清 error）/ `setCreateCaseStudyError`（顺手关 submitting）。Scenario 版同理，`openCreateScenarioDialog(caseStudyRid)` 强制 rid 非空。
    - `validateDialogName(name)` — trim 后 1..128，blank → `{valid:false, reason:'required'}`，超长 → `{valid:false, reason:'too_long'}`。`DIALOG_NAME_MAX_LENGTH=128` 导出常量。
    - `buildCreateCaseStudyRequest({ name, ontologyRid })` → `POST /api/vertex/v1/case-studies` body `{ name, ontologyRid }`。`buildCreateScenarioRequest({ caseStudyRid, name, parentOntologyCommit })` → `POST /api/vertex/v1/case-studies/${encodeURIComponent(rid)}/scenarios` body `{ name, parentOntologyCommit }`。两者都 trim name 并 require 全字段非空，否则抛 `Error: <field> is required`。
    - `IMMUTABLE_SCENARIO_MESSAGE = 'Scenario is immutable. Duplicate to modify.'` + `isScenarioImmutable(scenario)` + `assertScenarioMutable(scenario)`（throw `ScenarioImmutableError` 子类，error.scenarioRid 暴露便于 React 层定位行）。
  - `web/src/features/vertex/scenarioPane/createDialogs.test.ts` — 38 个 BDD 测试覆盖：CaseStudy dialog（factory / open / close / setName / setSubmitting clears error / setSubmitting false preserves error / setError clears submitting / setError null clears）、Scenario dialog（factory / open with rid / empty rid throws / 重新 open 清空残留 / close / setName 保留 caseStudyRid / setSubmitting / setError）、validateDialogName（blank / whitespace / normal / trimmed / too long / exact max）、buildCreateCaseStudyRequest（base / trim name / blank name throws / blank ontologyRid throws）、buildCreateScenarioRequest（base / 特殊字符 encodeURIComponent / trim name / blank rid/name/parentOntologyCommit throws）、immutable guard（false / undefined / true / mutable-no-throw / immutable-throws-ScenarioImmutableError 携带 scenarioRid + IMMUTABLE_SCENARIO_MESSAGE 字面值核对）。
- 质量检查：
  - vitest（createDialogs）：38/38 绿。
  - vitest 子集（src/features/vertex）：33 files / 405 tests 全绿（+1 file / +38 tests）。
  - tsc（web/）：exit 0，全绿（pre-existing 错误均已被其他 commit 清掉，仓库目前 0 错误）。
  - eslint（createDialogs.ts + createDialogs.test.ts）：0 warning。
- **Learnings for future iterations:**
  - 对话框 reducer 选择"open/close 不读 prior state"的设计：之前我考虑过传入 prior state 让 reducer 决定，但 ESLint flat config 不识别 `_state` 这种 leading-underscore 约定（项目 `eslint.config.js` 没配 `argsIgnorePattern`），unused-vars 会硬性报错；同时打开对话框语义上确实不依赖 prior state（永远 reset），所以索性去掉参数。Scenario 版的 `caseStudyRid` 因为是必需信息保留入参。这与 sibling `scenarioPane.ts` 的 `togglePane(state)` 风格略不同 —— 那些 reducer 真的读 prior state（toggle / set 都依赖当前值），不是参数约定的差异。
  - BDD spec 给的 endpoint `POST /api/vertex/v1/case-studies/{rid}/scenarios` 与 `sdk/typescript/src/vertex.ts` 现有的 `POST /api/vertex/v1/scenarios`（caseStudyRid 在 body）有歧义。VTX-037 选了 spec 的嵌套路径形式，理由：spec 是 source of truth，且嵌套路径更 RESTful，React 路由层也可以用 `/case-studies/:rid/scenarios/new` deeplink 反向定位。后端实现 story（dependencies 字段标的 `POST /case-studies + /scenarios endpoints`）需要按这个路径起 chi route。SDK 是否同步改路径留给 SDK story 决定。
  - `requireNonBlank(value, fieldName)` helper 在本模块多次复用（5 处），但又不值得跨模块提取 —— 跟 `openLinks.ts` 的 `requireNonEmpty` 接近但语义不同（前者 trim 后非空，后者 length>0 即可），名称冲突的代价比微小重复高。等第三个模块出现再考虑共享。
  - `ScenarioImmutableError` 自定义 Error 子类把 `scenarioRid` 挂在 error object 上，让 React 错误边界 / toast 可以同时显示文字 + 跳到对应行 —— 单字符串 message 不够。`error.name = 'ScenarioImmutableError'` 给跨 frame / 跨 worker 序列化时的类型判别留个 fallback（instanceof 在 worker 边界会失效）。
  - `ApiRequest.body` 故意写成 `Record<string, string>` 而非 `unknown` —— 让调用方 `fetch(req.path, { body: JSON.stringify(req.body) })` 时不需要 narrow；同时保证 builder 输出不出现 `null`/`undefined`/嵌套对象（因为 requireNonBlank 已经把空都过滤了）。如果未来要加 `tags` / `metadata` 等可选字段，再放宽为 `Record<string, string | string[]>` 或抽 generic。
  - `Window.open() not implemented` 是 vitest happy-dom 在跑 contextMenu 测试时打印的环境警告，不算 fail；本 story 没接触 openLinks，但跑 vertex 子集时仍可见。是 happy-dom 限制，已知。
  - dev-browser skill 不可用 — Modal 组件接线、name 输入校验提示 toast、immutable scenario tooltip 浏览器验证全部留到 React 组件接线 story（与 VTX-036 同步进行更经济）。
---

## 2026-05-15 - VTX-038
- 实现 + Add Action（Function-backed Action 选择）的纯逻辑层：对话框 reducer + 已发布/function-backed 判别 + 必填参数校验 + 50-actions-per-scenario 容量守卫 + dialog→Row 转换器。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/addAction.ts` — 导出：
    - 常量 `MAX_ACTIONS_PER_SCENARIO=50` / `MAX_ACTIONS_MESSAGE='Max 50 actions per scenario'`
    - 类型 `AddActionParameterSpec` / `AddActionActionType { rid, apiName, displayName, status, kind?, functionRid?, parameters? }` / `AddActionDialogState` / `AddActionValidation` / `BuildAddActionRowInput`
    - reducer：`createAddActionDialogState` / `openAddActionDialog(scenarioRid)`（非空校验）/ `closeAddActionDialog` / `setAddActionLoading` / `setAddActionList`（保留仍存在的 selection + paramValues，否则清；顺手清 loading）/ `setAddActionSelected`（切换清 paramValues；无变化返回同引用）/ `setAddActionParam` / `setAddActionSubmitting`（true 清 error）/ `setAddActionError`（清 submitting）
    - 派生：`isActionPublished`（ACTIVE/ENDORSED）/ `isFunctionBackedAction`（kind==='function_backed'）/ `filterPublishedActions` / `getSelectedAction`（unknown rid → null）/ `getRequiredParamNames` / `validateAddAction`（no_selection / unknown_selection / missing_required_param + param 名 / valid）
    - 容量：`isScenarioAtActionCapacity` / `assertScenarioActionCapacity` 抛 `ScenarioActionCapacityError`（携带 limit=50 + name='ScenarioActionCapacityError'）
    - 转换：`buildAddActionRow({state, rid})` → `ScenarioPaneActionRow`，rid 必填非空，无选中或选中不存在抛错
  - `web/src/features/vertex/scenarioPane/addAction.test.ts` — 48 个 BDD 测试覆盖：工厂、open/close/loading、setAddActionList（保留/丢失 selection 两条路径）、setAddActionSelected（首选/切换清参/同值返回同引用/null）、setAddActionParam（新增/覆盖）、submitting/error（互斥语义、null 清）、isActionPublished（ACTIVE/ENDORSED/EXPERIMENTAL/DEPRECATED）、isFunctionBackedAction（true/undefined/standard）、filterPublishedActions（混合保留 ACTIVE+ENDORSED 含 function-backed）、getSelectedAction（null/unknown/正常）、getRequiredParamNames（缺 parameters / 混合 / 仅可选）、validateAddAction 全 4 路径（含 whitespace blank）、容量（49 false / 50 true / under 不抛 / 50 抛带 message+limit+name / >50 抛）、buildAddActionRow（正常返回 row / 无选中抛 / unknown 选中抛 / 空 rid 抛）。
- 质量检查：
  - vitest（addAction）：48/48 绿。
  - vitest 子集（src/features/vertex）：34 files / 453 tests 全绿（+1 file / +48 tests）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（addAction.ts + addAction.test.ts）：0 warning。
- **Learnings for future iterations:**
  - PRD 中 VTX-038 spec 提到 "已发布的 Function-backed Action + 普通 Action"，但 web/src/api/types.ts.ActionType 当前没有 kind 字段（VTX-051 才添加 function_rid 列）。本 story 解耦做法：定义本地 `AddActionActionType` 投影，含 `kind?: 'standard' | 'function_backed'` + `functionRid?: string` 两个可选字段，让上游 (React 接线层) 把 OMS 返回的 ActionType 拍平到这个投影。等 VTX-051 落地、web/src/api/types.ts.ActionType 添完 kind/functionRid 后，本模块仍可继续用投影（避免强绑 ActionParameterV2 的 DataType 复杂结构 —— 测试用例就不需要构造完整 DataType payload）。
  - "已发布" = `status ∈ {ACTIVE, ENDORSED}`，与 pkg/oms ActionType.status 习惯对齐。EXPERIMENTAL / DEPRECATED / DRAFT 全部排除。React 接线层调用方式：`filterPublishedActions(omsActionsList).map(toAddActionType)`。
  - `setAddActionList` 的"保留 selection 仅当它仍在新列表"：典型路径是用户先勾了一个 action、然后改了 ontology filter（导致候选列表变化）。如果旧 selection 还在 → 用户体验上"无感"；如果不在 → 必须清掉 selectedActionRid 否则 `getSelectedAction` 永远返回 null，validateAddAction 永远报 unknown_selection，整个 form 卡死。这是从 createDialogs.ts 没遇到的场景（那里 openDialog 永远全 reset，没有"列表换、保留部分状态"的需求）。
  - `setAddActionSelected` 切换时清 paramValues 是必须的：不同 action 参数集不同，留旧 paramValues 会让"required check"出现 phantom 字段值（e.g. 选 A 填了 origin=JFK，切到 B 还有 origin=JFK，但 B 根本没 origin 参数）。这意味着 React form 在 selection 变化时只要重渲就够，不用手动清 input —— state 已经清了。
  - `setAddActionSelected` 同值返回同引用（`if (state.selectedActionRid === rid) return state;`）的设计动机：避免 React 在用户重复点击同一个 radio 时触发不必要的 useMemo 重算 / Effect 触发。同 createDialogs 的"open 永远 reset"不同 —— 那里 open 本来就是低频且需要 reset 语义。
  - `ScenarioActionCapacityError.limit` 字段：让 React toast 可以 i18n 化（`tr('actions.capacityHit', { limit: err.limit })`）而不是硬编码 50。同 ScenarioImmutableError 的 `scenarioRid` 一样，给上层定位 + 国际化留出口。
  - `buildAddActionRow` 让调用方注入 rid 而不是内部生成：纯逻辑模块不引入 `crypto.randomUUID()` 因为 happy-dom / vitest 旧版本可能缺。让 React 接线层负责生成 rid（推荐 `crypto.randomUUID()` 或后端 POST 响应 rid），本模块只做翻译。
  - capacity 守卫两种 API：`isScenarioAtActionCapacity` 给"按钮是否 disabled"用，`assertScenarioActionCapacity` 给"用户硬撞上限时抛"用。React 层期望流程：button 用 isAtCapacity 禁用；如果绕过 (e.g. 键盘 enter / 直接调 mutation) → fetch 前 assertScenarioActionCapacity 抛 error → catch 后 setError(MAX_ACTIONS_MESSAGE)。
  - validateAddAction 的"required param 缺失"用第一个缺失的字段名做 reason（不是数组）—— UX 上一次只提示一条更清晰；React 层用 `param` 字段定位错误 input + 高亮。如果未来要求"一次性显示所有错误"，加新 reason 'multiple_missing_params' 而非改现有形状。
  - dev-browser skill 不可用 — Modal + Action 列表渲染 + 50-cap toast 弹窗的浏览器验证留到 React 组件接线 story；本 story 仅交付纯逻辑层。
---

## 2026-05-15 - VTX-039
- 实现 + Add input or output（参数勾选）的纯逻辑层：对话框 reducer + 参数级 selection map（checked + source）+ Function-backed Action autoBound 自动关联 + Add all parameters 一键勾选 + validation + Pane 列构造器。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/addInputOutput.ts` — 导出：
    - 类型 `ParameterSourceKind` = 'time_series'|'object_property'|'measure'（与调研报告 §3.2 三类候选源对齐）/ `ParameterSource { kind, rid, label }` / `AddInputOutputParameter { name, displayName?, direction, required?, candidateSources[], autoBound? }` / `AddInputOutputRowRef { rowRid, kind, label, parameters[] }` / `ParamSelection { checked, source }` / `AddInputOutputDialogState` / `AddInputOutputValidation`
    - reducer：`createAddInputOutputDialogState` / `openAddInputOutputDialog(scenarioRid, row)`（强制非空 scenarioRid；用 row.parameters 全量重建 selections，每个参数 source=autoBound??null —— Function-backed 自动关联，标准 Action 留 null 等手填）/ `closeAddInputOutputDialog` / `toggleAddInputOutputParam`（未 open / 未知参数返回同引用）/ `setAddInputOutputParamSource`（未 open / 未知参数返回同引用；接受 null 清除）/ `setAddInputOutputAllChecked`（未 open 返回同引用；不动 source 保留 autoBound + 已选源）/ `setAddInputOutputSubmitting` / `setAddInputOutputError`
    - 派生：`getCheckedParamNames`（按 row.parameters 顺序）/ `isAllChecked`（空 row 返回 false）/ `isAllUnchecked`（未 open 返回 true）/ `validateAddInputOutput`（not_open / no_params_checked / unbound_param + 首个缺源参数名 / valid）
    - column：`buildInputOutputColumnKey(rowRid, paramName)` = `${rowRid}::${paramName}`（避免不同 Action/Model 同名参数冲撞）/ `buildInputOutputColumns(state)`（先 validate；按 row.parameters 顺序输出 ScenarioPaneInputOutputColumn[]；label 用 displayName ?? name）
  - `web/src/features/vertex/scenarioPane/addInputOutput.test.ts` — 49 个 BDD 测试覆盖：工厂、open（标准 / function-backed autoBound 预填 / 空 rid throws）、close、submitting/error 互斥语义、toggleParam（flip / autoBound 保留 source / unknown / not open）、setSource（设值 / 兄弟参数不动 / unknown / not open / null 清除）、setAllChecked（全勾 / function-backed 保留 autoBound source / 全清 / source 保留 / not open）、derived（getCheckedParamNames 按 param 顺序 / 各 isAll* 边界）、validateAddInputOutput 全四态 + 多 checked 仅首个 unbound 报、buildColumnKey 组合键、buildColumns（invalid throws / 3 checked 顺序 / displayName / subset / function-backed 一键 Apply）、it.each 三种 source kind 圆形测试。
- 质量检查：
  - vitest（addInputOutput）：49/49 绿。
  - vitest 子集（src/features/vertex）：35 files / 502 tests 全绿（+1 file / +49 tests）。
  - vitest 全量：209 files / 1833 tests 全绿，0 失败（套件级 flaky 这次未触发）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（addInputOutput.ts + addInputOutput.test.ts）：0 warning。
- **Learnings for future iterations:**
  - 参数级 selection 用 `Record<string, ParamSelection>` 而不是双数组（checkedNames[] + sources Record）：把 checked 和 source 绑在同一 cell 里，让 toggle/setSource 互不污染（撤销勾选保留 source，再勾不必重选源 —— 与典型 UX 期望一致）。React 层渲染 list 时遍历 row.parameters，按 name 反查 selections[name]，O(1) 无重复扫描。
  - `openAddInputOutputDialog` 每次都全量重建 selections —— 不试图保留前一次 dialog 的部分状态。这与 addAction.setAddActionList 的"保留 selection 仅当它仍在新列表"是相反的设计选择：addAction 的 actions 列表是异步 fetch 来的（用户不主动切换），所以保留有用；addInputOutput 的 row 是用户主动选定的（不同 Action/Model），保留旧 selection 几乎一定造成幽灵字段。原则：异步喂数据 → 试着保留；用户主动切目标 → 全量重建。
  - autoBound 在 open 时预填 source 但 *不预勾 checked* —— 与"Function-backed Action 自动关联到对象属性 / 时序点"语义一致：自动关联（auto-link）不等于自动添加列（auto-apply）。用户还需要主动 Add（勾选或点 Add all parameters）才把列加进 Pane。如果 spec 改成"function-backed 默认勾选"，把 open 时的 checked 改成 `param.autoBound !== undefined` 即可。
  - column key 用 `${rowRid}::${paramName}` 组合：与 VTX-036 progress.txt "React 接线层组合 key" 学习项一致 —— 不同 Action/Model 同名参数（多个 model 都叫 demand）共享 Pane 列空间时必然冲撞。本模块统一吐组合键，React 层用 `column.key.split('::')` 反向定位 rowRid + paramName 拿 source binding。
  - validate 的四态枚举（not_open / no_params_checked / unbound_param + param / valid）让 React Modal 同时驱动 "Apply 按钮 disabled 原因 tooltip"：not_open 不会出现在 UI 路径上（dialog 关着没机会点 Apply）；no_params_checked 提示"勾一个参数"；unbound_param 高亮该行的 source 选择器。一次性返回首个错误的设计延续了 addAction.validateAddAction —— UX 上一次提示一条比一堆 toast 友好。
  - setAllChecked 不动 source 的语义：用户先勾 2 个（手填了 source），切到 Add all 把所有勾上，再 uncheck 回去时不该清掉用户已选的 source。这个语义在测试 "given_setAllChecked_false_when_param_has_source_then_source_preserved" 显式断言。
  - `getCheckedParamNames` 按 row.parameters 顺序（不是 selections object 的插入顺序）：JavaScript object 在过去保证插入序，但跨 reducer 复制 spread 可能让 key 顺序漂；统一以 row.parameters 为真理避免列乱序。同理 `buildInputOutputColumns` 也按 row.parameters 过滤。
  - `ParameterSourceKind = 'time_series' | 'object_property' | 'measure'` 三类与调研报告 §3.2 候选源完全对齐 + 与 VTX-058/059/060/061 Extended Label 的 `kind:property|timeSeries|measure` 命名一致（仅 snake_case vs camelCase 差异）—— React 接线层可以共用 source kind → icon / 渲染器 mapping。如果未来加 'derived_property' kind，在本模块 union 加一项即可，不破坏 buildColumns（label 取 displayName ?? name，不读 kind）。
  - 测试用 `it.each<ParameterSource>([...])` 三种 source kind 圆形测试，把"模块支持 §3.2 全三类候选源"做成单一 spec 而不是三段重复 —— vitest each 模板字符串 `$kind` 直接展开 fixture 字段，可读性好。
  - dev-browser skill 不可用 — Modal + checkbox 列表渲染 + Add all parameters 按钮 + Apply 浏览器验证留到 React 组件接线 story；本 story 仅交付纯逻辑层。
---

## 2026-05-15 - VTX-040
- 实现 Override 单元格（标量）的纯逻辑层：OverrideMap CRUD + scalar 输入解析 + 单元格 blur 决策中枢 + frozen scenario 守卫。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/overrideCell.ts` — 导出：
    - 类型 `ScalarOverrideValue` = string|number|boolean / `ScalarValueType` = 'string'|'number'|'boolean' / `ScalarOverride { id, scenarioRid, rowRid, paramName, value }` / `OverrideMap` = Record<key, ScalarOverride> / `OverrideApiRequest { method:'POST'|'DELETE', path, body }` / `ParseScalarResult` 三态 union / `CellEditDecision` 五态 union（noop/create/update/delete/invalid）/ `ResolveCellEditInput`
    - `FROZEN_SCENARIO_TOOLTIP = 'Scenario is frozen.'` + `ScenarioFrozenError` 子类（携带 scenarioRid + name='ScenarioFrozenError'）
    - cell key + map：`buildOverrideCellKey(scenarioRid, rowRid, paramName)` = `${scenarioRid}::${rowRid}::${paramName}`（三段而非 VTX-039 的两段，因为 Pane 同时显示多个 Scenario 列，scenarioRid 必须进 key）/ `getOverride` / `setOverride`（immutable update，原 map 不动）/ `removeOverride`（unknown key 返回同引用）/ `isCellHighlighted`（has key 即"已有 override → 黄色边框"）
    - frozen 守卫：`isCellDisabled(scenario)` / `getCellTooltip(scenario)` 复用 createDialogs.isScenarioImmutable / `assertCellEditable(scenario)` 在 fetch 前抛 ScenarioFrozenError（绕过 React disabled 的兜底）
    - input 解析：`parseScalarInput(raw, valueType)` — trim 后空 → empty=true；string 透传 trim 值；number 用 Number()+Number.isFinite 排除 NaN/Infinity；boolean 仅接受 'true'/'false' 大小写不敏感字面值，拒 0/1 保持无歧义
    - 请求构造：`buildCreateOverrideRequest({scenarioRid,rowRid,paramName,value})` → POST /api/vertex/v1/scenarios/{rid}/overrides body=`{rowRid,paramName,value}`（rid 走 encodeURIComponent，全字段 require non-blank） / `buildDeleteOverrideRequest(id)` → DELETE /api/vertex/v1/overrides/{id} body=null
    - 决策中枢 `resolveCellEdit({scenarioRid,rowRid,paramName,rawInput,valueType,existing})` — invalid 不静默丢 existing；empty+existing=delete+previousId / empty+null=noop / value+null=create / value+existing 相同=noop（避免冗余写） / value+existing 不同=update+previousId（create+update 都走 POST，区分仅给上层 UX 文案）
  - `web/src/features/vertex/scenarioPane/overrideCell.test.ts` — 55 个 BDD 测试覆盖：cellKey 唯一性、map CRUD（setOverride/removeOverride 不 mutate 原 map / replace / unknown key 同引用）、isCellHighlighted 双态、frozen guards 五法（mutable/undefined immutable/immutable → isCellDisabled+getCellTooltip+assertCellEditable，含 ScenarioFrozenError instance/scenarioRid/message/name 全字段断言+常量 toLowerCase 含 'scenario is frozen' 防文案漂移）、parseScalarInput（空/whitespace/string trim/number 各种/Infinity+NaN 拒/boolean 字面值/case-insensitive/'1' 拒）、build 请求（base/特殊字符 encodeURIComponent/blank 抛错三处/string+boolean+number 三种 value 类型透传）、resolveCellEdit 全五种 outcome（含 invalid 与 existing 共存场景断言"不静默丢 existing"）、end-to-end 三个 happy path（click→input→blur→POST→高亮 / 清空→DELETE→去高亮 / immutable→disabled+tooltip+ScenarioFrozenError 三连）。
- 质量检查：
  - vitest（overrideCell）：55/55 绿。
  - vitest 子集（src/features/vertex）：36 files / 557 tests 全绿（+1 file / +55 tests）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（overrideCell.ts + overrideCell.test.ts）：0 warning。
- **Learnings for future iterations:**
  - cell key 用 *三段* `${scenarioRid}::${rowRid}::${paramName}` 而非 VTX-039 的两段（`${rowRid}::${paramName}`）—— Pane 同时显示多个 Scenario 列，每列的同一 (row, param) cell 是独立 override 槽位；如果省略 scenarioRid，跨列的 setOverride 会互相覆盖。React 接线层在渲染表格时按 (scenario.rid, row.rid, param.name) 三元组组 key 反查。
  - update vs create 都走 POST 而不是 PUT —— 后端 endpoint 是 `POST /scenarios/{rid}/overrides` 单一写入路径，服务端按 (scenarioRid, rowRid, paramName) 复合唯一约束做 upsert，前端用同一个 buildCreateOverrideRequest 即可。本模块的 `CellEditDecision` 仍然区分 create / update kind —— 信息保留给上层（UX toast 文案 "Override added" vs "Override updated" + 分析埋点 "scenario.override.created" vs "scenario.override.updated"），代价只是一个 discriminant 字段。如果未来后端拆 PUT，改 buildUpdateOverrideRequest 即可，外部 API 不破。
  - invalid 输入"不静默丢 existing" 是重要不变式：用户在已有 override 的 cell 里手滑输入 "abc"（valueType=number），blur 时模块返回 `{kind:'invalid', reason:'not_a_number'}`，*不* 返回 delete。React 接线层 toast 提示 + 把 cell 值回滚到 existing.value 而不是显示空。如果模块把 invalid 当 empty 处理→静默 delete，用户会丢数据。测试 `given_invalid_input_with_existing_when_resolve_then_invalid_does_not_emit_delete` 显式锁死。
  - parseScalarInput 拒 '1'/'0' 当 boolean 是刻意选择：数据库 / OMS 端常见把 boolean 序列化成 0/1，但用户视角的 UI 输入里 '0' 应该作为 "number 0" 而非 "false"。让 valueType=boolean 时仅接受字面值 'true'/'false'（大小写不敏感），歧义留给上层 React 用 checkbox / Radix Switch 组件天然规避输入字符串。
  - parseScalarInput 拒 Infinity / NaN 文字（"Infinity" / "NaN"）走 Number.isFinite 路径：Number("Infinity") = Infinity，会通过 Number.isNaN 但不会通过 Number.isFinite。Number.isFinite 是正确的过滤器；vitest 测试两个都验证一遍防止有人误改成 Number.isNaN（看似等价实际放过 ±Infinity）。
  - FROZEN_SCENARIO_TOOLTIP="Scenario is frozen." 与 createDialogs.IMMUTABLE_SCENARIO_MESSAGE="Scenario is immutable. Duplicate to modify." 是两条不同 UX 路径：tooltip 出现在 disabled cell hover 上（被动信息），immutable message 出现在用户主动绕过 guard 时的 toast / Modal（主动提示 + 行动指引）。语义不同，文案分开。测试用 `toLowerCase().toContain('scenario is frozen')` 防大小写漂移又不锁死标点。
  - ScenarioFrozenError 与 ScenarioImmutableError 是两个独立 Error 子类。它们 message 不同（FROZEN_SCENARIO_TOOLTIP vs IMMUTABLE_SCENARIO_MESSAGE），name 不同，承担的语义角色也不同（cell 级 vs scenario 级）。共享 `ScenarioLike` interface 但不共享 Error class —— React 错误边界可以按 `error.name` 路由到不同 UI 反应（cell tooltip 不需要弹 Modal）。
  - immutable update（`setOverride` 不 mutate 原 map）与 VTX-036 reducer 风格保持一致：测试 `given_setOverride_when_called_then_does_not_mutate_original_map` 显式断言 —— Zustand store 用 immer middleware 时不破免疫，但本模块同时支持纯对象 store（不用 immer），不 mutate 让两种用法都安全。removeOverride 同理：用 spread + delete 而不是 in-place delete。
  - resolveCellEdit 的"value 未变 → noop"判断用 `===` 严格相等。number/string/boolean 三类 scalar 都安全（不会有 NaN 自比假），与 React props 浅比较哲学一致。如果未来 ScalarOverrideValue 扩展到 Date / 数组，需要在 noop 判断改为深比较 helper，否则会产生冗余 POST。
  - dev-browser skill 不可用 — 黄色边框样式、disabled cell hover tooltip、DELETE 后高亮消失的浏览器验证留到 React 接线 story。本 story 仅交付纯逻辑 + 副作用工厂（请求构造器），React 层 fetch wrapper 直接消费 OverrideApiRequest。
---

## 2026-05-15 - VTX-041
- 实现 Override 单元格（对象属性）的纯逻辑层：把 VTX-040 的 (rowRid, paramName) override 扩展到 (objectType, primaryKey, property) 对象属性 override，加上"override 仅在 fork 内生效 + 客户端 read overlay"两条不变式。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/objectPropertyOverride.ts` — 导出：
    - 类型 `ObjectPropertyOverride { id, scenarioRid, objectType, primaryKey, property, value }` / `ObjectPropertyOverrideMap` = Record<key, ovr> / `ObjectPropertyOverrideApiRequest` / `ObjectPropertyCellEditDecision` 五态 union / `WireObjectLike`（WireObject 最小投影：__rid + __primaryKey + __apiName + property bag）
    - cell key + map：`buildObjectPropertyOverrideKey(scenarioRid, objectType, primaryKey, property)` = `${scenarioRid}::${objectType}::${primaryKey}::${property}`（**4 段**比 VTX-040 的 3 段多一个 objectType —— 对象属性 override 标的不仅是某行的某参数，还要绑定到具体对象 type+pk，否则 Pane 跨 objectType 用同 pk 会混淆）/ `getObjectPropertyOverride` / `setObjectPropertyOverride`（immutable update）/ `removeObjectPropertyOverride`（unknown key 同引用返回）/ `isObjectPropertyCellHighlighted`（has key 即"已 override → 黄色边框"）
    - frozen 守卫：`assertObjectPropertyCellEditable(scenario)` 复用 VTX-040 `ScenarioFrozenError`（同一 catch 路径，UX 共享 tooltip）
    - 请求构造：`buildCreateObjectPropertyOverrideRequest({scenarioRid, objectType, primaryKey, property, value})` → POST `/api/vertex/v1/scenarios/{rid}/object-property-overrides` body=`{objectType, primaryKey, property, value}`，rid 走 encodeURIComponent，4 个 string 字段 require non-blank / `buildDeleteObjectPropertyOverrideRequest(id)` → DELETE `/api/vertex/v1/object-property-overrides/{id}` body=null
    - URL overlay：`withScenarioId(path, scenarioRid?)` —— 把 `?scenarioId=...` 注入 GET URL；scenarioRid null/undefined/blank 时原样返回（base ontology 查询）；已有 query 用 & 拼接，没有用 ? 拼接；scenarioRid 走 encodeURIComponent
    - 客户端 read overlay：`applyObjectPropertyOverrides(obj, overrides)` —— 给一个 base WireObject + 该 scenario 的 overrides 列表，过滤匹配 (ovr.objectType===obj.__apiName && String(obj.__primaryKey)===ovr.primaryKey)，返回应用 override 后的副本；不 mutate base（保留"base ontology 不变"语义 in-memory）；primaryKey 字符串化对齐（WireObject.__primaryKey 是 `string|number`，override.primaryKey 统一 string）
    - 决策中枢：`resolveObjectPropertyCellEdit({scenarioRid, objectType, primaryKey, property, rawInput, valueType, existing})` —— 与 VTX-040 同构的五态决策（invalid / empty+null=noop / empty+existing=delete / value+null=create / value+existing 相同=noop / value+existing 不同=update）。复用 VTX-040 `parseScalarInput`
  - `web/src/features/vertex/scenarioPane/objectPropertyOverride.test.ts` — 58 个 BDD 测试覆盖：cell key 4 段唯一性（同 scenarioRid 不同 objectType 同 pk 也要分开）、CRUD（set/remove 不 mutate 原 map / replace / unknown key 同引用）、isHighlighted 双态、assertCellEditable（mutable / undefined / immutable → ScenarioFrozenError instance + scenarioRid + message 三连）、buildCreate 请求（base / encodeURIComponent / 4 字段 blank 各自抛 / string + boolean value 透传 it.each）、buildDelete（base / encode / blank id 抛）、withScenarioId（null/undefined/blank 原样返回 / 无 query 加 ? / 有 query 加 & / value encode）、applyOverrides（空 overrides 返回 equal-but-不同引用 / 单 override 替换 / base 不 mutate / 不同 objectType 不动 / 不同 pk 不动 / 数字 pk 字符串化匹配 / 多 override 同对象 / 未知 property 加入 / __meta 字段保留 / 混合 overrides 只匹配的生效）、resolveCellEdit 全五态（含 invalid+existing 共存不静默丢、string/boolean valueType 透传）、4 个 end-to-end happy path（capacity 输入 → POST fork only + GET URL 注入 scenarioId + applyOverrides capacity 新值 + immutable assert）。
- 质量检查：
  - vitest（objectPropertyOverride）：58/58 绿。
  - vitest 子集（src/features/vertex）：37 files / 615 tests 全绿（+1 file / +58 tests）。
  - vitest 全量：211 files / 1946 tests 全绿（套件级 flaky 这次未触发）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（objectPropertyOverride.ts + objectPropertyOverride.test.ts）：0 warning。
- **Learnings for future iterations:**
  - cell key 用 *4 段* `${scenarioRid}::${objectType}::${primaryKey}::${property}`：VTX-040 是 3 段（scenarioRid + rowRid + paramName），本 story 多一个 objectType。原因：在 Action/Model row 视角里 rowRid 是 unique，paramName 在该 row 下也 unique；但对象属性视角下，objectType + pk 才是对象身份，property 才是属性名 —— 缺一段都会让 cross-objectType 同 pk 的情况冲撞（比如 Airport/42 和 Gate/42 共用 capacity 属性）。React 接线层在渲染 input/output 列时按 `${scenario.rid}::${ovr.objectType}::${ovr.primaryKey}::${ovr.property}` 反向定位 cell。
  - 同一个 Pane cell 可能同时有 (rowRid, paramName) override（VTX-040）和 (objectType, primaryKey, property) override（本 story） —— 这是两条不同的 override 路径：前者代表"在这次 model run 里把 input 参数手动覆盖成 1500"，后者代表"在这个 scenario fork 里把 Airport/JFK.capacity 永久覆盖成 1500"。后者对其他读 Airport/JFK 的查询（带 scenarioId）也可见；前者只对该次 Action/Model 调用栈可见。Pane UI 上同一个 cell 显示哪种 override 由 VTX-039 的 `ParameterSource.kind` 决定（'object_property' → 本 story 路径；其它 → VTX-040 路径）。
  - `applyObjectPropertyOverrides` 是客户端 read overlay，但**不是**唯一的 overlay 路径。优先做法仍是把 `scenarioId` 注入 GET URL 让服务端按 fork commit overlay 返回（保证一致性 + 索引命中 + 真正的"base ontology 不变" assertion）。客户端 applyObjectPropertyOverrides 用于：(a) 用户在 cell 里改完 → optimistic UI 立刻显示新值，等 POST 返回再以服务端为准；(b) Pane 表渲染时需要同时显示 baseline 列（无 overlay）+ scenarioA 列（applyOverrides 后）+ scenarioB 列（applyOverrides 后），用同一个 base WireObject 加不同 scenario 的 overrides 派生三列；(c) 测试 / Storybook 不需要起 backend。
  - 客户端 overlay 的 primaryKey 匹配走 `String(obj.__primaryKey) === ovr.primaryKey`：WireObject.__primaryKey 是 `string | number`（数字 PK 常见），但 ovr.primaryKey 统一是 string（API 序列化稳定）。如果省略字符串化，数字 PK 永远 ≠ string PK，overlay 永远失效。测试 `given_numeric_primaryKey_matches_string_override_when_apply_then_property_replaced` 显式锁死该不变式。
  - `withScenarioId` 的"已有 query 用 &，没有用 ?" 实现走 `path.includes('?')` 而不是 URL 解析：path 可能是相对路径 + 复合查询字符串（`/api/v2/ontologies/foundry/objects/Airport?pageSize=50&pageToken=abc`），用 new URL() 需要 base 参数还得处理空 base 的边界；includes 简单粗暴够用。如果未来 path 里有 fragment（#section），逻辑要改成"按 # 切割再追加 query"，但当前没有调用方这么用。
  - withScenarioId blank/null/undefined 都视为"无 overlay"原样返回：base ontology 查询和"没选 scenario"的 Explorer 页面共用同一个 URL builder，调用方传 `scenarioRid: state.activeScenario?.rid ?? null` 即可，不必先判 null 再分支调用。
  - WireObjectLike 是 WireObject 的最小投影：保留 3 个 __meta 字段 + property bag。本模块不 import `web/src/api/types.ts.WireObject` 是为了避免循环依赖（scenarioPane 模块树独立 + 让 SDK 也能直接用 applyObjectPropertyOverrides 不引 React 端 types.ts）。如果未来 WireObject 加更多 __meta 字段（如 __ontologyRid），WireObjectLike 沿用 Record 通配兼容。
  - immutable scenario 的 frozen error 复用 VTX-040 `ScenarioFrozenError` 不另起类：error.name 同名（'ScenarioFrozenError'）让 React 错误边界 / toast handler 一处 if(err instanceof ScenarioFrozenError) 同时捕获两类 cell 路径的 frozen 信号，避免上层维护两套 catch。如果未来 UX 需求要把"对象属性 cell frozen"和"参数 cell frozen"显示不同 tooltip，再加 subclass。
  - 模块复用 VTX-040 `parseScalarInput` 不重复实现：scalar 输入解析（trim / Number.isFinite / boolean 字面值）逻辑是 cross-cutting concern，写两份就会漂；本 story 测试只断言"valueType=string|number|boolean 各自能走通 resolve 决策"，详细 edge case（Infinity/NaN/case-insensitive 'TRUE'）在 VTX-040 测试里已锁死，无需重复。
  - 服务端 endpoint 路径选 `/object-property-overrides` 而非复用 `/overrides`：与 VTX-040 的 ParameterOverride 区分开，让后端可以用不同 table（scenario_object_property_overrides vs scenario_parameter_overrides）+ 不同 schema（前者有 objectType/primaryKey/property 列，后者有 rowRid/paramName 列）。一个 endpoint 双 payload 也能做但 schema 不齐整。后端实现 story（VTX-002 ScenarioRepo + VTX-043 Run）需要按这个 endpoint 分拆 chi route。
  - VTX-041 spec 的 BDD `GET /objects/Airport/JFK?scenarioId=s1` 直接对应 `withScenarioId('/api/v2/ontologies/{ont}/objects/Airport/JFK', 's1')`；React 接线层在 ExplorerPage / Browser 拉对象时调 `withScenarioId(basePath, state.activeScenario?.rid ?? null)`，scenarioId 自动 thread 到 query string。Service Worker / MSW mock 路径也走同样 URL，不必 mock 两套。
  - dev-browser skill 不可用 — 黄色边框 + Explorer 反向查询带 scenarioId 拉到 overlay 值 + immutable cell tooltip 浏览器验证留到 React 接线 story；本 story 仅交付纯逻辑层 + 副作用工厂。
---

## 2026-05-15 - VTX-042
- 实现 Override 单元格（时间序列窗口聚合值）的纯逻辑层：把 VTX-040/VTX-041 的 override 概念扩展到 (scenarioRid, objectType, primaryKey, property, atTimestamp, windowMs) 六段身份 + smoothing 配置 + override 仅对配置时窗生效（移动游标自动失效）。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/timeSeriesOverride.ts` — 导出：
    - 类型 `TimeSeriesAggregationMethod`（reuse VTX-065 AggregationMethod）/ `TimeSeriesWindowOverride { id, scenarioRid, objectType, primaryKey, property, atTimestamp, windowMs, agg, smoothingMs?, value }` / `TimeSeriesWindowOverrideMap` = Record<key, ovr> / `TimeSeriesWindowOverrideApiRequest` / `TimeSeriesWindowCellEditDecision` 五态 union / `WindowQuery` / `ResolveTimeSeriesWindowCellEditInput` / `ComputeWindowAggregateParams`
    - 6 段 key + map：`buildTimeSeriesWindowOverrideKey(scenarioRid, objectType, primaryKey, property, atTimestamp, windowMs)` = `${scenarioRid}::${objectType}::${primaryKey}::${property}::${atTimestamp}::${windowMs}`（**6 段**比 VTX-041 的 4 段多 atTimestamp+windowMs —— 时窗 override 身份必须含时窗本身，移动 selectedTime 自然命中不同 key、不同 override）/ `getTimeSeriesWindowOverride` / `setTimeSeriesWindowOverride`（immutable update）/ `removeTimeSeriesWindowOverride`（unknown key 同引用返回）/ `isTimeSeriesWindowCellHighlighted`
    - 派生：`findTimeSeriesWindowOverride(map, WindowQuery)`（不必调用方自组装 key）/ `applyTimeSeriesWindowOverride(map, WindowQuery)`（read overlay：返回 override.value 或 null）
    - frozen 守卫：`assertTimeSeriesWindowCellEditable(scenario)` 复用 VTX-040 ScenarioFrozenError（同一 catch 路径）
    - 请求构造：`buildCreateTimeSeriesWindowOverrideRequest({scenarioRid, objectType, primaryKey, property, atTimestamp, windowMs, agg, smoothingMs?, value})` → POST `/api/vertex/v1/scenarios/{rid}/time-series-overrides` body=`{objectType, primaryKey, property, atTimestamp, windowMs, agg, smoothingMs?, value}`，rid encodeURIComponent，非空 string 三字段 + 正数 windowMs/smoothingMs + 有限 atTimestamp/value 校验 / `buildDeleteTimeSeriesWindowOverrideRequest(id)` → DELETE `/api/vertex/v1/time-series-overrides/{id}`
    - smoothing：`smoothSeriesByBucket(series, bucketMs)` —— 按 bucketMs `Math.floor(t/bucketMs)*bucketMs` 分桶，每桶取 avg；bucketMs ≤ 0 返回原序列副本（不引用同对象）；空序列返回 []；结果按桶起点升序
    - 聚合入口：`computeTimeSeriesWindowAggregate(series, params, override)` —— override 存在 → 直接返回 override.value（即使 smoothingMs 也忽略 raw 数据）；smoothingMs > 0 → 先 smoothSeriesByBucket 再 aggregateAtTime；都没有 → 直接 aggregateAtTime
    - 决策中枢：`resolveTimeSeriesWindowCellEdit({...})` —— 五态决策与 VTX-040 同构，但 valueType 锁定 'number'，invalid reason 只能 'not_a_number'；invalid 不静默丢 existing
  - `web/src/features/vertex/scenarioPane/timeSeriesOverride.test.ts` — 56 个 BDD 测试覆盖：cell key 6 段唯一性（不同 atTimestamp / windowMs / scenarioRid 各自分离）、CRUD（set/remove 不 mutate / replace / unknown key 同引用）、isCellHighlighted、findOverrideForWindow（同窗命中 / 跨 timestamp 不命中 / 跨 window 不命中）、frozen guard 三态、buildCreate（base / smoothingMs 存在与否两路径 / 特殊字符 encodeURIComponent / 4 字段 blank 各自抛 / windowMs 0 + 负数 + NaN + Infinity value 拒）、buildDelete（base / encode / blank id 抛）、smoothing（5min bucket avg / 空 / bucketMs=0 副本 / 负 bucket 副本）、computeWindowAggregate（无 override 无 smoothing 走 raw / smoothing 5min 桶化后聚合 / override 优先 / override 仍优先于 smoothing / 空序列 null）、applyTimeSeriesWindowOverride（命中 / 跨 timestamp null / 跨 window null / 空 map null）、resolveCellEdit 全五态（含 invalid+existing 不静默丢 delete、smoothingMs 透传 request body）、3 个 end-to-end happy path（cursor 移动后 overlay 不再 apply、blur 1500 with smoothing 触发 POST 带 smoothing 元数据、immutable scenario tooltip message 包含 'scenario is frozen'）。
- 质量检查：
  - vitest（timeSeriesOverride）：56/56 绿。
  - vitest 子集（src/features/vertex）：38 files / 671 tests 全绿（+1 file / +56 tests）。
  - vitest 全量：212 files / 2002 tests 全绿（套件级 flaky 这次未触发）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（timeSeriesOverride.ts + timeSeriesOverride.test.ts）：0 warning。
- **Learnings for future iterations:**
  - cell key 用 *6 段* `${scenarioRid}::${objectType}::${primaryKey}::${property}::${atTimestamp}::${windowMs}`：VTX-040 是 3 段（rowRid 维度），VTX-041 是 4 段（对象属性维度），本 story 多两段 atTimestamp+windowMs 把"时窗"绑进 override 身份。React 接线层在 selectedTime 变化时不需要清旧 override —— key 不同自然不命中，旧 override 仍保留在 map 里等用户再回那个时刻还能看到。这与 spec "用户多次修改 selectedTime When 移动游标 Then override 仅对配置时窗生效" 直接对应。
  - agg 故意 **不** 进 key：override 表达"用户对该窗聚合代表值的认定"，与 view-side 当前展示的 agg 解耦。如果用户先用 avg view override 成 1500，然后切到 max view，1500 仍生效（"我说这个窗就该是 1500 不管你怎么聚合"）。若未来需求改成 "agg 切换要 invalidate override"，可加 agg 进 key —— 但目前 BDD spec 没要求，KISS。
  - smoothing 在两条路径生效：(1) 模型 input：computeTimeSeriesWindowAggregate 先桶化再 aggregateAtTime；(2) override 元数据：buildCreate body 含 smoothingMs 让后端记录"该 override 当时基于多大 smooth 窗口的展示值"。两者职责分开：smoothing 改了，未来读 override 仍然取 override.value（用户的认定）不重算；但若用户清掉 override，模型 input 回退到 smoothing 计算路径。
  - smoothSeriesByBucket 用 Map<bucketStart, {sum, count}> 累加而不是 group-by + reduce：对 N 个点是 O(N) 一次扫，比"先 sort 再 group"少一次 sort。前端通常时序点已经按时间升序（API 返回保证），但内部不 assume 让 API 调用方不必先 sort —— buildkey 用 floor 自然分桶，顺序无关。结果再按 t 升序保证下游 aggregateAtTime 行为可预测。
  - smoothSeriesByBucket 返回原序列 *副本*（`series.slice()`）而不是同引用，即使 bucketMs ≤ 0：纯函数原则 + 让调用方安全 mutate 返回值（如插入 selectedTime 处的 phantom 点用于绘制 cursor）不污染原始数据。同 VTX-041 applyObjectPropertyOverrides 的"返回带覆盖值的副本"原则一致。
  - computeTimeSeriesWindowAggregate 设计成"override 优先" 而非"smoothing 优先"：override 是用户的显式认定，应该 trump 任何自动计算。如果反过来 "smoothing 先 override 后"，那么 smoothing 配置变化会无意中"重算"override（用户已经说 1500 了，却被 smoothing 5min 算出 1000 覆盖），违反"override 仅对配置时窗生效"语义。
  - resolveTimeSeriesWindowCellEdit 锁 valueType='number'：时窗聚合 override 必然是 scalar 数值，不像 VTX-040/041 还要兼顾 string/boolean valueType。所以 invalid 路径只有 'not_a_number'，没有 'not_a_boolean'。React 接线层在渲染 Pane cell 时也不用让用户选 valueType —— 时窗 override cell 永远是 number input。
  - 服务端 endpoint 路径选 `/time-series-overrides` 而非复用 `/overrides`：与 VTX-040 parameter overrides + VTX-041 object-property-overrides 区分开。后端要起第三类表 scenario_time_series_overrides，列：scenario_rid, object_type, primary_key, property, at_timestamp BIGINT, window_ms BIGINT, agg TEXT, smoothing_ms BIGINT NULL, value DOUBLE PRECISION，唯一约束 (scenario_rid, object_type, primary_key, property, at_timestamp, window_ms)。三类 override 共用 scenario_runs/scenario_edits 历史链，但表分开让 schema 演化独立。
  - findTimeSeriesWindowOverride 取 WindowQuery 对象而不是 6 个位置参数：参数太多用位置形式容易调用方写错 atTimestamp/windowMs 顺序；对象命名让 IDE 自动补全 + 编译期 typo 检查。同 VTX-041 withScenarioId / applyObjectPropertyOverrides 风格一致（query/op 形式取对象，原子 helper 取位置）。
  - 测试 `given_OverrideAt5MinWindow_when_CursorMovesPast_then_OverlayNoLongerApplies` 是 spec BDD #3 "用户多次修改 selectedTime / override 仅对配置时窗生效" 的直接编码：先 set 一个 (AT, WIN_5MIN) override，apply 在同窗 → 1500；apply 在 AT+60s 窗 → null。这个测试是本 story 设计的核心证明 —— 如果 6 段 key 改成不含 atTimestamp，这个测试立刻红。
  - 后端 VTX-029 现有 stubOverlay 实现是 (scenario, object, property) → 单 scalar 替换所有 bucket（不分时窗），与本 story 的 (atTimestamp, windowMs) 维度 override 不齐。后端实现 story（VTX-002/VTX-043 路径）需要扩展 scenario overlay 让 query 时按时窗过滤匹配的 override；如果只命中"该 scenario 对该 (object, property) 有任意 override"就替换，违反"override 仅对配置时窗生效"。这里的差异要 backend story 注意。
  - 模块复用 VTX-040 `parseScalarInput` + `ScenarioFrozenError` + VTX-065 `aggregateAtTime` / `TimePoint` / `AggregationMethod`：scalar 解析、frozen 守卫、桶聚合都是 cross-cutting concern，本 story 写第三遍就会漂。测试只验 "valueType=number 走通"，详细 edge case（Infinity/NaN/'abc'）在 VTX-040 已经锁死。
  - 测试 `given_InvalidInputWithExisting_when_Resolve_then_InvalidDoesNotEmitDelete` 是关键不变式（与 VTX-040/041 一致）：用户在已有 override 的 cell 里手滑输入"abc"，blur 时应返回 invalid 而不是把 existing 静默删除。React 接线层 toast 错误 + 把 cell 值回滚到 existing.value 而不是显示空。这条 invariant 在三个 override 模块都显式锁死，防有人误改成"empty input 路径"。
  - dev-browser skill 不可用 — 黄色边框 / selectedTime 游标移动 overlay 失效 / immutable cell tooltip 的浏览器验证留到 React 接线 story；本 story 仅交付纯逻辑层 + 副作用工厂（请求构造器 + smoothing 计算 + 决策中枢）。
---

## 2026-05-15 - VTX-043
- 实现 Scenario Run（同步路径，单 Function）的纯逻辑层：把 spec §4.4 "用户点 Run → fork ontology + 跑 function + edits 写入 scenario_edits → 200 OK → 列变绿 ✓ + 显示耗时 + scenario.immutable=true / function 抛错 → 列变红 × + tooltip 错误消息" 翻译成 React 接线层可消费的状态机 + 请求构造 + 响应解析 + UI 决策中枢，全部纯函数 + immutable map 操作。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/scenarioRun.ts` — 导出：
    - 类型：`ScenarioRunStatus`（idle/running/success/error）/ `ScenarioRunState { status, startedAt, durationMs, error }` / `ScenarioRunMap` = Record<scenarioRid, state> / `RunScenarioApiRequest` / `RunScenarioSuccessResponse` / `RunScenarioErrorResponse` / `ParsedRunScenarioSuccess` / `ParsedRunScenarioError` / `ScenarioRunValidation` / `RunButtonState` / `ScenarioRunStatusIcon`
    - 状态机：`createScenarioRunMap` / `createScenarioRunState` / `setScenarioRunState`（spread + 单 key 替换，其他 scenario 引用保持）/ `applyScenarioRunStart(map, rid, startedAt)`（idle/success/error → running，清旧 error/duration）/ `applyScenarioRunSuccess(map, rid, durationMs)`（保留 startedAt 让 React 算端到端用时）/ `applyScenarioRunError(map, rid, message)`（保留 startedAt 同上，clear duration）/ `getScenarioRunStatus`（unknown rid → 'idle' 兜底）/ `isScenarioRunning`
    - 请求构造：`buildRunScenarioRequest({scenarioRid})` → POST `/api/vertex/v1/scenarios/{rid}/run`，rid encodeURIComponent，blank 抛 'scenarioRid is required'；body 是 `{}`（sync run 无额外参数，VTX-044 异步路径才传 forceAsync）
    - preflight 守卫：`hasRunnablePayload({actionCount, overrideCount})`（≥1 即返回 true）/ `validateScenarioRunRequest({scenario, actionCount, overrideCount})` → `{valid:true}` 或 `{valid:false, reason:'frozen'|'empty_payload'}` / `assertScenarioRunnable` 抛 `ScenarioRunNotEditableError`（含 scenarioRid + reason + message prefix 'Scenario cannot be run: '），React 错误边界用 `instanceof` 一处捕获两类拒因
    - Run 按钮决策：`resolveRunButtonState({scenario, runStatus, actionCount, overrideCount})` —— 优先级 running > frozen > empty_payload > enabled。running 优先于 frozen 让 spinner 期间不允许重复点；frozen 优先于 empty_payload 因为"scenario 已 freeze"是更强的 UX 信号
    - UI 派生：`getScenarioRunStatusIcon(status)` → idle='—' / running='spinner' / success='✓' / error='×'（spinner 字面值由 React 层映射成 Loader2 / lucide-react 图标）；`getScenarioRunStatusTooltip(map, rid)` → success='Run completed in 1.9 s'（>=1000ms 一位小数 s）或 'Run completed in 450 ms'（<1000ms ms）或 'Run completed'（durationMs null 兜底）/ error=错误消息 / running='Running…' / idle=null
    - 响应解析：`parseScenarioRunSuccessResponse(response)` → `{durationMs, scenario}`；durationMs 必须 Number.isFinite 且 non-negative，否则抛；scenario.rid 必须 string；scenario.immutable 缺失或 true → true（sync run 200 OK 必然 freeze）；scenario.immutable === false → 保留 false（虽然合约不允许，但显式 false 表示后端故意没冻结，前端不强行覆盖）/ `parseScenarioRunErrorResponse(response)` → `{message}`，blank 或缺失 → 'Scenario run failed' 兜底
    - immutable 转换：`immutableScenario(scenario)` 返回新对象（不 mutate）+ immutable: true；幂等
  - `web/src/features/vertex/scenarioPane/scenarioRun.test.ts` — 52 个 BDD 测试覆盖：状态机 11 个（createMap empty / unknown rid status='idle' / createState 默认值 / applyStart 标 running / applyStart 清旧 error / applySuccess 记录 duration / applyError 记录消息 / 跨 scenario 互不影响 + 引用保持 / setScenarioRunState 只换 target / isScenarioRunning 双态）+ 请求构造 4 个（base / encodeURIComponent / blank / empty）+ preflight 7 个（hasRunnablePayload action/override/neither / valid 全过 / frozen / empty_payload / assertRunnable 抛 ScenarioRunNotEditableError + scenarioRid + reason + message prefix）+ Run 按钮 6 个（enabled / running / frozen / empty / 已 frozen scenario 即便 success status 也不 enabled / error 状态可重试）+ status icon 4 个（四态）+ tooltip 6 个（success ≥1000ms 一位小数 / error 消息 / running / idle / sub-second ms / success durationMs null 兜底）+ 响应解析 7 个（success 全字段 / 缺 immutable 强制 true / 缺 durationMs 抛 / 缺 scenario 抛 / 负 durationMs 抛 / error 带消息 / error 缺消息兜底 + error blank 兜底）+ immutable 转换 3 个（mutable → frozen / 已 frozen 幂等 / 缺字段补 true）+ 2 个 end-to-end happy path（点 Run → assert preflight 过 → POST 请求 → running → 200 OK parse → applySuccess + immutableScenario → tooltip 'Run completed in 1.9 s' / 点 Run → 500 错误 parseError → applyError → tooltip 错误消息 + scenario 仍 mutable + 按钮 enabled 重试）。
- 质量检查：
  - vitest（scenarioRun）：52/52 绿。
  - vitest 子集（src/features/vertex）：39 files / 723 tests 全绿（+1 file / +52 tests）。
  - vitest 全量：213 files / 2054 tests，仅触发 `src/components/__tests__/admin-removed.test.tsx` 已知 flaky（独立运行 3/3 pass，进度日志 Codebase Patterns 已记录该套件级 flaky）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（scenarioRun.ts + scenarioRun.test.ts）：0 warning。
- **Learnings for future iterations:**
  - ScenarioRunMap 用 `Record<scenarioRid, ScenarioRunState>` 而非 `Map<string, ScenarioRunState>`：React Zustand store 序列化更方便（Map 需要中间件），immutable 替换走 `{...map, [rid]: state}` spread 已经够快，scenario 数量级是 O(10) 不是 O(10^4)。setScenarioRunState 用 spread + 单 key 替换保证未变 scenario 引用稳定 —— React useMemo / useCallback 友好。
  - apply* 系列保留 startedAt：success 时 React 算 `Date.now() - startedAt` 拿到客户端测得的端到端用时（vs 后端给的 durationMs，前者含网络 RTT，后者是 pure function 执行时间）。Pane UI 显示后端 durationMs（更准），但分析埋点上报客户端 wall time 让 SRE 看到完整链路。
  - applyScenarioRunStart 清 error 是为了重试场景：用户上次失败 → tooltip 显示错误 → 用户改完 override 再点 Run → spinner 期间不能还显示旧错误。但 durationMs 也清掉是因为旧的耗时与新 run 无关（不像 startedAt 是 "本次" 起点）。
  - validateScenarioRunRequest 三态 valid/frozen/empty_payload：spec 没显式说"empty scenario 不能 Run"，但调研报告 §4.4 隐含"Run = 后端 fork + 跑 function/写 override"——没有 action 也没有 override 时 fork 是空的，POST 会被后端 400 拒。前端先短路省一次 RTT + 给用户更友好的 disabled tooltip。
  - resolveRunButtonState 优先级 running > frozen > empty_payload > enabled：running 优先是因为 spinner 期间禁止重复点（即便 scenario 还未冻结）；frozen 优先于 empty_payload 因为 frozen 是更强信号（empty scenario 用户能加 Override 解锁，frozen scenario 必须 duplicate）。这个优先级在 React 层 disabled tooltip 文案选择上也用到。
  - getScenarioRunStatusIcon 返回字面值 '—' / 'spinner' / '✓' / '×' 而不是 React 组件：纯逻辑模块不引 React/JSX，让 vitest 能在 happy-dom 之外驱动。React 接线层用 switch(icon){case 'spinner': return <Loader2 className="animate-spin"/>} 映射成 lucide-react 图标。
  - tooltip 单位切换 1000ms 边界：<1000ms 显示 'N ms'（更精确），≥1000ms 显示 'N.x s'（一位小数）。Pane Run 列宽度 ~120px，'4.250 s' 比 '4250 ms' 更人性，但 '450 ms' 比 '0.5 s' 更精确。spec 说 "显示执行耗时" 没规定格式，选这套边界是 Palantir Foundry Vertex / Datadog APM 通用习惯。
  - parseScenarioRunSuccessResponse 在 scenario.immutable 缺失时强制 true：后端 contract 是 sync run 200 OK 必然 freeze scenario，但 contract 漂移风险（后端代码 refactor 漏写字段）会让 UI 失去 freeze 信号，用户继续编辑 frozen scenario。前端兜底强制 true。但 immutable === false 时保留 false —— 显式 false 可能是 staging/test fixture 故意没 freeze，前端不强行覆盖。
  - parseScenarioRunErrorResponse 兜底 'Scenario run failed'：后端错误响应 message 字段缺失/blank 时显示啥？显示 '—' 用户看不懂，显示 stack trace 太丑。'Scenario run failed' 是 Palantir Foundry / GitHub Actions 常用 fallback 文案。React 接线层在网络错误（fetch reject）时也复用 'Scenario run failed' 让 tooltip 文案一致。
  - immutableScenario 是单独导出的工具函数，不和 applyScenarioRunSuccess 耦合：spec 要求"200 OK Then scenario.immutable=true"，但 immutable 不属于 ScenarioRunMap 的状态机（scenarios store 是单独的 Zustand slice）。如果耦合到 applyScenarioRunSuccess，模块边界会混乱。React 接线层调 `applyScenarioRunSuccess + setScenarios(scenarios.map(s => s.rid===rid ? immutableScenario(s) : s))` 两步走，slice 边界清晰。
  - Run 失败不 freeze scenario：spec BDD #3 没明说，但语义上 function 抛错说明 fork 没成功写入，scenario 应该保持 editable 让用户改 override 再试。如果 freeze 了，用户每次失败都得 duplicate，UX 暴击。这条 invariant 由 `applyScenarioRunError` 不调 immutableScenario 实现 —— 测试 e2e #2 显式断言 scenario.immutable 仍 falsy。
  - dev-browser skill 不可用 — Spinner / green ✓ / red × / duration tooltip / Run 按钮 disabled tooltip 的浏览器验证留到 React 接线 story（Pane 表 cell renderer + Run 按钮 + Toast）；本 story 仅交付纯逻辑层 + 副作用工厂（状态机 + 请求构造 + 响应解析 + UI 决策中枢）。
  - 后端 endpoint `/api/vertex/v1/scenarios/{rid}/run` 需要 backend story 实现：fork ontology（pkg/vertex/scenarioapply 已有 apply.go 框架）+ 跑 Function-backed Action（VTX-051 函数注册 + VTX-049 runtime 转发到 :9118 FastAPI）+ 把 edits 写入 scenario_edits 表（pkg/vertex/scenarioapply 现有 audit.go / followup.go）+ 200 OK 返回 {durationMs, scenario:{rid,name,immutable:true}}。本 story 仅锁定前端 API contract，后端实现细节走独立 story。
  - 测试 `given_state_for_one_scenario_when_mutate_other_then_first_state_unchanged_by_reference` 用 `expect(next[scenarioRid]).toBe(prior)` 锁定 setScenarioRunState 的引用不变性。React useMemo / useCallback 依赖该不变量 —— 如果未来有人改成 deep clone，这个测试立刻红，防引用漂走导致全表重渲。
---

## 2026-05-15 - VTX-044
- 实现 Scenario Run（异步路径，jobId + SSE）的纯逻辑层：把 spec §4.4 "Scenario 含 ≥ 2 models OR forceAsync=true → 立即 202 + jobId → SSE 推 progress/result/done/error/cancelled → UI spinner+pct → Cancel endpoint 触发 cancelled 事件 + cleanup" 翻译成 React 接线层可消费的状态机 + 异步请求构造（POST/GET-SSE/POST cancel 三件套）+ 响应解析 + UI 派生（icon + tooltip + overall pct），全部纯函数 + immutable map 操作。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/scenarioRunAsync.ts` — 导出：
    - 类型：`ScenarioRunJobStatus`（idle/pending/running/success/error/cancelled） / `ScenarioRunJobState { jobId, status, startedAt, durationMs, error, progressByModel, resultsByModel }` / `ScenarioRunJobMap` = Record<scenarioRid, state> / `ScenarioRunSseEvent` union（progress/result/done/error/cancelled）/ `AsyncRunScenarioApiRequest` / `CancelScenarioRunApiRequest` / `AcceptedScenarioRunResponse` / `ScenarioRunJobStatusIcon`
    - 异步判定：`shouldRunAsync({ modelCount, forceAsync? })` → `forceAsync === true || modelCount >= 2`。modelCount<2 且未 force 走 VTX-043 同步路径；React 接线层用该 helper 切 `buildRunScenarioRequest`（VTX-043）和 `buildAsyncRunScenarioRequest`（本 story）的二分。
    - 请求构造：`buildAsyncRunScenarioRequest({ scenarioRid, forceAsync? })` → POST `/api/vertex/v1/scenarios/{rid}/run`，rid encodeURIComponent，forceAsync=true 时 body={ forceAsync: true } 否则 body={} / `buildScenarioRunStreamUrl({ scenarioRid, jobId })` → `/api/vertex/v1/scenarios/{rid}/run/{jobId}/stream`（GET SSE），rid+jobId 双 encodeURIComponent / `buildCancelScenarioRunRequest({ scenarioRid, jobId })` → POST `/run/{jobId}/cancel` body={}。所有 builder 强制非空字段（trim 后），blank 抛 'X is required'。
    - 响应解析：`parseAcceptedScenarioRunResponse({ status:'accepted', jobId })` → `{ jobId }`，jobId 必须 string + trim 后非空，否则抛；`parseScenarioRunSseEvent(rawData: string)` —— JSON.parse + type 5 类分支：progress.model 非空 + pct Number.isFinite + clamp [0,100] / result.model 非空 + output 透传（null 保留为 null）/ done.durationMs Number.isFinite + non-negative / error.message blank 兜底 'Scenario run failed' + model 可选 / cancelled 无额外字段 / unknown type 抛 'unknown event type'。malformed JSON 抛（JSON.parse 自带）。
    - 状态机：`createScenarioRunJobMap()` / `createScenarioRunJobState({ jobId, startedAt? })`（pending 初始 + 空 progress/result map）/ `setScenarioRunJobState(map, rid, state)`（spread 单 key 替换，未变 scenario 引用稳定）/ `getScenarioRunJobStatus`（unknown rid → 'idle' 兜底）/ `isScenarioRunJobActive`（pending|running 为 true）/ `applyAcceptedScenarioRunJob(map, rid, jobId, startedAt)` —— 202 后初始化 pending，**清** prior progress/result/error/duration（重试场景下旧数据不干扰新 job UI）/ `applyScenarioRunSseEvent(map, rid, event)` —— 关键分支：progress→status='running' + progressByModel[model]=pct（pct 已 clamp）/ result→status='running' + resultsByModel[model]=output / done→status='success' + durationMs + error=null / error→status='error' + error 字段（model 存在时前缀 'M2: predict failed'，否则裸消息）/ cancelled→status='cancelled' / **未知 rid 同 map 引用返回**（React 不重渲；SSE 事件先于 POST response 到达或 scenario 已离开 Pane 都属此场景）。
    - UI 派生：`getModelProgressPct(state, model)` → number|null（unknown model 返回 null 让 React 渲 spinner 而非 0%）/ `getOverallProgressPct(state)` → number 0..100（success 强制 100；无模型 0；其他取已知模型平均，Math.round）/ `getScenarioRunJobStatusIcon(status)` → idle '—' / pending+running 都映射 'spinner' / success '✓' / error '×' / cancelled '⊘' / `getScenarioRunJobStatusTooltip(map, rid)` → pending 'Queued…' / running 'Running…' 或 'Running… N%'（有 progress 时显示百分比）/ success 'Run completed in X s/ms'（≥1000ms 一位小数 s，<1000ms 整数 ms，duration null 兜底 'Run completed'）/ error 模型前缀消息 / cancelled 'Cancelled' / idle null
    - 复用：`immutableScenario(scenario)` 也单独导出（async run done 后 React 接线层调它 freeze scenario，与 VTX-043 一致）
  - `web/src/features/vertex/scenarioPane/scenarioRunAsync.test.ts` — 82 个 BDD 测试覆盖：shouldRunAsync 7 个（modelCount 0/1/2/3 + forceAsync 三态 + omitted 默认 false）+ 异步请求构造 5 个（base / forceAsync flag / 不带 flag 时 body={} / 特殊字符 encode / blank 抛）+ stream URL 4 个（base / jobId 含 '/' encode / blank jobId 抛 / blank rid 抛）+ cancel 请求 4 个（base / blank jobId 抛 / blank rid 抛 / 特殊字符 encode）+ accepted parse 4 个（base / 缺字段抛 / blank 抛 / 非 string 抛）+ SSE parse 17 个（progress 3 类 + clamp 200→100 + clamp -5→0 + 缺 model 抛 + 非 finite pct 抛 / result 含 object output + null output + 缺 model 抛 / done base + 非 finite duration 抛 + 负 duration 抛 / error 含 model + 不含 model + blank message 兜底 + 缺 message 兜底 / cancelled / unknown type 抛 / malformed JSON 抛 / 空字符串抛）+ 状态机 19 个（createMap empty / unknown rid 'idle' / createJobState 默认 / applyAccepted 标 pending / applyAccepted 清 prior error+progress / progress→running+pct / 两 progress 累积 / result→running+output / done→success+duration / error 带 model 前缀消息 / error 不带 model 裸消息 / cancelled→cancelled / **未知 rid 应用事件同引用返回** / 跨 scenario 引用稳定 / isActive 五态）+ progress 派生 5 个（两模型 avg / 无模型 0 / success 强制 100 / 已知 model pct / unknown model null）+ icon+tooltip 12 个（六个 icon 状态 / 五个 tooltip 状态含 'Running… 50%' / sub-second 'X ms' / 'Run completed in X s' / pending→progress event 后 tooltip 含 'Running' / idle null）+ 3 个 end-to-end happy path（two-model 全生命周期 pending→running→success / cancel flow 含 cancelled 事件 / one-model 失败时 error 前缀含 'M2' + 保留 M1 progress）。
- 质量检查：
  - vitest（scenarioRunAsync）：82/82 绿。
  - vitest 子集（src/features/vertex）：40 files / 805 tests 全绿（+1 file / +82 tests）。
  - vitest 全量：214 files / 2136 tests 全绿（这次未触发 admin-removed flaky）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（scenarioRunAsync.ts + scenarioRunAsync.test.ts）：0 warning。
- **Learnings for future iterations:**
  - VTX-043 同步 run 和 VTX-044 异步 run 共用 `/api/vertex/v1/scenarios/{rid}/run` 同一 endpoint：后端按 modelCount + body.forceAsync 决定 200 vs 202 响应。前端 `shouldRunAsync` 是**前端预判**（决定调哪个 builder + 是否在 POST 后立刻打开 SSE），不影响后端真实判定。两者必须对齐（modelCount 计算口径一致），否则前端预判 sync 但后端返回 202，会把 jobId 丢失；前端预判 async 但后端返回 200（少了 model），会等不到 SSE 事件。React 接线层在调 POST 前 `if (shouldRunAsync(...))` 立刻准备 EventSource 订阅，response.status === 202 时打开，否则按 VTX-043 走同步 parse。
  - `scenarioRunAsync.ts` 和 VTX-115 的 `web/src/vertex/scenarioRunStream.ts` 是**两套不同模块**，故意分开：(a) 事件 schema 不同 —— VTX-115 是 SDK 通用 RunEvent（progress/log/retry/completed/failed），VTX-044 是 Pane-aware ScenarioRunSseEvent（progress{model,pct}/result{model,output}/done/error{model?}/cancelled），后者带 model 维度让 Pane 表按 model 行渲染单独 progress bar；(b) URL 形状不同 —— VTX-115 用 `/run` + lastEventId query 走 resilient reconnect，VTX-044 用 `/run/{jobId}/stream` 走显式 jobId 路径（spec 直接要求）；(c) 职责不同 —— VTX-115 包 EventSource 实例 + 重连 + REST fallback，VTX-044 只管事件解析 + 状态机 + UI 派生，让 React 接线层自己选 EventSource 客户端（可以是 VTX-115 也可以是裸 EventSource）。两者将来可以组合：用 VTX-115 的 reconnect 客户端拉 `/run/{jobId}/stream`，每条事件喂给 VTX-044 的 parser/state machine。
  - SSE 事件解析的 `applyScenarioRunSseEvent(map, rid, event)` 对**未知 rid** 返回 map 同引用（不创建新 state）：spec §4.4 BDD 没明确"事件先于 POST response 到达怎么办"，但实践中（高延迟网络、Service Worker 缓存 SSE 响应、用户已切换 Pane）会触发该 race。如果在 unknown rid 上盲目创建 state，pending → progress 翻 running 会显示一个用户根本没点 Run 的 scenario 在跑，且没有 jobId 来 cancel。"同引用返回不重渲"是最安全的 default —— React 接线层在收到 SSE 事件前必须先 dispatch `applyAcceptedScenarioRunJob`（202 response 解析后），保证 state 已就位。本 story 测试 `given_no_state_for_rid_when_apply_event_then_noop_same_map_reference` 显式锁死该不变式。
  - `progressByModel: Record<string, number>` + `resultsByModel: Record<string, unknown>` 而不是 `Array<{ model, pct, output }>`：spec 没规定数据结构，但 React 渲 Pane Run 列时按 model row 查 `progressByModel[model]` 是 O(1)，而 Array 是 O(N)。N 通常 ≤10（Scenario 模型数量级），差异不大但 Record 形式让 setState merge 更自然（spread 单 key 替换 vs Array filter+push）。如果未来需要"按事件到达顺序回放"也可以从 SSE event log 重建，不必绑死内部数据结构。
  - `getOverallProgressPct(state)` 在 status='success' 时强制返回 100：spec §4.4 BDD #2 说"done{durationMs}" 是终态事件，但 done 不一定附带最后的 progress=100 事件（后端实现可以在 done 之前不发最后一条 progress=100）。如果不强制 100，UI 会显示"Running… 80%" 然后跳到 success ✓ 但 tooltip 仍说"80%"，反直觉。强制 100 让 success 的 progress bar 自然填满。同理 'cancelled' 不强制改 progress（cancelled 时 UI 显示用户被中断时的进度，让用户看到"我在 50% 时取消的"信息更友好）。
  - error 事件的 model 字段是**可选**而不是必需：spec §4.4 BDD #2 写"error{model,message}" 但 BDD #4 描述 workflow 超时取消是另一回事，workflow-level error（如总耗时超 limit）没有具体 model，前端需支持裸消息。parseScenarioRunSseEvent 对 `model` 字段做 `typeof === 'string' && trim().length > 0` 双重检查防 'M1' / '' / undefined 三态混淆，store error 时拼接 'M2: predict failed'（有 model）或裸 'workflow timeout'（无 model）。React 接线层在 tooltip 直接显示 error 字段，不再二次拼接。
  - `pending` 状态独立于 `running` 的原因：202 response 返回但 SSE 还没推任何事件时，UI 状态。如果合并成 running，spec BDD #3 "progress 才显示百分比" 的语义需要单独判断 `Object.keys(progressByModel).length === 0`，模块边界混乱。pending 状态让 tooltip 'Queued…' 文案与 running 'Running… N%' 区分开（pending 时进度未知，running 时至少有一个 model 有 pct），React Pane 接线层可以渲不同 spinner 颜色（Loader2 vs PauseCircle）让用户分清"已排队等开始" vs "正在跑"。但 icon 都返回 'spinner' —— Pane 列宽有限不区分细节。
  - Cancel 是双向交互：前端 POST cancel endpoint → 后端 ctx.Done → 当前 Activity cleanup → 后端推 SSE cancelled 事件 → 前端状态翻 cancelled。前端**不要**乐观更新（POST cancel 后立刻把 status 设为 cancelled），因为 cancel 可能失败（job 已完成 / 后端拒绝）。等 SSE cancelled 事件到达再翻状态，让 UI 与后端真实状态一致。POST cancel 的 response 仅作"请求已收到"信号（202），不携带最终状态。本 story 不暴露"乐观 cancel" helper，强制 React 接线层走 server-side cancellation。
  - `clampPct(value)` 在 parseSseEvent 里做而不是 applySseEvent 里做：解析层是 schema 边界，后端发 pct=200 是 contract 漂移，应在最早入口 normalize 防止下游 reduce 平均时把脏值放大（200 + 50 / 2 = 125 → tooltip 'Running… 125%' 反直觉）。同 VTX-040 parseScalarInput 把 'TRUE' 折成 'true' 也是在解析层 normalize。State machine 层假设输入已经 normalize 过，单一职责。
  - 测试 `given_pending_state_when_getTooltip_then_queued` 包含两步断言（pending 显示 'Queued…' → 收到第一条 progress 后切到 'Running…'），是因为 pending→running 的 transition 是 UX 关键 —— 用户点 Run 看到 'Queued…' 反馈，几百 ms 后看到 'Running… 0%' 进入活跃跑动，是 Palantir Foundry / Datadog Workflows 标准 UX。如果合并 pending=running，UI 会从 idle 直接跳到 'Running…' 没有"请求已收到"的过渡 confirmation，用户会怀疑是否点了按钮。
  - 模块复用 `ScenarioRef`（VTX-036）+ `immutableScenario` 工具（与 VTX-043 一致命名）：让 React 接线层在 sync vs async 路径共用 freeze 逻辑（`if (success) setScenarios(scenarios.map(s => s.rid===rid ? immutableScenario(s) : s))`），不必区分 VTX-043 / VTX-044 的 success handler。
  - 后端 endpoint 需要 backend story 实现 3 件套：(1) POST `/api/vertex/v1/scenarios/{rid}/run` body forceAsync 触发 202 + jobId + 启动 goroutine workflow（VTX-057 轻量 worker pool）；(2) GET `/run/{jobId}/stream` 走 chi route + `text/event-stream` 流（pkg/streaming 或新建 pkg/vertex/sse）+ 按 jobId 查 `scenario_runs` 表的事件历史 + 实时推 broadcast；(3) POST `/run/{jobId}/cancel` 触发 ctx.Done + 推 cancelled 事件 + cleanup workflow。本 story 仅锁定前端 API contract，后端实现细节走独立 story（VTX-057 + VTX-002 scenario_runs 表）。
  - dev-browser skill 不可用 — SSE 客户端打开 EventSource + 收到事件 + spinner+pct DOM 渲染 / Cancel 按钮点击 + cancelled toast 显示 / Pane Run 列绿 ✓ 红 × ⊘ 三色图标的浏览器验证留到 React 接线 story；本 story 仅交付纯逻辑层（5 类 SSE 事件解析 + 状态机 6 态迁移 + UI 派生 + 3 个 endpoint builder）。
---

## 2026-05-15 - VTX-045
- 实现自动跑 Baseline 对照的纯逻辑层：把 spec §4.1 step 5.4 / Scenario options "Run baseline 开启 → 用户点 Run Scenario → 后台并行跑一份无 override/无 action 的副本 → baseline 完成后 Pane Baseline 列填 unmodified outputs / System Graph Extended Label 同时显示 simulated 1500 / baseline 1000 + 带颜色差异（绿/红）" 翻译成 React 接线层可消费的 options + 状态机 + 请求构造 + 响应解析 + 输出 keying + dispatch 决策 + 对比派生，全部纯函数 + immutable map 操作。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/baselineRun.ts` — 导出：
    - 类型：`ScenarioBaselineOptions { runBaseline }` / `BaselineRunStatus`（idle/running/success/error） / `BaselineOutputValue = string|number|boolean|null` / `BaselineOutputs = Record<string, BaselineOutputValue>` / `BaselineRunState { status, startedAt, durationMs, outputs, error }` / `BaselineRunMap = Record<caseStudyRid, state>` / `BaselineRunApiRequest` / `BaselineRunSuccessResponse` / `BaselineRunErrorResponse` / `ParsedBaselineRunSuccess` / `ParsedBaselineRunError` / `ShouldDispatchBaselineInput` / `BaselineCompareColor`（positive/negative/neutral） / `BaselineCompareResult { simulated, baseline, delta, deltaPct, colorHint }` / `FormatBaselineCompareOpts { decimals?, hideDelta? }` / `FormattedBaselineCompare { simulated, baseline, delta, colorHint }`
    - 常量：`BASELINE_RUN_FAILED_MESSAGE = 'Baseline run failed'`（与 VTX-043 'Scenario run failed' 命名对齐）
    - Options：`createScenarioBaselineOptions(init?)`（runBaseline 默认 false） / `setRunBaseline(options, enabled)`（同值返回**同引用**让 React useMemo/useCallback 友好） / `shouldRunBaseline(options)`
    - 状态机：`createBaselineRunMap()` / `createBaselineRunState()`（idle + 空 outputs Record） / `setBaselineRunState(map, caseStudyRid, state)`（spread 单 key 替换，未变 case 引用稳定） / `getBaselineRunStatus`（unknown rid → 'idle' 兜底） / `isBaselineRunning` / `applyBaselineRunStart(map, rid, startedAt)`（idle/success/error → running，**清** 旧 outputs/error/duration —— rerun 期间 UI 不显示陈旧 baseline 列值） / `applyBaselineRunSuccess(map, rid, durationMs, outputs)`（保留 startedAt 让 React 算端到端用时；outputs 做 `{ ...outputs }` 浅拷贝防共享引用，测试断言"input 外部 mutate 不污染存储"） / `applyBaselineRunError(map, rid, message)`（**保留** 旧 outputs —— 连接闪断 / 后端临时不可用时 UI 仍能显示上次成功的 baseline 列值 + tooltip 显示错误消息，比"全空 + 红×"友好得多）
    - 请求构造：`buildBaselineRunRequest({ caseStudyRid })` → POST `/api/vertex/v1/case-studies/{rid}/baseline/run` body={}，rid encodeURIComponent，blank 抛 'caseStudyRid is required'
    - 响应解析：`parseBaselineRunSuccessResponse(response)` → `{ durationMs, outputs }`，durationMs Number.isFinite + non-negative；outputs 标量校验 string|number|boolean|null，object 类型抛 `output ${k} must be a scalar` / `parseBaselineRunErrorResponse(response)` → `{ message }`，blank/缺失兜底 BASELINE_RUN_FAILED_MESSAGE
    - 输出 key builder 两套：`buildPaneBaselineOutputKey(rowRid, paramName)` → `${rowRid}::${paramName}`（Pane Baseline 列 cell 2 段） / `buildObjectBaselineOutputKey(objectType, primaryKey, property)` → `${objectType}::${primaryKey}::${property}`（System Graph Extended Label 3 段）。两种 key 在同一 outputs map 里**不冲突**（段数不同 + 语义域不同），React 层按渲染上下文选 builder。两个 builder 都强制非空字段 trim + 校验。
    - 输出读取：`getBaselineOutput(map, caseStudyRid, key)` → `BaselineOutputValue`，unknown caseStudyRid → null / unknown key → null / `applyBaselineOutputs(map, rid, outputs)` → merge 新输出到现有 outputs，**不替换** 其他 key（支持 SSE 流式 baseline run 逐步喂结果；no prior state 时用 createBaselineRunState 兜底，idle status 不动）
    - dispatch 决策：`shouldDispatchBaseline({ options, currentStatus, forceRerun? })` —— `options.runBaseline=false` 永远 false（开关是更强的"用户意愿"信号，禁用时不应有任何 baseline 流量；即使 forceRerun=true 也保持 false） / running → false（不重复 dispatch 撞同一份 fork） / success → 默认 false 复用缓存，forceRerun=true 强制 re-dispatch（同 ontology 变更后 React 显式 invalidate） / error → true（重试路径，让用户改完连接再点 Run 自动 retry） / idle → true
    - 对比派生：`computeBaselineCompare({ simulated, baseline })` → `{ simulated, baseline, delta, deltaPct, colorHint }`，simulated/baseline 非 finite 抛错（NaN/Infinity）；delta = simulated - baseline；deltaPct = baseline===0 ? null : (delta/baseline)*100（防除零，仍返回 colorHint）；colorHint = positive (delta>0) / negative (delta<0) / neutral (delta===0) / `getBaselineColorHint(result)` 薄 accessor / `formatBaselineCompareLabel(result, opts?)` → 3 个字符串 + colorHint：simulated = formatNumber（decimals=0 时用 Math.round 防 "-0" 字面值，decimals>0 用 toFixed）；baseline = `baseline ${formatNumber(baseline)}`；delta = `+50.0%`（deltaPct 存在且 >0）/ `-20.0%`（<0）/ `0.0%`（=0）/ baseline=0 时回退绝对带符号 delta `+500`/`-200`/`0`；hideDelta=true 时 delta = ''；百分比固定一位小数（与 Foundry Vertex 习惯一致），decimals 选项只控制 simulated/baseline 小数位
  - `web/src/features/vertex/scenarioPane/baselineRun.test.ts` — 62 个 BDD 测试覆盖：ScenarioBaselineOptions 5 个（默认 false / init true / setRunBaseline immutable + same-ref / shouldRunBaseline 二态）+ 状态机 12 个（createMap empty / unknown caseStudy → idle / createState 默认 / applyStart 清旧 outputs / applyStart 清旧 error / applySuccess 记录 outputs+duration / outputs 浅拷贝防共享引用 + 外部 mutate 不污染 / applyError 保留旧 outputs / 跨 caseStudy 引用稳定 / setBaselineRunState 只换 target / isBaselineRunning 双态）+ 请求构造 4 个（base / encodeURIComponent / blank+empty 抛）+ 响应解析 8 个（success base / 无 outputs → 空 {} / 缺 durationMs 抛 / 负 durationMs 抛 / NaN+Infinity durationMs 抛 / 非标量 outputs 抛 / error 带 message / error 缺 message → fallback / blank message → fallback）+ 输出 keying 8 个（buildPaneKey + blank 抛 / buildObjectKey + blank 抛 / getOutput 命中 / 缺 key → null / 缺 caseStudy → null / applyOutputs merge / applyOutputs 无 prior state 也能创建）+ dispatch 决策 7 个（runBaseline=false → 永远 false / runBaseline=true + idle → true / running → false / success → 默认 false / error → true 重试 / forceRerun+success → true / forceRerun+disabled → 仍 false）+ compute 7 个（simulated > baseline 正色 / < 负色 / = 中性 / baseline=0 deltaPct=null / NaN+Infinity simulated 抛 / NaN baseline 抛 / getColorHint accessor）+ format 8 个（positive '+50.0%' / negative '-20.0%' / zero '0.0%' / baseline=0 fallback '+500' / baseline=0 + simulated=0 → '0' / hideDelta='' / decimals=2 / fractional → Math.round 整数）+ 2 个 end-to-end happy path（options 启用 → dispatch=true → buildRequest → applyStart → 200 OK parse → applySuccess → getOutput (Pane key) → getOutput (Object key) → computeCompare + formatLabel → 已 success 后续 dispatch=false 复用缓存 / baseline fail 保留旧 outputs + error 状态 dispatch=true 重试）。
- 质量检查：
  - vitest（baselineRun）：62/62 绿。
  - vitest 子集（src/features/vertex）：41 files / 867 tests 全绿（+1 file / +62 tests）。
  - vitest 全量：215 files / 2198 tests 全绿（这次未触发 admin-removed flaky）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（baselineRun.ts + baselineRun.test.ts）：0 warning。
- **Learnings for future iterations:**
  - BaselineRunMap 用 **caseStudyRid** 而非 scenarioRid 作为 key：spec 说"baseline 是无 override/无 action 的副本"——这等价于"当前 ontology snapshot 直接跑 model"，与 scenarioRid 无关。同 CaseStudy 下所有 Scenario 共享同一份 baseline 输出，避免 N 个 Scenario × 1 份相同 baseline 的重复存储 + 重复 dispatch。React 接线层在 Pane Baseline 列读 outputs 时按 `state.caseStudy.rid` 索引；如果未来需要"per-ontology-version baseline"（用户切换 ontology branch），可以扩展 key 为 `${caseStudyRid}::${ontologyVersion}`，但目前 KISS。
  - applyBaselineRunStart **清** 旧 outputs，applyBaselineRunError **保留** 旧 outputs：这是两种不同的 UX 决策。Start 时清是因为"rerun 进行中"用户期待看到 loading 状态而非陈旧值；Error 时保留是因为"连接闪断、临时失败"用户希望看到上次的 baseline 而非"全空 + 红×"。前端可以在 React 层叠 tooltip 显示 error 字段让用户知道当前显示的是 stale 值。这个不对称是有意的——cf. 同一逻辑在 VTX-043 scenarioRun 里不存在（scenarioRun 不存 outputs），所以本 story 独有。
  - parseBaselineRunSuccessResponse 对 outputs 做标量校验（string|number|boolean|null）：如果后端不慎 marshal 出 `{ "predicted_delay": { "value": 1000, "unit": "ms" } }` 这种嵌套结构，前端早 fail 防止 React 层渲染时拿到 `[object Object]`。VTX-040 scalar override 也走同套语义——baseline 输出本质上是 scalar 域。如果将来需要 vector/series 输出，加一个 `parseBaselineRunSuccessResponseWithSeries` 分支而不是放宽现有 parser。
  - 两套 output key builder（Pane vs Object）的设计来自 spec：BDD #2 Pane Baseline 列按 row+param 索引，BDD #3 Extended Label 按 object property 索引。Pane key 是 2 段（rowRid::paramName），Object key 是 3 段（objectType::primaryKey::property），段数不同保证两套语义在同一 outputs map 里不会撞 key。React 接线层按渲染上下文选 builder 而不是要求 baseline 输出预先归类，简化后端契约。
  - shouldDispatchBaseline 的"success → 默认 false 复用缓存"决策：baseline 是 per-CaseStudy 的纯函数（无 override/无 action，跑当前 ontology snapshot），同 ontology 跑两次结果应该一样。所以缓存复用是默认值。但 ontology 可能在前端不可感知地变更（其他用户编辑、定时任务推送）—— React 层在 "用户切换 Case Study" / "ontology version 字段变化" / "用户显式点 Refresh baseline 按钮" 三个时机调用 forceRerun=true 显式 invalidate。这个边界让前端层负责 cache invalidation policy，而不是 baselineRun.ts 试图猜测何时该 rerun。
  - shouldDispatchBaseline 的"options 禁用 → 永远 false（即使 forceRerun=true）"：forceRerun 是"我已确认要 rerun"信号，但 options 是"用户是否开了 baseline 功能"信号。如果用户关了 baseline，那么任何 forceRerun 都不应该发请求（开关比 force 更强）。否则 React 层无意中传 forceRerun 会绕过用户的"我不要 baseline"意愿。这个语义在 test `given_forceRerun_when_options_disabled_then_still_false` 显式锁死。
  - computeBaselineCompare 在 baseline=0 时返回 deltaPct=null 而非 NaN/Infinity：div-by-zero 是 spec 没规定的边界，但 BDD #3 描述 "+50%" 颜色差异隐含 deltaPct 应该是个有限数。null 是显式"no percentage available" 信号，React 层判 `if (deltaPct === null) showAbsoluteDelta()` 比判 `Number.isFinite(deltaPct)` 更直观。formatBaselineCompareLabel 看到 deltaPct=null 自动回退到带符号的绝对 delta（'+500'）让 UI 仍有可用信息。同 VTX-040 parseScalarInput 拒 Infinity 一致——内部统一只承载有限数字 + 显式 null 兜底。
  - formatBaselineCompareLabel 把"simulated / baseline / delta"拆三段字符串而不是拼一个 "1500 (baseline 1000, +50%)"：React 接线层按 colorHint 给 delta 字段套 `<span class="text-green-600">{delta}</span>`，给 baseline 套 `<span class="text-gray-500">{baseline}</span>`，给 simulated 套 primary 文本。如果 format 返回单字符串就得在 React 层做正则切片着色，脆弱。拆三段让样式责任在 UI 层，逻辑责任在 baselineRun.ts。
  - formatNumber 在 decimals=0 时用 Math.round 而不是 toFixed(0)：toFixed(0) 对负零 -0.4 会返回 "-0"（字符串），UI 显示 "-0" 给用户看反直觉。Math.round(-0.4) 返回 0（Number），String(0) = "0"。toFixed 用在 decimals>0 路径，因为正负小数符号已经由数字本身携带。这个小坑见过几次了，做小数格式时记得 Math.round 路径。
  - "0.0%" vs "0%" 的取舍：本 story 选 "0.0%" 让 zero delta 与 ±50.0% 视觉对齐（一致的固定小数位让百分比列在 Pane 里宽度稳定，不会因为 ±0.0% 缩短）。如果未来需求改成 "exactly zero 显示 0%（无小数）"，加 opts.compactZeroDelta=true 而不是改 default。
  - 后端 endpoint `/api/vertex/v1/case-studies/{rid}/baseline/run` 需要 backend story 实现：(1) 拷贝当前 ontology snapshot 不带任何 override/action 跑所有 model；(2) 200 OK 返回 `{durationMs, outputs:Record<key,scalar>}` 其中 key 由 backend 按 `rowRid::paramName` 或 `objectType::primaryKey::property` 形态发出，与前端 buildKey 对齐；(3) baseline 结果应该考虑缓存（同 CaseStudy + 同 ontology snapshot 复用），避免每次 Run Scenario 都重跑；(4) 失败时返回 4xx/5xx + `{message}`。本 story 仅锁定前端 API contract，后端实现细节走独立 story（VTX-002 CaseStudyRepo + VTX-052 model mesh + VTX-057 worker pool 协同）。
  - 后端 outputs key 形态需要与前端 builder 完全对齐：如果后端发 `{ "row-1.predicted_delay": 1000 }`（点号分隔）而前端用 `${rowRid}::${paramName}` 双冒号，buildPaneBaselineOutputKey 返回 'row-1::predicted_delay' 不命中后端 key，UI 显示 null。前端契约严格用 `::` 双冒号 + 上述 buildKey 函数；后端实现要按同 spec 拼接 key，或者在序列化层走一个 normalize 函数。把这条单独标黑色——baseline run 是前端纯逻辑、后端纯数据，两边 keying 漂移 = UI 静默错误（无报错只是 null）。
  - dev-browser skill 不可用 — Pane Baseline 列 cell 渲染（'1000'  + 灰色 tooltip "no override applied"） / System Graph Extended Label 双行渲染 + 颜色 hint 套 tailwind 绿/红 class / Scenario options "Run baseline" 勾选框 toggle / 并行 dispatch（同时发两个 fetch + 状态机分别走）的浏览器验证留到 React 接线 story；本 story 仅交付纯逻辑层（options + 状态机 + 请求构造 + 响应解析 + 输出 keying + dispatch 决策 + 对比派生）。
---

## 2026-05-15 - VTX-046
- 实现 Scenario Diff API 的纯后端层：把 spec 附录 B "GET /scenarios/{id}/diff → {editedObjects, createdObjects, deletedObjects, deltas}" 翻译成一个新 package pkg/vertex/scenariodiff，pure Compute 函数 + chi Handler 暴露 GET /api/vertex/v1/scenarios/{rid}/diff，与 pkg/scenarios.Repo / pkg/scenarios.FoldObject 复用 ScenarioEdit / ObjectView / ObjectKey 类型，单测和 handler 测试都走 stub 接口避免起 PG。
- 新增文件：
  - `pkg/vertex/scenariodiff/diff.go` — 导出：
    - 类型 `ObjectRef { ObjectType, ObjectID }` / `CreatedObject { ObjectType, ObjectID, Properties }` / `PropertyDelta { ObjectType, ObjectID, Property, OldValue, NewValue }` / `EditedObject { ObjectType, ObjectID, Changes []PropertyDelta }` / `Diff { EditedObjects []EditedObject, CreatedObjects []CreatedObject, DeletedObjects []ObjectRef, Deltas []PropertyDelta }`
    - `BaseLoader` 接口（`LoadBase(ctx, ObjectKey) (*ObjectView, bool, error)`）让 Compute 不强绑 OSS；`NoBaseLoader{}` 兜底类型给只关心 created/deleted 的调用方
    - `Compute(ctx, edits, BaseLoader) (Diff, error)` 纯函数：按 Seq ASC 排序 edits（caller 不必 pre-sort），per ObjectKey 维护 (created, deleted, createdProps, modifiedProps) 状态机；createObject→created bucket（merge NewValue 属性 bag）；modifyProperty→已 created 则 patch createdProps，否则记 modifiedProps，已 deleted 则忽略（与 scenarios.FoldObject 同语义）；deleteObject→翻 deleted 同时清 created/props；delete-then-create 同 key 净效果 created，不进 deletedObjects。BaseLoader 只对 editedObjects 路径调用按需取 base 属性算 oldValue；base 缺这条 property → oldValue = JSON `null`（不省略字段让 wire 客户端可判 'no prior value' vs 'property not in diff'）；jsonEqual round-trip canonicalize 后相等的 modify 视为 non-change 丢弃。Deltas 是 EditedObjects.Changes 扁平化（同顺序）便于表格枚举；createdObjects 不进 deltas（无 oldValue 可比）。输出按 (objectType, objectId) 字典序稳定，EditedObjects 内 Changes 按 property 字典序。Link edits (addLink/deleteLink/unknown ops) silently dropped 不污染对象 buckets。所有 buckets 强制非 nil JSON 数组（[] 而非 null）让客户端无需 nil-guard。
  - `pkg/vertex/scenariodiff/handler.go` — 导出：
    - `EditsReader` 接口（`ListEdits(ctx, scenarioRID) ([]ScenarioEdit, error)`）与 scenarios.Repo.ListEdits 签名完全一致，*scenarios.Repo 可直接传入免 adapter
    - `Handler { reader, base }` + `NewHandler(reader, base)`（nil base 自动替换为 NoBaseLoader）+ `RegisterRoutes(chi.Router)` 挂 GET /api/vertex/v1/scenarios/{rid}/diff
    - 错误分流：scenarios.ErrScenarioNotFound→404 ScenarioNotFound + rid 参数 / 其他 repo error→500 ListEditsFailed / Compute 失败→500 ComputeDiffFailed
  - `pkg/vertex/scenariodiff/diff_test.go` — 13 个 BDD 测试覆盖：no-edits 全空、modifyProperty BDD 锁定 (oldValue, newValue) + 同时进 deltas、absent base oldValue=`null`、createObject 单独进 createdObjects、create-then-modify 仍 created bucket 且 latest value 胜出、deleteObject 单独进 deletedObjects、delete-then-create 净效果 created（不进 deletedObjects）、两次 modifyProperty latest wins、modifyProperty 值与 base 相同视为 non-change 丢弃、多 (objectType, objectId) 输出按字典序、BaseLoader 错误 bubble up（errors.Is）、addLink/deleteLink 不污染对象 buckets、deltas 与 editedObjects.Changes property 顺序对齐 + lexicographic。
  - `pkg/vertex/scenariodiff/handler_test.go` — 4 个 HTTP wire 测试：modifyProperty edit → 200 + 完整 wire shape（含 oldValue=100 / newValue=150 + deltas + 空 createdObjects/deletedObjects 序列化为 [] 不是 null）、unknown scenarioRID → 404、generic repo error → 500、no edits → 200 + 四个 [] buckets。
- 质量检查：
  - `go test ./pkg/vertex/scenariodiff/...` — 17/17 全绿。
  - `go vet ./pkg/vertex/scenariodiff/...` — 0 错误。
  - `gofmt -d pkg/vertex/scenariodiff/` — 0 diff。
  - `go build ./pkg/... ./internal/...` — 0 错误（cmd/server build 因 web/dist 缺失而独立 fail，与本 story 无关，是 frontend 未构建的预存基线状态）。
  - `go test ./pkg/vertex/... ./pkg/scenarios/...` — 全部 vertex 子包 + scenarios 包 0 回归。
  - golangci-lint 未安装；新代码无 emoji + 通过 gofmt 等价标准检查。
- **Learnings for future iterations:**
  - VTX-046 是本 stream 第一个**纯后端** Go story（前面 VTX-031..045 全在 web/src/features/vertex 写 TS 纯逻辑），新增 package 路径选 `pkg/vertex/scenariodiff/` 紧贴 `pkg/vertex/scenarioapply/` —— 同属 "Vertex scenario lifecycle" 子树。命名 scenariodiff 而非 scenariodiffsvc 与 scenarioapply 风格对齐（apply / diff 都是动词 + 名词的复合名词）。
  - 复用 pkg/scenarios.Repo 接口而不是引入 VTX-002 自己的 ScenarioRepo：spec dependencies 字段写 "VTX-002 ScenarioRepo"，但本仓库 pkg/scenarios 已经有完整 Repo 接口（含 ListEdits）+ 数据类型（ScenarioEdit + ObjectKey + ObjectView + FoldObject 语义），完全够用。EditsReader 接口故意写成 ListEdits 单方法 slice — 与 scenarios.Repo.ListEdits 签名 byte-equal 兼容（*scenarios.Repo 自动满足 EditsReader 接口），同时 handler 测试 stub 只需 1 行 method 实现。Go 隐式接口 + 单方法接口的组合让"VTX-002 ScenarioRepo"不必新建任何接口/类型。
  - `BaseLoader` 接口的"按需 LoadBase 而不是一次性 LoadAll(scenario)" 设计动机：scenario 典型只 modify O(10) objects，但 base ontology 可能有 O(10^5) objects，把"为 diff 取 base"做成按 key 拉取避免无谓全表 scan。LoadBase 返回 `(*ObjectView, bool, error)` 而不是 `(*ObjectView, error)` + ErrNotFound：bool false 是"无此 base"的正常信号（让 oldValue 自然降级为 JSON null），把 not-found 升格为 error 会让 Compute 每次都得 errors.Is 一层 boilerplate。和 scenarios.FoldObject 的 `*ObjectView base` 参数风格一致（nil 表示无 base，不是错误）。
  - **oldValue=JSON `null` vs 字段缺失**：spec BDD #2 只说 "editedObjects 含该 obj + {property, oldValue, newValue}" 没明说 absent base 怎么编码。选 JSON null 而不是省略 oldValue 字段：(a) 让 TypeScript 客户端的 `PropertyDelta` 类型可以是 `oldValue: JsonValue | null` 单一类型（vs `oldValue?: JsonValue` 可选字段，后者 wire 上不会出现 key），客户端代码减少 in 判断；(b) 与 PostgreSQL JSONB NULL vs JSON null 的语义对齐 —— 数据库里 NULL 是"未知"、JSON 'null' 是"已知是空"，diff 路径 absent base 更接近后者；(c) jsonEqual round-trip 在 oldValue=`null`、newValue=`null` 时正确判 non-change（避免把 "设为 null" 当成 change）。这条不变式由 test `Given_ModifyPropertyOnAbsentBase_When_Diff_Then_OldValueIsJSONNull` 显式锁死。
  - **delete-then-create 同 key 的"净效果 created"决策**：spec 没写这种边界，但 scenarios.FoldObject 已经按"重新创建丢弃旧 props"语义实现（删完再 create 不带 prop carryover）。本 story 沿用 —— 如果同 key 先 delete 再 create，diff 输出只在 createdObjects 含新对象 + 新 props，不在 deletedObjects 提及旧 identity。理由：deletedObjects 列表表达的是"scenario 应用后基线里不再存在的对象"，但重新创建后对象身份仍然占据该 key 槽位（虽然 props 全换），从客户端"我要 sync 哪些 key 进/出索引"视角看就是 created。与 FoldObject 语义同源避免 diff 与 fold 给出矛盾结论。
  - **modify-same-as-base 视为 non-change 丢弃**：spec 没写但是必要不变式。用户在 UI 上把 capacity 改成 150 又改回 100（base 也是 100），diff 应该是空的 —— 否则 Pane 表会显示 phantom edit 给用户错觉。byte-equal 比较太严（`100` 和 `1e2` 都是 100 但字符串不同），所以走 `jsonEqual` round-trip：parse → marshal canonicalize 后再 byte 比较，能识别等价的数字/字符串/对象/数组。代价是每条 modify 多两次 json.Unmarshal+Marshal，但 modify 数量级 O(10)，可接受。
  - **deltas[] 是 editedObjects.Changes 的扁平化** —— spec 把它列在 createdObjects 之外的独立 bucket，最自然的解释是"按 property 行枚举的 flat 视图"便于表格客户端枚举（不必嵌套循环 editedObjects.Changes）。createdObjects 不进 deltas 是 deliberate：created 对象的"新值"是 createObject 的 NewValue，但没有"旧值"可比（base 无此对象），把它强制塞进 deltas 会让 deltas[].oldValue 永远是 null + 与 editedObjects 路径的 "modify 走 deltas / create 不走" 形成混合语义。客户端要"所有新值流"自己 union createdObjects + deltas 即可，业务规则不进底层。
  - **buckets 强制非 nil JSON 数组（`[]` 而非 `null`）**：Go map[string]any{} marshal 出 `{}`，但 nil slice marshal 出 `null`。spec wire shape `editedObjects:[...]` 隐含数组形态，客户端用 `for...of` / `.map` 迭代 null 会炸。Compute 出口给四个 buckets 显式初始化 `[]EditedObject{}` 等空 slice，保证序列化总是 `[]`。test `Given_NoEdits_When_GETDiff_Then_AllBucketsEmpty` 用 `resp.CreatedObjects == nil` 断言（unmarshal 空数组为非 nil slice）锁死该不变式。
  - **handler 错误码分流：404 vs 500**：scenarios.ErrScenarioNotFound 是用户级别的"打错 RID"信号，map 到 404 让前端按"该 scenario 不存在"重定向 / toast 而不是显示"服务端错误"。其他 repo error（DB 不可达、连接超时）map 到 500 + 错误消息塞 params.error 让运维能从 access log 直接定位。错误名 `ScenarioNotFound` / `ListEditsFailed` / `ComputeDiffFailed` 与 graphsvc/handler.go 的 `GraphNotFound` / `ListVersionsFailed` 命名模式一致 —— `<Subject><Verb>Failed` 是 vertex API 错误标准。
  - **main.go 暂未注册 scenariodiff 路由**：本 story 只交付 package + handler，没改 cmd/server/main.go。原因：scenariodiff handler 需要 scenarios.Repo 实例 + BaseLoader 实现（OSS 反向取 base view），后者还没有 vertex 端的封装（pkg/oss/scenario_overlay.go 是给 OSS 自己读用的，不是给 scenariodiff 用的 LoadBase 形态）。强行接 main.go 会引入 OSS→scenariodiff 的耦合且没有 spec 任务对应。把"wiring 到 main.go" 当作未来 story（VTX-WIRE-046 或并到 VTX-002 backend wiring）单独做更干净，避免本 story commit 改动溢出。这种"endpoint 包就位但路由暂不挂"模式仓库其它 vertex 包也有先例（grep `RegisterRoutes` 历史 commit 可见多次"先 land package + tests，下一个 commit wire 到 main.go"）。
  - **scenarios.ErrScenarioNotFound 不是由 ListEdits 自然返回**：pg_repo.go 里 ListEdits 当 RID 不存在时返回空 slice + nil error（与 SQL `SELECT ... WHERE scenario_rid = ?` 语义一致）。本 story 的 EditsReader stub 测试故意返回 `scenarios.ErrScenarioNotFound` 模拟"调用方在 ListEdits 之前先 GetScenario 拒绝不存在 RID"的路径，让 handler 能正确分流。如果未来真实 wiring 时不在 handler 里先 GetScenario，nil edits + 200 + 四空数组 也是合理的（不存在 scenario 等价于 "无 edits" 的 wire 行为），不必强求 404。本 story 把 404 路径作为"调用方主动信号" 保留接口能力，handler 不强制走 GetScenario 避免双 PG round-trip。
  - **不引 OSS 实现 LoadBase**：spec dependencies 写 "VTX-002 ScenarioRepo" 但没说 LoadBase 必须用 OSS。本 story 仅定义 BaseLoader 接口让 Compute 可单测，real implementation 走 OSS get + property bag 转 ObjectView 是 wiring 工作（与上一条同理）。如果未来 wiring story 发现 OSS 反向取 base 性能不够，可以引入 batch LoadBaseMany 接口（一次拉 N 个 key 的 base），现有接口设计兼容这种扩展（每个 key 一次调用 → batch 优化只是底层实现细节）。
  - **diff vs fold 语义不重复实现**：scenarios.FoldObject 已经把"replay edits over base → ObjectView" 写好了；本 story 没有用 FoldObject + diff(base, folded) 的方案而是单 pass replay，原因：FoldObject 是 per-key 函数（必须先按 key 分组 edits），而 diff 输出要按全局 (created/edited/deleted) 三 bucket 分组，从 ObjectView 反向推 bucket 类型需要 base/folded 两边 nil/non-nil 状态机；单 pass replay 直接维护 (created, deleted) 状态 + props 累加，少一次 base 加载。trade-off 是 diff 自己重新实现了 fold 状态机（"delete clear created"、"modify-after-delete ignored"），与 FoldObject 形成两份相同语义代码 —— 但 scenarios 包的 fold 是"算 ObjectView"，scenariodiff 包的 replay 是"算 bucket 归属 + per-property new value"，两者输出形状不同，硬抽公共骨架反而让两边都变难读。如果未来出现第三个 replay 场景，再抽公共 walker。
  - **VTX-002 ScenarioRepo 已就位** —— grep `pkg/scenarios/scenarios.go` 看到 Repo 接口含全部 8 个方法（CreateCaseStudy/CreateScenario/AppendEdit/ListEdits/Freeze/Get*/UpsertOverride/ListOverrides），pkg/scenarios/pg_repo.go 是 PG 实现。spec 写 dependencies "VTX-002 ScenarioRepo" 实际上是已经满足的依赖（commit 历史里 stream A 早期就 land 了），本 stream 直接复用。下一个 story 写"用 ScenarioRepo + OSS 拼接 scenariodiff 进 main.go"时不必再造 Repo。
  - 后端 testing 模式：handler 测试用 `chi.Router + httptest.NewRecorder + httptest.NewRequest` —— 与 pkg/vertex/graphsvc/handler_test.go 同款。stub Repo / Reader 接口 dependency injection 而不是起 PG container（test/e2e 才用 testcontainers）。`scenarios.ErrScenarioNotFound` 走 errors.Is 而不是 ==（sentinel error 包 fmt.Errorf 时仍能识别）。这套"interface + stub" 模式贯穿整个 pkg/vertex 子树，新 story 沿用即可。
---

## 2026-05-15 - VTX-047
- 实现多 Scenario 横向并列对比的纯逻辑层：把 spec §4.5 "Case Study 含 Baseline + 3 Scenarios → Pane 显示 4 列并列 / System Graph 节点 Extended Label 显示所有 Scenarios + Baseline 多列读数 / 用户点 Scenario 列头 → 该 Scenario 设为主视图（节点着色按该 Scenario 数据）" 翻译成 React 接线层可消费的主视图状态 ActiveScenarioState + 列定义派生 + 多列读数派生 + 节点着色源选择，全部纯函数 + 同值返回同引用。
- 新增文件：
  - `web/src/features/vertex/scenarioPane/multiScenarioCompare.ts` — 导出：
    - 类型：`ActiveScenarioState { activeScenarioRid: string | null }`（null = Baseline 主视图） / `CreateActiveScenarioStateInit` / `MultiScenarioColumnKind` = 'baseline'|'scenario' / `MultiScenarioColumn { key, label, kind, scenarioRid?, isActive }` / `ScenarioOutputs` = Record<key, BaselineOutputValue>（复用 VTX-045 scalar 域）/ `ScenarioOutputsByRid` = Record<scenarioRid, ScenarioOutputs> / `MultiScenarioReading { columnKey, kind, scenarioRid?, value, compare: BaselineCompareResult | null, colorHint: BaselineCompareColor | null }` / `MultiScenarioPaneRowReadingsInput` / `MultiScenarioObjectReadingsInput` / `NodeColoringSource` = { kind:'baseline' } | { kind:'scenario', scenarioRid }
    - 常量：`BASELINE_COLUMN_KEY = 'baseline'` / `BASELINE_COLUMN_LABEL = 'Baseline'`
    - ActiveScenarioState reducer：`createActiveScenarioState(init?)`（默认 null）/ `setActiveScenario(state, scenarioRid|null)`（同值返回**同引用**）/ `isBaselineActive` / `isScenarioActive(state, rid)` / `clearActiveIfMissing(state, presentRids[])` —— React 接线层在 VTX-036 removeScenario 删掉当前主视图 scenario 后调用，把 active 回退到 Baseline 避免悬挂引用
    - 列定义：`getMultiScenarioColumns(paneState, activeState)` —— 无 caseStudy 返回 []，否则首列 Baseline + paneState.scenarios 顺序，isActive 按 active.activeScenarioRid === 列 rid（或 null 配对 baseline 列）一次性算齐 / `setActiveColumnFromHeader(state, columnKey)` —— 'baseline' 字面值 → setActive(null)，其它视为 scenarioRid → setActive(rid)，同值返回同引用
    - outputs map 读取：`getScenarioOutput(outputsByRid, scenarioRid, key)` —— unknown rid → null / unknown key → null / explicit null 保留（in 操作符判断防漏掉 explicit-null vs missing-key 的语义差异）
    - 单元格读数：`buildReading`（私有）—— scenario 列 + 两侧都是有限 number 时调 VTX-045 `computeBaselineCompare` 派生 delta+deltaPct+colorHint，否则 compare=null colorHint=null；scenarioRid undefined 时不写入 reading（避免 baseline 列 reading 含 scenarioRid 字段） / `getMultiScenarioPaneRowReadings({ paneState, activeState, baselineOutputs, scenarioOutputsByRid, rowRid, paramName })` —— 用 VTX-045 `buildPaneBaselineOutputKey(rowRid, paramName)` 作为 outputs map 的 key，遍历列输出 readings[] / `getMultiScenarioObjectReadings({ ..., objectType, primaryKey, property })` —— 用 VTX-045 `buildObjectBaselineOutputKey` 作为 key，其余同构（共享私有 `buildReadingsForKey` helper）
    - 节点着色：`getNodeColoringSource(state)` → `{ kind:'baseline' }` 或 `{ kind:'scenario', scenarioRid }` / `getNodeColoringOutputs({ activeState, baselineOutputs, scenarioOutputsByRid })` → Baseline 主视图返回 baselineOutputs（同引用），scenario 主视图返回 `scenarioOutputsByRid[rid]` 或 `Object.freeze({})` 兜底（避免 undefined 在 React 渲染层炸）
  - `web/src/features/vertex/scenarioPane/multiScenarioCompare.test.ts` — 42 个 BDD 测试覆盖：ActiveScenarioState 11 个（createState 默认 / init.rid 透传 / setActiveScenario immutable / setActiveScenario 同值同引用 / setActiveScenario null 切回 Baseline / setActiveScenario null 在 Baseline 时同引用 / isBaselineActive+isScenarioActive 三态 / clearActiveIfMissing 三态 + 空 list 退回 Baseline）+ getMultiScenarioColumns 4 个（空 caseStudy / 仅 baseline / 4 列含 isActive flag 正确 / scenario active 时 isActive 翻列）+ setActiveColumnFromHeader 4 个（baseline / scenario / 同值同引用双态）+ getScenarioOutput 4 个（命中 / unknown rid → null / unknown key → null / explicit null 保留）+ getMultiScenarioPaneRowReadings 7 个（无 caseStudy → [] / 4 列全数值 readings 含 compare+colorHint 三色（positive +500 delta、negative -200 delta、neutral 0 delta） / baseline 缺值 → 所有 scenario 列 compare=null / scenario 缺值 → value=null compare=null / 非数值 baseline → 无 compare / 非数值 scenario value（boolean）→ 无 compare / active scenario 不改 readings 顺序）+ getMultiScenarioObjectReadings 4 个（无 caseStudy / 4 列 readings / baseline=0 deltaPct=null 但 colorHint 仍非 null（'positive'）/ baseline=null → scenario compare null）+ NodeColoring 5 个（baseline source / scenario source / outputs 跟 Baseline / outputs 跟 active scenario / 缺 scenario outputs → frozen 空对象）+ 2 个 end-to-end happy path（点 Scenario 列头 → active 切换 + cols.isActive 翻 + 节点 outputs 跟着切 + 再点 Baseline 切回 / active scenario 被移除 → clearActiveIfMissing 把 active 回退到 Baseline）。
- 质量检查：
  - vitest（multiScenarioCompare）：42/42 绿。
  - vitest 子集（src/features/vertex）：42 files / 909 tests 全绿（+1 file / +42 tests vs VTX-045 的 41 files / 867 tests，VTX-046 是 Go-only 改动不动前端基线）。
  - vitest 全量：216 files / 2240 tests 全绿（这次未触发 admin-removed flaky）。
  - tsc（web/）：exit 0，0 错误。
  - eslint（multiScenarioCompare.ts + multiScenarioCompare.test.ts）：0 warning。
- **Learnings for future iterations:**
  - ActiveScenarioState **不**塞进 ScenarioPaneState：scenarioPane.ts 是"Pane 表的结构"（列/行/CaseStudy/Scenarios），active 主视图是"用户当前 UI focus"（与表结构正交，可以横跨 Pane 与 SystemGraph 同时影响渲染）。如果耦合，scenarioPane reducer 必须每次 addScenario/removeScenario 处理 active 回退，模块边界混乱。本 story 选独立 state + clearActiveIfMissing 让 React 接线层显式两步走（removeScenario → clearActiveIfMissing），避免隐式联动。React 接线层把 ActiveScenarioState 作为 Zustand store 的独立 slice 即可。
  - BASELINE_COLUMN_KEY='baseline' 字面值故意与 scenario rid 形态错开：scenario rid 是 `ri.vertex.main.scenario.s-A` 格式（永远以 'ri.' 开头），'baseline' 不会撞 rid。setActiveColumnFromHeader 用 `columnKey === 'baseline'` 判断分支即可，不必把 BASELINE_COLUMN_KEY 跟随用户输入做转义。这与 React 路由的 `:type` 路径段习惯一致（baseline / explorer / quiver 都是已知 magic string）。
  - getMultiScenarioColumns 第一列总是 Baseline 而不是按 paneState.firstScenarioIsBaseline 开关：spec 没说"baseline 可隐藏"，且 BDD #1 显式要求"4 列并列：Baseline / S1 / S2 / S3"暗示 Baseline 是固定首列。如果未来 UX 改成"Baseline 可隐藏"，加 opts.includeBaseline=false 而不是改默认。Baseline 列的 isActive=true 表示"未选任何 scenario"，是 active state 的 null 标记的自然投影。
  - MultiScenarioReading.compare 用 BaselineCompareResult|null 而不是单独 deltaPct 字段：React 接线层渲染列时需要 simulated/baseline/delta/deltaPct/colorHint 五个字段一起（VTX-045 formatBaselineCompareLabel 接受整个 BaselineCompareResult），单字段返回会让调用方反向重组。Baseline 列自身 compare=null 不是 simulated===baseline 的 trivial compare（{delta:0,deltaPct:0,colorHint:'neutral'}）—— 后者会让 Baseline 列显示 "+0.0%" 标签，干扰视觉。null 明确表达"this column has no comparison target"。
  - buildReading 私有 helper + buildReadingsForKey 私有 helper：Pane row readings 和 Object readings 唯一差异是 outputs map 的 key 构造（buildPaneBaselineOutputKey vs buildObjectBaselineOutputKey），其它逻辑（遍历列、查 baseline、查 scenario、算 compare）共享。把共享部分抽进 buildReadingsForKey 让两个 public helper 只有 4 行身体（构造 key + 调 helper），避免维护两份相同 reading 算法。
  - getMultiScenarioColumns 缺 caseStudy 返回 [] 而不是只回 Baseline 列：spec BDD #1 把"Case Study 含 Baseline + 3 Scenarios"作为前提，无 Case Study 时 Pane 表头本身应该空（VTX-036 getColumnHeaders 已经这么做），与 paneState.caseStudy === null 的语义一致。React 层无 caseStudy 时根本不渲染 Pane 列头，本 helper 返回 [] 让 React 层直接 .map 出空数组而不必先判空。
  - getNodeColoringOutputs 用 `Object.freeze({})` 兜底而不是新建空对象：缺 scenario outputs 场景（用户切到尚未跑过的 scenario）每次都返回同一个 frozen 引用让 React useMemo 不会因为"每次都新 {}"而误认为变化触发重渲。Object.freeze 顺便防御调用方写入污染共享空对象。
  - getScenarioOutput 用 `key in outputs` 而不是 `outputs[key] !== undefined`：scenarioOutputs 允许显式 null 值（与 VTX-045 BaselineOutputValue 包含 null 一致），但不允许 undefined（合约）。`in` 操作符正确区分"key 不在 map"（→ null 兜底）和"key 在 map 但值是 null"（→ 保留 null）。后者是后端显式发送 "this output is intentionally null" 的合法语义，前端应保留信号而不是当成"缺失"。
  - clearActiveIfMissing 接 `presentScenarioRids[]` 而不是 ScenarioPaneState：本 helper 不关心 paneState 的其他字段（rows / inputOutputColumns / caseStudy），只需要 scenarios 的 rid 列表。接 string[] 让 React 接线层灵活：可以传 `paneState.scenarios.map(s => s.rid)` 也可以传"backend authoritative scenario list"。这与 VTX-045 shouldDispatchBaseline 接 options + status 而不是 BaselineRunState 全状态对象的设计选择一致——helper 接最小依赖避免膨胀。
  - 模块复用 VTX-045 computeBaselineCompare / BaselineCompareResult / BaselineCompareColor / BaselineOutputValue / buildPaneBaselineOutputKey / buildObjectBaselineOutputKey + VTX-036 ScenarioPaneState 不重复实现：compare 派生 + outputs 标量域 + key 构造都是 cross-cutting concern，本 story 在它们之上派生"按 scenario 排列的多列读数"，是分层而非平移。如果未来 BaselineOutputValue 扩展（如加 'series' 类型），computeBaselineCompare 改完，本模块自然继承（已通过 typeof 'number' && Number.isFinite 守卫，新类型不会误进入 compare 路径）。
  - Pane Baseline 列 readings 在 baseline=0 时 deltaPct=null 但 colorHint 仍非 null —— 不变式由 test `given_baseline_zero_when_get_then_scenario_deltaPct_null_but_colorHint_present` 在 Object readings 路径锁死。VTX-045 computeBaselineCompare 已经在 baseline=0 时返回 deltaPct=null + colorHint 仍按 delta 符号算（'positive' / 'negative' / 'neutral'），本模块直接透传。React 接线层 formatBaselineCompareLabel(result) 会按 deltaPct === null 回退到绝对带符号 delta（'+500'），与 VTX-062 baseline vs simulated 对比 Extended Label 的渲染逻辑天然兼容。
  - 本 story 设计了三个独立 helper（getColumns + getReadings + getColoring）而不是一个 mega helper "getEverythingForRender"：React 接线层在不同组件分别消费 —— Pane Table headerGroup 用 getColumns；Pane Table cell renderer 用 getMultiScenarioPaneRowReadings；SystemGraph 节点 Extended Label 用 getMultiScenarioObjectReadings；SystemGraph 节点着色器用 getNodeColoringSource/Outputs。组件粒度分开避免一个 hook 拉所有数据导致无关重渲。VTX-036 progress 也提到 "helper 不要膨胀为业务规则枢纽"。
  - VTX-062 (priority 62) "baseline vs simulated 对比 Extended Label" 是本 story 的下游消费者：将来 React 接线层调 getMultiScenarioObjectReadings 得到 N 列 reading[]，对每个 reading[i].kind === 'scenario' && reading[i].compare !== null 套 formatBaselineCompareLabel(reading[i].compare!) 渲染 "1500 (baseline 1000, +50%)" 字面值 + 按 colorHint 上色。本 story 把数据派生 + spec BDD #1/#2 多列读数交付，VTX-062 只负责把单条 reading 渲染成 React 节点（与 VTX-045 formatBaselineCompareLabel 拼接）。
  - dev-browser skill 不可用 — Pane 4 列并列 TanStack Table headerGroup 渲染、Scenario 列头点击切换主视图 + 高亮、SystemGraph 节点 Extended Label 多行 + 颜色差异（绿/红/灰）、节点着色按 active scenario 切换的浏览器验证留到 React 接线 story（Pane ColumnDef + SystemGraph 节点 overlay + Zustand active scenario slice）。
---
