# Ralph Progress Log
Started: Tue May 19 13:26:47 CST 2026
Branch: ralph/self-iteration-20260519-132647
---

## Codebase Patterns
- US-444 full-stack probes should only use `test.skip` for the shared backend-down portability guard. If the backend is reachable, quiet, missing, 404/503, or shallowly wired service behavior should be an assertion failure guarded by a source-level contract test.
- VTX-099 full-stack probes should only skip when the shared backend health check is down. With a reachable backend, missing System Graph records, empty graph payloads, and failed render surfaces should be assertions guarded by a source-level contract test.
- US-444 helper and README guidance is now source-contracted too: docs may describe `skipWhenBackendDown` as the portability guard, but must not reintroduce optional 404/503 endpoint skip guidance.
- Phase 6 upper-layer probes should remain skip-free and should assert concrete OSv2 response shape before using seeded fixtures: `objectTypes`, `actionTypes`, `interfaceTypes`, metadata `linkTypes`, and `loadObjectsOrInterfaces` page payloads should fail loudly on missing arrays or missing `totalCount` instead of falling through shallow route reachability.
- Phase 7 upper-layer probes should remain skip-free and should assert concrete seeded identity, policy, audit, actionTypes, and realtime subscription behavior; optional empty audit checks or missing `data` array checks let reachable service regressions look like passing probes.
- ObjectSet composer support for a new backend variant usually needs both `web/src/components/objectsets/ObjectSetBuilder.tsx` controls/defaults and `web/src/lib/objectSetBuilder.ts` node validation/round-trip updates.
- ObjectSet composer filter operators must align with `pkg/oss/where/converter.go`; UI-only pseudo-operators should either be normalized to backend wire format or rejected before Execute.
- Run Web TypeScript checks from `web/` with `npx tsc --noEmit` (or `npm --prefix web run build`); `npm --prefix web exec tsc -- --noEmit` does not pass the flag correctly in this environment.

## Kickoff
- Current branch verified with `git branch --show-current`: `ralph/self-iteration-20260519-132647`.
- Previous active PRD belonged to `ralph/self-iteration-20260519-122726` and all stories had `passes: true`; archived active `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` to `scripts/ralph/archive/self-iteration-20260519-133057/`.
- Root symlinks verified before refresh: `prd.json -> scripts/ralph/prd.json`, `progress.txt -> scripts/ralph/progress.txt`.
- No `PR CI auto-fix context` was present in the active progress file.
- GitHub CI at kickoff: latest main CI run 26077819348 completed success; Codespaces prebuild 26077819155 was still in progress.
- Local known-risk checks:
  - `go test ./cmd/server -run 'TestBuildEmbeddingProvider|TestEmbeddedWebDist|TestSPA'` passed.
  - `npm --prefix web test -- --run src/components/admin/__tests__/ComplianceReportsPage.test.tsx` passed: 1 file, 7 tests.
- Dogfood inspection:
  - DOG-002 dashboard object type totals, DOG-003 index bootstrap/stream ingest, DOG-004 containsAnyTerm browser search, and DOG-005 explorer property flags have committed regression coverage in this checkout.
  - The next small stale surface is ObjectSet composer filter validation: it still exposes `neq`, which `pkg/oss/where/converter.go` does not support directly, and it treats blank field/value filters as executable.
- New PRD created with three focused ObjectSet filter stories. SELF-440 is the only story selected for this Codex execution.

## Selected Story
- Selected SELF-440 because it is the highest-priority incomplete story and prevents the composer from sending backend-rejected WhereClause filters before broader logical/text filter work.
---

## Tue May 19 13:33:53 CST 2026 - SELF-440
- What was implemented:
  - Added source-level ObjectSet filter WhereClause validation for supported backend operators, missing fields, missing comparison values, logical `and`/`or` children, and `not` child clauses.
  - Kept `isNull` executable with a field and no comparison value.
  - Removed the unsupported `neq` pseudo-operator from the composer filter operator menu so new filters cannot select a backend-rejected operator.
  - Added BDD coverage for blank `eq`, unsupported `neq`, and valid `isNull` filter clauses.
- TDD red:
  - `npm --prefix web test -- --run src/lib/__tests__/objectSetBuilder.test.ts src/components/objectsets/__tests__/ObjectSetComposer.filterValidation.bdd.test.tsx src/components/objectsets/__tests__/ObjectSetBuilder.test.tsx` failed as expected before implementation:
    - `validateNode` returned `[]` for a blank `eq` filter.
    - ObjectSetComposer left Execute enabled for blank `eq` and unsupported `neq` filters.
- Verification:
  - `npm --prefix web test -- --run src/lib/__tests__/objectSetBuilder.test.ts src/components/objectsets/__tests__/ObjectSetComposer.filterValidation.bdd.test.tsx src/components/objectsets/__tests__/ObjectSetBuilder.test.tsx` passed: 3 files, 33 tests.
  - From `web/`: `npx tsc --noEmit` passed.
  - `git diff --check` passed.
- Files changed:
  - `web/src/components/objectsets/ObjectSetBuilder.tsx`
  - `web/src/components/objectsets/__tests__/ObjectSetComposer.filterValidation.bdd.test.tsx`
  - `web/src/lib/objectSetBuilder.ts`
  - `web/src/lib/__tests__/objectSetBuilder.test.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Keep the ObjectSet composer filter allow-list synchronized with `pkg/oss/where/converter.go`; when adding UI aliases such as not-equals, normalize them to backend-supported `not` clauses instead of sending pseudo-operators.
  - `isNull` is the current exception to the field/value rule: it requires a field but intentionally does not require a comparison value.
- Result: SELF-440 marked `passes: true`; SELF-441 and SELF-442 remain open for later Ralph iterations.
---
